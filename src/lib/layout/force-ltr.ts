/** CSS class that keeps content LTR when the page is RTL (see `.force-ltr` in styles.css). */
export const FORCE_LTR_CLASS = 'force-ltr'

/** Opt out of global RTL mirroring for a Lucide icon (see styles.css). */
export const ICON_NO_RTL_FLIP_CLASS = 'icon-no-rtl-flip'
