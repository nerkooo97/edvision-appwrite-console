/**
 * Minimum width for blog and changelog cover images. Google Discover requires
 * large images (at least 1200px wide) plus the `max-image-preview:large`
 * robots directive to feature content with a large image preview.
 */
export const MIN_COVER_IMAGE_WIDTH = 1200
