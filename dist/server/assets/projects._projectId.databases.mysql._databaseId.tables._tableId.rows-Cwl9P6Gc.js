import { t as cn } from "./utils-DoqqkI3X.js";
import "./runtime-config-DK7G0iKr.js";
import "./debug-endpoint-BvungD5q.js";
import { h as fetchConsoleAccount } from "./sdk-DjIJ_hjn.js";
import "./console-session-cookie-7RV5Zfr4.js";
import "./i18n-Db4baE06.js";
import "./ticket-layout-B97VGq99.js";
import "./ticket-types-BpqSrvYB.js";
import { n as useT } from "./translate-DZcqveGn.js";
import { i as getErrorMessage } from "./error-formatting-CL2hjGy5.js";
import "./console-projects-C0b0tMaH.js";
import "./constants-CL7SLzjY.js";
import "./constants-Dd6QzW31.js";
import { f as ROWS_DEFAULT_PAGE_SIZE } from "./constants-BDeF927R.js";
import "./organizations-BKtnlNrj.js";
import "./console-profiles-D__E5Kgi.js";
import { Op as useCommitMysqlRowEdits, am as MYSQL_ROW_CTID_COLUMN, em as useMysqlTableRows, im as useUpdateMysqlTableRow, jp as useDeleteMysqlTableRows, kp as useCreateMysqlTableRow, om as buildMysqlRowIdentityFromRow, sm as getMysqlRowKey } from "./hooks-BONwG3Mt.js";
import { Ar as getDatabaseTableRowColumnWidthsFromPrefs, D as syncConsoleAccountAfterMutation, O as updateAccountPrefs, Rr as mergeDatabaseTableRowColumnWidthsTableIntoPrefs } from "./auth-BPuxYQAc.js";
import { R as useProject } from "./projects-BaTJenfQ.js";
import "./appwrite-id-L15yEGeF.js";
import "./databases-Dh0pwZ6h.js";
import "./database-specs-CBc802K0.js";
import { l as isNumericInlineFieldType, m as rowCellValuesEqual, s as isDateTimeInlineFieldType } from "./database-row-inline-edits-CdyGeTxj.js";
import { l as normalizeMysqlTableRouteId, o as mysqlNav } from "./mysql-database-routes-CVHkJzTt.js";
import { At as isMysqlColumnInlineEditable, Dt as getMysqlColumnEditMeta, Ft as mysqlColumnAutoGeneratesOnInsert, It as mysqlColumnCanOmitOnCreate, Lt as parseAndValidateMysqlCellInput, Mt as isMysqlColumnRequiredOnCreate, Nt as isMysqlColumnSystemGenerated, Ot as getMysqlInlineFieldType, Pt as makeMysqlPendingEditKey, Rt as shouldOmitMysqlColumnOnRowCreate, Tt as mysqlRowsFilterColumnsFromTableColumns, ct as getSearch, ht as urlFromRouterLocation, jt as isMysqlColumnRequired, pt as queryParamToMap, st as getQueryParam, tt as buildListSearchParams, ut as mapToQueryParam, xn as isMysqlPrimaryKeyColumn, zt as valueToMysqlEditString } from "./form-field-type-badge-C7qMzJo0.js";
import "./chart-interval-Dbrn19qD.js";
import "./is-marketing-page-dgx45Oqy.js";
import "./use-console-impersonation-revision-BiI0c7pX.js";
import "./affiliates-BOg1SHC6.js";
import "./social-stats-X1CQqP0k.js";
import "./cimd-CRIktQxf.js";
import "./account-applications-Bo8bAeE0.js";
import "./date-format-BD1j7PxK.js";
import "./invite-url-B18y3cBt.js";
import "./format-metric-6jsfxd5f.js";
import "./domains-Bfw8HsXF.js";
import "./parse-spec-DW3UGcrS.js";
import "./page-direction-CnacIIOa.js";
import { t as Button } from "./button-Bnm2QhOm.js";
import { t as Input } from "./input-yKHNPhDZ.js";
import "./collapsible-BcDIDOgI.js";
import "./popover-BjTNxuf9.js";
import "./tabs-XaWkg9jR.js";
import { c as SelectValue, i as SelectItem, n as SelectContent, s as SelectTrigger, t as Select } from "./select-BYGLGp-f.js";
import { r as useAuth } from "./RequireAuth-DahioVQe.js";
import "./HeaderAlertBar-CK7Gy4sE.js";
import { t as Badge } from "./badge-L9aO6DfA.js";
import { t as Label } from "./label-D8nNLJBa.js";
import { t as Switch } from "./switch-D-U5gDIQ.js";
import { t as Textarea } from "./textarea-CfKMnSVC.js";
import "./modal-auto-focus-BO41bKmA.js";
import { a as DialogHeader, n as DialogContent, o as DialogTitle, r as DialogDescription, t as Dialog } from "./dialog-CFWmsJbI.js";
import "./command-Cizl9kMJ.js";
import "./use-localized-date-format-Dy8J1Ssn.js";
import "./EmptyState-DZDwceHm.js";
import "./dropdown-menu-DH51wH-m.js";
import { t as Pagination } from "./Pagination-BDei8M4v.js";
import "./sheet-CbM5lIV1.js";
import { t as BaseDrawer } from "./BaseDrawer-B4vv4Sf_.js";
import "./calendar-6OJ5dwYN.js";
import { i as TooltipTrigger, n as TooltipContent, t as Tooltip } from "./tooltip-DUssQZhw.js";
import "./use-console-profile-DiUZxZ_6.js";
import { l as horizontalResizeDeltaPx, m as setBodyResizeDragActive, s as applyColumnResizeRailPosition, t as COLUMN_RESIZE_RAILS_LAYER_CLASS } from "./horizontal-resize-BcegzCwH.js";
import { t as Checkbox } from "./checkbox-r_hqIB3d.js";
import { t as DateTimePicker } from "./DateTimePicker-DySgezub.js";
import "./SearchableSelect-DPl0hr1b.js";
import "./mysql-table-route-loader-BP-PeWrP.js";
import { t as Route } from "./projects._projectId.databases.mysql._databaseId.tables._tableId.rows-Cc9eQ2Vf.js";
import { t as getColumnIcon } from "./column-icons-CL3QmOrR.js";
import "./ToolbarCountBadge-WR-HvODH.js";
import { t as FiltersPopover } from "./FiltersPopover-De49yhdY.js";
import { a as useDatabaseTableOperationsAccess } from "./DatabaseOperationsLockContext-B72U5wFZ.js";
import { c as SPREADSHEET_STICKY_START_HEADER_SHADOW, r as SPREADSHEET_SCROLL_LAYER_CLASS, s as SPREADSHEET_STICKY_START_EDGE_SHADOW } from "./spreadsheet-sticky-CpUihTZG.js";
import { a as MYSQL_HEADER_CELL_BORDER_CLASS, c as MYSQL_ROWS_DATA_COLUMN_MIN_WIDTH_PX, d as MYSQL_STICKY_THEAD_CLASS, f as computeMysqlRowsTableWidthPx, h as getMysqlRowsDataColumnHeaderStyle, i as MYSQL_DATA_COLUMN_RESIZE_RAIL_HANDLE_CLASS, l as MYSQL_ROWS_TABLE_EDGE_COL_PX, m as getMysqlRowsDataColumnColStyle, o as MYSQL_ROWS_DATA_COLUMN_DEFAULT_WIDTH_PX, r as MYSQL_BODY_CELL_BORDER_CLASS, s as MYSQL_ROWS_DATA_COLUMN_MAX_WIDTH_PX } from "./mysql-spreadsheet-chrome-CfmSw3G0.js";
import { a as isSpreadsheetRtlText, n as formatSpreadsheetCellValue } from "./spreadsheet-cell-formatting-C5Cs2qWo.js";
import { t as MysqlTableRowsEmptyState } from "./MysqlTableRowsEmptyState-CfSPYYpp.js";
import { n as useMysqlTableHeaderSlot } from "./MysqlTableHeaderSlotContext-DhaZhCAt.js";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useLocation, useNavigate, useParams, useSearch } from "@tanstack/react-router";
import { createContext, useCallback, useContext, useEffect, useLayoutEffect, useMemo, useRef, useState } from "react";
import { useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { flushSync } from "react-dom";
var MysqlRowsEditSessionContext = createContext(null);
function useMysqlRowsEditSession() {
	return useContext(MysqlRowsEditSessionContext);
}
function MysqlRowsEditSessionProvider({ projectId, databaseId, tableId, canWrite = true, children }) {
	const t = useT();
	const [pendingEdits, setPendingEdits] = useState(() => /* @__PURE__ */ new Map());
	const activeInlineEditRef = useRef(null);
	const suppressNextDrawerOpenRef = useRef(false);
	const commitMutation = useCommitMysqlRowEdits(projectId, databaseId, tableId);
	const markSuppressNextDrawerOpen = useCallback(() => {
		suppressNextDrawerOpenRef.current = true;
	}, []);
	const consumeSuppressNextDrawerOpen = useCallback(() => {
		if (!suppressNextDrawerOpenRef.current) return false;
		suppressNextDrawerOpenRef.current = false;
		return true;
	}, []);
	const beginInlineEdit = useCallback((params) => {
		const key = makeMysqlPendingEditKey(params.tableId, params.rowKey, params.columnKey);
		if (activeInlineEditRef.current && activeInlineEditRef.current.key !== key) {
			if (activeInlineEditRef.current.commit() === false) return false;
			activeInlineEditRef.current = null;
		}
		activeInlineEditRef.current = {
			key,
			commit: params.commit
		};
		return true;
	}, []);
	const endInlineEdit = useCallback((editTableId, rowKey, columnKey) => {
		const key = makeMysqlPendingEditKey(editTableId, rowKey, columnKey);
		if (activeInlineEditRef.current?.key === key) activeInlineEditRef.current = null;
	}, []);
	const commitActiveInlineEdit = useCallback(() => {
		const active = activeInlineEditRef.current;
		if (!active) return false;
		if (active.commit() === false) return false;
		activeInlineEditRef.current = null;
		suppressNextDrawerOpenRef.current = true;
		return true;
	}, []);
	const setCellEdit = useCallback((edit) => {
		if (!canWrite) return;
		const key = makeMysqlPendingEditKey(edit.tableId, edit.rowKey, edit.columnKey);
		if (rowCellValuesEqual(edit.value, edit.originalValue)) {
			setPendingEdits((prev) => {
				if (!prev.has(key)) return prev;
				const next = new Map(prev);
				next.delete(key);
				return next;
			});
			return;
		}
		setPendingEdits((prev) => {
			const next = new Map(prev);
			next.set(key, {
				...edit,
				databaseId
			});
			return next;
		});
	}, [canWrite, databaseId]);
	const getCellDisplayValue = useCallback((editTableId, rowKey, columnKey, originalValue) => {
		const key = makeMysqlPendingEditKey(editTableId, rowKey, columnKey);
		return pendingEdits.get(key)?.value ?? originalValue;
	}, [pendingEdits]);
	const isCellEdited = useCallback((editTableId, rowKey, columnKey) => {
		const key = makeMysqlPendingEditKey(editTableId, rowKey, columnKey);
		return pendingEdits.has(key);
	}, [pendingEdits]);
	const isRowEdited = useCallback((editTableId, rowKey) => {
		const prefix = `${editTableId}:${rowKey}:`;
		for (const key of pendingEdits.keys()) if (key.startsWith(prefix)) return true;
		return false;
	}, [pendingEdits]);
	const discardAll = useCallback(() => {
		setPendingEdits(/* @__PURE__ */ new Map());
		activeInlineEditRef.current = null;
	}, []);
	const discardRows = useCallback((rowKeys) => {
		if (rowKeys.length === 0) return;
		const rowKeySet = new Set(rowKeys);
		setPendingEdits((prev) => {
			let changed = false;
			const next = new Map(prev);
			for (const [key, edit] of prev) if (rowKeySet.has(edit.rowKey)) {
				next.delete(key);
				changed = true;
			}
			return changed ? next : prev;
		});
		const active = activeInlineEditRef.current;
		if (active && rowKeys.some((rowKey) => active.key.includes(`:${rowKey}:`))) activeInlineEditRef.current = null;
	}, []);
	const commit = useCallback(() => {
		if (!canWrite || pendingEdits.size === 0) return;
		const edits = Array.from(pendingEdits.values());
		commitMutation.mutate(edits, {
			onSuccess: () => {
				setPendingEdits(/* @__PURE__ */ new Map());
				toast.success(t("Changes saved"));
			},
			onError: (error) => {
				toast.error(getErrorMessage(error) ?? t("Failed to save changes"));
			}
		});
	}, [
		canWrite,
		commitMutation,
		pendingEdits,
		t
	]);
	const pendingRowCount = useMemo(() => {
		const rowKeys = /* @__PURE__ */ new Set();
		for (const edit of pendingEdits.values()) rowKeys.add(edit.rowKey);
		return rowKeys.size;
	}, [pendingEdits]);
	const value = useMemo(() => ({
		canWrite,
		pendingCount: pendingEdits.size,
		pendingRowCount,
		getCellDisplayValue,
		isCellEdited,
		isRowEdited,
		setCellEdit,
		discardAll,
		discardRows,
		commit,
		isCommitting: commitMutation.isPending,
		beginInlineEdit,
		endInlineEdit,
		commitActiveInlineEdit,
		markSuppressNextDrawerOpen,
		consumeSuppressNextDrawerOpen
	}), [
		beginInlineEdit,
		canWrite,
		commit,
		commitActiveInlineEdit,
		commitMutation.isPending,
		consumeSuppressNextDrawerOpen,
		discardAll,
		discardRows,
		endInlineEdit,
		getCellDisplayValue,
		isCellEdited,
		isRowEdited,
		markSuppressNextDrawerOpen,
		pendingEdits.size,
		pendingRowCount,
		setCellEdit
	]);
	return /* @__PURE__ */ jsxs(MysqlRowsEditSessionContext.Provider, {
		value,
		children: [children, pendingEdits.size > 0 ? /* @__PURE__ */ jsxs("div", {
			className: "fixed bottom-4 start-1/2 z-50 flex -translate-x-1/2 items-center gap-3 rounded-lg border border-border bg-background px-4 py-3 shadow-lg",
			children: [
				/* @__PURE__ */ jsxs(Badge, {
					variant: "warning",
					className: "text-[11px] shrink-0",
					children: [
						pendingEdits.size,
						" unsaved change",
						pendingEdits.size === 1 ? "" : "s"
					]
				}),
				/* @__PURE__ */ jsx(Button, {
					variant: "outline",
					size: "sm",
					className: "h-8 text-[13px]",
					onClick: discardAll,
					disabled: commitMutation.isPending,
					children: t("Discard")
				}),
				/* @__PURE__ */ jsx(Button, {
					size: "sm",
					className: "h-8 text-[13px]",
					onClick: commit,
					disabled: commitMutation.isPending,
					children: t("Commit changes")
				})
			]
		}) : null]
	});
}
var CELL_SURFACE_CLASS = "absolute inset-0 flex items-center px-3 py-1.5 text-start";
var INLINE_INPUT_CLASS = "h-7 w-full border-amber-500/40 bg-background px-2 text-[12px] shadow-none";
function MysqlInlineTableCell({ tableId, rowKey, column, identity, originalValue, canWrite = true, className, onCancelDrawerOpen, onCellClick }) {
	const t = useT();
	const editSession = useMysqlRowsEditSession();
	const [isEditing, setIsEditing] = useState(false);
	const [draft, setDraft] = useState("");
	const [validationError, setValidationError] = useState(null);
	const inputRef = useRef(null);
	const editContainerRef = useRef(null);
	const commitLocalEditRef = useRef(() => false);
	const meta = getMysqlColumnEditMeta(column);
	const editable = canWrite && (editSession?.canWrite ?? true) && isMysqlColumnInlineEditable(column);
	const fieldType = getMysqlInlineFieldType(meta, originalValue);
	const isRequired = isMysqlColumnRequired(column);
	const displayValue = editSession?.getCellDisplayValue(tableId, rowKey, column.column_name, originalValue) ?? originalValue;
	const isEdited = editSession?.isCellEdited(tableId, rowKey, column.column_name) ?? false;
	const formattedCell = useMemo(() => formatSpreadsheetCellValue(displayValue), [displayValue]);
	const shownDisplay = formattedCell.display;
	const shownIsNull = formattedCell.isNull;
	const shownTitle = formattedCell.full;
	const shownDir = isSpreadsheetRtlText(shownTitle) ? "rtl" : void 0;
	const cellTitle = editable ? `${shownTitle} · Double-click to edit inline` : shownTitle;
	const startEditing = useCallback((event) => {
		event?.stopPropagation();
		event?.preventDefault();
		if (!editable) return;
		onCancelDrawerOpen?.();
		setValidationError(null);
		if (!(editSession?.beginInlineEdit({
			tableId,
			rowKey,
			columnKey: column.column_name,
			commit: () => commitLocalEditRef.current()
		}) ?? true)) return;
		setDraft(valueToMysqlEditString(displayValue, meta));
		setIsEditing(true);
	}, [
		column.column_name,
		displayValue,
		editable,
		editSession,
		meta,
		onCancelDrawerOpen,
		rowKey,
		tableId
	]);
	const commitLocalEdit = useCallback(() => {
		if (!editSession) {
			setIsEditing(false);
			return true;
		}
		const result = parseAndValidateMysqlCellInput(draft, column);
		if (!result.ok) {
			setValidationError(result.error);
			toast.error(t(result.error));
			return false;
		}
		editSession.setCellEdit({
			tableId,
			rowKey,
			columnKey: column.column_name,
			originalValue,
			value: result.value,
			identity
		});
		setIsEditing(false);
		editSession.endInlineEdit(tableId, rowKey, column.column_name);
		return true;
	}, [
		column,
		draft,
		editSession,
		identity,
		originalValue,
		rowKey,
		tableId
	]);
	commitLocalEditRef.current = commitLocalEdit;
	useEffect(() => {
		if (!isEditing) return;
		const onPointerDown = (event) => {
			const target = event.target;
			if (editContainerRef.current?.contains(target)) return;
			if (target.closest("[data-radix-popper-content-wrapper]")) return;
			if (target.closest("[data-slot=\"select-trigger\"]")) return;
			if (target.closest("[data-slot=\"select-content\"]")) return;
			if (target.closest("[data-slot=\"dropdown-menu-trigger\"]")) return;
			if (target.closest("[data-slot=\"dropdown-menu-content\"]")) return;
			if (commitLocalEditRef.current()) editSession?.markSuppressNextDrawerOpen();
		};
		document.addEventListener("pointerdown", onPointerDown, true);
		return () => document.removeEventListener("pointerdown", onPointerDown, true);
	}, [editSession, isEditing]);
	useEffect(() => {
		if (isEditing && inputRef.current) {
			inputRef.current.focus();
			inputRef.current.select();
		}
	}, [isEditing]);
	const handleKeyDown = (event) => {
		if (event.key === "Enter") {
			event.preventDefault();
			commitLocalEdit();
		}
		if (event.key === "Escape") {
			event.preventDefault();
			setIsEditing(false);
			editSession?.endInlineEdit(tableId, rowKey, column.column_name);
		}
	};
	const renderEditingSurface = () => /* @__PURE__ */ jsx("div", {
		ref: editContainerRef,
		className: CELL_SURFACE_CLASS,
		onClick: (event) => event.stopPropagation(),
		onMouseDown: (event) => event.stopPropagation(),
		children: fieldType === "boolean" ? /* @__PURE__ */ jsx(Switch, {
			checked: draft === "true",
			onCheckedChange: (checked) => {
				setDraft(checked ? "true" : isRequired ? "false" : "");
			}
		}) : isDateTimeInlineFieldType(fieldType) ? /* @__PURE__ */ jsx(DateTimePicker, {
			value: draft || null,
			onChange: (value) => setDraft(value ?? ""),
			className: "h-7 w-full text-[12px]"
		}) : /* @__PURE__ */ jsx(Input, {
			ref: inputRef,
			type: isNumericInlineFieldType(fieldType) ? "number" : "text",
			value: draft,
			onChange: (event) => setDraft(event.target.value),
			onKeyDown: handleKeyDown,
			className: INLINE_INPUT_CLASS,
			"aria-invalid": validationError ? true : void 0
		})
	});
	const renderReadOnlySurface = () => /* @__PURE__ */ jsx("div", {
		className: CELL_SURFACE_CLASS,
		title: cellTitle,
		onClick: (event) => {
			event.stopPropagation();
			onCellClick?.(event);
		},
		onMouseDown: (event) => event.stopPropagation(),
		children: /* @__PURE__ */ jsx("span", {
			className: cn("block min-w-0 max-w-full truncate whitespace-nowrap text-[12px]", shownIsNull ? "text-foreground/60" : "text-foreground"),
			dir: shownDir,
			children: shownDisplay
		})
	});
	const renderEditableSurface = () => {
		if (isEditing) return renderEditingSurface();
		return /* @__PURE__ */ jsx("div", {
			className: cn(CELL_SURFACE_CLASS, isEdited && "bg-amber-500/20", !isEdited && "hover:bg-muted/60"),
			title: cellTitle,
			onClick: (event) => {
				event.stopPropagation();
				onCellClick?.(event);
			},
			onDoubleClick: startEditing,
			onMouseDown: (event) => event.stopPropagation(),
			children: /* @__PURE__ */ jsx("span", {
				className: cn("block min-w-0 max-w-full truncate whitespace-nowrap text-[12px]", shownIsNull ? "text-foreground/60" : "text-foreground", isEdited && "font-medium text-amber-950 dark:text-amber-50"),
				dir: shownDir,
				children: shownDisplay
			})
		});
	};
	return /* @__PURE__ */ jsx("td", {
		"data-column": column.column_name,
		className: cn("relative h-px p-0", MYSQL_BODY_CELL_BORDER_CLASS, className),
		children: editable ? renderEditableSurface() : renderReadOnlySurface()
	});
}
function clampColumnWidth(width) {
	return Math.min(640, Math.max(72, width));
}
function useMysqlRowsColumnResize(databaseId, tableId, columnKeys, scrollRef) {
	const queryClient = useQueryClient();
	const { account } = useAuth();
	const columnKeysRef = useRef(columnKeys);
	columnKeysRef.current = columnKeys;
	const widthsFromPrefs = useMemo(() => {
		const prefs = account?.prefs;
		const raw = getDatabaseTableRowColumnWidthsFromPrefs(prefs, databaseId, tableId);
		const next = {};
		for (const [key, value] of Object.entries(raw)) {
			if (!key) continue;
			const n = typeof value === "number" ? value : Number(value);
			if (!Number.isFinite(n)) continue;
			next[key] = clampColumnWidth(n);
		}
		return next;
	}, [
		account,
		databaseId,
		tableId
	]);
	const [columnWidths, setColumnWidths] = useState(widthsFromPrefs);
	const [resizingColumnKey, setResizingColumnKey] = useState(null);
	const columnWidthsRef = useRef({});
	if (resizingColumnKey == null) columnWidthsRef.current = columnWidths;
	const tableLayerRef = useRef(null);
	const headerThRefs = useRef(/* @__PURE__ */ new Map());
	const colRefs = useRef(/* @__PURE__ */ new Map());
	const railRefs = useRef(/* @__PURE__ */ new Map());
	useLayoutEffect(() => {
		setColumnWidths(widthsFromPrefs);
	}, [
		databaseId,
		tableId,
		widthsFromPrefs
	]);
	const getColumnWidthPx = useCallback((columnKey) => {
		const width = columnWidths[columnKey];
		if (typeof width === "number" && Number.isFinite(width)) return clampColumnWidth(width);
		return 150;
	}, [columnWidths]);
	const repositionRails = useCallback(() => {
		const layer = tableLayerRef.current;
		if (!layer) return;
		for (const columnKey of columnKeysRef.current) {
			const th = headerThRefs.current.get(columnKey);
			const rail = railRefs.current.get(columnKey);
			if (!th || !rail) continue;
			applyColumnResizeRailPosition(rail, layer, th, { maxInsetInlineStartPx: columnKeysRef.current[columnKeysRef.current.length - 1] === columnKey ? layer.getBoundingClientRect().width : void 0 });
		}
	}, []);
	const applyDraggedWidthPx = useCallback((columnKey, widthPx) => {
		const next = clampColumnWidth(Math.round(widthPx));
		const isLastColumn = columnKeysRef.current[columnKeysRef.current.length - 1] === columnKey;
		columnWidthsRef.current = {
			...columnWidthsRef.current,
			[columnKey]: next
		};
		const colEl = colRefs.current.get(columnKey);
		if (colEl) {
			colEl.style.width = `${next}px`;
			colEl.style.minWidth = `${next}px`;
			if (isLastColumn) colEl.style.maxWidth = "";
			else colEl.style.maxWidth = `${next}px`;
		}
		const thEl = headerThRefs.current.get(columnKey);
		if (thEl) {
			thEl.style.width = `${next}px`;
			thEl.style.minWidth = `${next}px`;
			if (isLastColumn) thEl.style.maxWidth = "";
			else thEl.style.maxWidth = `${next}px`;
		}
		repositionRails();
	}, [repositionRails]);
	const persistColumnWidths = useCallback(async (widths, allowedKeys) => {
		const allowed = new Set(allowedKeys.filter(Boolean));
		const pruned = {};
		for (const key of allowed) {
			const width = widths[key];
			if (typeof width === "number" && Number.isFinite(width)) pruned[key] = clampColumnWidth(width);
		}
		try {
			syncConsoleAccountAfterMutation(queryClient, { apiResult: await updateAccountPrefs(mergeDatabaseTableRowColumnWidthsTableIntoPrefs((await fetchConsoleAccount()).prefs || {}, databaseId, tableId, pruned)) });
		} catch {}
	}, [
		databaseId,
		queryClient,
		tableId
	]);
	const handleResizePointerDown = useCallback((columnKey) => (event) => {
		if (!columnKey) return;
		event.preventDefault();
		event.stopPropagation();
		setBodyResizeDragActive(true);
		const button = event.currentTarget;
		button.setPointerCapture(event.pointerId);
		const startX = event.clientX;
		const initialWidth = getColumnWidthPx(columnKey);
		flushSync(() => {
			setResizingColumnKey(columnKey);
		});
		applyDraggedWidthPx(columnKey, initialWidth);
		const onMove = (ev) => {
			applyDraggedWidthPx(columnKey, initialWidth + horizontalResizeDeltaPx(startX, ev.clientX));
		};
		const onUp = () => {
			setBodyResizeDragActive(false);
			try {
				button.releasePointerCapture(event.pointerId);
			} catch {}
			window.removeEventListener("pointermove", onMove);
			window.removeEventListener("pointerup", onUp);
			window.removeEventListener("pointercancel", onUp);
			flushSync(() => {
				setResizingColumnKey(null);
				setColumnWidths({ ...columnWidthsRef.current });
			});
			persistColumnWidths(columnWidthsRef.current, columnKeysRef.current);
		};
		window.addEventListener("pointermove", onMove);
		window.addEventListener("pointerup", onUp);
		window.addEventListener("pointercancel", onUp);
	}, [
		applyDraggedWidthPx,
		getColumnWidthPx,
		persistColumnWidths
	]);
	const columnResizeLayoutKey = columnKeys.join("");
	useLayoutEffect(() => {
		repositionRails();
	}, [
		columnResizeLayoutKey,
		columnWidths,
		repositionRails
	]);
	useLayoutEffect(() => {
		const scroll = scrollRef.current;
		const layer = tableLayerRef.current;
		if (!scroll || !layer || columnKeysRef.current.length === 0) return;
		const measure = () => {
			repositionRails();
		};
		measure();
		const ro = new ResizeObserver(measure);
		ro.observe(scroll);
		ro.observe(layer);
		scroll.addEventListener("scroll", measure, { passive: true });
		window.addEventListener("resize", measure);
		return () => {
			ro.disconnect();
			scroll.removeEventListener("scroll", measure);
			window.removeEventListener("resize", measure);
		};
	}, [
		columnResizeLayoutKey,
		columnWidths,
		repositionRails,
		scrollRef
	]);
	return {
		tableLayerRef,
		headerThRefs,
		colRefs,
		railRefs,
		getColumnWidthPx,
		resizingColumnKey,
		handleResizePointerDown,
		repositionRails
	};
}
function MysqlRowsSpreadsheet({ databaseId, tableId, columns, rows, rowNumberOffset = 0, canWrite = true, isLoading = false, emptyContent, onOpenRow, selectedRowKeys, onToggleRow, onToggleAllRows, currentPage, totalItems, pageSize, onPageChange, onPageSizeChange, onPaginationInteract }) {
	const t = useT();
	const editSession = useMysqlRowsEditSession();
	const scrollRef = useRef(null);
	const drawerOpenTimerRef = useRef(null);
	const selectionEnabled = canWrite && selectedRowKeys != null && onToggleRow != null;
	const visibleColumns = useMemo(() => columns.filter((column) => column.column_name !== MYSQL_ROW_CTID_COLUMN), [columns]);
	const rowEntries = useMemo(() => rows.map((row) => ({
		row,
		rowKey: getMysqlRowKey(row, columns),
		identity: buildMysqlRowIdentityFromRow(row, columns)
	})), [columns, rows]);
	const selectedOnPageCount = useMemo(() => {
		if (!selectedRowKeys) return 0;
		return rowEntries.filter((entry) => selectedRowKeys.has(entry.rowKey)).length;
	}, [rowEntries, selectedRowKeys]);
	const allPageSelected = selectionEnabled && rowEntries.length > 0 && selectedOnPageCount === rowEntries.length;
	const somePageSelected = selectionEnabled && selectedOnPageCount > 0 && !allPageSelected;
	const columnKeys = useMemo(() => visibleColumns.map((column) => column.column_name), [visibleColumns]);
	const { tableLayerRef, headerThRefs, colRefs, railRefs, getColumnWidthPx, resizingColumnKey, handleResizePointerDown } = useMysqlRowsColumnResize(databaseId, tableId, columnKeys, scrollRef);
	const tableMinWidthPx = useMemo(() => computeMysqlRowsTableWidthPx(columnKeys, getColumnWidthPx), [columnKeys, getColumnWidthPx]);
	const cancelDrawerOpen = useCallback(() => {
		if (drawerOpenTimerRef.current) {
			clearTimeout(drawerOpenTimerRef.current);
			drawerOpenTimerRef.current = null;
		}
	}, []);
	const handleCellClick = useCallback((row, columnName) => (event) => {
		if (event.detail > 1) return;
		cancelDrawerOpen();
		drawerOpenTimerRef.current = setTimeout(() => {
			if (editSession?.consumeSuppressNextDrawerOpen()) return;
			onOpenRow?.(row, columnName);
		}, 200);
	}, [
		cancelDrawerOpen,
		editSession,
		onOpenRow
	]);
	useEffect(() => {
		return () => {
			cancelDrawerOpen();
		};
	}, [cancelDrawerOpen]);
	const handlePaginationInteract = useCallback(() => {
		cancelDrawerOpen();
		onPaginationInteract?.();
	}, [cancelDrawerOpen, onPaginationInteract]);
	const showEmptyTable = !isLoading && (visibleColumns.length === 0 || rows.length === 0);
	return /* @__PURE__ */ jsxs("div", {
		className: "flex h-full min-h-0 flex-col",
		children: [/* @__PURE__ */ jsx("div", {
			ref: scrollRef,
			className: "min-h-0 flex-1 overflow-auto overscroll-contain",
			children: isLoading && rows.length === 0 ? /* @__PURE__ */ jsx("div", {
				className: "flex h-full min-h-[12rem] items-center justify-center text-[13px] text-muted-foreground",
				children: t("Loading rows…")
			}) : showEmptyTable ? /* @__PURE__ */ jsx("div", {
				className: "flex h-full min-h-[12rem] items-center justify-center px-4",
				children: emptyContent ?? /* @__PURE__ */ jsx("p", {
					className: "text-[13px] text-muted-foreground",
					children: t("No rows to display.")
				})
			}) : /* @__PURE__ */ jsxs("div", {
				ref: tableLayerRef,
				className: SPREADSHEET_SCROLL_LAYER_CLASS,
				style: { minWidth: tableMinWidthPx },
				children: [/* @__PURE__ */ jsxs("table", {
					className: "relative z-0 w-full table-fixed border-collapse",
					children: [
						/* @__PURE__ */ jsxs("colgroup", { children: [/* @__PURE__ */ jsx("col", { style: {
							width: 40,
							minWidth: 40,
							maxWidth: 40
						} }), visibleColumns.map((column, columnIndex) => {
							const key = column.column_name;
							const width = getColumnWidthPx(key);
							const isDragResize = resizingColumnKey === key;
							return /* @__PURE__ */ jsx("col", {
								ref: (node) => {
									if (node) colRefs.current.set(key, node);
									else colRefs.current.delete(key);
								},
								style: getMysqlRowsDataColumnColStyle(columnIndex, visibleColumns.length, width, isDragResize)
							}, key);
						})] }),
						/* @__PURE__ */ jsx("thead", {
							className: MYSQL_STICKY_THEAD_CLASS,
							children: /* @__PURE__ */ jsxs("tr", { children: [/* @__PURE__ */ jsx("th", {
								className: cn("sticky start-0 z-30 bg-background px-2 py-2 text-center text-[12px] font-semibold uppercase tracking-wider text-muted-foreground", SPREADSHEET_STICKY_START_HEADER_SHADOW),
								style: {
									width: 40,
									minWidth: 40,
									maxWidth: 40
								},
								children: selectionEnabled ? /* @__PURE__ */ jsx("div", {
									className: "flex justify-center",
									children: /* @__PURE__ */ jsx(Checkbox, {
										checked: allPageSelected ? true : somePageSelected ? "indeterminate" : false,
										onCheckedChange: (checked) => {
											onToggleAllRows?.(rowEntries.map(({ rowKey, identity }) => ({
												rowKey,
												identity
											})), checked === true);
										},
										onClick: (event) => event.stopPropagation(),
										"aria-label": t("Select all rows")
									})
								}) : "#"
							}), visibleColumns.map((column, columnIndex) => {
								const key = column.column_name;
								const ColumnIcon = getColumnIcon(column.udt_name || column.data_type);
								const isPrimaryKey = isMysqlPrimaryKeyColumn(column);
								const isDragResize = resizingColumnKey === key;
								const width = getColumnWidthPx(key);
								return /* @__PURE__ */ jsx("th", {
									ref: (node) => {
										if (node) headerThRefs.current.set(key, node);
										else headerThRefs.current.delete(key);
									},
									className: cn("px-3 py-2", MYSQL_HEADER_CELL_BORDER_CLASS, columnIndex === visibleColumns.length - 1 && "border-e-0"),
									style: getMysqlRowsDataColumnHeaderStyle(columnIndex, visibleColumns.length, width, isDragResize),
									children: /* @__PURE__ */ jsxs("div", {
										className: "flex min-w-0 items-center gap-2 pe-1.5",
										children: [
											/* @__PURE__ */ jsx(ColumnIcon, { className: "h-3.5 w-3.5 shrink-0 text-muted-foreground" }),
											/* @__PURE__ */ jsx("span", {
												className: "min-w-0 truncate text-[12px] font-medium text-foreground",
												children: key
											}),
											isPrimaryKey ? /* @__PURE__ */ jsxs(Tooltip, { children: [/* @__PURE__ */ jsx(TooltipTrigger, {
												asChild: true,
												children: /* @__PURE__ */ jsx("span", {
													className: "inline-flex shrink-0 items-center rounded border border-border bg-muted/50 px-1 py-px text-[9px] font-semibold uppercase tracking-wider text-muted-foreground",
													children: "PK"
												})
											}), /* @__PURE__ */ jsx(TooltipContent, {
												side: "top",
												children: t("Primary key")
											})] }) : null
										]
									})
								}, key);
							})] })
						}),
						/* @__PURE__ */ jsx("tbody", { children: rowEntries.map(({ row, rowKey, identity }, rowIndex) => {
							const isSelected = selectedRowKeys?.has(rowKey) ?? false;
							const hasPendingEdits = editSession?.isRowEdited(tableId, rowKey) ?? false;
							return /* @__PURE__ */ jsxs("tr", {
								className: cn("group cursor-pointer transition-colors", isSelected && !hasPendingEdits && "bg-muted", hasPendingEdits && "bg-amber-500/10 ring-1 ring-inset ring-amber-500/20 hover:bg-amber-500/15", !hasPendingEdits && !isSelected && "hover:bg-muted/50", !hasPendingEdits && !isSelected && rowIndex % 2 === 1 && "bg-muted/15"),
								children: [/* @__PURE__ */ jsx("td", {
									className: cn("sticky start-0 z-10 border-b border-border px-2 py-2.5 text-center", isSelected || hasPendingEdits ? "bg-muted" : "bg-background", hasPendingEdits && "bg-amber-500/10", SPREADSHEET_STICKY_START_EDGE_SHADOW),
									onClick: (event) => event.stopPropagation(),
									children: selectionEnabled ? /* @__PURE__ */ jsx("div", {
										className: "flex justify-center",
										children: /* @__PURE__ */ jsx(Checkbox, {
											checked: isSelected,
											onCheckedChange: () => onToggleRow?.(rowKey, identity),
											onClick: (event) => event.stopPropagation(),
											"aria-label": t("Select row")
										})
									}) : /* @__PURE__ */ jsx("span", {
										className: "text-[11px] tabular-nums text-muted-foreground",
										children: rowNumberOffset + rowIndex + 1
									})
								}), visibleColumns.map((column, columnIndex) => {
									const rawValue = row[column.column_name];
									return /* @__PURE__ */ jsx(MysqlInlineTableCell, {
										tableId,
										rowKey,
										column,
										identity,
										originalValue: rawValue,
										canWrite,
										className: columnIndex === visibleColumns.length - 1 ? "border-e-0" : void 0,
										onCancelDrawerOpen: cancelDrawerOpen,
										onCellClick: handleCellClick(row, column.column_name)
									}, column.column_name);
								})]
							}, rowKey);
						}) })
					]
				}), /* @__PURE__ */ jsx("div", {
					className: COLUMN_RESIZE_RAILS_LAYER_CLASS,
					"aria-hidden": true,
					children: visibleColumns.map((column) => {
						const key = column.column_name;
						return /* @__PURE__ */ jsx("button", {
							ref: (node) => {
								if (node) railRefs.current.set(key, node);
								else railRefs.current.delete(key);
							},
							type: "button",
							"aria-label": `Resize ${key} column width`,
							"aria-orientation": "vertical",
							role: "separator",
							tabIndex: 0,
							onPointerDown: handleResizePointerDown(key),
							className: cn(MYSQL_DATA_COLUMN_RESIZE_RAIL_HANDLE_CLASS, resizingColumnKey === key && "before:opacity-100", "focus-visible:ring-1 focus-visible:ring-ring focus-visible:ring-offset-1 focus-visible:ring-offset-background")
						}, `col-resize-rail-${key}`);
					})
				})]
			})
		}), /* @__PURE__ */ jsx("div", {
			className: "h-[54px] shrink-0 border-t border-border bg-background",
			children: /* @__PURE__ */ jsx("div", {
				className: "@container flex h-full items-center px-4 sm:px-6",
				onPointerDownCapture: handlePaginationInteract,
				children: /* @__PURE__ */ jsx(Pagination, {
					currentPage,
					totalItems,
					pageSize,
					pageSizeOptions: [
						10,
						25,
						50,
						100
					],
					onPageChange,
					onPageSizeChange,
					itemLabel: "rows",
					className: "h-full min-h-0 w-full border-0 mt-0 py-0"
				})
			})
		})]
	});
}
function isJsonType(typeId) {
	return typeId === "json" || typeId === "jsonb";
}
function isLongTextType(typeId) {
	return typeId === "text";
}
function MysqlRowNullOverlay({ columnName, checked, onCheckedChange, className }) {
	const t = useT();
	const id = `mysql-row-field-${columnName}-null`;
	return /* @__PURE__ */ jsx("div", {
		className: cn("pointer-events-none absolute flex items-center gap-2", className),
		children: /* @__PURE__ */ jsxs("div", {
			className: "pointer-events-auto flex items-center gap-1.5",
			children: [/* @__PURE__ */ jsx(Checkbox, {
				id,
				checked,
				onCheckedChange: (value) => onCheckedChange(value === true),
				onClick: (event) => event.stopPropagation(),
				className: "h-4 w-4"
			}), /* @__PURE__ */ jsx("label", {
				htmlFor: id,
				className: "cursor-pointer select-none text-[11px] text-muted-foreground",
				children: t("Null")
			})]
		})
	});
}
function MysqlRowEditDrawer({ open, onOpenChange, projectId, databaseId, tableId, row, identity, columns, focusedField, canWrite = true }) {
	const t = useT();
	const isCreate = row == null;
	const [draft, setDraft] = useState({});
	const [errors, setErrors] = useState({});
	const createMutation = useCreateMysqlTableRow(projectId, databaseId, tableId);
	const updateMutation = useUpdateMysqlTableRow(projectId, databaseId, tableId);
	const editableColumns = useMemo(() => columns.filter((column) => {
		if (isCreate) return !shouldOmitMysqlColumnOnRowCreate(column);
		return true;
	}), [columns, isCreate]);
	useEffect(() => {
		if (!open) return;
		const nextDraft = {};
		for (const column of editableColumns) {
			const meta = getMysqlColumnEditMeta(column);
			const fieldType = getMysqlInlineFieldType(meta);
			const value = row?.[column.column_name];
			if (value === null) nextDraft[column.column_name] = null;
			else if (value === void 0) nextDraft[column.column_name] = fieldType === "boolean" ? "false" : "";
			else nextDraft[column.column_name] = valueToMysqlEditString(value, meta);
		}
		setDraft(nextDraft);
		setErrors({});
	}, [
		editableColumns,
		open,
		row
	]);
	const handleFieldChange = (columnName, value) => {
		setDraft((prev) => ({
			...prev,
			[columnName]: value
		}));
		setErrors((prev) => {
			if (!prev[columnName]) return prev;
			const next = { ...prev };
			delete next[columnName];
			return next;
		});
	};
	const handleNullToggle = (columnName, isNull) => {
		if (isNull) {
			handleFieldChange(columnName, null);
			return;
		}
		handleFieldChange(columnName, "");
	};
	const handleSave = async () => {
		if (!canWrite) return;
		const values = {};
		const nextErrors = {};
		for (const column of editableColumns) {
			if (isCreate && shouldOmitMysqlColumnOnRowCreate(column)) continue;
			if (!isCreate && isMysqlColumnSystemGenerated(column)) continue;
			const rawDraft = draft[column.column_name];
			if (rawDraft === null) {
				if (isCreate ? isMysqlColumnRequiredOnCreate(column) : isMysqlColumnRequired(column)) {
					nextErrors[column.column_name] = "This field is required.";
					continue;
				}
				values[column.column_name] = null;
				continue;
			}
			if (isCreate && !(rawDraft ?? "").trim()) {
				if (mysqlColumnCanOmitOnCreate(column)) continue;
				if (column.is_nullable === "YES") {
					values[column.column_name] = null;
					continue;
				}
				nextErrors[column.column_name] = "This field is required.";
				continue;
			}
			const result = parseAndValidateMysqlCellInput(rawDraft ?? "", column);
			if (!result.ok) {
				nextErrors[column.column_name] = result.error;
				continue;
			}
			values[column.column_name] = result.value;
		}
		if (Object.keys(nextErrors).length > 0) {
			setErrors(nextErrors);
			toast.error(t("Fix validation errors before saving."));
			return;
		}
		try {
			if (isCreate) {
				await createMutation.mutateAsync(values);
				toast.success(t("Row created"));
			} else if (identity) {
				const changes = {};
				for (const column of editableColumns) {
					if (isMysqlColumnSystemGenerated(column)) continue;
					const original = row?.[column.column_name];
					const next = values[column.column_name];
					if (JSON.stringify(original) !== JSON.stringify(next)) changes[column.column_name] = next;
				}
				if (Object.keys(changes).length === 0) {
					onOpenChange(false);
					return;
				}
				await updateMutation.mutateAsync({
					identity,
					changes
				});
				toast.success(t("Row updated"));
			}
			onOpenChange(false);
		} catch (error) {
			toast.error(getErrorMessage(error) ?? t("Failed to save row"));
		}
	};
	const isSaving = createMutation.isPending || updateMutation.isPending;
	return /* @__PURE__ */ jsx(BaseDrawer, {
		open,
		onOpenChange,
		title: isCreate ? t("Create row") : t("Update row"),
		description: isCreate ? t("Add a new row. Nullable columns can be left empty or set to null.") : t("Update row values. Nullable columns can be cleared or set to null."),
		maxWidth: "sm:max-w-xl",
		disableAutoFocus: true,
		children: /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", { className: "border-t border-border" }), /* @__PURE__ */ jsxs("div", {
			className: "flex min-h-0 flex-1 flex-col",
			children: [/* @__PURE__ */ jsx("div", {
				className: "flex-1 overflow-y-auto px-6 pb-4 pt-4 space-y-4",
				children: /* @__PURE__ */ jsxs("div", {
					className: "space-y-3",
					children: [/* @__PURE__ */ jsx("h4", {
						className: "text-[11px] font-medium uppercase tracking-wide text-muted-foreground",
						children: t("Row data")
					}), /* @__PURE__ */ jsx("div", {
						className: "space-y-4",
						children: editableColumns.map((column) => {
							const meta = getMysqlColumnEditMeta(column);
							const fieldType = getMysqlInlineFieldType(meta);
							const required = isCreate ? isMysqlColumnRequiredOnCreate(column) : isMysqlColumnRequired(column);
							const autoGenerates = isCreate && mysqlColumnAutoGeneratesOnInsert(column);
							const canOmitOnCreate = isCreate && mysqlColumnCanOmitOnCreate(column);
							const emptyPlaceholder = autoGenerates ? t("Leave blank to auto-generate") : column.is_nullable === "YES" ? "NULL" : void 0;
							const draftValue = draft[column.column_name];
							const isNull = draftValue === null;
							const stringValue = isNull ? "" : draftValue ?? "";
							const error = errors[column.column_name];
							const inputId = `mysql-row-field-${column.column_name}`;
							const readOnly = !canWrite || !isCreate && isMysqlColumnSystemGenerated(column);
							const showNullToggle = !readOnly && column.is_nullable === "YES";
							const useTextarea = isJsonType(meta.typeId) || isLongTextType(meta.typeId);
							return /* @__PURE__ */ jsxs("div", {
								className: "space-y-1.5",
								children: [
									/* @__PURE__ */ jsxs(Label, {
										htmlFor: inputId,
										className: "flex items-center gap-1.5 text-[12px] font-medium text-foreground",
										children: [/* @__PURE__ */ jsx("span", { children: column.column_name }), required ? /* @__PURE__ */ jsx("span", {
											className: "ms-0.5 text-[12px] font-semibold text-destructive",
											"aria-label": t("Required field"),
											children: "*"
										}) : null]
									}),
									readOnly ? /* @__PURE__ */ jsx(Input, {
										id: inputId,
										value: stringValue,
										readOnly: true,
										className: "h-9 bg-muted/30 text-[13px]"
									}) : fieldType === "boolean" ? /* @__PURE__ */ jsxs("div", {
										className: "flex items-center gap-2",
										children: [/* @__PURE__ */ jsx(Switch, {
											id: inputId,
											checked: stringValue === "true",
											onCheckedChange: (checked) => handleFieldChange(column.column_name, checked ? "true" : "false")
										}), /* @__PURE__ */ jsx("span", {
											className: "text-[12px] text-muted-foreground",
											children: stringValue === "true" ? t("True") : t("False")
										})]
									}) : fieldType === "enum" && (meta.enumValues?.length ?? 0) > 0 ? /* @__PURE__ */ jsxs(Select, {
										value: isNull ? void 0 : stringValue || void 0,
										onValueChange: (next) => handleFieldChange(column.column_name, next),
										disabled: isNull,
										children: [/* @__PURE__ */ jsx(SelectTrigger, {
											id: inputId,
											className: "h-9 w-full text-[13px]",
											"aria-invalid": error ? true : void 0,
											children: /* @__PURE__ */ jsx(SelectValue, { placeholder: required ? t("Select a value") : emptyPlaceholder })
										}), /* @__PURE__ */ jsx(SelectContent, { children: meta.enumValues.map((option) => /* @__PURE__ */ jsx(SelectItem, {
											value: option,
											children: option
										}, option)) })]
									}) : isDateTimeInlineFieldType(fieldType) ? /* @__PURE__ */ jsx(DateTimePicker, {
										id: inputId,
										value: isNull ? null : stringValue || null,
										onChange: (next) => handleFieldChange(column.column_name, next ?? (canOmitOnCreate ? "" : null)),
										clearable: !required,
										placeholder: required ? "Select date & time" : emptyPlaceholder,
										className: "w-full",
										autoFocus: focusedField === column.column_name
									}) : isNumericInlineFieldType(fieldType) ? /* @__PURE__ */ jsxs("div", {
										className: "space-y-1.5",
										children: [/* @__PURE__ */ jsx(Input, {
											id: inputId,
											type: "number",
											inputMode: "numeric",
											value: stringValue,
											onChange: (event) => {
												const next = event.target.value;
												handleFieldChange(column.column_name, next === "" && !canOmitOnCreate ? null : next);
											},
											placeholder: required ? void 0 : emptyPlaceholder,
											className: "h-9 text-[13px]",
											"aria-invalid": error ? true : void 0,
											autoFocus: focusedField === column.column_name
										}), !required && isNull ? /* @__PURE__ */ jsx("p", {
											className: "text-[11px] text-muted-foreground",
											children: "NULL"
										}) : null]
									}) : useTextarea ? /* @__PURE__ */ jsxs("div", {
										className: "relative",
										children: [/* @__PURE__ */ jsx(Textarea, {
											id: inputId,
											value: stringValue,
											disabled: isNull,
											onChange: (event) => handleFieldChange(column.column_name, event.target.value),
											rows: isJsonType(meta.typeId) ? 6 : 4,
											placeholder: required ? void 0 : emptyPlaceholder,
											className: cn("min-h-[36px] max-h-[600px] resize-none text-[13px] font-mono", isNull && "cursor-not-allowed opacity-50", showNullToggle ? "pb-8 pe-28" : "pb-2"),
											"aria-invalid": error ? true : void 0,
											autoFocus: focusedField === column.column_name
										}), showNullToggle ? /* @__PURE__ */ jsx(MysqlRowNullOverlay, {
											columnName: column.column_name,
											checked: isNull,
											onCheckedChange: (checked) => handleNullToggle(column.column_name, checked),
											className: "bottom-2 end-2"
										}) : null]
									}) : /* @__PURE__ */ jsxs("div", {
										className: "relative",
										children: [/* @__PURE__ */ jsx(Input, {
											id: inputId,
											type: "text",
											value: stringValue,
											disabled: isNull,
											onChange: (event) => handleFieldChange(column.column_name, event.target.value),
											placeholder: required ? void 0 : emptyPlaceholder,
											className: cn("h-9 text-[13px]", isNull && "cursor-not-allowed opacity-50", showNullToggle && "pe-28"),
											"aria-invalid": error ? true : void 0,
											autoFocus: focusedField === column.column_name
										}), showNullToggle ? /* @__PURE__ */ jsx(MysqlRowNullOverlay, {
											columnName: column.column_name,
											checked: isNull,
											onCheckedChange: (checked) => handleNullToggle(column.column_name, checked),
											className: "top-1/2 end-2 -translate-y-1/2"
										}) : null]
									}),
									error ? /* @__PURE__ */ jsx("p", {
										className: "text-[12px] text-destructive",
										children: t(error)
									}) : null
								]
							}, column.column_name);
						})
					})]
				})
			}), /* @__PURE__ */ jsxs("div", {
				className: "shrink-0 px-6 py-4 border-t border-border bg-muted/30 flex flex-col gap-2 sm:flex-row sm:justify-start",
				children: [/* @__PURE__ */ jsx(Button, {
					onClick: () => void handleSave(),
					disabled: !canWrite || isSaving,
					children: isCreate ? t("Create") : t("Update")
				}), /* @__PURE__ */ jsx(Button, {
					variant: "outline",
					onClick: () => onOpenChange(false),
					disabled: isSaving,
					children: t("Cancel")
				})]
			})]
		})] })
	});
}
function MysqlTableRowsView({ databaseId, tableId }) {
	const t = useT();
	const { projectId } = useParams({ strict: false });
	const navigate = useNavigate();
	const location = useLocation();
	const routeSearch = useSearch({ strict: false });
	const { project } = useProject(projectId);
	const { canWrite, writeTooltip } = useDatabaseTableOperationsAccess({ permissionDeniedTooltip: t("You don't have permission to modify rows.") });
	const rowsListUrl = useMemo(() => urlFromRouterLocation(location, window.location.origin), [location.pathname, location.search]);
	const urlSearch = getSearch(rowsListUrl)?.trim() || (routeSearch?.search)?.trim() || void 0;
	const filterMap = useMemo(() => queryParamToMap(getQueryParam(rowsListUrl) ?? routeSearch?.query ?? null), [rowsListUrl, routeSearch?.query]);
	const filterKeys = useMemo(() => filterMap.size > 0 ? Array.from(filterMap.keys()) : void 0, [filterMap]);
	const hasActiveFilters = filterMap.size > 0 || Boolean(urlSearch);
	const [rowsFiltersOpen, setRowsFiltersOpen] = useState(false);
	const [pageSize, setPageSize] = useState(25);
	const [requestedPage, setRequestedPage] = useState(1);
	const [displayedPage, setDisplayedPage] = useState(1);
	const strippedPaginationFromUrlRef = useRef(false);
	const [drawerOpen, setDrawerOpen] = useState(false);
	const [drawerRow, setDrawerRow] = useState(null);
	const [drawerIdentity, setDrawerIdentity] = useState(null);
	const [drawerFocusedField, setDrawerFocusedField] = useState();
	const [selectedRows, setSelectedRows] = useState(() => /* @__PURE__ */ new Map());
	const [bulkDeleteDialogOpen, setBulkDeleteDialogOpen] = useState(false);
	const clearSelection = useCallback(() => {
		setSelectedRows(/* @__PURE__ */ new Map());
		setBulkDeleteDialogOpen(false);
	}, []);
	useEffect(() => {
		setPageSize(25);
		setRequestedPage(1);
		setDisplayedPage(1);
		clearSelection();
	}, [tableId, clearSelection]);
	useEffect(() => {
		setRequestedPage(1);
		setDisplayedPage(1);
		clearSelection();
	}, [
		urlSearch,
		filterKeys,
		clearSelection
	]);
	useEffect(() => {
		if (strippedPaginationFromUrlRef.current) return;
		if (routeSearch?.page == null && routeSearch?.limit == null) return;
		strippedPaginationFromUrlRef.current = true;
		const query = getQueryParam(rowsListUrl) ?? routeSearch?.query ?? void 0;
		navigate({
			...mysqlNav({
				projectId,
				databaseId
			}).table({ tableId }).rows(),
			search: buildListSearchParams({
				search: urlSearch ?? "",
				query: query || void 0
			}),
			replace: true
		});
	}, [
		databaseId,
		navigate,
		projectId,
		routeSearch?.limit,
		routeSearch?.page,
		routeSearch?.query,
		rowsListUrl,
		tableId,
		urlSearch
	]);
	const rowsListParams = useMemo(() => ({
		search: urlSearch,
		filterKeys
	}), [filterKeys, urlSearch]);
	const { total: requestedTotal, isFetching: requestedFetching } = useMysqlTableRows(projectId, databaseId, tableId, requestedPage - 1, pageSize, rowsListParams);
	const { rows, total, tableColumns, isLoading, isFetching, refetch } = useMysqlTableRows(projectId, databaseId, tableId, displayedPage - 1, pageSize, rowsListParams);
	useEffect(() => {
		if (!requestedFetching && requestedPage !== displayedPage) setDisplayedPage(requestedPage);
	}, [
		displayedPage,
		requestedFetching,
		requestedPage
	]);
	const navigateToRowsList = useCallback((updates) => {
		const nextSearch = buildListSearchParams({
			search: updates.search !== void 0 ? updates.search : urlSearch ?? "",
			query: updates.query
		});
		navigate({
			...mysqlNav({
				projectId,
				databaseId
			}).table({ tableId }).rows(),
			search: nextSearch,
			replace: true
		});
	}, [
		databaseId,
		navigate,
		projectId,
		tableId,
		urlSearch
	]);
	const handleApplyFilter = useCallback((compactKey, _queryString, replaceKey) => {
		const nextMap = new Map(filterMap);
		if (replaceKey) {
			for (const key of nextMap.keys()) if (key.c === replaceKey.c && key.o === replaceKey.o && JSON.stringify(key.v) === JSON.stringify(replaceKey.v)) nextMap.delete(key);
		}
		nextMap.set(compactKey, "");
		navigateToRowsList({ query: mapToQueryParam(nextMap) });
	}, [filterMap, navigateToRowsList]);
	const handleRemoveFilter = useCallback((compactKey) => {
		const nextMap = new Map(filterMap);
		nextMap.delete(compactKey);
		navigateToRowsList({ query: mapToQueryParam(nextMap) });
	}, [filterMap, navigateToRowsList]);
	const handleClearAllFilters = useCallback(() => {
		navigateToRowsList({ query: "" });
	}, [navigateToRowsList]);
	const handleSearchChange = useCallback((value) => {
		navigateToRowsList({ search: value });
	}, [navigateToRowsList]);
	const handlePageChange = useCallback((page) => {
		setRequestedPage(page);
		clearSelection();
	}, [clearSelection]);
	const handlePageSizeChange = useCallback((limit) => {
		setPageSize(limit);
		setRequestedPage(1);
		setDisplayedPage(1);
		clearSelection();
	}, [clearSelection]);
	const handlePaginationInteract = useCallback(() => {
		setDrawerOpen(false);
	}, []);
	const handleToggleRow = useCallback((rowKey, identity) => {
		setSelectedRows((prev) => {
			const next = new Map(prev);
			if (next.has(rowKey)) next.delete(rowKey);
			else next.set(rowKey, identity);
			return next;
		});
	}, []);
	const handleToggleAllRows = useCallback((pageRows, selectAll) => {
		setSelectedRows((prev) => {
			const next = new Map(prev);
			if (selectAll) for (const { rowKey, identity } of pageRows) next.set(rowKey, identity);
			else for (const { rowKey } of pageRows) next.delete(rowKey);
			return next;
		});
	}, []);
	const openCreateRow = useCallback(() => {
		setDrawerRow(null);
		setDrawerIdentity(null);
		setDrawerFocusedField(void 0);
		setDrawerOpen(true);
	}, []);
	const openRowInDrawer = useCallback((row, focusedField) => {
		const identity = buildMysqlRowIdentityFromRow(row, tableColumns);
		const rowForDrawer = { ...row };
		delete rowForDrawer[MYSQL_ROW_CTID_COLUMN];
		setDrawerRow(rowForDrawer);
		setDrawerIdentity(identity);
		setDrawerFocusedField(focusedField);
		setDrawerOpen(true);
	}, [tableColumns]);
	const drawerRowKey = useMemo(() => {
		if (!drawerRow) return null;
		if (drawerIdentity?.primaryKeyValues) return JSON.stringify(drawerIdentity.primaryKeyValues);
		if (drawerIdentity?.ctid) return `ctid:${drawerIdentity.ctid}`;
		return getMysqlRowKey(drawerRow, tableColumns);
	}, [
		drawerIdentity,
		drawerRow,
		tableColumns
	]);
	const filterColumns = useMemo(() => mysqlRowsFilterColumnsFromTableColumns(tableColumns), [tableColumns]);
	const filterScope = `mysql.rows.${databaseId}.${tableId}`;
	const filterTrigger = useMemo(() => /* @__PURE__ */ jsx(FiltersPopover, {
		open: rowsFiltersOpen,
		onOpenChange: setRowsFiltersOpen,
		columns: filterColumns,
		filterMap,
		onApplyFilter: handleApplyFilter,
		onRemoveFilter: handleRemoveFilter,
		onClearAll: handleClearAllFilters,
		filterScope,
		resourceLabel: "rows",
		teamId: project?.teamId
	}), [
		filterColumns,
		filterMap,
		filterScope,
		handleApplyFilter,
		handleClearAllFilters,
		handleRemoveFilter,
		project?.teamId,
		rowsFiltersOpen
	]);
	const handleRefresh = useCallback(() => {
		refetch();
	}, [refetch]);
	const showRowsSearch = hasActiveFilters || (requestedTotal ?? total) > 0;
	useMysqlTableHeaderSlot({
		searchPlaceholder: showRowsSearch ? "Search rows..." : void 0,
		searchValue: showRowsSearch ? urlSearch ?? "" : void 0,
		onSearchChange: showRowsSearch ? handleSearchChange : void 0,
		createLabel: canWrite ? t("Create row") : void 0,
		onCreate: canWrite ? openCreateRow : void 0,
		createDisabled: !canWrite,
		createDisabledTooltip: writeTooltip,
		showRefresh: true,
		onRefresh: handleRefresh,
		isRefreshing: isFetching,
		filterTrigger
	});
	const emptyContent = hasActiveFilters ? /* @__PURE__ */ jsxs("div", {
		className: "text-center",
		children: [/* @__PURE__ */ jsx("p", {
			className: "text-[13px] font-medium text-foreground",
			children: t("No records match your filters")
		}), /* @__PURE__ */ jsx("p", {
			className: "mt-1 text-[13px] text-muted-foreground",
			children: t("Try adjusting or clearing filters.")
		})]
	}) : /* @__PURE__ */ jsx(MysqlTableRowsEmptyState, {});
	const selectedRowKeys = useMemo(() => new Set(selectedRows.keys()), [selectedRows]);
	return /* @__PURE__ */ jsxs(MysqlRowsEditSessionProvider, {
		projectId,
		databaseId,
		tableId,
		canWrite,
		children: [
			/* @__PURE__ */ jsx("div", {
				className: "flex min-h-0 flex-1 flex-col overflow-hidden",
				children: /* @__PURE__ */ jsx(MysqlRowsSpreadsheet, {
					databaseId,
					tableId,
					columns: tableColumns,
					rows,
					rowNumberOffset: (displayedPage - 1) * pageSize,
					canWrite,
					isLoading: isLoading && rows.length === 0,
					emptyContent,
					onOpenRow: openRowInDrawer,
					selectedRowKeys: canWrite ? selectedRowKeys : void 0,
					onToggleRow: canWrite ? handleToggleRow : void 0,
					onToggleAllRows: canWrite ? handleToggleAllRows : void 0,
					currentPage: displayedPage,
					totalItems: requestedFetching ? total : requestedTotal ?? total,
					pageSize,
					onPageChange: handlePageChange,
					onPageSizeChange: handlePageSizeChange,
					onPaginationInteract: handlePaginationInteract
				})
			}),
			canWrite ? /* @__PURE__ */ jsx(MysqlRowsBulkDelete, {
				projectId,
				databaseId,
				tableId,
				selectedRows,
				dialogOpen: bulkDeleteDialogOpen,
				onDialogOpenChange: setBulkDeleteDialogOpen,
				onClearSelection: clearSelection,
				onDeleted: (rowKeys) => {
					if (drawerRowKey && rowKeys.includes(drawerRowKey)) {
						setDrawerOpen(false);
						setDrawerRow(null);
						setDrawerIdentity(null);
					}
				}
			}) : null,
			/* @__PURE__ */ jsx(MysqlRowEditDrawer, {
				open: drawerOpen,
				onOpenChange: setDrawerOpen,
				projectId,
				databaseId,
				tableId,
				row: drawerRow,
				identity: drawerIdentity,
				columns: tableColumns,
				focusedField: drawerFocusedField,
				canWrite
			})
		]
	});
}
function MysqlRowsBulkDelete({ projectId, databaseId, tableId, selectedRows, dialogOpen, onDialogOpenChange, onClearSelection, onDeleted }) {
	const t = useT();
	const editSession = useMysqlRowsEditSession();
	const deleteMutation = useDeleteMysqlTableRows(projectId, databaseId, tableId);
	const selectedCount = selectedRows.size;
	const handleConfirmDelete = () => {
		if (selectedCount === 0) return;
		const rowKeys = Array.from(selectedRows.keys());
		const identities = Array.from(selectedRows.values());
		deleteMutation.mutate(identities, {
			onSuccess: () => {
				editSession?.discardRows(rowKeys);
				onDeleted(rowKeys);
				onClearSelection();
				onDialogOpenChange(false);
				toast.success(`${t("Successfully deleted")} ${selectedCount} ${selectedCount === 1 ? t("row") : t("rows")}`);
			},
			onError: (error) => {
				toast.error(getErrorMessage(error) ?? t("Failed to delete rows"));
			}
		});
	};
	if (selectedCount === 0) return null;
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
		className: "fixed bottom-4 start-1/2 z-50 w-[min(100%,calc(100vw-2rem))] max-w-md -translate-x-1/2 px-2 sm:px-0 sm:w-auto sm:max-w-none",
		children: /* @__PURE__ */ jsxs("div", {
			className: "mx-auto flex min-w-0 items-center justify-between gap-2 rounded-lg border border-border bg-background px-4 py-3 sm:min-w-[400px] sm:gap-3 sm:px-6",
			children: [/* @__PURE__ */ jsx(Badge, {
				variant: "secondary",
				className: "h-6 px-2.5",
				children: selectedCount === 1 ? t("1 row selected") : `${selectedCount} ${t("rows selected")}`
			}), /* @__PURE__ */ jsxs("div", {
				className: "flex items-center gap-2",
				children: [/* @__PURE__ */ jsx(Button, {
					variant: "ghost",
					size: "sm",
					onClick: onClearSelection,
					className: "h-8 text-xs",
					children: t("Cancel")
				}), /* @__PURE__ */ jsx(Button, {
					variant: "destructive",
					size: "sm",
					onClick: () => onDialogOpenChange(true),
					disabled: deleteMutation.isPending,
					className: "h-8 gap-2",
					children: t("Delete")
				})]
			})]
		})
	}), /* @__PURE__ */ jsx(Dialog, {
		open: dialogOpen,
		onOpenChange: onDialogOpenChange,
		children: /* @__PURE__ */ jsxs(DialogContent, {
			className: "sm:max-w-md p-0",
			children: [/* @__PURE__ */ jsxs(DialogHeader, {
				className: "px-6 pt-6 pb-4 text-start",
				children: [/* @__PURE__ */ jsx(DialogTitle, { children: selectedCount === 1 ? t("Delete row") : t("Delete rows") }), /* @__PURE__ */ jsxs(DialogDescription, {
					className: "text-[13px] mt-2",
					children: [
						t("Are you sure you want to delete"),
						" ",
						selectedCount,
						" ",
						selectedCount === 1 ? t("row") : t("rows"),
						"?",
						" ",
						t("This action cannot be undone.")
					]
				})]
			}), /* @__PURE__ */ jsxs("div", {
				className: "px-6 py-4 border-t border-border bg-muted/30 flex flex-col-reverse gap-2 sm:flex-row sm:justify-end",
				children: [/* @__PURE__ */ jsx(Button, {
					variant: "outline",
					onClick: () => onDialogOpenChange(false),
					disabled: deleteMutation.isPending,
					children: t("Cancel")
				}), /* @__PURE__ */ jsx(Button, {
					variant: "destructive",
					onClick: handleConfirmDelete,
					disabled: deleteMutation.isPending,
					children: t("Delete")
				})]
			})]
		})
	})] });
}
function MysqlRowsPage() {
	const { databaseId, tableId } = Route.useParams();
	return /* @__PURE__ */ jsx(MysqlTableRowsView, {
		databaseId,
		tableId: normalizeMysqlTableRouteId(tableId)
	});
}
export { MysqlRowsPage as component };
