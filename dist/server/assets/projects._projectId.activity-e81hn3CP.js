import { t as icons_exports } from "./icons-Dg0oCYUO.js";
import { a as truncateMiddle, t as cn } from "./utils-DoqqkI3X.js";
import "./runtime-config-DK7G0iKr.js";
import "./debug-endpoint-BvungD5q.js";
import { d as sdk } from "./sdk-DjIJ_hjn.js";
import "./console-session-cookie-7RV5Zfr4.js";
import { f as useDebugOverrides } from "./i18n-Db4baE06.js";
import "./ticket-layout-B97VGq99.js";
import "./ticket-types-BpqSrvYB.js";
import { n as useT, t as translate } from "./translate-DZcqveGn.js";
import "./error-formatting-CL2hjGy5.js";
import "./console-projects-C0b0tMaH.js";
import "./constants-CL7SLzjY.js";
import "./constants-Dd6QzW31.js";
import { t as ACTIVITY_DEFAULT_PAGE_SIZE } from "./constants-BDeF927R.js";
import { Mt as useOrganizationPlan, ln as getPlanNameFromTier } from "./organizations-BKtnlNrj.js";
import "./console-profiles-D__E5Kgi.js";
import { Js as normalizeCountryCode, Ws as useCountryLookups, ii as activityEventQueryOptions, oi as useProjectActivities, qs as getCountryDisplayName, si as useProjectActivity } from "./hooks-BONwG3Mt.js";
import "./auth-BPuxYQAc.js";
import { R as useProject } from "./projects-BaTJenfQ.js";
import "./appwrite-id-L15yEGeF.js";
import "./databases-Dh0pwZ6h.js";
import "./database-specs-CBc802K0.js";
import "./database-row-inline-edits-CdyGeTxj.js";
import { I as getActivitiesFilterColumns, gt as buildFilterQueryString, pt as queryParamToMap, ut as mapToQueryParam } from "./form-field-type-badge-C7qMzJo0.js";
import "./chart-interval-Dbrn19qD.js";
import "./is-marketing-page-dgx45Oqy.js";
import "./use-console-impersonation-revision-BiI0c7pX.js";
import "./affiliates-BOg1SHC6.js";
import "./social-stats-X1CQqP0k.js";
import "./cimd-CRIktQxf.js";
import "./account-applications-Bo8bAeE0.js";
import { t as formatLocalizedDate } from "./date-format-BD1j7PxK.js";
import "./invite-url-B18y3cBt.js";
import { r as createCompactCountAxisTickFormatter } from "./format-metric-6jsfxd5f.js";
import "./domains-Bfw8HsXF.js";
import "./parse-spec-DW3UGcrS.js";
import "./page-direction-CnacIIOa.js";
import { t as Button } from "./button-Bnm2QhOm.js";
import "./input-yKHNPhDZ.js";
import { n as CollapsibleContent, r as CollapsibleTrigger, t as Collapsible } from "./collapsible-BcDIDOgI.js";
import "./popover-BjTNxuf9.js";
import "./tabs-XaWkg9jR.js";
import "./select-BYGLGp-f.js";
import { a as openInNewWindow, i as openInNewTab, r as copyToClipboard, t as buildConsoleUrl } from "./context-menu-D55xedo-.js";
import { t as CodeBlock } from "./CodeBlock-BGAzMP_K.js";
import "./WizardLayout-DWqXFGuX.js";
import { a as TableHeader, i as TableHead, n as TableBody, o as TableRow, r as TableCell, t as Table$1 } from "./table-CsPM4E9L.js";
import "./RequireAuth-DahioVQe.js";
import "./HeaderAlertBar-CK7Gy4sE.js";
import "./modal-auto-focus-BO41bKmA.js";
import "./dialog-CFWmsJbI.js";
import "./command-Cizl9kMJ.js";
import { t as Skeleton } from "./skeleton-8d0Q_D56.js";
import { t as InitialsAvatar } from "./Avatar-D1PavDBA.js";
import "./use-localized-date-format-Dy8J1Ssn.js";
import { t as DateTooltip } from "./DateTooltip-wgOggQgS.js";
import { t as EmptyState } from "./EmptyState-DZDwceHm.js";
import "./dropdown-menu-DH51wH-m.js";
import { t as Pagination } from "./Pagination-BDei8M4v.js";
import { n as AlertDescription, r as AlertTitle, t as Alert } from "./alert-BTaNwkUC.js";
import "./sheet-CbM5lIV1.js";
import { t as BaseDrawer } from "./BaseDrawer-B4vv4Sf_.js";
import { a as ContextMenuSub, c as ContextMenuTrigger, i as ContextMenuSeparator, n as ContextMenuContent, o as ContextMenuSubContent, r as ContextMenuItem, s as ContextMenuSubTrigger, t as ContextMenu } from "./context-menu-Ca6WjjAw.js";
import { t as ContextMenuIcon } from "./ContextMenuIcon-DPnw7e0V.js";
import { t as McpIcon } from "./McpIcon-D1Jv-oq2.js";
import "./calendar-6OJ5dwYN.js";
import "./use-media-min-width-T-T6WgXi.js";
import { t as DateRangePicker } from "./DateRangePicker-BwmpXdP_.js";
import { i as TooltipTrigger, n as TooltipContent, r as TooltipProvider, t as Tooltip } from "./tooltip-DUssQZhw.js";
import { A as warn, Gt as Layer, j as Tooltip$1, k as ResponsiveContainer, n as YAxis } from "./CartesianChart-IK-OMdOm.js";
import { n as Bar, r as Cell, t as BarChart } from "./BarChart-DNAI4HHT.js";
import { o as USAGE_CHART_Y_AXIS_WIDTH, t as SeriesChartXAxis } from "./ChartXAxis-Sg7PTtJF.js";
import "./chart-panel-CCGEGd61.js";
import "./use-console-profile-DiUZxZ_6.js";
import { t as HostnameFaviconIcon } from "./HostnameFaviconIcon-BEkCxwPN.js";
import { t as ChartSeriesDot } from "./ChartSeriesDot-DRSaLZ-2.js";
import { t as CopyableId } from "./CopyableId-DPIWAPIb.js";
import "./DateTimePicker-DySgezub.js";
import { t as Route$1 } from "./projects._projectId.activity-DqRiJkfG.js";
import "./SearchableSelect-DPl0hr1b.js";
import "./RefreshButton-BA9lQ7jC.js";
import { t as formatIpForDisplay } from "./format-ip-BZEMAGGm.js";
import { t as ServiceHeader } from "./ServiceHeader-C9TuKD27.js";
import "./ToolbarCountBadge-WR-HvODH.js";
import { t as FiltersPopover } from "./FiltersPopover-De49yhdY.js";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { Link, getRouteApi } from "@tanstack/react-router";
import * as React$1 from "react";
import { cloneElement, createElement, isValidElement, memo, useCallback, useEffect, useId, useMemo, useRef, useState } from "react";
import { useIsFetching, useQueryClient } from "@tanstack/react-query";
import { endOfDay, startOfDay, subDays, subHours } from "date-fns";
import { toast } from "sonner";
import { ChevronDown, Copy, ExternalLink, FileJson, LayoutList, Link2, Square } from "lucide-react";
var _excluded = ["component"];
function _objectWithoutProperties(e, t) {
	if (null == e) return {};
	var o, r, i = _objectWithoutPropertiesLoose(e, t);
	if (Object.getOwnPropertySymbols) {
		var n = Object.getOwnPropertySymbols(e);
		for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
	}
	return i;
}
function _objectWithoutPropertiesLoose(r, e) {
	if (null == r) return {};
	var t = {};
	for (var n in r) if ({}.hasOwnProperty.call(r, n)) {
		if (-1 !== e.indexOf(n)) continue;
		t[n] = r[n];
	}
	return t;
}
function Customized(_ref) {
	var { component } = _ref, props = _objectWithoutProperties(_ref, _excluded);
	var child;
	if (/* @__PURE__ */ isValidElement(component)) child = /* @__PURE__ */ cloneElement(component, props);
	else if (typeof component === "function") child = /* @__PURE__ */ createElement(component, props);
	else warn(false, "Customized's props `component` must be React.element or Function, but got %s.", typeof component);
	return /* @__PURE__ */ React$1.createElement(Layer, { className: "recharts-customized-wrapper" }, child);
}
Customized.displayName = "Customized";
var SIMPLE_ROOTS = new Set([
	"bucket",
	"database",
	"file",
	"function",
	"identity",
	"message",
	"migrations",
	"provider",
	"report",
	"rule",
	"schedule",
	"site",
	"sites",
	"subscriber",
	"target",
	"team",
	"token",
	"tokens",
	"topic",
	"user",
	"vectorsdb",
	"webhook"
]);
function normalizeRootSegment(segment) {
	const s = segment.trim().toLowerCase();
	if (s === "sites") return "site";
	return s;
}
function normalizePairKey(key) {
	const k = key.trim().toLowerCase();
	if (k === "collections") return "collection";
	return k;
}
function isProjectDottedScope(first) {
	return first.startsWith("project.") && first.length > 8 && first !== "projections";
}
function parseActivityResourcePath(input) {
	if (input == null) return null;
	const raw = input.trim();
	if (!raw) return null;
	const segments = raw.split("/").map((s) => s.trim()).filter((s) => s.length > 0);
	if (segments.length === 0) return null;
	const [first, ...rest] = segments;
	const rootNorm = normalizeRootSegment(first);
	if (isProjectDottedScope(first)) {
		const scope = first.slice(8);
		const id = rest[0] ?? null;
		return {
			raw,
			root: "project",
			projectScope: scope || null,
			databaseId: null,
			pairs: scope && id != null && id !== "" ? [{
				key: scope,
				value: id
			}] : [],
			leafId: id,
			isRecognizedPattern: true
		};
	}
	if (rootNorm === "project" && rest.length >= 1) return {
		raw,
		root: "project",
		projectScope: null,
		databaseId: null,
		pairs: [],
		leafId: rest.join("/") || null,
		isRecognizedPattern: true
	};
	if (rootNorm === "database") {
		const databaseId = rest[0] ?? null;
		const pairs = [];
		for (let i = 1; i + 1 < rest.length; i += 2) {
			const key = normalizePairKey(rest[i]);
			const value = rest[i + 1];
			pairs.push({
				key,
				value
			});
		}
		return {
			raw,
			root: "database",
			projectScope: null,
			databaseId,
			pairs,
			leafId: (pairs.length > 0 ? pairs[pairs.length - 1].value : databaseId) ?? null,
			isRecognizedPattern: true
		};
	}
	if (rootNorm === "vectorsdb") {
		const pairs = [];
		for (let i = 0; i + 1 < rest.length; i += 2) pairs.push({
			key: normalizePairKey(rest[i]),
			value: rest[i + 1]
		});
		return {
			raw,
			root: "vectorsdb",
			projectScope: null,
			databaseId: null,
			pairs,
			leafId: pairs.length > 0 ? pairs[pairs.length - 1].value : rest[0] ?? null,
			isRecognizedPattern: true
		};
	}
	if (SIMPLE_ROOTS.has(rootNorm)) {
		const pairs = [];
		if (rest.length >= 2) for (let i = 0; i + 1 < rest.length - 1; i += 2) pairs.push({
			key: normalizePairKey(rest[i]),
			value: rest[i + 1]
		});
		return {
			raw,
			root: rootNorm,
			projectScope: null,
			databaseId: null,
			pairs,
			leafId: rest.length > 0 ? rest[rest.length - 1] : null,
			isRecognizedPattern: true
		};
	}
	return {
		raw,
		root: rootNorm,
		projectScope: null,
		databaseId: null,
		pairs: [],
		leafId: rest[0] ?? null,
		isRecognizedPattern: false
	};
}
function inferActivityUiResourceTypeFromPath(parsed) {
	const { root, pairs } = parsed;
	if (root === "site") return "site";
	if (root === "rule") return "rule";
	if (root === "bucket") return "bucket";
	if (root === "file") return "file";
	if (root === "function") return "function";
	if (root === "user") return "user";
	if (root === "team") return "team";
	if (root === "database") {
		const keys = new Set(pairs.map((p) => p.key));
		if (keys.has("document")) return "document";
		if (keys.has("collection")) return "collection";
		return "database";
	}
	if (root === "project") return "project";
	return "project";
}
var UNLIMITED_ACTIVITY_LOG_RETENTION_THRESHOLD = 36500;
function getActivityLogRetentionDaysFromPlan(plan) {
	const days = plan?.activityLogs;
	if (days == null || !Number.isFinite(days) || days <= 0) return 30;
	if (days >= UNLIMITED_ACTIVITY_LOG_RETENTION_THRESHOLD) return UNLIMITED_ACTIVITY_LOG_RETENTION_THRESHOLD;
	return days;
}
function getActivityLogRetentionHoursFromPlan(plan) {
	return getActivityLogRetentionDaysFromPlan(plan) * 24;
}
function getDefaultActivityDateRangeFromRetentionDays(days) {
	const now = /* @__PURE__ */ new Date();
	const safeDays = !Number.isFinite(days) || days <= 0 ? 30 : days >= UNLIMITED_ACTIVITY_LOG_RETENTION_THRESHOLD ? 30 : days;
	const hoursExact = safeDays * 24;
	if (hoursExact < 24) return {
		from: subHours(now, Math.max(1, Math.round(hoursExact))),
		to: now
	};
	return {
		from: startOfDay(subDays(now, Math.max(1, Math.round(safeDays)) - 1)),
		to: endOfDay(now)
	};
}
function formatActivityLogRetentionLabel(days) {
	if (!Number.isFinite(days) || days <= 0 || days >= UNLIMITED_ACTIVITY_LOG_RETENTION_THRESHOLD) return "Unlimited";
	const hoursExact = days * 24;
	if (hoursExact < 24) {
		const hours = Math.max(1, Math.round(hoursExact));
		return hours === 1 ? "1 hour" : `${hours} hours`;
	}
	const wholeDays = Math.max(1, Math.round(days));
	return wholeDays === 1 ? "1 day" : `${wholeDays} days`;
}
function formatActivityEventJson(event) {
	return JSON.stringify(event, null, 2);
}
function getActivityCountryCode(event) {
	const legacyCode = normalizeCountryCode(event.countryCode);
	if (legacyCode) return legacyCode.toLowerCase();
	const fromCountry = normalizeCountryCode(event.country);
	return fromCountry ? fromCountry.toLowerCase() : null;
}
function getActivityCountryDisplayName(event, lookups) {
	const legacyName = event.countryName?.trim();
	if (legacyName) return legacyName;
	const code = getActivityCountryCode(event);
	if (code) return getCountryDisplayName(code, lookups) ?? code.toUpperCase();
	const country = event.country?.trim();
	if (country) return getCountryDisplayName(country, lookups);
	return null;
}
function isRegularUserType(actorType) {
	const normalized = (actorType ?? "").toLowerCase();
	return normalized === "user" || normalized === "users";
}
function isMcpSdkActivity(event) {
	if (!event) return false;
	if (event.sdk?.trim().toLowerCase() === "mcp") return true;
	return /^AppwriteMCP(?:\/|\b)/i.test(event.userAgent?.trim() ?? "");
}
function hasHumanEmail(actorType) {
	const normalized = (actorType ?? "").toLowerCase();
	return isRegularUserType(actorType) || normalized === "admin";
}
function userTypeBadge(actorType) {
	const normalized = (actorType ?? "").toLowerCase();
	if (isRegularUserType(actorType)) return {
		label: "User",
		tone: "bg-emerald-500/10 text-emerald-600 dark:text-emerald-400"
	};
	if (normalized === "admin") return {
		label: "Admin",
		tone: "bg-violet-500/10 text-violet-600 dark:text-violet-400"
	};
	if (normalized === "guest") return {
		label: "Guest",
		tone: "bg-sky-500/10 text-sky-600 dark:text-sky-400"
	};
	if (normalized === "keyproject") return {
		label: "Project key",
		tone: "bg-amber-500/10 text-amber-600 dark:text-amber-400"
	};
	if (normalized === "keyaccount") return {
		label: "Account key",
		tone: "bg-amber-500/10 text-amber-600 dark:text-amber-400"
	};
	if (normalized === "keyorganization") return {
		label: "Org key",
		tone: "bg-amber-500/10 text-amber-600 dark:text-amber-400"
	};
	if (normalized.startsWith("key")) return {
		label: "API key",
		tone: "bg-amber-500/10 text-amber-600 dark:text-amber-400"
	};
	return {
		label: "System",
		tone: "bg-slate-500/10 text-slate-600 dark:text-slate-400"
	};
}
var avatarFrame = "flex h-6 w-6 shrink-0 items-center justify-center rounded-full border border-border/50 shadow-sm";
function UserTypeAvatar({ actorType, actorName, className }) {
	const t = useT();
	if (isRegularUserType(actorType)) return /* @__PURE__ */ jsx(InitialsAvatar, {
		name: actorName,
		size: "sm",
		className: cn(avatarFrame, className)
	});
	const normalized = (actorType ?? "").toLowerCase();
	let icon;
	let tone;
	let label;
	if (normalized === "admin") {
		icon = /* @__PURE__ */ jsx(icons_exports.ShieldUser, { className: "h-3.5 w-3.5" });
		tone = "bg-violet-500/10 text-violet-600 dark:text-violet-400";
		label = "Admin";
	} else if (normalized === "guest") {
		icon = /* @__PURE__ */ jsx(icons_exports.Ghost, { className: "h-3.5 w-3.5" });
		tone = "bg-sky-500/10 text-sky-600 dark:text-sky-400";
		label = "Guest";
	} else if (normalized === "keyproject" || normalized === "keyaccount" || normalized === "keyorganization" || normalized.startsWith("key")) {
		icon = /* @__PURE__ */ jsx(icons_exports.Key, { className: "h-3.5 w-3.5" });
		tone = "bg-amber-500/10 text-amber-600 dark:text-amber-400";
		if (normalized === "keyproject") label = "Project API key";
		else if (normalized === "keyaccount") label = "Account API key";
		else if (normalized === "keyorganization") label = "Organization API key";
		else label = "API key";
	} else {
		icon = /* @__PURE__ */ jsx(icons_exports.Server, { className: "h-3.5 w-3.5" });
		tone = "bg-slate-500/10 text-slate-600 dark:text-slate-400";
		label = "System";
	}
	return /* @__PURE__ */ jsx("div", {
		className: cn(avatarFrame, tone, className),
		"aria-label": t(label),
		title: t(label),
		children: icon
	});
}
var actionLabels$1 = {
	create: "Created",
	update: "Updated",
	delete: "Deleted",
	execute: "Executed",
	upload: "Uploaded",
	login: "Logged in",
	logout: "Logged out",
	view: "Viewed"
};
var actionIcons$1 = {
	create: /* @__PURE__ */ jsx(icons_exports.Plus, { className: "h-4 w-4" }),
	update: /* @__PURE__ */ jsx(icons_exports.Pencil, { className: "h-4 w-4" }),
	delete: /* @__PURE__ */ jsx(icons_exports.Trash2, { className: "h-4 w-4" }),
	execute: /* @__PURE__ */ jsx(icons_exports.Zap, { className: "h-4 w-4" }),
	upload: /* @__PURE__ */ jsx(icons_exports.Upload, { className: "h-4 w-4" }),
	login: /* @__PURE__ */ jsx(icons_exports.LogIn, { className: "h-4 w-4" }),
	logout: /* @__PURE__ */ jsx(icons_exports.LogOut, { className: "h-4 w-4" }),
	view: /* @__PURE__ */ jsx(icons_exports.Activity, { className: "h-4 w-4" })
};
var actionColors$1 = {
	create: "bg-emerald-500/10 text-emerald-600 dark:text-emerald-400",
	update: "bg-blue-500/10 text-blue-600 dark:text-blue-400",
	delete: "bg-red-500/10 text-red-600 dark:text-red-400",
	execute: "bg-amber-500/10 text-amber-600 dark:text-amber-400",
	upload: "bg-violet-500/10 text-violet-600 dark:text-violet-400",
	login: "bg-cyan-500/10 text-cyan-600 dark:text-cyan-400",
	logout: "bg-slate-500/10 text-slate-600 dark:text-slate-400",
	view: "bg-indigo-500/10 text-indigo-600 dark:text-indigo-400"
};
var resourceIcons$1 = {
	document: /* @__PURE__ */ jsx(icons_exports.FileText, { className: "h-4 w-4" }),
	collection: /* @__PURE__ */ jsx(icons_exports.Folder, { className: "h-4 w-4" }),
	database: /* @__PURE__ */ jsx(icons_exports.Database, { className: "h-4 w-4" }),
	file: /* @__PURE__ */ jsx(icons_exports.FileText, { className: "h-4 w-4" }),
	bucket: /* @__PURE__ */ jsx(icons_exports.Folder, { className: "h-4 w-4" }),
	function: /* @__PURE__ */ jsx(icons_exports.Zap, { className: "h-4 w-4" }),
	user: /* @__PURE__ */ jsx(icons_exports.User, { className: "h-4 w-4" }),
	team: /* @__PURE__ */ jsx(icons_exports.User, { className: "h-4 w-4" }),
	site: /* @__PURE__ */ jsx(icons_exports.Globe, { className: "h-4 w-4" }),
	rule: /* @__PURE__ */ jsx(icons_exports.ListChecks, { className: "h-4 w-4" }),
	project: /* @__PURE__ */ jsx(icons_exports.Server, { className: "h-4 w-4" })
};
function ActivityResourcePrimary({ resourceType, resourceId, resourceLabel }) {
	const t = useT();
	const primary = resourceId?.trim() || resourceLabel.trim() || "-";
	return /* @__PURE__ */ jsxs("div", {
		className: "flex min-w-0 items-center gap-2.5",
		children: [/* @__PURE__ */ jsx("div", {
			className: "flex h-7 w-7 shrink-0 items-center justify-center rounded-md bg-muted text-muted-foreground",
			children: resourceIcons$1[resourceType]
		}), /* @__PURE__ */ jsxs("div", {
			className: "min-w-0 flex flex-col gap-1",
			children: [/* @__PURE__ */ jsx("p", {
				className: "break-words text-[13px] font-medium leading-snug text-foreground",
				children: primary
			}), /* @__PURE__ */ jsx("p", {
				className: "text-[11px] capitalize text-muted-foreground",
				children: t(resourceType)
			})]
		})]
	});
}
function DetailSection({ title, children, bodyClassName }) {
	return /* @__PURE__ */ jsxs("section", {
		className: "rounded-xl border border-border bg-card/40 overflow-hidden",
		children: [/* @__PURE__ */ jsx("div", {
			className: "border-b border-border bg-muted/20 px-4 py-2.5",
			children: /* @__PURE__ */ jsx("h3", {
				className: "text-[11px] font-semibold uppercase tracking-wider text-muted-foreground",
				children: title
			})
		}), /* @__PURE__ */ jsx("div", {
			className: cn("px-4 py-3", bodyClassName),
			children
		})]
	});
}
function DetailField({ label, children, className }) {
	return /* @__PURE__ */ jsxs("div", {
		className: cn("min-w-0", className),
		children: [/* @__PURE__ */ jsx("p", {
			className: "text-[10px] font-medium uppercase tracking-wider text-muted-foreground",
			children: label
		}), /* @__PURE__ */ jsx("div", {
			className: "mt-1",
			children
		})]
	});
}
function formatValue(value) {
	const v = value?.trim();
	return v && v.length > 0 ? v : "-";
}
var AVATAR_SERVICE_FRAME = "flex h-6 w-6 shrink-0 items-center justify-center overflow-hidden rounded-lg border border-border/50 bg-muted/30";
var AVATAR_SERVICE_IMG = "h-full w-full object-contain p-0.5";
var AVATAR_SERVICE_FETCH_PX = 40;
function ActivityBrowserIcon({ code }) {
	const [failed, setFailed] = useState(false);
	const trimmed = code?.trim() ?? "";
	if (!trimmed || failed) return /* @__PURE__ */ jsx("div", {
		className: AVATAR_SERVICE_FRAME,
		"aria-hidden": true,
		children: /* @__PURE__ */ jsx(icons_exports.Monitor, { className: "h-3.5 w-3.5 text-muted-foreground" })
	});
	return /* @__PURE__ */ jsx("div", {
		className: AVATAR_SERVICE_FRAME,
		children: /* @__PURE__ */ jsx("img", {
			src: sdk.forConsole.avatars.getBrowser({
				code: trimmed,
				width: AVATAR_SERVICE_FETCH_PX,
				height: AVATAR_SERVICE_FETCH_PX
			}),
			alt: "",
			className: AVATAR_SERVICE_IMG,
			onError: () => setFailed(true)
		})
	});
}
function ActivityLogDrawer({ open, onOpenChange, event, display }) {
	const t = useT();
	const { lookups: countryLookups } = useCountryLookups();
	const handleCopyActivityPermalink = useCallback(() => {
		const id = event?.$id;
		if (!id) return;
		const url = new URL(window.location.href);
		url.searchParams.set("event", id);
		navigator.clipboard.writeText(url.toString()).then(() => {
			toast.success(t("Link to this activity copied"));
		}, () => {
			toast.error(t("Could not copy link"));
		});
	}, [event?.$id, t]);
	if (!event || !display) return null;
	const badge = userTypeBadge(event.actorType);
	const action = display.action;
	const resourceType = display.resourceType;
	const secondaryLine = hasHumanEmail(event.actorType) ? formatValue(event.actorEmail) : formatValue(event.actorId);
	const rawJson = formatActivityEventJson(event);
	const clientSummary = [event.clientName, event.clientVersion ? `v${event.clientVersion}` : ""].filter(Boolean).join(" ");
	const osSummary = [event.osName || event.osCode, event.osVersion ? `(${event.osVersion})` : ""].filter(Boolean).join(" ");
	const deviceSummary = [event.deviceBrand, event.deviceModel || event.deviceName].filter(Boolean).join(" · ");
	const countryNameLine = getActivityCountryDisplayName(event, countryLookups) ?? "";
	const countryCode = getActivityCountryCode(event) ?? "";
	const flagUrl = countryCode.length === 2 ? sdk.forConsole.avatars.getFlag({
		code: countryCode,
		width: AVATAR_SERVICE_FETCH_PX,
		height: AVATAR_SERVICE_FETCH_PX,
		quality: 100
	}) : null;
	const flagAlt = countryNameLine ? `${countryNameLine} flag` : countryCode ? `${countryCode.toUpperCase()} flag` : t("Location unknown");
	return /* @__PURE__ */ jsx(BaseDrawer, {
		open,
		onOpenChange,
		title: t("Activity log"),
		description: `${t("Details for activity")} ${event.$id}`,
		maxWidth: "sm:max-w-xl",
		side: "right",
		headerActions: /* @__PURE__ */ jsx(TooltipProvider, {
			delayDuration: 0,
			children: /* @__PURE__ */ jsxs(Tooltip, { children: [/* @__PURE__ */ jsx(TooltipTrigger, {
				asChild: true,
				children: /* @__PURE__ */ jsxs(Button, {
					type: "button",
					variant: "ghost",
					size: "sm",
					className: "h-8 w-8 shrink-0 p-0",
					onClick: handleCopyActivityPermalink,
					children: [/* @__PURE__ */ jsx(icons_exports.Link2, { className: "h-4 w-4" }), /* @__PURE__ */ jsx("span", {
						className: "sr-only",
						children: t("Copy link to this activity")
					})]
				})
			}), /* @__PURE__ */ jsx(TooltipContent, {
				side: "bottom",
				children: t("Copy link to this activity")
			})] })
		}),
		children: /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", { className: "border-t border-border shrink-0" }), /* @__PURE__ */ jsx("div", {
			className: "flex flex-1 flex-col min-h-0",
			children: /* @__PURE__ */ jsx("div", {
				className: "flex-1 overflow-y-auto",
				children: /* @__PURE__ */ jsxs("div", {
					className: "space-y-5 px-6 py-6",
					children: [
						/* @__PURE__ */ jsx("div", {
							className: "rounded-xl border border-border bg-gradient-to-br from-muted/40 via-background to-background p-4",
							children: /* @__PURE__ */ jsxs("div", {
								className: "flex flex-wrap items-start gap-4",
								children: [/* @__PURE__ */ jsx("div", {
									className: cn("flex h-12 w-12 shrink-0 items-center justify-center rounded-lg", actionColors$1[action]),
									children: actionIcons$1[action]
								}), /* @__PURE__ */ jsxs("div", {
									className: "min-w-0 flex-1",
									children: [
										/* @__PURE__ */ jsxs("div", {
											className: "flex flex-wrap items-center gap-2",
											children: [/* @__PURE__ */ jsx("span", {
												className: "text-[16px] font-semibold tracking-tight text-foreground",
												children: t(actionLabels$1[action])
											}), /* @__PURE__ */ jsx("span", {
												className: "rounded-md border border-border bg-background/80 px-2 py-0.5 font-mono text-[11px] text-muted-foreground",
												children: event.event
											})]
										}),
										/* @__PURE__ */ jsx("div", {
											className: "mt-1.5",
											children: /* @__PURE__ */ jsx(ActivityResourcePrimary, {
												resourceType,
												resourceId: event.resourceId,
												resourceLabel: display.resourceName
											})
										}),
										/* @__PURE__ */ jsxs("div", {
											className: "mt-3 flex flex-wrap items-center gap-2",
											children: [/* @__PURE__ */ jsx("span", {
												className: cn("rounded px-2 py-0.5 text-[10px] font-semibold uppercase tracking-wider", badge.tone),
												children: t(badge.label)
											}), /* @__PURE__ */ jsx("span", {
												className: "text-[12px] text-muted-foreground",
												children: /* @__PURE__ */ jsx(DateTooltip, {
													date: event.time,
													showFormattedDate: true,
													className: "text-[12px]"
												})
											})]
										})
									]
								})]
							})
						}),
						/* @__PURE__ */ jsx(DetailSection, {
							title: t("Actor"),
							children: /* @__PURE__ */ jsxs("div", {
								className: "grid gap-4 sm:grid-cols-2",
								children: [
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Name"),
										children: /* @__PURE__ */ jsxs("div", {
											className: "flex items-center gap-3",
											children: [/* @__PURE__ */ jsx(UserTypeAvatar, {
												actorType: event.actorType,
												actorName: event.actorName?.trim() || event.actorEmail?.trim() || t("Unknown")
											}), /* @__PURE__ */ jsxs("div", {
												className: "flex min-w-0 items-center gap-1.5",
												children: [/* @__PURE__ */ jsx("p", {
													className: "min-w-0 truncate text-[13px] font-medium text-foreground",
													children: formatValue(event.actorName)
												}), isMcpSdkActivity(event) ? /* @__PURE__ */ jsxs(Tooltip, { children: [/* @__PURE__ */ jsx(TooltipTrigger, {
													asChild: true,
													children: /* @__PURE__ */ jsx("span", {
														className: "inline-flex shrink-0 text-muted-foreground",
														"aria-label": t("Via MCP"),
														children: /* @__PURE__ */ jsx(McpIcon, { className: "h-3.5 w-3.5" })
													})
												}), /* @__PURE__ */ jsx(TooltipContent, {
													side: "top",
													children: t("Via MCP")
												})] }) : null]
											})]
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: hasHumanEmail(event.actorType) ? t("Email") : t("Actor ID"),
										children: /* @__PURE__ */ jsx("p", {
											className: cn("truncate text-[13px] text-muted-foreground", !hasHumanEmail(event.actorType) && "font-mono"),
											children: secondaryLine
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Actor type"),
										children: /* @__PURE__ */ jsx("span", {
											className: cn("inline-flex rounded px-2 py-0.5 text-[10px] font-semibold uppercase tracking-wider", badge.tone),
											children: formatValue(event.actorType)
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Actor ID"),
										children: event.actorId?.trim() ? /* @__PURE__ */ jsx(CopyableId, {
											id: event.actorId,
											size: "xs",
											maxWidth: 220
										}) : /* @__PURE__ */ jsx("p", {
											className: "font-mono text-[12px] text-muted-foreground",
											children: "-"
										})
									})
								]
							})
						}),
						/* @__PURE__ */ jsx(DetailSection, {
							title: t("Resource"),
							children: /* @__PURE__ */ jsxs("div", {
								className: "grid gap-4 sm:grid-cols-2",
								children: [
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Resource"),
										className: "sm:col-span-2",
										children: /* @__PURE__ */ jsx(ActivityResourcePrimary, {
											resourceType,
											resourceId: event.resourceId,
											resourceLabel: display.resourceName
										})
									}),
									event.resourceType?.trim() ? /* @__PURE__ */ jsx(DetailField, {
										label: t("Resource type (API)"),
										className: "sm:col-span-2",
										children: /* @__PURE__ */ jsx("p", {
											className: "font-mono text-[12px] text-muted-foreground",
											children: event.resourceType
										})
									}) : null,
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Resource path"),
										className: "sm:col-span-2",
										children: /* @__PURE__ */ jsx("p", {
											className: "break-all rounded-md border border-border bg-muted/30 px-2.5 py-2 font-mono text-[11px] leading-relaxed text-muted-foreground",
											children: formatValue(event.resource)
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Resource parent"),
										className: "sm:col-span-2",
										children: /* @__PURE__ */ jsx("p", {
											className: "break-all rounded-md border border-border bg-muted/30 px-2.5 py-2 font-mono text-[11px] leading-relaxed text-muted-foreground",
											children: formatValue(event.resourceParent)
										})
									})
								]
							})
						}),
						/* @__PURE__ */ jsx(DetailSection, {
							title: t("Request context"),
							children: /* @__PURE__ */ jsxs("div", {
								className: "grid gap-4 sm:grid-cols-2",
								children: [
									/* @__PURE__ */ jsx(DetailField, {
										label: t("IP address"),
										children: event.ip?.trim() ? /* @__PURE__ */ jsx(CopyableId, {
											id: event.ip.trim(),
											displayText: formatIpForDisplay(event.ip.trim(), 44) ?? event.ip.trim(),
											size: "md",
											maxWidth: 360,
											className: "max-w-full"
										}) : /* @__PURE__ */ jsx("p", {
											className: "font-mono text-[12px] text-foreground",
											children: "-"
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Hostname"),
										children: /* @__PURE__ */ jsxs("div", {
											className: "flex items-center gap-2",
											children: [/* @__PURE__ */ jsx(HostnameFaviconIcon, {
												hostname: event.hostname,
												size: "md"
											}), /* @__PURE__ */ jsx("p", {
												className: "min-w-0 break-all font-mono text-[12px] text-muted-foreground",
												children: formatValue(event.hostname)
											})]
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: t("User agent"),
										className: "sm:col-span-2",
										children: /* @__PURE__ */ jsx("p", {
											className: "break-all rounded-md border border-border bg-muted/30 px-2.5 py-2 font-mono text-[11px] leading-relaxed text-muted-foreground",
											children: formatValue(event.userAgent)
										})
									})
								]
							})
						}),
						/* @__PURE__ */ jsx(DetailSection, {
							title: t("Client & device"),
							children: /* @__PURE__ */ jsxs("div", {
								className: "grid gap-4 sm:grid-cols-2",
								children: [
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Client"),
										children: /* @__PURE__ */ jsxs("div", {
											className: "flex items-start gap-2",
											children: [/* @__PURE__ */ jsx(ActivityBrowserIcon, { code: event.clientCode }), /* @__PURE__ */ jsxs("div", {
												className: "min-w-0",
												children: [/* @__PURE__ */ jsx("p", {
													className: "text-[13px] text-foreground",
													children: clientSummary || "-"
												}), /* @__PURE__ */ jsx("p", {
													className: "mt-0.5 font-mono text-[11px] text-muted-foreground",
													children: event.clientType || event.clientCode ? [event.clientType, event.clientCode].filter(Boolean).join(" · ") : "-"
												})]
											})]
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Engine"),
										children: /* @__PURE__ */ jsx("p", {
											className: "font-mono text-[12px] text-muted-foreground",
											children: [event.clientEngine, event.clientEngineVersion].filter((s) => s?.trim()).join(" ") || "-"
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Operating system"),
										children: /* @__PURE__ */ jsx("p", {
											className: "text-[13px] text-foreground",
											children: osSummary || "-"
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Device"),
										children: /* @__PURE__ */ jsx("p", {
											className: "text-[13px] text-foreground",
											children: deviceSummary || "-"
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Location"),
										className: "sm:col-span-2",
										children: /* @__PURE__ */ jsxs("div", {
											className: "flex items-start gap-2.5",
											children: [flagUrl ? /* @__PURE__ */ jsx("div", {
												className: AVATAR_SERVICE_FRAME,
												role: "img",
												"aria-label": flagAlt,
												children: /* @__PURE__ */ jsx("img", {
													src: flagUrl,
													alt: "",
													className: AVATAR_SERVICE_IMG
												})
											}) : /* @__PURE__ */ jsx("div", {
												className: AVATAR_SERVICE_FRAME,
												"aria-hidden": true,
												children: /* @__PURE__ */ jsx(icons_exports.Globe, { className: "h-3.5 w-3.5 text-muted-foreground" })
											}), /* @__PURE__ */ jsx("div", {
												className: "min-w-0 flex flex-col gap-0.5",
												children: /* @__PURE__ */ jsx("p", {
													className: "text-[13px] text-foreground",
													children: countryNameLine || "-"
												})
											})]
										})
									})
								]
							})
						}),
						/* @__PURE__ */ jsx(DetailSection, {
							title: t("Scope"),
							children: /* @__PURE__ */ jsxs("div", {
								className: "grid gap-4 sm:grid-cols-2",
								children: [
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Project ID"),
										children: /* @__PURE__ */ jsx(CopyableId, {
											id: event.projectId,
											size: "xs",
											maxWidth: 220
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Team ID"),
										children: /* @__PURE__ */ jsx(CopyableId, {
											id: event.teamId,
											size: "xs",
											maxWidth: 220
										})
									}),
									/* @__PURE__ */ jsx(DetailField, {
										label: t("Event ID"),
										className: "sm:col-span-2",
										children: /* @__PURE__ */ jsx(CopyableId, {
											id: event.$id,
											size: "xs",
											maxWidth: 280
										})
									})
								]
							})
						}),
						/* @__PURE__ */ jsx(DetailSection, {
							title: t("Raw payload"),
							bodyClassName: "p-0",
							children: /* @__PURE__ */ jsx(CodeBlock, {
								code: rawJson,
								language: "json",
								variant: "headless",
								copyInside: true
							})
						})
					]
				})
			})
		})] })
	});
}
var BRAND_SECONDARY_100 = {
	mint: "#85DBD8",
	purple: "#7C67FE",
	orange: "#FE9567"
};
var RESOURCE_SERIES = [
	{
		key: "user",
		label: "User"
	},
	{
		key: "database",
		label: "Database"
	},
	{
		key: "function",
		label: "Function"
	},
	{
		key: "file",
		label: "File"
	},
	{
		key: "bucket",
		label: "Bucket"
	},
	{
		key: "document",
		label: "Document"
	},
	{
		key: "collection",
		label: "Collection"
	},
	{
		key: "team",
		label: "Team"
	},
	{
		key: "site",
		label: "Site"
	},
	{
		key: "rule",
		label: "Rule"
	},
	{
		key: "project",
		label: "Project"
	}
];
var OPACITY_STEPS = [
	1,
	.91,
	.82,
	.71,
	.6,
	.48,
	.34
];
var SHADES_PER_BASE = OPACITY_STEPS.length;
var SERIES_BASE_COLORS = [
	BRAND_SECONDARY_100.mint,
	BRAND_SECONDARY_100.purple,
	BRAND_SECONDARY_100.orange
];
var BAR_GLOBAL_ALPHA = .86;
function hexToRgb(hex) {
	const n = hex.replace("#", "").trim();
	if (n.length !== 6 || !/^[0-9a-fA-F]{6}$/.test(n)) return null;
	return {
		r: Number.parseInt(n.slice(0, 2), 16),
		g: Number.parseInt(n.slice(2, 4), 16),
		b: Number.parseInt(n.slice(4, 6), 16)
	};
}
function rgba(hex, alpha) {
	const rgb = hexToRgb(hex);
	if (!rgb) return hex;
	const a = Math.min(1, Math.max(0, alpha));
	return `rgba(${rgb.r},${rgb.g},${rgb.b},${a})`;
}
function seriesShade(seriesIndex) {
	const baseIdx = Math.floor(seriesIndex / SHADES_PER_BASE);
	const stepIdx = seriesIndex % SHADES_PER_BASE;
	return {
		baseHex: SERIES_BASE_COLORS[baseIdx % SERIES_BASE_COLORS.length],
		opacity: OPACITY_STEPS[stepIdx] ?? OPACITY_STEPS[OPACITY_STEPS.length - 1]
	};
}
function effectiveBarAlpha(seriesIndex) {
	const { opacity } = seriesShade(seriesIndex);
	return Math.min(1, opacity * BAR_GLOBAL_ALPHA);
}
function seriesFill(seriesIndex) {
	const { baseHex, opacity } = seriesShade(seriesIndex);
	return rgba(baseHex, Math.min(1, opacity * BAR_GLOBAL_ALPHA));
}
function seriesFillHover(seriesIndex) {
	const { baseHex, opacity } = seriesShade(seriesIndex);
	return rgba(baseHex, Math.min(1, (opacity + .22) * BAR_GLOBAL_ALPHA));
}
function seriesFillEmphasis(seriesIndex) {
	const { baseHex, opacity } = seriesShade(seriesIndex);
	return rgba(baseHex, Math.min(1, (opacity + .38) * BAR_GLOBAL_ALPHA));
}
function mulberry32(seed) {
	let a = seed >>> 0;
	return () => {
		a += 1831565813;
		let t = a;
		t = Math.imul(t ^ t >>> 15, t | 1);
		t ^= t + Math.imul(t ^ t >>> 7, t | 61);
		return ((t ^ t >>> 14) >>> 0) / 4294967296;
	};
}
function shuffledIndices(length, rng) {
	const arr = Array.from({ length }, (_, i) => i);
	for (let i = length - 1; i > 0; i--) {
		const j = Math.floor(rng() * (i + 1));
		const tmp = arr[i];
		arr[i] = arr[j];
		arr[j] = tmp;
	}
	return arr;
}
function colorSlotPermutationSeed(rangeFrom, rangeTo) {
	return (rangeFrom.getTime() ^ Math.imul(rangeTo.getTime(), 2654435769)) >>> 0;
}
function ChartPlotBottomFade({ offset, gradientId }) {
	if (!offset?.width || !offset.height) return null;
	const { left, top, width, height } = offset;
	return /* @__PURE__ */ jsxs("g", {
		pointerEvents: "none",
		"aria-hidden": true,
		children: [/* @__PURE__ */ jsx("defs", { children: /* @__PURE__ */ jsxs("linearGradient", {
			id: gradientId,
			x1: "0",
			y1: "0",
			x2: "0",
			y2: "1",
			children: [
				/* @__PURE__ */ jsx("stop", {
					offset: "0%",
					stopColor: "var(--background)",
					stopOpacity: "0"
				}),
				/* @__PURE__ */ jsx("stop", {
					offset: "62%",
					stopColor: "var(--background)",
					stopOpacity: "0"
				}),
				/* @__PURE__ */ jsx("stop", {
					offset: "100%",
					stopColor: "var(--background)",
					stopOpacity: "0.2"
				})
			]
		}) }), /* @__PURE__ */ jsx("rect", {
			x: left,
			y: top,
			width,
			height,
			fill: `url(#${gradientId})`
		})]
	});
}
function parseActiveBarIndex(activeTooltipIndex) {
	if (activeTooltipIndex === void 0 || activeTooltipIndex === null) return null;
	const n = typeof activeTooltipIndex === "number" ? activeTooltipIndex : Number.parseInt(String(activeTooltipIndex), 10);
	return Number.isNaN(n) ? null : n;
}
function pointerInChartWrapper(e) {
	const ne = e.nativeEvent;
	if (!(ne instanceof MouseEvent)) return {
		x: 0,
		y: 0,
		w: 0,
		h: 0
	};
	const el = e.currentTarget;
	if (!(el instanceof HTMLElement)) return {
		x: 0,
		y: 0,
		w: 0,
		h: 0
	};
	const rect = el.getBoundingClientRect();
	const scaleX = rect.width / el.offsetWidth || 1;
	const scaleY = rect.height / el.offsetHeight || 1;
	return {
		x: Math.round((ne.clientX - rect.left) / scaleX),
		y: Math.round((ne.clientY - rect.top) / scaleY),
		w: el.offsetWidth,
		h: el.offsetHeight
	};
}
function clampVolumeTooltipPosition(px, py, chartW, chartH) {
	const GAP = 36;
	const EDGE = 6;
	const EST_W = 300;
	const EST_H = 220;
	let x = px + GAP;
	let y = py + GAP;
	if (chartW > 0 && x + EST_W > chartW - EDGE) x = px - GAP - EST_W;
	if (chartW > 0) x = Math.min(Math.max(EDGE, x), Math.max(EDGE, chartW - EST_W - EDGE));
	if (chartH > 0 && y + EST_H > chartH - EDGE) y = py - GAP - EST_H;
	if (chartH > 0) y = Math.min(Math.max(EDGE, y), Math.max(EDGE, chartH - EST_H - EDGE));
	return {
		x,
		y
	};
}
function buildMockVolumeByResource(from, to) {
	const spanMs = Math.max(to.getTime() - from.getTime(), 6e4);
	const hours = spanMs / (3600 * 1e3);
	const targetPoints = hours <= 36 ? 18 : hours <= 168 ? 14 : Math.min(20, Math.max(10, Math.ceil(hours / 24)));
	const step = spanMs / targetPoints;
	return Array.from({ length: targetPoints + 1 }, (_, i) => {
		const t = new Date(from.getTime() + i * step);
		const label = spanMs < 2880 * 60 * 1e3 ? formatLocalizedDate(t, "MMM d, HH:mm") : spanMs < 336 * 60 * 60 * 1e3 ? formatLocalizedDate(t, "EEE d") : formatLocalizedDate(t, "MMM d");
		const base = 12 + i % 4 * 6;
		const row = {
			t: t.toISOString(),
			label,
			...Object.fromEntries(RESOURCE_SERIES.map(({ key }) => [key, 0]))
		};
		RESOURCE_SERIES.forEach(({ key }, s) => {
			const wave = Math.sin(i / 2.2 + s * .4) * 14 + Math.cos(i / 3.5 + s * .6) * 9 + i / targetPoints * 18;
			row[key] = Math.max(0, Math.round(base + wave + s * 5));
		});
		return row;
	});
}
function VolumeTooltip({ active, payload, label, highlightedDataKey, activeResourceTypeFilter }) {
	const t = useT();
	if (!active || !payload?.length) return null;
	const heading = typeof label === "string" || typeof label === "number" ? String(label) : "";
	const total = payload.reduce((sum, p) => sum + (Number(p.value) || 0), 0);
	const hi = highlightedDataKey != null ? String(highlightedDataKey) : null;
	const filterKey = activeResourceTypeFilter != null && RESOURCE_SERIES.some((r) => r.key === activeResourceTypeFilter) ? activeResourceTypeFilter : null;
	return /* @__PURE__ */ jsxs("div", {
		className: "min-w-[280px] max-w-[min(calc(100vw-2rem),22rem)] rounded-lg border border-border bg-popover px-3 py-2.5",
		children: [
			/* @__PURE__ */ jsx("p", {
				className: "mb-2 text-[12px] font-medium text-foreground",
				children: heading
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "space-y-1.5",
				children: [payload.map((p) => {
					const rowKey = String(p.dataKey ?? "");
					const isSelected = hi !== null && rowKey === hi || hi === null && filterKey != null && rowKey === filterKey;
					return /* @__PURE__ */ jsxs("div", {
						className: cn("flex min-h-[28px] items-center justify-between gap-8 rounded-md px-1.5 -mx-1 py-0.5", isSelected && "bg-muted/55"),
						children: [/* @__PURE__ */ jsxs("span", {
							className: cn("flex min-w-0 items-center gap-1.5 text-[11px] font-medium leading-5 tabular-nums", isSelected ? "text-foreground" : "text-muted-foreground"),
							children: [typeof p.color === "string" ? /* @__PURE__ */ jsx(ChartSeriesDot, { color: p.color }) : null, /* @__PURE__ */ jsx("span", {
								className: "min-w-0 truncate",
								children: typeof p.name === "string" ? t(p.name) : p.name
							})]
						}), /* @__PURE__ */ jsx("span", {
							className: cn("shrink-0 text-[12px] font-medium tabular-nums leading-5", isSelected ? "text-foreground" : "text-muted-foreground"),
							children: (p.value ?? 0).toLocaleString()
						})]
					}, rowKey || String(p.name));
				}), /* @__PURE__ */ jsxs("div", {
					className: "flex items-center justify-between gap-8 border-t border-border pt-1.5",
					children: [/* @__PURE__ */ jsx("span", {
						className: "text-[11px] text-muted-foreground",
						children: t("Total")
					}), /* @__PURE__ */ jsx("span", {
						className: "text-[12px] font-medium text-foreground tabular-nums",
						children: total.toLocaleString()
					})]
				})]
			}),
			/* @__PURE__ */ jsx("p", {
				className: "mt-2 text-[10px] text-muted-foreground/50",
				children: t("Mock data")
			})
		]
	});
}
function volumeStackedBarsPropsEqual(prev, next) {
	return prev.hoveredIndex === next.hoveredIndex && prev.hoveredResourceKey === next.hoveredResourceKey && prev.chartData === next.chartData && prev.colorSlots === next.colorSlots && prev.filteredResourceKey === next.filteredResourceKey && prev.lastSeriesIndex === next.lastSeriesIndex && prev.onBarSegmentPointer === next.onBarSegmentPointer;
}
var VolumeChartStackedBars = memo(function VolumeChartStackedBars$1({ chartData, colorSlots, hoveredIndex, hoveredResourceKey, filteredResourceKey, lastSeriesIndex, onBarSegmentPointer }) {
	return /* @__PURE__ */ jsx(Fragment, { children: RESOURCE_SERIES.map(({ key, label }, seriesIndex) => /* @__PURE__ */ jsx(Bar, {
		name: label,
		dataKey: key,
		stackId: "vol",
		isAnimationActive: false,
		fill: seriesFill(colorSlots[seriesIndex]),
		onMouseEnter: (_data, index) => onBarSegmentPointer(key, index),
		onMouseMove: (_data, index) => onBarSegmentPointer(key, index),
		radius: lastSeriesIndex === 0 ? [
			5,
			5,
			5,
			5
		] : seriesIndex === 0 ? [
			0,
			0,
			5,
			5
		] : seriesIndex === lastSeriesIndex ? [
			5,
			5,
			0,
			0
		] : [
			0,
			0,
			0,
			0
		],
		children: chartData.map((_, index) => {
			const slot = colorSlots[seriesIndex];
			const colActive = hoveredIndex === index;
			const hasSegmentHover = hoveredResourceKey !== null;
			const isHoveredSegment = colActive && hasSegmentHover && key === hoveredResourceKey;
			const isDimmedSiblingInColumn = colActive && hasSegmentHover && key !== hoveredResourceKey;
			const isOtherColumn = hoveredIndex !== null && !colActive;
			let fill;
			let cellOpacity = 1;
			if (isHoveredSegment) fill = seriesFillEmphasis(slot);
			else if (isDimmedSiblingInColumn) {
				fill = seriesFill(slot);
				cellOpacity = .32;
			} else if (colActive) fill = seriesFillHover(slot);
			else {
				fill = seriesFill(slot);
				if (isOtherColumn) cellOpacity = .5;
			}
			if (filteredResourceKey != null && key !== filteredResourceKey && !isHoveredSegment) cellOpacity *= .42;
			else if (filteredResourceKey != null && key === filteredResourceKey && !isHoveredSegment && !isDimmedSiblingInColumn) fill = seriesFillHover(slot);
			return /* @__PURE__ */ jsx(Cell, {
				fill,
				opacity: cellOpacity
			}, `${key}-${index}`);
		})
	}, key)) });
}, volumeStackedBarsPropsEqual);
function ActivityLogVolumeChart({ rangeFrom, rangeTo, className, activeResourceTypeFilter, onLegendResourceTypeClick }) {
	const t = useT();
	const plotBottomFadeId = useId().replace(/:/g, "");
	const [open, setOpen] = useState(true);
	const [hoveredIndex, setHoveredIndex] = useState(null);
	const [hoveredResourceKey, setHoveredResourceKey] = useState(null);
	const [tooltipPointer, setTooltipPointer] = useState(null);
	const lastTooltipColumnRef = useRef(null);
	const tooltipPointerRafRef = useRef(null);
	const pendingTooltipPointerRef = useRef(null);
	const flushTooltipPointer = useCallback(() => {
		tooltipPointerRafRef.current = null;
		const next = pendingTooltipPointerRef.current;
		pendingTooltipPointerRef.current = null;
		if (!next) return;
		setTooltipPointer((prev) => prev != null && prev.x === next.x && prev.y === next.y && prev.w === next.w && prev.h === next.h ? prev : next);
	}, []);
	const scheduleTooltipPointerUpdate = useCallback((next) => {
		pendingTooltipPointerRef.current = next;
		if (tooltipPointerRafRef.current !== null) return;
		tooltipPointerRafRef.current = requestAnimationFrame(flushTooltipPointer);
	}, [flushTooltipPointer]);
	useEffect(() => {
		return () => {
			if (tooltipPointerRafRef.current !== null) {
				cancelAnimationFrame(tooltipPointerRafRef.current);
				tooltipPointerRafRef.current = null;
			}
		};
	}, []);
	const chartData = useMemo(() => buildMockVolumeByResource(rangeFrom, rangeTo), [rangeFrom, rangeTo]);
	const chartAxisMax = useMemo(() => chartData.reduce((max, row) => {
		const total = RESOURCE_SERIES.reduce((sum, { key }) => sum + (Number(row[key]) || 0), 0);
		return Math.max(max, total);
	}, 0), [chartData]);
	const yAxisTickFormatter = useMemo(() => createCompactCountAxisTickFormatter(chartAxisMax), [chartAxisMax]);
	const colorSlots = useMemo(() => {
		const rng = mulberry32(colorSlotPermutationSeed(rangeFrom, rangeTo));
		return shuffledIndices(RESOURCE_SERIES.length, rng);
	}, [rangeFrom, rangeTo]);
	const legendDotLight = useMemo(() => new Set(RESOURCE_SERIES.map((_, i) => i).filter((i) => effectiveBarAlpha(colorSlots[i]) < .75)), [colorSlots]);
	const filteredResourceKey = useMemo(() => {
		if (activeResourceTypeFilter == null || !RESOURCE_SERIES.some((r) => r.key === activeResourceTypeFilter)) return null;
		return activeResourceTypeFilter;
	}, [activeResourceTypeFilter]);
	const chartAnimationKey = useMemo(() => `${rangeFrom.toISOString()}-${rangeTo.toISOString()}-${chartData.length}`, [
		rangeFrom,
		rangeTo,
		chartData.length
	]);
	const lastSeriesIndex = RESOURCE_SERIES.length - 1;
	const handleBarSegmentPointer = useCallback((key, dataIndex) => {
		lastTooltipColumnRef.current = dataIndex;
		setHoveredResourceKey(key);
	}, []);
	return /* @__PURE__ */ jsx(Collapsible, {
		open,
		onOpenChange: setOpen,
		className: cn("flex w-full flex-col border-b border-border", className),
		children: /* @__PURE__ */ jsxs("div", {
			className: "px-4 py-4 sm:px-6",
			children: [/* @__PURE__ */ jsxs("div", {
				className: cn("flex items-center justify-between gap-3", open && "mb-3"),
				children: [/* @__PURE__ */ jsx("h3", {
					className: "min-w-0 text-[13px] font-medium text-foreground",
					children: t("Volume by resource")
				}), /* @__PURE__ */ jsx(CollapsibleTrigger, {
					asChild: true,
					children: /* @__PURE__ */ jsxs(Button, {
						type: "button",
						variant: "ghost",
						size: "sm",
						className: "h-8 shrink-0 gap-1.5 px-2 text-[12px] text-muted-foreground hover:text-foreground",
						"aria-expanded": open,
						"aria-label": open ? t("Hide volume chart") : t("Show volume chart"),
						children: [/* @__PURE__ */ jsx(ChevronDown, { className: cn("h-4 w-4 transition-transform duration-200", open && "-rotate-180") }), open ? t("Hide") : t("Show")]
					})
				})]
			}), /* @__PURE__ */ jsx(CollapsibleContent, { children: /* @__PURE__ */ jsxs("div", {
				className: "flex flex-col",
				children: [/* @__PURE__ */ jsx("div", {
					className: "flex-1 text-muted-foreground",
					children: chartData.length === 0 ? /* @__PURE__ */ jsx("div", {
						className: "flex h-48 items-center justify-center rounded-lg border border-dashed border-border bg-muted/20 text-[13px] text-muted-foreground",
						children: t("No data for this date range")
					}) : /* @__PURE__ */ jsx("div", {
						className: cn("h-48 w-full overflow-visible animate-in fade-in-0 slide-in-from-bottom-1 duration-500 motion-reduce:animate-none"),
						children: /* @__PURE__ */ jsx(ResponsiveContainer, {
							width: "100%",
							height: "100%",
							children: /* @__PURE__ */ jsxs(BarChart, {
								data: chartData,
								margin: {
									top: 0,
									right: 0,
									left: 0,
									bottom: 0
								},
								barCategoryGap: "15%",
								onMouseMove: (state, e) => {
									const col = parseActiveBarIndex(state.activeTooltipIndex);
									if (col != null) {
										const prevCol = lastTooltipColumnRef.current;
										if (prevCol != null && col !== prevCol) setHoveredResourceKey(null);
										lastTooltipColumnRef.current = col;
									}
									setHoveredIndex((prev) => {
										if (col == null) return prev;
										return prev === col ? prev : col;
									});
									scheduleTooltipPointerUpdate(pointerInChartWrapper(e));
								},
								onMouseLeave: () => {
									if (tooltipPointerRafRef.current !== null) {
										cancelAnimationFrame(tooltipPointerRafRef.current);
										tooltipPointerRafRef.current = null;
									}
									pendingTooltipPointerRef.current = null;
									setHoveredIndex(null);
									setHoveredResourceKey(null);
									lastTooltipColumnRef.current = null;
									setTooltipPointer(null);
								},
								children: [
									/* @__PURE__ */ jsx(SeriesChartXAxis, {
										pointCount: chartData.length,
										dataKey: "label"
									}),
									/* @__PURE__ */ jsx(YAxis, {
										axisLine: false,
										tickLine: false,
										tick: {
											fill: "currentColor",
											fontSize: 10
										},
										tickFormatter: yAxisTickFormatter,
										dx: -5,
										width: 48
									}),
									/* @__PURE__ */ jsx(Tooltip$1, {
										content: /* @__PURE__ */ jsx(VolumeTooltip, {
											highlightedDataKey: hoveredResourceKey,
											activeResourceTypeFilter: filteredResourceKey
										}),
										cursor: false,
										isAnimationActive: false,
										allowEscapeViewBox: {
											x: true,
											y: true
										},
										wrapperStyle: {
											zIndex: 50,
											pointerEvents: "none"
										},
										position: tooltipPointer ? clampVolumeTooltipPosition(tooltipPointer.x, tooltipPointer.y, tooltipPointer.w, tooltipPointer.h) : void 0
									}),
									/* @__PURE__ */ jsx(VolumeChartStackedBars, {
										chartData,
										colorSlots,
										hoveredIndex,
										hoveredResourceKey,
										filteredResourceKey,
										lastSeriesIndex,
										onBarSegmentPointer: handleBarSegmentPointer
									}),
									/* @__PURE__ */ jsx(Customized, { component: (chartProps) => /* @__PURE__ */ jsx(ChartPlotBottomFade, {
										offset: chartProps.offset,
										gradientId: plotBottomFadeId
									}) })
								]
							})
						})
					}, chartAnimationKey)
				}), /* @__PURE__ */ jsx("div", {
					className: "mt-3 flex flex-wrap items-center justify-center gap-x-4 gap-y-2",
					children: RESOURCE_SERIES.map(({ key, label }, i) => {
						const isActive = filteredResourceKey === key;
						const interactive = !!onLegendResourceTypeClick;
						const content = /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
							className: cn("h-2 w-2 shrink-0 rounded-full", legendDotLight.has(i) && "ring-1 ring-border"),
							style: { backgroundColor: seriesFill(colorSlots[i]) }
						}), /* @__PURE__ */ jsx("span", {
							className: cn("text-[11px]", isActive ? "font-medium text-foreground" : "text-muted-foreground"),
							children: t(label)
						})] });
						if (!interactive) return /* @__PURE__ */ jsx("div", {
							className: "flex items-center gap-1.5 opacity-80",
							children: content
						}, key);
						return /* @__PURE__ */ jsx("button", {
							type: "button",
							"aria-pressed": isActive,
							"aria-label": `${t("Filter activities by")} ${t(label)}`,
							onClick: () => onLegendResourceTypeClick?.(key),
							className: cn("flex cursor-pointer items-center gap-1.5 rounded-md px-2 py-1 transition-colors", "hover:bg-muted/60 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background", isActive && "bg-muted/50 ring-1 ring-border"),
							children: content
						}, key);
					})
				})]
			}) })]
		})
	});
}
function activityPermalink(projectId, eventId) {
	return buildConsoleUrl(`/projects/${projectId}/activity?event=${encodeURIComponent(eventId)}`);
}
function ActivityLogRowContextMenu({ projectId, event, onOpenDetails, children }) {
	const t = useT();
	if (!event?.$id) return /* @__PURE__ */ jsx(Fragment, { children });
	const eventHref = activityPermalink(projectId, event.$id);
	const hasName = !!event.actorName?.trim();
	return /* @__PURE__ */ jsxs(ContextMenu, { children: [/* @__PURE__ */ jsx(ContextMenuTrigger, {
		asChild: true,
		children
	}), /* @__PURE__ */ jsxs(ContextMenuContent, {
		className: "w-56",
		children: [
			/* @__PURE__ */ jsxs(ContextMenuItem, {
				onSelect: () => onOpenDetails(),
				children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: LayoutList }), t("Overview")]
			}),
			/* @__PURE__ */ jsx(ContextMenuSeparator, {}),
			/* @__PURE__ */ jsxs(ContextMenuSub, { children: [/* @__PURE__ */ jsxs(ContextMenuSubTrigger, { children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Copy }), t("Copy")] }), /* @__PURE__ */ jsxs(ContextMenuSubContent, { children: [
				/* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: () => copyToClipboard("ID", event.$id),
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Copy }), t("Copy ID")]
				}),
				hasName && /* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: () => copyToClipboard("Name", event.actorName),
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Copy }), t("Copy name")]
				}),
				/* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: () => copyToClipboard("Link", eventHref),
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Link2 }), t("Copy link")]
				}),
				/* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: () => copyToClipboard("JSON", formatActivityEventJson(event)),
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: FileJson }), t("Copy as JSON")]
				})
			] })] }),
			/* @__PURE__ */ jsx(ContextMenuSeparator, {}),
			/* @__PURE__ */ jsxs(ContextMenuItem, {
				onSelect: () => openInNewTab(eventHref),
				children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: ExternalLink }), t("Open in new tab")]
			}),
			/* @__PURE__ */ jsxs(ContextMenuItem, {
				onSelect: () => openInNewWindow(eventHref),
				children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Square }), t("Open in new window")]
			})
		]
	})] });
}
var activityRouteApi = getRouteApi("/_public/projects/$projectId/activity");
var ACTIVITY_TABLE_SKELETON_ROWS_CAP = 24;
var ACTIVITY_RESOURCE_DISPLAY_MAX = 56;
function ActivityTableCountryCell({ countryName }) {
	const name = countryName?.trim() ?? "";
	return /* @__PURE__ */ jsx("p", {
		className: "truncate text-[13px] text-muted-foreground",
		title: name || void 0,
		children: name || "-"
	});
}
function ActivityLogsTableColGroup() {
	return /* @__PURE__ */ jsxs("colgroup", { children: [
		/* @__PURE__ */ jsx("col", { className: "w-[14%]" }),
		/* @__PURE__ */ jsx("col", { className: "w-[12%]" }),
		/* @__PURE__ */ jsx("col", { className: "w-[10%]" }),
		/* @__PURE__ */ jsx("col", { className: "" }),
		/* @__PURE__ */ jsx("col", { className: "w-[12%]" }),
		/* @__PURE__ */ jsx("col", { className: "w-[10%]" }),
		/* @__PURE__ */ jsx("col", { className: "w-[10%]" })
	] });
}
function ActivityLogsTableHead() {
	const t = useT();
	return /* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, {
		className: "hover:bg-transparent border-b border-border",
		children: [
			/* @__PURE__ */ jsx(TableHead, {
				className: "sticky top-0 z-10 bg-background px-4 py-3 text-[12px] font-semibold text-muted-foreground uppercase tracking-wider ps-6 sm:ps-8 shadow-[inset_0_-1px_0_var(--border)]",
				children: t("Event")
			}),
			/* @__PURE__ */ jsx(TableHead, {
				className: "sticky top-0 z-10 bg-background px-4 py-3 text-[12px] font-semibold text-muted-foreground uppercase tracking-wider shadow-[inset_0_-1px_0_var(--border)]",
				children: t("Actor")
			}),
			/* @__PURE__ */ jsx(TableHead, {
				className: "sticky top-0 z-10 bg-background px-4 py-3 text-[12px] font-semibold text-muted-foreground uppercase tracking-wider shadow-[inset_0_-1px_0_var(--border)]",
				children: t("Type")
			}),
			/* @__PURE__ */ jsx(TableHead, {
				className: "sticky top-0 z-10 bg-background px-4 py-3 text-[12px] font-semibold text-muted-foreground uppercase tracking-wider shadow-[inset_0_-1px_0_var(--border)]",
				children: t("Resource")
			}),
			/* @__PURE__ */ jsx(TableHead, {
				className: "sticky top-0 z-10 bg-background px-4 py-3 text-[12px] font-semibold text-muted-foreground uppercase tracking-wider shadow-[inset_0_-1px_0_var(--border)]",
				children: t("IP address")
			}),
			/* @__PURE__ */ jsx(TableHead, {
				className: "sticky top-0 z-10 bg-background px-4 py-3 text-[12px] font-semibold text-muted-foreground uppercase tracking-wider shadow-[inset_0_-1px_0_var(--border)]",
				children: t("Country")
			}),
			/* @__PURE__ */ jsx(TableHead, {
				className: "sticky top-0 z-10 bg-background px-4 py-3 pe-6 text-[12px] font-semibold text-muted-foreground uppercase tracking-wider sm:pe-8 shadow-[inset_0_-1px_0_var(--border)]",
				children: t("Time")
			})
		]
	}) });
}
function ActivityLogsSkeletonRows({ rowCount }) {
	return /* @__PURE__ */ jsx(Fragment, { children: Array.from({ length: rowCount }, (_, i) => /* @__PURE__ */ jsxs(TableRow, {
		className: "pointer-events-none hover:bg-transparent",
		"aria-hidden": true,
		children: [
			/* @__PURE__ */ jsx(TableCell, {
				className: "min-w-0 px-4 py-3 ps-6 sm:ps-8",
				children: /* @__PURE__ */ jsxs("div", {
					className: "flex min-w-0 items-center gap-2",
					children: [/* @__PURE__ */ jsx(Skeleton, { className: "h-7 w-7 shrink-0 rounded-md" }), /* @__PURE__ */ jsx(Skeleton, { className: "h-3.5 min-w-0 flex-1 max-w-full" })]
				})
			}),
			/* @__PURE__ */ jsx(TableCell, {
				className: "min-w-0 px-4 py-3",
				children: /* @__PURE__ */ jsxs("div", {
					className: "flex min-w-0 items-center gap-2.5",
					children: [/* @__PURE__ */ jsx(Skeleton, { className: "h-8 w-8 shrink-0 rounded-full" }), /* @__PURE__ */ jsxs("div", {
						className: "min-w-0 flex flex-1 flex-col gap-1",
						children: [/* @__PURE__ */ jsx(Skeleton, { className: "h-4 w-[7.5rem] max-w-full" }), /* @__PURE__ */ jsx(Skeleton, { className: "h-3 w-24 max-w-full" })]
					})]
				})
			}),
			/* @__PURE__ */ jsx(TableCell, {
				className: "min-w-0 whitespace-nowrap px-4 py-3",
				children: /* @__PURE__ */ jsx(Skeleton, { className: "h-5 w-20 rounded px-1.5" })
			}),
			/* @__PURE__ */ jsx(TableCell, {
				className: "min-w-0 whitespace-normal px-4 py-3 align-top",
				children: /* @__PURE__ */ jsxs("div", {
					className: "flex min-w-0 items-center gap-2.5",
					children: [/* @__PURE__ */ jsx(Skeleton, { className: "h-7 w-7 shrink-0 rounded-md" }), /* @__PURE__ */ jsxs("div", {
						className: "min-w-0 flex flex-1 flex-col gap-1",
						children: [/* @__PURE__ */ jsx(Skeleton, { className: "h-4 w-[11rem] max-w-full" }), /* @__PURE__ */ jsx(Skeleton, { className: "h-3 w-14 max-w-full" })]
					})]
				})
			}),
			/* @__PURE__ */ jsx(TableCell, {
				className: "min-w-0 px-4 py-3",
				children: /* @__PURE__ */ jsx(Skeleton, { className: "h-3.5 w-28 max-w-full" })
			}),
			/* @__PURE__ */ jsx(TableCell, {
				className: "min-w-0 px-4 py-3",
				children: /* @__PURE__ */ jsx(Skeleton, { className: "h-3.5 w-20 max-w-full" })
			}),
			/* @__PURE__ */ jsx(TableCell, {
				className: "min-w-0 px-4 py-3 pe-6 sm:pe-8",
				children: /* @__PURE__ */ jsx(Skeleton, { className: "h-3.5 w-[6.5rem]" })
			})
		]
	}, i)) });
}
function ActivityLogsPaginationSkeleton() {
	return /* @__PURE__ */ jsx("div", {
		className: "h-[54px] shrink-0 border-t border-border bg-background px-4 sm:px-6",
		children: /* @__PURE__ */ jsxs("div", {
			className: "@container flex h-full min-h-8 w-full items-center justify-between gap-2 py-3",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex min-w-0 items-center gap-3",
				children: [/* @__PURE__ */ jsx(Skeleton, { className: "hidden h-4 w-36 @[600px]:block" }), /* @__PURE__ */ jsxs("div", {
					className: "hidden items-center gap-2 @[800px]:flex",
					children: [
						/* @__PURE__ */ jsx(Skeleton, { className: "h-4 w-8" }),
						/* @__PURE__ */ jsx(Skeleton, { className: "h-8 w-[72px] rounded-md" }),
						/* @__PURE__ */ jsx(Skeleton, { className: "h-4 w-14" })
					]
				})]
			}), /* @__PURE__ */ jsx(Skeleton, { className: "h-8 w-[200px] max-w-[45%] shrink-0 rounded-md" })]
		})
	});
}
function ActivityLogsLoadingTable({ rowCount }) {
	const t = useT();
	const rows = Math.min(rowCount, ACTIVITY_TABLE_SKELETON_ROWS_CAP);
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
		className: "relative min-h-0 flex-1 overflow-auto",
		role: "status",
		"aria-live": "polite",
		"aria-busy": "true",
		"aria-label": t("Loading activities"),
		children: /* @__PURE__ */ jsxs(Table$1, {
			withScrollContainer: false,
			className: "table-fixed w-full",
			children: [
				/* @__PURE__ */ jsx(ActivityLogsTableColGroup, {}),
				/* @__PURE__ */ jsx(ActivityLogsTableHead, {}),
				/* @__PURE__ */ jsx(TableBody, { children: /* @__PURE__ */ jsx(ActivityLogsSkeletonRows, { rowCount: rows }) })
			]
		})
	}), /* @__PURE__ */ jsx(ActivityLogsPaginationSkeleton, {})] });
}
var actionIcons = {
	create: /* @__PURE__ */ jsx(icons_exports.Plus, { className: "h-3.5 w-3.5" }),
	update: /* @__PURE__ */ jsx(icons_exports.Pencil, { className: "h-3.5 w-3.5" }),
	delete: /* @__PURE__ */ jsx(icons_exports.Trash2, { className: "h-3.5 w-3.5" }),
	execute: /* @__PURE__ */ jsx(icons_exports.Zap, { className: "h-3.5 w-3.5" }),
	upload: /* @__PURE__ */ jsx(icons_exports.Upload, { className: "h-3.5 w-3.5" }),
	login: /* @__PURE__ */ jsx(icons_exports.LogIn, { className: "h-3.5 w-3.5" }),
	logout: /* @__PURE__ */ jsx(icons_exports.LogOut, { className: "h-3.5 w-3.5" }),
	view: /* @__PURE__ */ jsx(icons_exports.Eye, { className: "h-3.5 w-3.5" })
};
var actionColors = {
	create: "bg-emerald-500/10 text-emerald-600 dark:text-emerald-400",
	update: "bg-blue-500/10 text-blue-600 dark:text-blue-400",
	delete: "bg-red-500/10 text-red-600 dark:text-red-400",
	execute: "bg-amber-500/10 text-amber-600 dark:text-amber-400",
	upload: "bg-violet-500/10 text-violet-600 dark:text-violet-400",
	login: "bg-cyan-500/10 text-cyan-600 dark:text-cyan-400",
	logout: "bg-slate-500/10 text-slate-600 dark:text-slate-400",
	view: "bg-indigo-500/10 text-indigo-600 dark:text-indigo-400"
};
var actionLabels = {
	create: "Created",
	update: "Updated",
	delete: "Deleted",
	execute: "Executed",
	upload: "Uploaded",
	login: "Logged in",
	logout: "Logged out",
	view: "Viewed"
};
var resourceIcons = {
	document: /* @__PURE__ */ jsx(icons_exports.FileText, { className: "h-4 w-4" }),
	collection: /* @__PURE__ */ jsx(icons_exports.Folder, { className: "h-4 w-4" }),
	database: /* @__PURE__ */ jsx(icons_exports.Database, { className: "h-4 w-4" }),
	file: /* @__PURE__ */ jsx(icons_exports.FileText, { className: "h-4 w-4" }),
	bucket: /* @__PURE__ */ jsx(icons_exports.Folder, { className: "h-4 w-4" }),
	function: /* @__PURE__ */ jsx(icons_exports.Zap, { className: "h-4 w-4" }),
	user: /* @__PURE__ */ jsx(icons_exports.Users, { className: "h-4 w-4" }),
	team: /* @__PURE__ */ jsx(icons_exports.Users, { className: "h-4 w-4" }),
	site: /* @__PURE__ */ jsx(icons_exports.Globe, { className: "h-4 w-4" }),
	rule: /* @__PURE__ */ jsx(icons_exports.ListChecks, { className: "h-4 w-4" }),
	project: /* @__PURE__ */ jsx(icons_exports.Server, { className: "h-4 w-4" })
};
function eventToActionType(event) {
	const parts = event.split(".");
	if (parts.includes("sessions")) {
		if (parts.includes("create")) return "login";
		if (parts.includes("delete")) return "logout";
	}
	if (parts.includes("executions") && parts.includes("create")) return "execute";
	if (parts.includes("files") && parts.includes("create")) return "upload";
	if (parts.includes("create")) return "create";
	if (parts.includes("update")) return "update";
	if (parts.includes("upsert")) return "update";
	if (parts.includes("delete")) return "delete";
	return "view";
}
function resourcePathToResourceType(resource) {
	const parsed = parseActivityResourcePath(resource);
	if (!parsed?.isRecognizedPattern) return null;
	return inferActivityUiResourceTypeFromPath(parsed);
}
function eventToResourceType(resourceType, event, resource) {
	const normalized = (resourceType ?? "").toLowerCase();
	const eventParts = event.split(".");
	if (normalized.startsWith("document") || eventParts.includes("documents")) return "document";
	if (normalized.startsWith("collection") || eventParts.includes("collections")) return "collection";
	if (normalized.startsWith("database") || eventParts.includes("databases")) return "database";
	if (normalized.startsWith("file") || eventParts.includes("files")) return "file";
	if (normalized.startsWith("bucket") || eventParts.includes("buckets")) return "bucket";
	if (normalized.startsWith("function") || eventParts.includes("functions")) return "function";
	if (normalized.startsWith("site") || eventParts.includes("sites")) return "site";
	if (normalized === "rule" || normalized === "rules") return "rule";
	if (event.startsWith("rule.")) return "rule";
	if (normalized.startsWith("team") || eventParts.includes("teams")) return "team";
	if (normalized.startsWith("user") || eventParts.includes("users")) return "user";
	const fromPath = resourcePathToResourceType(resource);
	if (fromPath) return fromPath;
	return "project";
}
function resourceLabelFromEvent(activity) {
	const pathLeaf = parseActivityResourcePath(activity.resource)?.leafId;
	if (pathLeaf) return pathLeaf;
	if (activity.resourceId) return activity.resourceId;
	const segments = activity.event.split(".");
	const verbs = new Set([
		"create",
		"update",
		"delete",
		"upsert",
		"read",
		"list",
		"createSession",
		"deleteSession"
	]);
	for (let i = segments.length - 1; i >= 0; i--) {
		const seg = segments[i];
		if (!verbs.has(seg) && seg && !/^[a-z]+$/.test(seg)) return seg;
	}
	return activity.event;
}
function toDisplayActivity(event, countryLookups) {
	return {
		$id: event.$id,
		actorId: event.actorId,
		actorType: event.actorType || "",
		actorName: event.actorName || event.actorEmail || translate("Unknown"),
		actorEmail: event.actorEmail || "",
		action: eventToActionType(event.event),
		resourceType: eventToResourceType(event.resourceType, event.event, event.resource),
		resourceId: event.resourceId || "",
		resourceName: resourceLabelFromEvent(event),
		description: event.event,
		ipAddress: event.ip?.trim() || null,
		countryCode: getActivityCountryCode(event),
		countryName: getActivityCountryDisplayName(event, countryLookups),
		timestamp: event.time,
		rawEvent: event.event
	};
}
function View({ projectId }) {
	const t = useT();
	const queryClient = useQueryClient();
	const navigate = activityRouteApi.useNavigate();
	const { event: eventIdFromUrl, query: queryFromSearch } = activityRouteApi.useSearch();
	const { project } = useProject(projectId);
	const { plan: organizationPlan } = useOrganizationPlan(project?.teamId);
	const { showActivityChart } = useDebugOverrides();
	const { lookups: countryLookups, countries } = useCountryLookups();
	const [filtersOpen, setFiltersOpen] = useState(false);
	const [drawerOpen, setDrawerOpen] = useState(false);
	const [selectedEvent, setSelectedEvent] = useState(null);
	const [currentPage, setCurrentPage] = useState(1);
	const [listCursor, setListCursor] = useState({
		cursorAfter: null,
		cursorBefore: null
	});
	const [pageSize, setPageSize] = useState(150);
	const resetListPosition = useCallback(() => {
		setCurrentPage(1);
		setListCursor({
			cursorAfter: null,
			cursorBefore: null
		});
	}, []);
	const activityLogRetentionDays = useMemo(() => getActivityLogRetentionDaysFromPlan(organizationPlan), [organizationPlan]);
	const activityLogRetentionHours = useMemo(() => getActivityLogRetentionHoursFromPlan(organizationPlan), [organizationPlan]);
	const activityLogRetentionLabel = useMemo(() => formatActivityLogRetentionLabel(activityLogRetentionDays), [activityLogRetentionDays]);
	const isFreePlan = useMemo(() => getPlanNameFromTier(organizationPlan?.$id), [organizationPlan?.$id]) === "free";
	const defaultActivityDateRange = useMemo(() => getDefaultActivityDateRangeFromRetentionDays(activityLogRetentionDays), [activityLogRetentionDays]);
	const filterMap = useMemo(() => queryParamToMap(queryFromSearch ?? null), [queryFromSearch]);
	const activityFilterColumns = useMemo(() => {
		return getActivitiesFilterColumns(countries.map((country) => ({
			value: country.code.toLowerCase(),
			label: country.name
		})).sort((a, b) => a.label.localeCompare(b.label)));
	}, [countries]);
	const { events, hasMore, isLoading, refetch } = useProjectActivities({
		projectId,
		limit: pageSize,
		cursorAfter: listCursor.cursorAfter,
		cursorBefore: listCursor.cursorBefore,
		planRetentionHours: activityLogRetentionHours,
		filterQueryKey: queryFromSearch ?? null
	});
	const activityListRefreshing = useIsFetching({ queryKey: [
		"activities",
		"project",
		projectId
	] }) > 0;
	useEffect(() => {
		resetListPosition();
	}, [queryFromSearch, resetListPosition]);
	const dateRangeFromFilters = useMemo(() => {
		for (const [k] of filterMap) if (k.c === "time" && k.o === "between" && k.v != null) {
			const parts = (Array.isArray(k.v) ? k.v.join(",") : String(k.v)).split(",").map((p) => p.trim()).filter(Boolean);
			if (parts.length >= 2) {
				const from = new Date(parts[0]);
				const to = new Date(parts[1]);
				if (!Number.isNaN(from.getTime()) && !Number.isNaN(to.getTime())) return {
					from,
					to
				};
			}
		}
	}, [filterMap]);
	const dateRangeForPicker = dateRangeFromFilters ?? defaultActivityDateRange;
	const volumeChartRange = useMemo(() => {
		return {
			from: dateRangeFromFilters?.from ?? defaultActivityDateRange.from,
			to: dateRangeFromFilters?.to ?? defaultActivityDateRange.to
		};
	}, [dateRangeFromFilters, defaultActivityDateRange]);
	const applyFilter = useCallback((compactKey, queryStr, replaceKey) => {
		const normalizedKey = compactKey.c === "country" && compactKey.v != null && compactKey.v !== "" ? {
			...compactKey,
			v: Array.isArray(compactKey.v) ? compactKey.v.map((item) => String(item).toLowerCase()) : String(compactKey.v).toLowerCase()
		} : compactKey;
		const normalizedQueryStr = normalizedKey.c === "country" ? buildFilterQueryString(normalizedKey.o, normalizedKey.c, normalizedKey.v) : queryStr;
		const next = new Map(filterMap);
		if (replaceKey) next.delete(replaceKey);
		next.set(normalizedKey, normalizedQueryStr);
		navigate({
			search: (prev) => ({
				...prev,
				query: mapToQueryParam(next) || void 0
			}),
			replace: true
		});
	}, [filterMap, navigate]);
	const removeFilter = useCallback((compactKey) => {
		const next = new Map(filterMap);
		next.delete(compactKey);
		navigate({
			search: (prev) => ({
				...prev,
				query: next.size > 0 ? mapToQueryParam(next) : void 0
			}),
			replace: true
		});
	}, [filterMap, navigate]);
	const clearAllFilters = useCallback(() => {
		navigate({
			search: (prev) => {
				const { event: ev } = prev;
				return { ...ev ? { event: ev } : {} };
			},
			replace: true
		});
		setFiltersOpen(false);
	}, [navigate]);
	const handleDateRangeChange = useCallback((range) => {
		const next = new Map(filterMap);
		for (const key of [...next.keys()]) if (key.c === "time") next.delete(key);
		if (range?.from && range?.to) {
			const v = `${range.from.toISOString()},${range.to.toISOString()}`;
			const compactKey = {
				c: "time",
				o: "between",
				v
			};
			next.set(compactKey, buildFilterQueryString("between", "time", v));
		}
		navigate({
			search: (prev) => ({
				...prev,
				query: next.size > 0 ? mapToQueryParam(next) : void 0
			}),
			replace: true
		});
	}, [filterMap, navigate]);
	const activeResourceTypeFilter = useMemo(() => {
		for (const [k] of filterMap) if (k.c === "resourceType" && (k.o === "equal" || k.o === "is") && k.v != null) return String(k.v);
		return null;
	}, [filterMap]);
	const handleLegendResourceTypeClick = useCallback((resourceKey) => {
		const next = new Map(filterMap);
		for (const key of [...next.keys()]) if (key.c === "resourceType") next.delete(key);
		let current = null;
		for (const [k] of filterMap) if (k.c === "resourceType" && (k.o === "equal" || k.o === "is") && k.v != null) {
			current = String(k.v);
			break;
		}
		if (current === resourceKey) {
			navigate({
				search: (prev) => ({
					...prev,
					query: next.size > 0 ? mapToQueryParam(next) : void 0
				}),
				replace: true
			});
			return;
		}
		const compactKey = {
			c: "resourceType",
			o: "equal",
			v: resourceKey
		};
		next.set(compactKey, buildFilterQueryString("equal", "resourceType", resourceKey));
		navigate({
			search: (prev) => ({
				...prev,
				query: mapToQueryParam(next) || void 0
			}),
			replace: true
		});
	}, [filterMap, navigate]);
	const eventOnCurrentList = useMemo(() => eventIdFromUrl ? events.some((e) => e.$id === eventIdFromUrl) : false, [eventIdFromUrl, events]);
	const { event: fetchedEventById, error: singleEventError } = useProjectActivity(projectId, eventIdFromUrl && !eventOnCurrentList ? eventIdFromUrl : void 0);
	useEffect(() => {
		if (!eventIdFromUrl) return;
		const fromList = events.find((e) => e.$id === eventIdFromUrl);
		if (fromList) {
			setSelectedEvent(fromList);
			setDrawerOpen(true);
			return;
		}
		if (fetchedEventById?.$id === eventIdFromUrl) {
			setSelectedEvent(fetchedEventById);
			setDrawerOpen(true);
		}
	}, [
		eventIdFromUrl,
		events,
		fetchedEventById
	]);
	useEffect(() => {
		if (!eventIdFromUrl || !singleEventError) return;
		toast.error(t("Activity log not found or unavailable."));
		navigate({
			search: (prev) => ({
				...prev,
				event: void 0
			}),
			replace: true
		});
	}, [
		eventIdFromUrl,
		singleEventError,
		navigate,
		t
	]);
	useEffect(() => {
		if (eventIdFromUrl) return;
		if (!drawerOpen) return;
		setDrawerOpen(false);
		setSelectedEvent(null);
	}, [eventIdFromUrl, drawerOpen]);
	const handlePageChange = useCallback((nextPage) => {
		if (nextPage <= 1) {
			resetListPosition();
			return;
		}
		if (nextPage === currentPage + 1) {
			const lastId = events.at(-1)?.$id;
			if (!lastId) return;
			setCurrentPage(nextPage);
			setListCursor({
				cursorAfter: lastId,
				cursorBefore: null
			});
			return;
		}
		if (nextPage === currentPage - 1) {
			const firstId = events[0]?.$id;
			if (!firstId) return;
			setCurrentPage(nextPage);
			setListCursor({
				cursorAfter: null,
				cursorBefore: firstId
			});
		}
	}, [
		currentPage,
		events,
		resetListPosition
	]);
	const prefetchActivityEventOnHover = useCallback((eventId) => {
		if (!projectId || !eventId) return;
		queryClient.prefetchQuery(activityEventQueryOptions(projectId, eventId)).catch(() => {});
	}, [projectId, queryClient]);
	const openActivityDrawer = useCallback((event) => {
		setSelectedEvent(queryClient.getQueryData([
			"activity",
			"project",
			projectId,
			event.$id
		]) ?? event);
		setDrawerOpen(true);
		navigate({
			search: (prev) => ({
				...prev,
				event: event.$id
			}),
			replace: true
		});
	}, [
		navigate,
		projectId,
		queryClient
	]);
	const closeActivityDrawer = useCallback(() => {
		setDrawerOpen(false);
		setSelectedEvent(null);
		navigate({
			search: (prev) => ({
				...prev,
				event: void 0
			}),
			replace: true
		});
	}, [navigate]);
	return /* @__PURE__ */ jsxs("div", {
		className: "flex flex-col h-full min-h-0",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "sticky top-0 z-20 bg-background shrink-0",
				children: [/* @__PURE__ */ jsx(ServiceHeader, {
					title: t("Activity"),
					showFilters: true,
					filterTrigger: /* @__PURE__ */ jsxs("div", {
						className: "flex shrink-0 items-center gap-2",
						children: [/* @__PURE__ */ jsx(FiltersPopover, {
							open: filtersOpen,
							onOpenChange: setFiltersOpen,
							columns: activityFilterColumns,
							filterMap,
							onRemoveFilter: removeFilter,
							onClearAll: clearAllFilters,
							onApplyFilter: applyFilter,
							resourceLabel: t("activities"),
							filterScope: "activity",
							onApplyQuery: (queryParam) => {
								navigate({
									search: (prev) => ({
										...prev,
										query: queryParam ?? void 0
									}),
									replace: true
								});
							},
							teamId: project?.teamId,
							onReset: () => {
								navigate({
									search: (prev) => {
										const { event: ev } = prev;
										return { ...ev ? { event: ev } : {} };
									},
									replace: true
								});
							}
						}), /* @__PURE__ */ jsx(DateRangePicker, {
							dateRange: dateRangeForPicker,
							onDateRangeChange: handleDateRangeChange,
							className: "h-9 min-w-[200px]",
							popoverContentAlign: "start"
						})]
					}),
					beforeRefreshButtons: /* @__PURE__ */ jsxs(Tooltip, { children: [/* @__PURE__ */ jsx(TooltipTrigger, {
						asChild: true,
						children: /* @__PURE__ */ jsxs("button", {
							type: "button",
							className: cn("inline-flex max-w-[10.5rem] shrink-0 items-center gap-1.5 rounded-md px-1.5 py-1 text-start sm:max-w-[13rem]", "border border-transparent text-muted-foreground", "hover:border-border hover:bg-muted/50 hover:text-foreground", "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background"),
							children: [/* @__PURE__ */ jsx(icons_exports.Clock, { className: "h-3.5 w-3.5 shrink-0 text-blue-600 dark:text-blue-400" }), /* @__PURE__ */ jsxs("span", {
								className: "min-w-0 truncate text-[11px] leading-tight",
								children: [/* @__PURE__ */ jsxs("span", {
									className: "text-muted-foreground",
									children: [t("Retention"), " "]
								}), /* @__PURE__ */ jsx("span", {
									className: "font-medium text-foreground",
									children: t(activityLogRetentionLabel)
								})]
							})]
						})
					}), /* @__PURE__ */ jsxs(TooltipContent, {
						side: "bottom",
						align: "end",
						className: "max-w-sm text-[12px] leading-snug text-balance",
						children: [/* @__PURE__ */ jsx("p", {
							className: "font-medium text-background",
							children: t("Activity retention")
						}), /* @__PURE__ */ jsxs("p", {
							className: "mt-1.5 text-background/85",
							children: [
								t("Your plan includes"),
								" ",
								/* @__PURE__ */ jsx("span", {
									className: "font-medium text-background",
									children: t(activityLogRetentionLabel)
								}),
								" ",
								t("of activity history."),
								isFreePlan && /* @__PURE__ */ jsxs(Fragment, { children: [" ", t("Upgrade or contact sales for longer retention.")] })
							]
						})]
					})] }),
					showRefresh: true,
					onRefresh: () => {
						refetch();
					},
					isRefreshing: activityListRefreshing,
					fullWidthBorder: true,
					fullWidth: true,
					showToolbarBottomBorder: true
				}), isFreePlan && /* @__PURE__ */ jsx("div", {
					className: "border-b border-border bg-amber-500/5",
					children: /* @__PURE__ */ jsx("div", {
						className: "w-full px-4 py-3 sm:px-6",
						children: /* @__PURE__ */ jsxs(Alert, {
							variant: "default",
							className: "border-amber-500/30 bg-transparent",
							children: [/* @__PURE__ */ jsx(icons_exports.AlertCircle, { className: "h-4 w-4 text-amber-500" }), /* @__PURE__ */ jsxs("div", {
								className: "flex min-w-0 flex-1 items-center justify-between gap-4",
								children: [/* @__PURE__ */ jsxs("div", {
									className: "min-w-0 flex-1",
									children: [/* @__PURE__ */ jsx(AlertTitle, {
										className: "text-[13px] font-medium text-amber-600 dark:text-amber-400",
										children: t("Limited activity history")
									}), /* @__PURE__ */ jsxs(AlertDescription, {
										className: "col-start-2 block min-w-0 truncate whitespace-nowrap text-[12px] text-amber-600/80 dark:text-amber-400/80",
										children: [
											t("Your plan includes"),
											" ",
											/* @__PURE__ */ jsx("span", {
												className: "font-medium",
												children: t(activityLogRetentionLabel)
											}),
											" ",
											t("of activity history. Upgrade for longer retention.")
										]
									})]
								}), project?.teamId && /* @__PURE__ */ jsx(Button, {
									asChild: true,
									size: "sm",
									className: "h-8 shrink-0 bg-amber-500 px-3 text-[12px] font-medium text-amber-950 hover:bg-amber-400 dark:bg-amber-500 dark:text-amber-950 dark:hover:bg-amber-400",
									children: /* @__PURE__ */ jsx(Link, {
										to: "/upgrade",
										search: { orgId: project.teamId },
										children: t("Upgrade")
									})
								})]
							})]
						})
					})
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "flex flex-1 min-h-0 flex-col",
				children: [showActivityChart && /* @__PURE__ */ jsx("div", {
					className: "shrink-0 pb-4",
					children: /* @__PURE__ */ jsx(ActivityLogVolumeChart, {
						rangeFrom: volumeChartRange.from,
						rangeTo: volumeChartRange.to,
						activeResourceTypeFilter,
						onLegendResourceTypeClick: handleLegendResourceTypeClick
					})
				}), isLoading && events.length === 0 ? /* @__PURE__ */ jsx(ActivityLogsLoadingTable, { rowCount: pageSize }) : events.length > 0 ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
					className: "min-h-0 flex-1 overflow-auto",
					children: /* @__PURE__ */ jsxs(Table$1, {
						withScrollContainer: false,
						className: "table-fixed w-full",
						children: [
							/* @__PURE__ */ jsx(ActivityLogsTableColGroup, {}),
							/* @__PURE__ */ jsx(ActivityLogsTableHead, {}),
							/* @__PURE__ */ jsx(TableBody, { children: events.map((rawEvent) => {
								const activity = toDisplayActivity(rawEvent, countryLookups);
								const resourcePrimary = activity.resourceId?.trim() || activity.resourceName || "-";
								const resourceDisplay = truncateMiddle(resourcePrimary, ACTIVITY_RESOURCE_DISPLAY_MAX);
								const resourceTitle = resourcePrimary !== "-" && resourcePrimary.length > ACTIVITY_RESOURCE_DISPLAY_MAX ? resourcePrimary : void 0;
								return /* @__PURE__ */ jsx(ActivityLogRowContextMenu, {
									projectId,
									event: rawEvent,
									onOpenDetails: () => openActivityDrawer(rawEvent),
									children: /* @__PURE__ */ jsxs(TableRow, {
										role: "button",
										tabIndex: 0,
										"data-state": drawerOpen && selectedEvent?.$id === activity.$id ? "selected" : void 0,
										"aria-label": `${t("Open activity details")}: ${activity.rawEvent}`,
										className: cn("cursor-pointer", drawerOpen && selectedEvent?.$id === activity.$id && "bg-muted/60 hover:bg-muted/60"),
										onClick: () => openActivityDrawer(rawEvent),
										onMouseEnter: () => prefetchActivityEventOnHover(rawEvent.$id),
										onFocus: () => prefetchActivityEventOnHover(rawEvent.$id),
										onKeyDown: (e) => {
											if (e.key === "Enter" || e.key === " ") {
												e.preventDefault();
												openActivityDrawer(rawEvent);
											}
										},
										children: [
											/* @__PURE__ */ jsx(TableCell, {
												className: "min-w-0 px-4 py-3 ps-6 sm:ps-8",
												children: /* @__PURE__ */ jsxs("div", {
													className: "flex min-w-0 items-center gap-2",
													children: [/* @__PURE__ */ jsx("div", {
														className: cn("flex h-7 w-7 shrink-0 items-center justify-center rounded-md", actionColors[activity.action]),
														title: t(actionLabels[activity.action]),
														children: actionIcons[activity.action]
													}), /* @__PURE__ */ jsx("p", {
														className: "min-w-0 truncate font-mono text-[12px] text-muted-foreground",
														title: activity.description ?? void 0,
														children: activity.description || "-"
													})]
												})
											}),
											/* @__PURE__ */ jsx(TableCell, {
												className: "min-w-0 px-4 py-3",
												children: /* @__PURE__ */ jsxs("div", {
													className: "flex items-center gap-2.5",
													children: [/* @__PURE__ */ jsx(UserTypeAvatar, {
														actorType: activity.actorType,
														actorName: activity.actorName,
														className: "shadow-none"
													}), /* @__PURE__ */ jsxs("div", {
														className: "min-w-0",
														children: [/* @__PURE__ */ jsxs("div", {
															className: "flex min-w-0 items-center gap-1.5",
															children: [/* @__PURE__ */ jsx("p", {
																className: "truncate text-[13px] font-medium text-foreground",
																children: activity.actorName
															}), isMcpSdkActivity(rawEvent) ? /* @__PURE__ */ jsxs(Tooltip, { children: [/* @__PURE__ */ jsx(TooltipTrigger, {
																asChild: true,
																children: /* @__PURE__ */ jsx("span", {
																	className: "inline-flex shrink-0 text-muted-foreground",
																	"aria-label": t("Via MCP"),
																	onClick: (e) => e.stopPropagation(),
																	onKeyDown: (e) => e.stopPropagation(),
																	children: /* @__PURE__ */ jsx(McpIcon, { className: "h-3.5 w-3.5" })
																})
															}), /* @__PURE__ */ jsx(TooltipContent, {
																side: "top",
																children: t("Via MCP")
															})] }) : null]
														}), /* @__PURE__ */ jsx("p", {
															className: cn("truncate text-[11px] text-muted-foreground", hasHumanEmail(activity.actorType) ? "" : "font-mono"),
															children: hasHumanEmail(activity.actorType) ? activity.actorEmail || activity.actorId || "-" : activity.actorId || "-"
														})]
													})]
												})
											}),
											/* @__PURE__ */ jsx(TableCell, {
												className: "min-w-0 whitespace-nowrap px-4 py-3",
												children: (() => {
													const badge = userTypeBadge(activity.actorType);
													return /* @__PURE__ */ jsx("span", {
														className: cn("inline-block rounded px-1.5 py-0.5 text-[10px] font-medium uppercase tracking-wider", badge.tone),
														children: t(badge.label)
													});
												})()
											}),
											/* @__PURE__ */ jsx(TableCell, {
												className: "min-w-0 whitespace-normal px-4 py-3 align-top",
												children: /* @__PURE__ */ jsxs("div", {
													className: "flex min-w-0 items-center gap-2.5",
													children: [/* @__PURE__ */ jsx("div", {
														className: "flex h-7 w-7 shrink-0 items-center justify-center rounded-md bg-muted text-muted-foreground",
														children: resourceIcons[activity.resourceType]
													}), /* @__PURE__ */ jsxs("div", {
														className: "min-w-0 flex flex-col gap-1",
														children: [/* @__PURE__ */ jsx("p", {
															className: "min-w-0 overflow-hidden text-ellipsis whitespace-nowrap text-[13px] font-medium leading-snug text-foreground",
															title: resourceTitle,
															children: resourceDisplay
														}), /* @__PURE__ */ jsx("p", {
															className: "text-[11px] capitalize text-muted-foreground",
															children: t(activity.resourceType)
														})]
													})]
												})
											}),
											/* @__PURE__ */ jsx(TableCell, {
												className: "min-w-0 px-4 py-3",
												children: activity.ipAddress ? /* @__PURE__ */ jsx(CopyableId, {
													id: activity.ipAddress,
													displayText: formatIpForDisplay(activity.ipAddress, 32) ?? activity.ipAddress,
													size: "md",
													maxWidth: 128,
													className: "max-w-full"
												}) : /* @__PURE__ */ jsx("span", {
													className: "font-mono text-[12px] text-muted-foreground",
													children: "-"
												})
											}),
											/* @__PURE__ */ jsx(TableCell, {
												className: "min-w-0 px-4 py-3",
												children: /* @__PURE__ */ jsx(ActivityTableCountryCell, { countryName: activity.countryName })
											}),
											/* @__PURE__ */ jsx(TableCell, {
												className: "min-w-0 px-4 py-3 pe-6 sm:pe-8",
												children: /* @__PURE__ */ jsx(DateTooltip, {
													date: activity.timestamp,
													className: "text-[12px] text-muted-foreground"
												})
											})
										]
									})
								}, activity.$id);
							}) })
						]
					})
				}), /* @__PURE__ */ jsx("div", {
					className: "h-[54px] shrink-0 border-t border-border bg-background px-4 sm:px-6",
					children: /* @__PURE__ */ jsx(Pagination, {
						currentPage,
						totalItems: 0,
						totalKnown: false,
						hasNextPage: hasMore,
						pageSize,
						pageSizeOptions: [
							10,
							25,
							50,
							100,
							150
						],
						onPageChange: handlePageChange,
						onPageSizeChange: (size) => {
							setPageSize(size);
							resetListPosition();
						},
						displayItemRange: events.length === 0 ? {
							start: 0,
							end: 0
						} : {
							start: (currentPage - 1) * pageSize + 1,
							end: (currentPage - 1) * pageSize + events.length
						},
						itemLabel: t("activities"),
						className: "h-full min-h-0 border-0 mt-0 py-0"
					})
				})] }) : /* @__PURE__ */ jsx(EmptyState, {
					icon: icons_exports.Activity,
					title: filterMap.size > 0 ? void 0 : t("No activities yet"),
					description: filterMap.size > 0 ? void 0 : t("Activity will appear here as you use your project"),
					isEmpty: filterMap.size === 0,
					hasFilters: filterMap.size > 0,
					variant: "centered"
				})]
			}),
			/* @__PURE__ */ jsx(ActivityLogDrawer, {
				open: drawerOpen,
				onOpenChange: (open) => {
					if (open) {
						setDrawerOpen(true);
						return;
					}
					closeActivityDrawer();
				},
				event: selectedEvent,
				display: selectedEvent ? {
					action: eventToActionType(selectedEvent.event),
					resourceType: eventToResourceType(selectedEvent.resourceType, selectedEvent.event, selectedEvent.resource),
					resourceName: resourceLabelFromEvent(selectedEvent)
				} : null
			})
		]
	});
}
function ActivityPage() {
	const { projectId } = Route$1.useParams();
	return /* @__PURE__ */ jsx(View, { projectId });
}
export { ActivityPage as component };
