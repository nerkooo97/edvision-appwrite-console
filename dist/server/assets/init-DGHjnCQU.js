import { n as World, t as useGlobeThemeConfig } from "./use-globe-theme-config-BGQROv5i.js";
import { i as withAlpha, n as getInitGlobeBrandRgb } from "./init-globe-theme-DHztu-Dy.js";
import { i as isResolvedThemeDarkChrome, r as isHtmlDarkChrome } from "./html-theme-zz5wyKPq.js";
import { t as cn } from "./utils-DoqqkI3X.js";
import "./runtime-config-DK7G0iKr.js";
import "./debug-endpoint-BvungD5q.js";
import { _ as hasLikelyConsoleSession, d as sdk } from "./sdk-DjIJ_hjn.js";
import "./console-session-cookie-7RV5Zfr4.js";
import { f as useDebugOverrides } from "./i18n-Db4baE06.js";
import { _ as initTicketInsetStyle, g as initTicketHolderNameFontSizeClass, h as initTicketDisplayAspectRatio, m as initTicketContentGridStyle, o as INIT_TICKET_BOTTOM_TRIM_PERCENT, p as INIT_TICKET_STUB_LABEL_INSET, s as INIT_TICKET_COLLAPSED_WIDTH_PX, t as INIT_TICKET_ASPECT_RATIO, u as INIT_TICKET_MAX_WIDTH_PX, x as initTicketStubTitleClass } from "./ticket-layout-B97VGq99.js";
import "./ticket-types-BpqSrvYB.js";
import { n as useT } from "./translate-DZcqveGn.js";
import "./error-formatting-CL2hjGy5.js";
import "./console-projects-C0b0tMaH.js";
import { t as getDefaultSiteOrigin } from "./site-origin-DqNp3EP0.js";
import "./constants-CL7SLzjY.js";
import "./constants-B5zUV45z.js";
import "./constants-Dd6QzW31.js";
import { n as getFrameworkIconFile } from "./icons-Dw9jcxHS.js";
import { i as getInitTicketStackOption, n as INIT_TICKET_MAX_STACK, r as INIT_TICKET_STACK_OPTIONS } from "./ticket-stack-pzBI8NL4.js";
import { t as buildInitTicketRenderData } from "./ticket-render-data-DrkrIeNP.js";
import { d as readInitTicketPrefsFromAccountPrefs, f as readInitTicketPrefsFromStorage, h as writeInitTicketPrefsToStorage, i as buildInitTicketShareMessage, m as writeInitTicketGuestPrefsToStorage, n as INIT_TICKET_PREFS_CHANGE_EVENT, o as getInitTicketHolderTitle, s as mergeInitTicketPrefsIntoAccountPrefs, t as DEFAULT_INIT_TICKET_PREFS, u as readInitTicketGuestPrefsFromStorage } from "./ticket-prefs-DDmwAY1F.js";
import { d as LONG_STALE_TIME } from "./constants-BDeF927R.js";
import "./organizations-BKtnlNrj.js";
import "./console-profiles-D__E5Kgi.js";
import { zs as fetchLocale } from "./hooks-BONwG3Mt.js";
import { i as accountIdentitiesQueryOptions, s as consoleAccountQueryOptions } from "./auth-BPuxYQAc.js";
import "./projects-BaTJenfQ.js";
import "./appwrite-id-L15yEGeF.js";
import "./databases-Dh0pwZ6h.js";
import "./database-specs-CBc802K0.js";
import "./database-row-inline-edits-CdyGeTxj.js";
import "./form-field-type-badge-C7qMzJo0.js";
import "./chart-interval-Dbrn19qD.js";
import "./is-marketing-page-dgx45Oqy.js";
import "./use-console-impersonation-revision-BiI0c7pX.js";
import "./affiliates-BOg1SHC6.js";
import "./social-stats-X1CQqP0k.js";
import "./cimd-CRIktQxf.js";
import "./account-applications-Bo8bAeE0.js";
import "./date-format-BD1j7PxK.js";
import "./invite-url-B18y3cBt.js";
import "./format-metric-6jsfxd5f.js";
import "./domains-Bfw8HsXF.js";
import "./parse-spec-DW3UGcrS.js";
import "./page-direction-CnacIIOa.js";
import "./AgentChat-DNlva4IH.js";
import { t as shouldSuppressGlobalShortcuts } from "./global-shortcut-suppress-C5j6k0iy.js";
import "./DebugMode-DFSPYy81.js";
import "./ThinkingBubble-U48KAaRY.js";
import "./slider-BKjrzSmD.js";
import { t as Button } from "./button-Bnm2QhOm.js";
import { t as Input } from "./input-yKHNPhDZ.js";
import "./collapsible-BcDIDOgI.js";
import { n as PopoverContent, r as PopoverTrigger, t as Popover } from "./popover-BjTNxuf9.js";
import "./tabs-XaWkg9jR.js";
import "./select-BYGLGp-f.js";
import { n as analyticsAttrs } from "./analytics-actions-FGYQVzYg.js";
import "./context-menu-D55xedo-.js";
import "./CodeBlock-BGAzMP_K.js";
import "./WizardLayout-DWqXFGuX.js";
import "./code-language-RiwE0Xft.js";
import "./prose-typography-BMJgwhz7.js";
import "./table-CsPM4E9L.js";
import "./RequireAuth-DahioVQe.js";
import "./HeaderAlertBar-CK7Gy4sE.js";
import "./agent-paths-CTRM_FvO.js";
import "./mcp-CgjPVMsn.js";
import "./debug-mcp-endpoint-B4hkK2QF.js";
import { t as Badge } from "./badge-L9aO6DfA.js";
import { t as Label } from "./label-D8nNLJBa.js";
import "./switch-D-U5gDIQ.js";
import "./textarea-CfKMnSVC.js";
import "./modal-auto-focus-BO41bKmA.js";
import "./dialog-CFWmsJbI.js";
import "./command-Cizl9kMJ.js";
import { t as Skeleton } from "./skeleton-8d0Q_D56.js";
import { t as PUBLIC_ICON_MUTED_CLASSES } from "./public-icon-classes-CaQtbn94.js";
import { t as InitialsAvatar } from "./Avatar-D1PavDBA.js";
import "./use-localized-date-format-Dy8J1Ssn.js";
import "./DateTooltip-wgOggQgS.js";
import "./EmptyState-DZDwceHm.js";
import { a as DropdownMenuItem, p as DropdownMenuTrigger, r as DropdownMenuContent, t as DropdownMenu } from "./dropdown-menu-DH51wH-m.js";
import "./Pagination-BDei8M4v.js";
import "./alert-BTaNwkUC.js";
import "./CronScheduleEditor-Dctcdb1H.js";
import "./overlay-lock-CIY7GeXu.js";
import { c as OFFCANVAS_START_CLOSED, l as SIDEBAR_EDGE_TOGGLE_OVERFLOW } from "./sheet-CbM5lIV1.js";
import { t as BaseDrawer } from "./BaseDrawer-B4vv4Sf_.js";
import "./context-menu-Ca6WjjAw.js";
import "./ContextMenuIcon-DPnw7e0V.js";
import { t as useKeyboardShortcut } from "./use-keyboard-shortcuts-C2m0wYFf.js";
import "./display-DbRQIyxk.js";
import "./McpIcon-D1Jv-oq2.js";
import "./SettingsSearchContext-DPkuZW4D.js";
import "./SettingsLayoutShell-B7hNlMNA.js";
import "./calendar-6OJ5dwYN.js";
import "./use-media-min-width-T-T6WgXi.js";
import "./DateRangePicker-BwmpXdP_.js";
import { n as ToggleGroupItem, t as ToggleGroup } from "./toggle-group-qQyCSBun.js";
import { i as TooltipTrigger, n as TooltipContent, r as TooltipProvider, t as Tooltip } from "./tooltip-DUssQZhw.js";
import "./UsageChartIntervalToggle-Bbo7DqjH.js";
import "./CartesianChart-IK-OMdOm.js";
import "./UsageChartBrushReferenceArea-PlRYgCRx.js";
import "./ChartXAxis-Sg7PTtJF.js";
import "./chart-panel-CCGEGd61.js";
import "./OverviewChartPanelError-D9UA3Ssz.js";
import "./upgrade-curtain-D427ml_E.js";
import "./addons-DpAB_yDA.js";
import "./stripe-B07yV6XF.js";
import "./utils-DMkzhjmw.js";
import "./EnablePremiumGeoDBDialog-B57n4Kqr.js";
import "./use-console-profile-DiUZxZ_6.js";
import "./HostnameFaviconIcon-BEkCxwPN.js";
import "./console-project-scopes-nd4nTLJF.js";
import "./UsageTimeSeriesChartCard-DvSTn7lw.js";
import "./DatabaseTypeIcon-CqLDDPFP.js";
import { r as PostgresElephantIcon } from "./database-mascot-icons-mAQ4uqbH.js";
import "./chart-animation-CE90Rh4_.js";
import { t as DocsRouteLink } from "./DocsRouteLink-cLPNKZ9F.js";
import { m as parseDocsPagePath, p as parseBlogPagePath } from "./urls-BIlyr2O2.js";
import "./ChartSeriesDot-DRSaLZ-2.js";
import "./UsageSectionChartError-Bjd51l80.js";
import "./horizontal-resize-BcegzCwH.js";
import "./resizable-CfBrThFG.js";
import "./SentryContext-BM5Kx9zs.js";
import "./checkbox-r_hqIB3d.js";
import "./CopyableId-DPIWAPIb.js";
import "./ProjectConnectDialogContext-DgcmISfV.js";
import "./Icon-BtIL187e.js";
import { t as FrameworkIcon } from "./FrameworkIcon-DTkSe6r3.js";
import "./MCPSection-k-iSVVVO.js";
import "./ConnectCodeExample-Ujo3XkoF.js";
import "./radio-group-aZurL4eE.js";
import "./DateTimePicker-DySgezub.js";
import "./accordion-DmQmnCa5.js";
import "./separator-B2hXZdKL.js";
import "./ScopeEditor-DGe3mP1w.js";
import "./ApiKeyDrawer-C9r-i_O0.js";
import "./use-user-os-Cwg5asTC.js";
import "./PostgresCopyableField-eNLUNLhf.js";
import "./TerraformIcon-DDZR7KCM.js";
import "./providers-8aVvAoJZ.js";
import "./agent-discovery-SMCX1bvP.js";
import "./analytics-C_KnVoso.js";
import "./avif-support-fkUYDvxs.js";
import { t as registerConsoleRealtimeListener } from "./console-hub-DIz9opmN.js";
import "./agent-docs-feature-COYbd_m1.js";
import "./navigation-BOrhbgOp.js";
import { c as useInitThemeUsesDarkImage, l as OPEN_COMMAND_CENTER_SHORTCUT_OPTIONS, o as CommandCenter, s as useInitThemeImageSrc, t as ConsoleLayout } from "./ConsoleLayout-c5WGBGep.js";
import "./partners-docs-feature-C-dnxcxd.js";
import { t as BlogPageAnchor } from "./BlogPageAnchor-BwvdqqDT.js";
import "./MarketingSiteLink-Cxu9rqvv.js";
import { i as ProductBentoSoftLights } from "./HomeSoftLights-BsLce5-B.js";
import "./manifest-THOJt7eC.js";
import "./parse-params-BpMT2Ilk.js";
import { a as INIT_GET_INVOLVED_SECTION_ID, c as INIT_RELEASE_AVAILABILITY_SECTION_ID, l as INIT_TICKET_SECTION_ID, n as getActiveLaunchEvent, o as INIT_GLOBE_SECTION_ID, p as isInitDailyPrizeRevealed, s as INIT_PRIZES_SECTION_ID, u as applyInitEventVisibility } from "./events-s0i9XY3r.js";
import "./progress-DDUqzOsb.js";
import "./postgres-sql-editor-actions-C3EmLCXJ.js";
import "./postgres-sql-editor-shortcuts-CXAI529H.js";
import "./mysql-sql-editor-actions-Bp-OLXJY.js";
import "./mysql-sql-editor-shortcuts-vHKpSCi0.js";
import { a as useInitLowPowerAnimationDecision, i as openGoogleCalendarEventUrl, r as downloadInitScheduleItemCalendar, t as buildGoogleCalendarScheduleItemEventUrl } from "./init-calendar-D7kvXbWz.js";
import "./network-connectivity-D-2A27IF.js";
import "./CloudStatusBanner-CLph43RA.js";
import { t as InitWordmark } from "./InitWordmark-DLiURix2.js";
import "./date-utils-C_g8GS8c.js";
import "./id-input-CfyIhJ26.js";
import "./use-mobile-C9thwzsE.js";
import "./feedback-BwuMSGir.js";
import "./support-BA-5OzxM.js";
import "./CloudMarkIcon-ChnstmGW.js";
import "./content-NlXhGy_g.js";
import "./nav-badge-CqWauT26.js";
import "./registry-C4rxXMsK.js";
import { a as SECONDARY_SIDEBAR_NAV_LINK_COLLAPSED_CLASS, o as SECONDARY_SIDEBAR_NAV_LINK_GRID_CLASS } from "./secondary-sidebar-nav-F3D8GuLT.js";
import "./og-image-DdV5MU0-.js";
import "./page-meta-DY0pOkK9.js";
import { a as getInitTicketStorageConfig, s as isInitTicketStorageConfigured, t as buildInitTicketShareUrl } from "./init-ticket-share-FwW0y3EN.js";
import { a as getInitDayCardId, c as InitDayCountdown, i as INIT_DAY_CARD_SCROLL_OFFSET_PX, n as DatabasesProductVisual, o as scrollToInitDayCard, r as isLaunchEventDayLocked, s as scrollToInitDayFromHash, t as FirewallProductVisual } from "./FirewallProductVisual-B4ibK_Hf.js";
import { n as PostgresTypedSql, o as productBentoContainer, s as productBentoIdle, t as PostgresIdleSql } from "./MockSyntax-BoA_nXCY.js";
import { t as scrambleSensitiveText } from "./scramble-sensitive-text-Uoy_pXqO.js";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { Link } from "@tanstack/react-router";
import { createContext, forwardRef, useCallback, useContext, useEffect, useId, useImperativeHandle, useMemo, useRef, useState } from "react";
import { Channel, Client, ID, Permission, Query, Role, Storage } from "@appwrite.io/console";
import { useQuery } from "@tanstack/react-query";
import { toast } from "sonner";
import { ArrowUpRight, BookOpen, CalendarPlus, CheckCircle2, ChevronDown, ChevronLeft, ChevronRight, Cloud, Copy, Download, Eye, FileText, Flame, Gift, Globe, Heart, HelpCircle, Link2, Linkedin, Loader2, Lock, LogIn, Moon, PartyPopper, Play, Rocket, Server, Share2, SlidersHorizontal, Sparkles, Sun, Ticket, Trophy, Video, X } from "lucide-react";
import { useTheme } from "next-themes";
import { AnimatePresence, motion, useReducedMotion } from "motion/react";
import { animate, createTimeline, createTimer, stagger, utils } from "animejs";
import { toCanvas } from "html-to-image";
var RELEASE_DEBOUNCE_MS = 300;
var retainCount = 0;
var unregister = null;
var attachPromise = null;
var releaseTimer = null;
var dispatch = () => void 0;
async function attachPresencesListener() {
	if (releaseTimer) {
		clearTimeout(releaseTimer);
		releaseTimer = null;
	}
	if (unregister) return;
	if (attachPromise) {
		await attachPromise;
		return;
	}
	attachPromise = (async () => {
		unregister = await registerConsoleRealtimeListener([Channel.presences()], (event) => {
			dispatch(event);
		});
	})();
	try {
		await attachPromise;
	} finally {
		attachPromise = null;
	}
}
async function detachPresencesListener() {
	if (unregister) {
		await unregister();
		unregister = null;
	}
}
function retainInitPresencesRealtimeListener(handler) {
	dispatch = handler;
	retainCount += 1;
	attachPresencesListener();
	let released = false;
	return () => {
		if (released) return;
		released = true;
		retainCount = Math.max(0, retainCount - 1);
		if (retainCount > 0) return;
		if (releaseTimer) clearTimeout(releaseTimer);
		releaseTimer = setTimeout(() => {
			releaseTimer = null;
			if (retainCount === 0) detachPresencesListener();
		}, RELEASE_DEBOUNCE_MS);
	};
}
const INIT_PRESENCE_ACTIVITY_ON_INIT = "On Init";
const INIT_PRESENCE_ACTIVITY_LEFT = "Left Init";
const INIT_PRESENCE_ACTIVITY_OFFLINE = "Offline";
const INIT_PRE_EVENT_HYPE_ACTIVITIES = [
	"Counting down to Init",
	"Getting hyped for launch week",
	"Exploring the schedule",
	"Claiming a ticket",
	"Customizing a ticket",
	"Ready for five days of launches",
	"Waiting for kickoff",
	"Saving a front-row seat",
	"Browsing what is coming",
	"Psyched for Init",
	"Plotting the week ahead",
	"Marking the calendar",
	"Init week loading",
	"First launch drops soon"
];
var LOCKED_DAY_ACTIVITY_TEMPLATES = [
	(day) => `Waiting for day ${day}`,
	(day) => `Anticipating day ${day}`,
	(day) => `Counting down to day ${day}`,
	(day) => `Day ${day} launch soon`,
	(day) => `Saving room for day ${day}`,
	(day) => `Curious about day ${day}`
];
function hashPresenceSeed(seed) {
	let hash = 0;
	for (let index = 0; index < seed.length; index += 1) {
		hash = (hash << 5) - hash + seed.charCodeAt(index);
		hash |= 0;
	}
	return Math.abs(hash);
}
function pickInitPresenceActivity(activities, seed) {
	if (activities.length === 0) return INIT_PRESENCE_ACTIVITY_ON_INIT;
	return activities[hashPresenceSeed(seed) % activities.length];
}
function getInitLockedDayActivityPool(day) {
	return LOCKED_DAY_ACTIVITY_TEMPLATES.map((template) => template(day));
}
function getAllInitWaitingActivities(maxDay = 5) {
	const activities = new Set(INIT_PRE_EVENT_HYPE_ACTIVITIES);
	for (let day = 1; day <= maxDay; day += 1) for (const activity of getInitLockedDayActivityPool(day)) activities.add(activity);
	return [...activities];
}
function clampInitPresenceActivity(activity) {
	const trimmed = activity.trim();
	if (trimmed.length <= 32) return trimmed;
	const readingDay = trimmed.match(/^Reading Day (\d+): .+$/i);
	if (readingDay) return `Reading day ${readingDay[1]}`;
	const viewingDay = trimmed.match(/^Viewing Day (\d+): .+$/i);
	if (viewingDay) return `Viewing day ${viewingDay[1]}`;
	if (trimmed.startsWith("Checking: ")) return "Checking session";
	const exploring = trimmed.match(/^Exploring (.+)$/);
	if (exploring && (exploring[1]?.length ?? 0) > 18) return `Exploring ${exploring[1].slice(0, 18).trimEnd()}…`;
	const cut = trimmed.slice(0, 32);
	const lastSpace = cut.lastIndexOf(" ");
	if (lastSpace >= 22) return `${cut.slice(0, lastSpace)}…`;
	return `${cut}…`;
}
function buildInitPreEventBaselineActivity(seed) {
	return pickInitPresenceActivity(INIT_PRE_EVENT_HYPE_ACTIVITIES, `baseline:${seed}`);
}
function buildInitWaitingForDayActivity(day, options) {
	const currentDay = options?.currentDay ?? day;
	const seed = options?.seed ?? `waiting:${day}`;
	if (currentDay <= 0) return pickInitPresenceActivity(INIT_PRE_EVENT_HYPE_ACTIVITIES, seed);
	return pickInitPresenceActivity(getInitLockedDayActivityPool(day), seed);
}
function buildInitViewingDayActivity(day, title) {
	if (title) return `Viewing Day ${day}: ${title}`;
	return `Viewing day ${day}`;
}
function buildInitReadingDayActivity(day, options) {
	if (options?.locked) return buildInitWaitingForDayActivity(day, {
		currentDay: options.currentDay,
		seed: options.seed
	});
	if (options?.title) return `Reading Day ${day}: ${options.title}`;
	return `Reading day ${day}`;
}
function buildInitPreviewingDayActivity(day, options) {
	if (options.locked) return buildInitWaitingForDayActivity(day, {
		currentDay: options.currentDay,
		seed: options.seed
	});
	return buildInitViewingDayActivity(day, options.title);
}
function buildInitViewingTicketActivity() {
	return "Viewing ticket";
}
function buildInitCustomizingTicketActivity() {
	return "Customizing a ticket";
}
function buildInitRecordingTicketActivity() {
	return "Recording ticket video";
}
function buildInitCheckingScheduleActivity(day) {
	if (day) return `Checking Day ${day} schedule`;
	return "Checking schedule";
}
function buildInitPlayingWithJoolActivity() {
	return "Playing with Jool";
}
function buildInitExploringGlobeActivity() {
	return "Exploring the globe";
}
function buildInitExploringActivity(label) {
	return `Exploring ${label}`;
}
var DAILY_PRIZE_PRESENCE_ACTIVITIES = {
	1: "Eyeing day 1 swag",
	2: "Keyboard envy",
	3: "Dark keyboard dreams",
	4: "Bottle and tee wishlist"
};
function buildInitViewingDailyPrizeActivity(day, _prizeLabel) {
	return DAILY_PRIZE_PRESENCE_ACTIVITIES[day] ?? `Eyeing day ${day} swag`;
}
function buildInitViewingGrandPrizeActivity(_title) {
	return "Grand prize dreams";
}
function buildInitRunningGiveawayRaffleActivity(day) {
	return `Drawing day ${day} swag`;
}
function buildInitRunningGrandPrizeRevealActivity() {
	return "Revealing grand prize winner";
}
function buildInitSpinningGiveawayRaffleActivity() {
	return "Spinning the wheel";
}
function buildInitSwitchingThemeActivity(theme) {
	return theme === "light" ? "Going light" : "Going dark";
}
function formatInitPresenceActivityDisplay(activity) {
	return clampInitPresenceActivity(activity);
}
const INIT_REACTIONS = [
	{
		id: "fire",
		icon: Flame,
		label: "Fire",
		activity: "Feeling the hype"
	},
	{
		id: "celebrate",
		icon: PartyPopper,
		label: "Celebrate",
		activity: "Celebrating"
	},
	{
		id: "mind-blown",
		icon: Sparkles,
		label: "Mind blown",
		activity: "Mind blown"
	},
	{
		id: "heart",
		icon: Heart,
		label: "Love it",
		activity: "Loving it"
	},
	{
		id: "rocket",
		icon: Rocket,
		label: "Ship it",
		activity: "Ready to ship"
	},
	{
		id: "eyes",
		icon: Eye,
		label: "Watching",
		activity: "Watching closely"
	}
];
var REACTION_BY_ID = new Map(INIT_REACTIONS.map((reaction) => [reaction.id, reaction]));
var REACTION_BY_ACTIVITY = new Map(INIT_REACTIONS.map((reaction) => [reaction.activity, reaction]));
var STORAGE_PREFIX = "console.init.reaction.";
const INIT_REACTION_DURATION_MS = 5e3;
function getInitReactionById(reactionId) {
	return REACTION_BY_ID.get(reactionId);
}
function getInitReactionActivity(reactionId) {
	return REACTION_BY_ID.get(reactionId)?.activity;
}
function getInitReactionStorageKey(eventId) {
	return `${STORAGE_PREFIX}${eventId}`;
}
function parseStoredInitReaction(raw) {
	try {
		const parsed = JSON.parse(raw);
		if (typeof parsed.reactionId === "string" && typeof parsed.expiresAt === "number") return {
			reactionId: parsed.reactionId,
			expiresAt: parsed.expiresAt
		};
	} catch {}
	if (REACTION_BY_ID.has(raw)) return {
		reactionId: raw,
		expiresAt: Date.now() + INIT_REACTION_DURATION_MS
	};
	return null;
}
function readStoredInitReaction(eventId) {
	if (typeof window === "undefined") return null;
	try {
		const raw = localStorage.getItem(getInitReactionStorageKey(eventId));
		if (!raw) return null;
		const stored = parseStoredInitReaction(raw);
		if (!stored) {
			localStorage.removeItem(getInitReactionStorageKey(eventId));
			return null;
		}
		if (Date.now() >= stored.expiresAt) {
			localStorage.removeItem(getInitReactionStorageKey(eventId));
			return null;
		}
		return stored;
	} catch {
		return null;
	}
}
function writeStoredInitReaction(eventId, reactionId) {
	if (typeof window === "undefined") return;
	try {
		const key = getInitReactionStorageKey(eventId);
		if (reactionId) {
			const payload = {
				reactionId,
				expiresAt: Date.now() + INIT_REACTION_DURATION_MS
			};
			localStorage.setItem(key, JSON.stringify(payload));
		} else localStorage.removeItem(key);
	} catch {}
}
function parseInitReactingActivity(activity) {
	const trimmed = activity.trim();
	const byActivity = REACTION_BY_ACTIVITY.get(trimmed);
	if (byActivity) return byActivity;
	const legacyMatch = trimmed.match(/^Reacting:\s*(.+)$/);
	if (legacyMatch) return getInitReactionById(legacyMatch[1].trim()) ?? null;
	return null;
}
function buildInitReactingActivity(reactionId) {
	return getInitReactionActivity(reactionId) ?? "On Init";
}
function getInitReactionCounts(users) {
	const counts = /* @__PURE__ */ new Map();
	for (const user of users) {
		const reaction = parseInitReactingActivity(user.activity);
		if (!reaction) continue;
		counts.set(reaction.id, (counts.get(reaction.id) ?? 0) + 1);
	}
	return counts;
}
function buildInitPresenceActivityAllowlist(event) {
	const allowed = new Set([
		INIT_PRESENCE_ACTIVITY_ON_INIT,
		INIT_PRESENCE_ACTIVITY_LEFT,
		INIT_PRESENCE_ACTIVITY_OFFLINE,
		buildInitViewingTicketActivity(),
		buildInitCustomizingTicketActivity(),
		buildInitRecordingTicketActivity(),
		buildInitCheckingScheduleActivity(),
		buildInitPlayingWithJoolActivity(),
		buildInitExploringGlobeActivity(),
		buildInitSwitchingThemeActivity("light"),
		buildInitSwitchingThemeActivity("dark")
	]);
	for (const reaction of INIT_REACTIONS) allowed.add(reaction.activity);
	for (const item of event.getInvolved) allowed.add(buildInitExploringActivity(item.title));
	if (event.prizes?.sectionTitle) allowed.add(buildInitExploringActivity(event.prizes.sectionTitle));
	allowed.add(buildInitExploringActivity("Ways to get involved"));
	allowed.add(buildInitExploringActivity("Keep exploring"));
	if (event.prizes) {
		for (const giveaway of event.prizes.dailyGiveaways) {
			allowed.add(buildInitViewingDailyPrizeActivity(giveaway.day, giveaway.prizeDescription));
			allowed.add(buildInitRunningGiveawayRaffleActivity(giveaway.day));
		}
		allowed.add(buildInitViewingGrandPrizeActivity(event.prizes.grandPrize.title));
		allowed.add(buildInitRunningGrandPrizeRevealActivity());
		allowed.add(buildInitSpinningGiveawayRaffleActivity());
	}
	for (const day of event.days) {
		allowed.add(buildInitViewingDayActivity(day.day, day.title));
		allowed.add(buildInitReadingDayActivity(day.day, { title: day.title }));
		allowed.add(buildInitCheckingScheduleActivity(day.day));
	}
	for (const activity of getAllInitWaitingActivities(event.days.reduce((max, day) => Math.max(max, day.day), 0))) allowed.add(activity);
	return allowed;
}
function normalizeInitPresenceActivity(activity) {
	const trimmed = activity.trim();
	const legacyMatch = trimmed.match(/^Reacting:\s*(.+)$/);
	if (!legacyMatch) return trimmed;
	return getInitReactionActivity(legacyMatch[1].trim()) ?? trimmed;
}
function isAllowedInitPresenceActivity(activity, allowlist) {
	const normalized = normalizeInitPresenceActivity(activity);
	if (!normalized) return false;
	return allowlist.has(normalized);
}
function sanitizeInitPresenceActivity(activity, allowlist) {
	if (!activity) return INIT_PRESENCE_ACTIVITY_ON_INIT;
	const normalized = normalizeInitPresenceActivity(activity);
	if (!isAllowedInitPresenceActivity(normalized, allowlist)) return INIT_PRESENCE_ACTIVITY_ON_INIT;
	return normalized;
}
function parseInitPresenceTheme(value) {
	if (value === "light" || value === "dark") return value;
}
function resolveInitPresenceTheme(resolvedTheme) {
	if (resolvedTheme === "light") return "light";
	if (resolvedTheme === "dark") return "dark";
	if (!resolvedTheme) return isHtmlDarkChrome() ? "dark" : "light";
	if (isResolvedThemeDarkChrome(resolvedTheme)) return "dark";
	return isHtmlDarkChrome() ? "dark" : "light";
}
function countInitPresenceThemes(users) {
	let light = 0;
	let dark = 0;
	for (const user of users) if (user.theme === "light") light += 1;
	else if (user.theme === "dark") dark += 1;
	return {
		light,
		dark
	};
}
const INIT_PRESENCE_HEARTBEAT_MS = 3e4;
var LEGACY_PRESENCE_ID_STORAGE_PREFIX = "console.init.presenceId.";
function buildInitOnlineStatus(eventId) {
	return `init:${eventId}`;
}
function buildInitAwayStatus(eventId) {
	return `init:${eventId}:away`;
}
function buildInitPresenceId(userId) {
	return userId;
}
function clearLegacyInitPresenceStorage() {
	if (typeof window === "undefined") return;
	try {
		for (let index = window.localStorage.length - 1; index >= 0; index -= 1) {
			const key = window.localStorage.key(index);
			if (key?.startsWith(LEGACY_PRESENCE_ID_STORAGE_PREFIX)) window.localStorage.removeItem(key);
		}
	} catch {}
}
function buildInitPresenceExpiresAt(ttlSeconds = 90) {
	return new Date(Date.now() + ttlSeconds * 1e3).toISOString();
}
function buildInitPresencePermissions(userId) {
	return [
		Permission.read(Role.users()),
		Permission.update(Role.user(userId)),
		Permission.delete(Role.user(userId))
	];
}
function parseInitPresenceMetadata(metadata) {
	if (typeof metadata === "string") try {
		metadata = JSON.parse(metadata);
	} catch {
		return null;
	}
	if (!metadata || typeof metadata !== "object") return null;
	const record = metadata;
	const eventId = typeof record.eventId === "string" ? record.eventId.trim() : "";
	if (!eventId) return null;
	const name = typeof record.name === "string" ? record.name.trim() : "";
	const countryCode = typeof record.countryCode === "string" ? record.countryCode.trim().toUpperCase() : "";
	return {
		eventId,
		name,
		activity: typeof record.activity === "string" ? record.activity : void 0,
		isLive: record.isLive === true,
		theme: parseInitPresenceTheme(record.theme),
		countryCode: countryCode || void 0
	};
}
function presenceMatchesInitEvent(presence, eventId) {
	const metadata = parseInitPresenceMetadata(presence.metadata);
	if (metadata?.eventId === eventId) return true;
	const status = presence.status;
	return status === buildInitOnlineStatus(eventId) || status === buildInitAwayStatus(eventId) || status === "online" && metadata?.eventId === eventId || status === "away" && metadata?.eventId === eventId;
}
function isPresenceActive(presence) {
	if (!presence.expiresAt) return true;
	return new Date(presence.expiresAt).getTime() > Date.now();
}
function isInitOnlineStatus(presence, eventId) {
	if (presence.status === buildInitOnlineStatus(eventId)) return true;
	if (presence.status !== "online") return false;
	return parseInitPresenceMetadata(presence.metadata)?.eventId === eventId;
}
function isInitAwayStatus(presence, eventId) {
	if (presence.status === buildInitAwayStatus(eventId)) return true;
	if (presence.status !== "away") return false;
	return parseInitPresenceMetadata(presence.metadata)?.eventId === eventId;
}
function presenceUpdatedAt(presence) {
	return new Date(presence.$updatedAt ?? 0).getTime();
}
function normalizeInitPresenceRecord(presence) {
	if (!presence) return null;
	const userId = presence.userId?.trim() || presence.$id?.trim();
	if (!userId) return null;
	let metadata = presence.metadata;
	if (typeof metadata === "string") try {
		metadata = JSON.parse(metadata);
	} catch {}
	return {
		...presence,
		userId,
		metadata
	};
}
function mergePresenceRecordUpdate(previous, incoming) {
	if (!previous) return incoming;
	return {
		...previous,
		...incoming,
		userId: incoming.userId,
		status: incoming.status || previous.status,
		metadata: incoming.metadata ?? previous.metadata,
		expiresAt: incoming.expiresAt ?? previous.expiresAt,
		$updatedAt: incoming.$updatedAt ?? previous.$updatedAt
	};
}
function isPresenceDeleteEvent(events) {
	return events.some((event) => event.includes("presences.") && event.endsWith(".delete"));
}
function isPresenceMutationEvent(events) {
	return events.some((event) => event.includes("presences.") && (event.endsWith(".upsert") || event.endsWith(".update")));
}
function applyInitPresenceRealtimeRecord(maps, rawPresence, eventId, options) {
	const nextOnline = new Map(maps.online);
	const nextAway = new Map(maps.away);
	const normalizedDelete = normalizeInitPresenceRecord(rawPresence);
	if (options?.deleted) {
		const userId = normalizedDelete?.userId;
		if (userId) {
			nextOnline.delete(userId);
			nextAway.delete(userId);
		}
		return {
			online: nextOnline,
			away: nextAway
		};
	}
	const normalized = normalizeInitPresenceRecord(rawPresence);
	if (!normalized) return {
		online: nextOnline,
		away: nextAway
	};
	const presence = mergePresenceRecordUpdate(nextOnline.get(normalized.userId) ?? nextAway.get(normalized.userId), normalized);
	if (!presenceMatchesInitEvent(presence, eventId)) {
		nextOnline.delete(presence.userId);
		nextAway.delete(presence.userId);
		return {
			online: nextOnline,
			away: nextAway
		};
	}
	if (!isPresenceActive(presence)) {
		nextOnline.delete(presence.userId);
		nextAway.delete(presence.userId);
		return {
			online: nextOnline,
			away: nextAway
		};
	}
	if (isInitAwayStatus(presence, eventId)) {
		nextAway.set(presence.userId, presence);
		nextOnline.delete(presence.userId);
	} else if (isInitOnlineStatus(presence, eventId)) {
		nextOnline.set(presence.userId, presence);
		nextAway.delete(presence.userId);
	}
	return reconcileExclusivePresenceMaps(nextOnline, nextAway, eventId);
}
function overlayInitPresenceListFetch(previous, onlineFromApi, awayFromApi, eventId) {
	const nextOnline = new Map(previous.online);
	const nextAway = new Map(previous.away);
	for (const [userId, presence] of onlineFromApi) {
		if (!isInitOnlineStatus(presence, eventId) || !isPresenceActive(presence)) continue;
		nextOnline.set(userId, presence);
		nextAway.delete(userId);
	}
	for (const [userId, presence] of awayFromApi) {
		if (!isInitAwayStatus(presence, eventId) || !isPresenceActive(presence)) continue;
		nextAway.set(userId, presence);
		nextOnline.delete(userId);
	}
	for (const [userId, presence] of nextOnline) if (!isPresenceActive(presence)) nextOnline.delete(userId);
	for (const [userId, presence] of nextAway) if (!isPresenceActive(presence)) nextAway.delete(userId);
	return reconcileExclusivePresenceMaps(nextOnline, nextAway, eventId);
}
function pruneExpiredPresenceMaps(maps) {
	const nextOnline = new Map(maps.online);
	const nextAway = new Map(maps.away);
	for (const [userId, presence] of nextOnline) if (!isPresenceActive(presence)) nextOnline.delete(userId);
	for (const [userId, presence] of nextAway) if (!isPresenceActive(presence)) nextAway.delete(userId);
	return {
		online: nextOnline,
		away: nextAway
	};
}
function reconcileExclusivePresenceMaps(online, away, eventId) {
	const nextOnline = new Map(online);
	const nextAway = new Map(away);
	for (const userId of new Set([...nextOnline.keys(), ...nextAway.keys()])) {
		const onlinePresence = nextOnline.get(userId);
		const awayPresence = nextAway.get(userId);
		if (!onlinePresence || !awayPresence) continue;
		const onlineValid = isInitOnlineStatus(onlinePresence, eventId) && isPresenceActive(onlinePresence);
		const awayValid = isInitAwayStatus(awayPresence, eventId) && isPresenceActive(awayPresence);
		if (awayValid && !onlineValid) {
			nextOnline.delete(userId);
			continue;
		}
		if (onlineValid && !awayValid) {
			nextAway.delete(userId);
			continue;
		}
		if (!onlineValid && !awayValid) {
			nextOnline.delete(userId);
			nextAway.delete(userId);
			continue;
		}
		if (presenceUpdatedAt(awayPresence) >= presenceUpdatedAt(onlinePresence)) nextOnline.delete(userId);
		else nextAway.delete(userId);
	}
	return {
		online: nextOnline,
		away: nextAway
	};
}
function presenceToOnlineUser(presence, activityAllowlist) {
	const metadata = parseInitPresenceMetadata(presence.metadata);
	return {
		id: presence.userId,
		name: metadata?.name || "Console user",
		activity: sanitizeInitPresenceActivity(metadata?.activity, activityAllowlist),
		isLive: metadata?.isLive,
		theme: metadata?.theme,
		countryCode: metadata?.countryCode
	};
}
function sortOnlineUsers(users) {
	return [...users].sort((a, b) => {
		const liveDelta = Number(Boolean(b.isLive)) - Number(Boolean(a.isLive));
		if (liveDelta !== 0) return liveDelta;
		return a.name.localeCompare(b.name);
	});
}
function aggregateInitCommunityCountries(presences, activityAllowlist) {
	const counts = /* @__PURE__ */ new Map();
	for (const presence of presences) {
		const user = presenceToOnlineUser(presence, activityAllowlist);
		if (!user.countryCode) continue;
		const code = user.countryCode.toUpperCase();
		counts.set(code, (counts.get(code) ?? 0) + 1);
	}
	return Array.from(counts.entries()).map(([code, count]) => ({
		code,
		count
	})).sort((a, b) => b.count - a.count || a.code.localeCompare(b.code));
}
function enrichPresenceRecord(presence, params) {
	return {
		...presence,
		status: params.status,
		metadata: params.metadata
	};
}
async function listInitPresences(eventId, mode, limit = 50) {
	const scopedStatus = mode === "online" ? buildInitOnlineStatus(eventId) : buildInitAwayStatus(eventId);
	const matchesMode = mode === "online" ? isInitOnlineStatus : isInitAwayStatus;
	return ((await sdk.forConsole.presences.list({
		queries: [
			Query.equal("status", [scopedStatus]),
			Query.limit(limit),
			Query.orderDesc("$updatedAt")
		],
		total: false
	})).presences ?? []).filter((presence) => {
		if (!presenceMatchesInitEvent(presence, eventId)) return false;
		if (!isPresenceActive(presence)) return false;
		return matchesMode(presence, eventId);
	});
}
async function upsertInitPresence(params) {
	const presenceId = buildInitPresenceId(params.userId);
	return enrichPresenceRecord(await sdk.forConsole.presences.upsert({
		presenceId,
		userId: params.userId,
		status: params.status,
		metadata: params.metadata,
		permissions: buildInitPresencePermissions(params.userId),
		expiresAt: buildInitPresenceExpiresAt()
	}), {
		status: params.status,
		metadata: params.metadata
	});
}
function buildPresenceMapForEvent(presences, eventId) {
	const map = /* @__PURE__ */ new Map();
	for (const presence of presences) {
		const normalized = normalizeInitPresenceRecord(presence);
		if (!normalized || !presenceMatchesInitEvent(normalized, eventId)) continue;
		map.set(normalized.userId, normalized);
	}
	return map;
}
function mapPresencesToOnlineUsers(presences, activityAllowlist) {
	const users = [];
	for (const presence of presences) users.push(presenceToOnlineUser(presence, activityAllowlist));
	return sortOnlineUsers(users);
}
var SIDEBAR_USER_LIMIT = 16;
var AWAY_USER_LIMIT = 8;
var ACTIVITY_PUBLISH_DEBOUNCE_MS = 300;
var INIT_PRESENCE_LIST_REFRESH_MS = 5 * 6e4;
var INIT_PARTICIPANT_ONLINE_SESSION_KEY = "console.init.participantOnline";
function readParticipantOnlinePreference() {
	if (typeof window === "undefined") return true;
	try {
		return window.sessionStorage.getItem(INIT_PARTICIPANT_ONLINE_SESSION_KEY) !== "false";
	} catch {
		return true;
	}
}
function writeParticipantOnlinePreference(online) {
	if (typeof window === "undefined") return;
	try {
		window.sessionStorage.setItem(INIT_PARTICIPANT_ONLINE_SESSION_KEY, online ? "true" : "false");
	} catch {}
}
var EMPTY_STATE = {
	onlineUsers: [],
	recentlyOnlineUsers: [],
	onlineCount: 0,
	othersOnlineCount: 0,
	onlineThemeCounts: {
		light: 0,
		dark: 0
	},
	communityCountries: [],
	communityDeveloperCount: 0,
	isReady: false,
	participantStatus: "online",
	isParticipantStatusUpdating: false,
	setParticipantStatus: async () => void 0,
	setBaselineActivity: () => void 0,
	setTransientActivity: () => void 0,
	setPriorityActivity: () => void 0,
	syncPresenceTheme: async () => void 0
};
var createEmptyPresenceMaps = () => ({
	online: /* @__PURE__ */ new Map(),
	away: /* @__PURE__ */ new Map()
});
function reconcileMapsForEvent(maps, eventId) {
	return reconcileExclusivePresenceMaps(maps.online, maps.away, eventId);
}
function useInitOnlinePresence(event, options) {
	const eventId = event?.id;
	const activityAllowlist = useMemo(() => event ? buildInitPresenceActivityAllowlist(event) : null, [event]);
	const enabled = Boolean(eventId) && (options?.enabled ?? true);
	const { theme, resolvedTheme } = useTheme();
	const resolvedThemeRef = useRef(resolvedTheme);
	resolvedThemeRef.current = resolvedTheme;
	const publishThemeRef = useRef(void 0);
	const lastPublishedThemeRef = useRef(null);
	const pendingThemePublishRef = useRef(null);
	const initialThemeSyncDoneRef = useRef(false);
	const themeReady = resolvedTheme !== void 0;
	const { data: account } = useQuery({
		...consoleAccountQueryOptions(),
		enabled
	});
	const { data: localeData } = useQuery({
		queryKey: ["locale", "console"],
		queryFn: fetchLocale,
		staleTime: LONG_STALE_TIME,
		enabled
	});
	const countryCodeRef = useRef(void 0);
	countryCodeRef.current = localeData?.countryCode?.trim().toUpperCase() || void 0;
	const [presenceMaps, setPresenceMaps] = useState(() => ({
		online: /* @__PURE__ */ new Map(),
		away: /* @__PURE__ */ new Map()
	}));
	const [isReady, setIsReady] = useState(false);
	const [participantStatus, setParticipantStatusState] = useState("online");
	const [isParticipantStatusUpdating, setIsParticipantStatusUpdating] = useState(false);
	const [activityDisplayVersion, setActivityDisplayVersion] = useState(0);
	const presenceIdRef = useRef(null);
	const upsertingRef = useRef(false);
	const participantOnlineRef = useRef(true);
	const baselineActivityRef = useRef(INIT_PRESENCE_ACTIVITY_ON_INIT);
	const transientActivityRef = useRef(null);
	const priorityActivityRef = useRef(null);
	const publishDebounceRef = useRef(null);
	const themeActivityResetRef = useRef(null);
	const scheduleThemeActivityResetRef = useRef(() => void 0);
	const eventIdRef = useRef(eventId);
	eventIdRef.current = eventId;
	const publishPresenceRef = useRef(async () => false);
	const resolveActivity = useCallback((away) => {
		if (away) return participantOnlineRef.current ? INIT_PRESENCE_ACTIVITY_LEFT : INIT_PRESENCE_ACTIVITY_OFFLINE;
		if (transientActivityRef.current) return transientActivityRef.current;
		if (priorityActivityRef.current) return priorityActivityRef.current;
		return baselineActivityRef.current;
	}, []);
	const bumpActivityDisplay = useCallback(() => {
		setActivityDisplayVersion((version) => version + 1);
	}, []);
	const accountUserId = account?.$id;
	const accountName = account?.name?.trim() || account?.email?.trim() || "";
	const accountUserIdRef = useRef(accountUserId);
	accountUserIdRef.current = accountUserId;
	const buildMetadata = useCallback((away) => {
		if (!eventId || !accountName) return null;
		return {
			eventId,
			name: accountName,
			activity: resolveActivity(away),
			theme: publishThemeRef.current ?? resolveInitPresenceTheme(resolvedThemeRef.current),
			countryCode: countryCodeRef.current
		};
	}, [
		accountName,
		eventId,
		resolveActivity
	]);
	const refreshLists = useCallback(async (scopeEventId) => {
		const [online, away] = await Promise.all([listInitPresences(scopeEventId, "online"), listInitPresences(scopeEventId, "away", AWAY_USER_LIMIT)]);
		setPresenceMaps((previous) => {
			let next = overlayInitPresenceListFetch(previous, buildPresenceMapForEvent(online, scopeEventId), buildPresenceMapForEvent(away, scopeEventId), scopeEventId);
			const selfId = accountUserIdRef.current;
			if (selfId && participantOnlineRef.current && !next.online.has(selfId)) {
				const selfPresence = previous.online.get(selfId);
				if (selfPresence && isInitOnlineStatus(selfPresence, scopeEventId)) next = reconcileMapsForEvent({
					online: new Map(next.online).set(selfId, selfPresence),
					away: new Map(next.away)
				}, scopeEventId);
			}
			if (selfId && !participantOnlineRef.current && !next.away.has(selfId)) {
				const selfPresence = previous.away.get(selfId);
				if (selfPresence && isInitAwayStatus(selfPresence, scopeEventId)) next = reconcileMapsForEvent({
					online: new Map(next.online),
					away: new Map(next.away).set(selfId, selfPresence)
				}, scopeEventId);
			}
			return next;
		});
	}, []);
	const schedulePresencePublish = useCallback(() => {
		if (publishDebounceRef.current) window.clearTimeout(publishDebounceRef.current);
		publishDebounceRef.current = window.setTimeout(() => {
			publishPresenceRef.current(!participantOnlineRef.current, { refresh: false });
		}, ACTIVITY_PUBLISH_DEBOUNCE_MS);
	}, []);
	const setBaselineActivity = useCallback((activity) => {
		const next = activity.trim();
		if (!next || baselineActivityRef.current === next) return;
		baselineActivityRef.current = next;
		if (!transientActivityRef.current && !priorityActivityRef.current) {
			bumpActivityDisplay();
			schedulePresencePublish();
		}
	}, [bumpActivityDisplay, schedulePresencePublish]);
	const setTransientActivity = useCallback((activity) => {
		const next = activity?.trim() || null;
		if (transientActivityRef.current === next) return;
		transientActivityRef.current = next;
		bumpActivityDisplay();
		schedulePresencePublish();
	}, [bumpActivityDisplay, schedulePresencePublish]);
	const setPriorityActivity = useCallback((activity) => {
		const next = activity?.trim() || null;
		if (priorityActivityRef.current === next) return;
		priorityActivityRef.current = next;
		bumpActivityDisplay();
		schedulePresencePublish();
	}, [bumpActivityDisplay, schedulePresencePublish]);
	const publishPresence = useCallback(async (away, options$1) => {
		if (!enabled || !eventId || !accountUserId) return false;
		const metadata = buildMetadata(away);
		if (!metadata) return false;
		const status = away ? buildInitAwayStatus(eventId) : buildInitOnlineStatus(eventId);
		presenceIdRef.current = buildInitPresenceId(accountUserId);
		if (upsertingRef.current) return false;
		upsertingRef.current = true;
		try {
			const presence = await upsertInitPresence({
				userId: accountUserId,
				status,
				metadata
			});
			presenceIdRef.current = presence.$id;
			if (away) setPresenceMaps((previous) => {
				const nextOnline = new Map(previous.online);
				nextOnline.delete(presence.userId);
				const nextAway = new Map(previous.away);
				nextAway.set(presence.userId, presence);
				return reconcileMapsForEvent({
					online: nextOnline,
					away: nextAway
				}, eventId);
			});
			else setPresenceMaps((previous) => {
				const nextOnline = new Map(previous.online);
				nextOnline.set(presence.userId, presence);
				const nextAway = new Map(previous.away);
				nextAway.delete(presence.userId);
				return reconcileMapsForEvent({
					online: nextOnline,
					away: nextAway
				}, eventId);
			});
			if (options$1?.refresh !== false) await refreshLists(eventId);
			if (metadata.theme) lastPublishedThemeRef.current = metadata.theme;
			return true;
		} catch {
			return false;
		} finally {
			upsertingRef.current = false;
			publishThemeRef.current = void 0;
			const pendingTheme = pendingThemePublishRef.current;
			if (pendingTheme) {
				pendingThemePublishRef.current = null;
				publishThemeRef.current = pendingTheme;
				publishPresenceRef.current(!participantOnlineRef.current, { refresh: false }).then((published) => {
					if (published) scheduleThemeActivityResetRef.current();
				});
			}
		}
	}, [
		accountUserId,
		buildMetadata,
		enabled,
		eventId,
		refreshLists
	]);
	publishPresenceRef.current = publishPresence;
	const scheduleThemeActivityReset = useCallback(() => {
		if (themeActivityResetRef.current) window.clearTimeout(themeActivityResetRef.current);
		themeActivityResetRef.current = window.setTimeout(() => {
			themeActivityResetRef.current = null;
			transientActivityRef.current = null;
			bumpActivityDisplay();
			publishPresence(!participantOnlineRef.current, { refresh: false });
		}, INIT_REACTION_DURATION_MS);
	}, [bumpActivityDisplay, publishPresence]);
	scheduleThemeActivityResetRef.current = scheduleThemeActivityReset;
	const syncPresenceTheme = useCallback(async (themeOverride) => {
		if (!enabled || !eventId || !accountUserId) return;
		const nextTheme = themeOverride ?? resolveInitPresenceTheme(resolvedThemeRef.current);
		if (lastPublishedThemeRef.current === nextTheme) {
			initialThemeSyncDoneRef.current = true;
			return;
		}
		const showThemeActivity = themeOverride != null || initialThemeSyncDoneRef.current;
		if (showThemeActivity) {
			transientActivityRef.current = buildInitSwitchingThemeActivity(nextTheme);
			bumpActivityDisplay();
		}
		if (upsertingRef.current) {
			pendingThemePublishRef.current = nextTheme;
			return;
		}
		publishThemeRef.current = nextTheme;
		await publishPresence(!participantOnlineRef.current, { refresh: false });
		initialThemeSyncDoneRef.current = true;
		if (showThemeActivity) scheduleThemeActivityReset();
	}, [
		accountUserId,
		bumpActivityDisplay,
		enabled,
		eventId,
		publishPresence,
		scheduleThemeActivityReset
	]);
	const setParticipantStatus = useCallback(async (status) => {
		if (!enabled || !eventId || !accountUserId) return;
		const online = status === "online";
		if (participantOnlineRef.current === online) return;
		setIsParticipantStatusUpdating(true);
		participantOnlineRef.current = online;
		setParticipantStatusState(status);
		writeParticipantOnlinePreference(online);
		if (!online) {
			transientActivityRef.current = null;
			priorityActivityRef.current = null;
			bumpActivityDisplay();
		}
		try {
			await publishPresence(!online);
		} finally {
			setIsParticipantStatusUpdating(false);
		}
	}, [
		accountUserId,
		bumpActivityDisplay,
		enabled,
		eventId,
		publishPresence
	]);
	useEffect(() => {
		if (!enabled || !eventId) {
			setPresenceMaps(createEmptyPresenceMaps());
			setIsReady(false);
			baselineActivityRef.current = INIT_PRESENCE_ACTIVITY_ON_INIT;
			transientActivityRef.current = null;
			priorityActivityRef.current = null;
			participantOnlineRef.current = true;
			lastPublishedThemeRef.current = null;
			pendingThemePublishRef.current = null;
			initialThemeSyncDoneRef.current = false;
			setParticipantStatusState("online");
			return;
		}
		if (!accountUserId) {
			setPresenceMaps(createEmptyPresenceMaps());
			setIsReady(true);
			baselineActivityRef.current = INIT_PRESENCE_ACTIVITY_ON_INIT;
			transientActivityRef.current = null;
			priorityActivityRef.current = null;
			participantOnlineRef.current = true;
			lastPublishedThemeRef.current = null;
			pendingThemePublishRef.current = null;
			initialThemeSyncDoneRef.current = false;
			setParticipantStatusState("online");
			return;
		}
		if (!themeReady) return;
		const startOnline = readParticipantOnlinePreference();
		participantOnlineRef.current = startOnline;
		setParticipantStatusState(startOnline ? "online" : "offline");
		let cancelled = false;
		clearLegacyInitPresenceStorage();
		const bootstrap = async () => {
			setIsReady(false);
			const published = await publishPresenceRef.current(!startOnline, { refresh: false });
			if (cancelled) return;
			try {
				await refreshLists(eventId);
				if (!cancelled && published) {
					await new Promise((resolve) => {
						window.setTimeout(resolve, 500);
					});
					if (!cancelled) await refreshLists(eventId);
				}
			} catch {
				if (published && !cancelled) {
					await publishPresenceRef.current(!startOnline, { refresh: false });
					await refreshLists(eventId);
				}
			} finally {
				if (!cancelled) setIsReady(true);
			}
		};
		bootstrap();
		return () => {
			cancelled = true;
			if (publishDebounceRef.current) window.clearTimeout(publishDebounceRef.current);
			if (themeActivityResetRef.current) {
				window.clearTimeout(themeActivityResetRef.current);
				themeActivityResetRef.current = null;
			}
		};
	}, [
		accountUserId,
		enabled,
		eventId,
		refreshLists,
		themeReady
	]);
	useEffect(() => {
		if (!enabled || !eventId || !accountUserId || !isReady || !themeReady) return;
		syncPresenceTheme();
	}, [
		accountUserId,
		enabled,
		eventId,
		isReady,
		themeReady,
		resolvedTheme,
		theme,
		syncPresenceTheme
	]);
	useEffect(() => {
		if (!enabled || !eventId || !accountUserId || !isReady || !localeData?.countryCode) return;
		publishPresence(!participantOnlineRef.current, { refresh: false });
	}, [
		accountUserId,
		enabled,
		eventId,
		isReady,
		localeData?.countryCode,
		publishPresence
	]);
	useEffect(() => {
		if (!enabled || !eventId || !accountUserId) return;
		const heartbeat = window.setInterval(() => {
			publishPresence(!participantOnlineRef.current, { refresh: false });
			setPresenceMaps((previous) => pruneExpiredPresenceMaps(previous));
		}, INIT_PRESENCE_HEARTBEAT_MS);
		const listRefresh = window.setInterval(() => {
			refreshLists(eventId);
		}, INIT_PRESENCE_LIST_REFRESH_MS);
		const onFocus = () => {
			if (participantOnlineRef.current) publishPresence(false, { refresh: false });
		};
		const onVisibility = () => {
			if (document.visibilityState !== "visible" || !participantOnlineRef.current) return;
			publishPresence(false, { refresh: false });
			refreshLists(eventId);
		};
		window.addEventListener("focus", onFocus);
		document.addEventListener("visibilitychange", onVisibility);
		return () => {
			window.clearInterval(heartbeat);
			window.clearInterval(listRefresh);
			window.removeEventListener("focus", onFocus);
			document.removeEventListener("visibilitychange", onVisibility);
		};
	}, [
		accountUserId,
		enabled,
		eventId,
		publishPresence,
		refreshLists
	]);
	useEffect(() => {
		if (!enabled || !eventId || !accountUserId) return;
		const onPageHide = () => {
			publishPresence(true, { refresh: false });
		};
		window.addEventListener("pagehide", onPageHide);
		return () => {
			window.removeEventListener("pagehide", onPageHide);
		};
	}, [
		accountUserId,
		enabled,
		eventId,
		publishPresence
	]);
	const handlePresenceRealtimeRef = useRef((_event) => void 0);
	useEffect(() => {
		handlePresenceRealtimeRef.current = (event$1) => {
			const scopeEventId = eventIdRef.current;
			if (!scopeEventId) return;
			const payload = event$1.payload;
			const isDelete = isPresenceDeleteEvent(event$1.events);
			const isMutation = isPresenceMutationEvent(event$1.events);
			if (!isDelete && !isMutation) return;
			if (!isDelete && !payload) return;
			setPresenceMaps((previous) => applyInitPresenceRealtimeRecord(previous, payload, scopeEventId, { deleted: isDelete }));
		};
	});
	useEffect(() => {
		if (!enabled || !eventId || !accountUserId) return;
		return retainInitPresencesRealtimeListener((event$1) => {
			handlePresenceRealtimeRef.current(event$1);
		});
	}, [
		accountUserId,
		enabled,
		eventId
	]);
	return useMemo(() => {
		if (!enabled || !activityAllowlist) return EMPTY_STATE;
		const selfOnlineActivity = accountUserId ? resolveActivity(false) : null;
		const allOnlineUsers = mapPresencesToOnlineUsers(presenceMaps.online.values(), activityAllowlist).map((user) => accountUserId && selfOnlineActivity && user.id === accountUserId ? {
			...user,
			activity: selfOnlineActivity
		} : user);
		const onlineUsers = allOnlineUsers.slice(0, SIDEBAR_USER_LIMIT);
		const recentlyOnlineUsers = mapPresencesToOnlineUsers(presenceMaps.away.values(), activityAllowlist).slice(0, AWAY_USER_LIMIT);
		const onlineCount = presenceMaps.online.size;
		const othersOnlineCount = Math.max(0, onlineCount - onlineUsers.length);
		const onlineThemeCounts = countInitPresenceThemes(allOnlineUsers);
		const communityCountries = aggregateInitCommunityCountries(presenceMaps.online.values(), activityAllowlist);
		return {
			onlineUsers,
			recentlyOnlineUsers,
			onlineCount,
			othersOnlineCount,
			onlineThemeCounts,
			communityCountries,
			communityDeveloperCount: communityCountries.reduce((total, country) => total + country.count, 0),
			isReady,
			participantStatus,
			isParticipantStatusUpdating,
			setParticipantStatus,
			setBaselineActivity,
			setTransientActivity,
			setPriorityActivity,
			syncPresenceTheme
		};
	}, [
		accountUserId,
		activityAllowlist,
		activityDisplayVersion,
		enabled,
		isReady,
		isParticipantStatusUpdating,
		presenceMaps,
		participantStatus,
		resolveActivity,
		setBaselineActivity,
		setParticipantStatus,
		setTransientActivity,
		setPriorityActivity,
		syncPresenceTheme
	]);
}
var InitPresenceContext = createContext(null);
function InitPresenceProvider({ event, enabled, children }) {
	const presence = useInitOnlinePresence(event, { enabled });
	const value = useMemo(() => ({
		onlineUsers: presence.onlineUsers,
		recentlyOnlineUsers: presence.recentlyOnlineUsers,
		onlineCount: presence.onlineCount,
		othersOnlineCount: presence.othersOnlineCount,
		onlineThemeCounts: presence.onlineThemeCounts,
		communityCountries: presence.communityCountries,
		communityDeveloperCount: presence.communityDeveloperCount,
		isReady: presence.isReady,
		participantStatus: presence.participantStatus,
		isParticipantStatusUpdating: presence.isParticipantStatusUpdating,
		setParticipantStatus: presence.setParticipantStatus,
		setBaselineActivity: presence.setBaselineActivity,
		setTransientActivity: presence.setTransientActivity,
		setPriorityActivity: presence.setPriorityActivity,
		syncPresenceTheme: presence.syncPresenceTheme
	}), [presence]);
	return /* @__PURE__ */ jsx(InitPresenceContext.Provider, {
		value,
		children
	});
}
function useInitPresence() {
	const context = useContext(InitPresenceContext);
	if (!context) return {
		onlineUsers: [],
		recentlyOnlineUsers: [],
		onlineCount: 0,
		othersOnlineCount: 0,
		onlineThemeCounts: {
			light: 0,
			dark: 0
		},
		communityCountries: [],
		communityDeveloperCount: 0,
		isReady: false,
		participantStatus: "online",
		isParticipantStatusUpdating: false,
		setParticipantStatus: async () => void 0,
		setBaselineActivity: () => void 0,
		setTransientActivity: () => void 0,
		setPriorityActivity: () => void 0,
		syncPresenceTheme: async () => void 0
	};
	return context;
}
function useInitPresenceActivity() {
	const { setBaselineActivity, setTransientActivity, setPriorityActivity } = useInitPresence();
	return {
		setBaselineActivity,
		setTransientActivity,
		setPriorityActivity
	};
}
const INIT_DAY_SCROLL_SPY_OFFSET_PX = 112;
function useInitScrollSpyDay(dayNumbers) {
	const sortedDays = useMemo(() => [...dayNumbers].sort((a, b) => a - b), [dayNumbers]);
	const [activeDay, setActiveDay] = useState(sortedDays[0] ?? 1);
	useEffect(() => {
		setActiveDay(sortedDays[0] ?? 1);
	}, [sortedDays]);
	useEffect(() => {
		const main = document.getElementById("main-content");
		if (!main || sortedDays.length === 0) return;
		let frame = 0;
		const update = () => {
			cancelAnimationFrame(frame);
			frame = requestAnimationFrame(() => {
				const anchor = main.getBoundingClientRect().top + INIT_DAY_SCROLL_SPY_OFFSET_PX;
				let current = sortedDays[0];
				for (const day of sortedDays) {
					const el = document.getElementById(getInitDayCardId(day));
					if (!el) continue;
					if (el.getBoundingClientRect().top <= anchor) current = day;
				}
				setActiveDay(current);
			});
		};
		update();
		main.addEventListener("scroll", update, { passive: true });
		window.addEventListener("resize", update);
		const resizeObserver = new ResizeObserver(update);
		resizeObserver.observe(main);
		return () => {
			cancelAnimationFrame(frame);
			main.removeEventListener("scroll", update);
			window.removeEventListener("resize", update);
			resizeObserver.disconnect();
		};
	}, [sortedDays]);
	return activeDay;
}
function EventCtaButton({ cta, variant, size = "sm" }) {
	const content = /* @__PURE__ */ jsxs(Fragment, { children: [cta.label.toLowerCase().includes("ticket") ? /* @__PURE__ */ jsx(Ticket, { className: "size-4" }) : null, cta.label] });
	const sizeClass = size === "lg" ? "h-10 text-[14px]" : size === "default" ? "h-9 text-[13px]" : "h-8 text-[13px]";
	if (cta.to) return /* @__PURE__ */ jsx(Button, {
		variant,
		size,
		className: sizeClass,
		asChild: true,
		children: /* @__PURE__ */ jsx(Link, {
			to: cta.to,
			search: cta.redirect ? { redirect: cta.redirect } : void 0,
			children: content
		})
	});
	if (cta.href) return /* @__PURE__ */ jsx(Button, {
		variant,
		size,
		className: sizeClass,
		asChild: true,
		children: /* @__PURE__ */ jsx("a", {
			href: cta.href,
			target: cta.external !== false ? "_blank" : void 0,
			rel: cta.external !== false ? "noopener noreferrer" : void 0,
			children: content
		})
	});
	return /* @__PURE__ */ jsx(Button, {
		variant,
		size,
		className: sizeClass,
		children: content
	});
}
function isExternalInitHref(href) {
	return /^https?:\/\//i.test(href) || href.startsWith("//");
}
function buildInitDayPreviewActivity(day, currentDay, seed) {
	if (isLaunchEventDayLocked(day)) return buildInitWaitingForDayActivity(day.day, {
		currentDay,
		seed: seed ?? `preview:${day.day}`
	});
	return buildInitPreviewingDayActivity(day.day, {
		locked: false,
		title: day.title
	});
}
function buildInitDayReadingActivity(day, currentDay, seed) {
	if (isLaunchEventDayLocked(day)) return buildInitWaitingForDayActivity(day.day, {
		currentDay,
		seed: seed ?? `reading:${day.day}`
	});
	return buildInitReadingDayActivity(day.day, { title: day.title });
}
function findInitDayView(days, dayNumber) {
	return days.find((day) => day.day === dayNumber);
}
function resolveInitScrollSpyBaselineDay(days, activeDay, currentDay) {
	const activeDayView = findInitDayView(days, activeDay);
	if (activeDayView && !isLaunchEventDayLocked(activeDayView)) return activeDayView;
	if (currentDay <= 0) return void 0;
	const currentDayView = findInitDayView(days, currentDay);
	if (currentDayView && !isLaunchEventDayLocked(currentDayView)) return currentDayView;
	return days.filter((day) => !isLaunchEventDayLocked(day) && day.day <= currentDay).sort((a, b) => b.day - a.day)[0];
}
function InitCollapsedDayNav({ days, activeDay, currentDay }) {
	const { setTransientActivity } = useInitPresenceActivity();
	if (days.length === 0) return null;
	return /* @__PURE__ */ jsx("nav", {
		className: "flex min-w-0 justify-center gap-0.5 overflow-x-auto px-1 [-ms-overflow-style:none] [scrollbar-width:none] [&::-webkit-scrollbar]:hidden",
		"aria-label": "Init days",
		children: days.map((day) => {
			const isActive = day.day === activeDay;
			return /* @__PURE__ */ jsxs(Button, {
				type: "button",
				variant: "ghost",
				size: "sm",
				className: cn("h-8 shrink-0 rounded-md px-2.5 text-[12px] font-medium text-muted-foreground", isActive && "bg-accent text-foreground"),
				"aria-current": isActive ? "true" : void 0,
				onClick: () => scrollToInitDayCard(day.day),
				onMouseEnter: () => setTransientActivity(buildInitDayPreviewActivity(day, currentDay)),
				onMouseLeave: () => setTransientActivity(null),
				onFocus: () => setTransientActivity(buildInitDayPreviewActivity(day, currentDay)),
				onBlur: () => setTransientActivity(null),
				children: ["Day ", day.day]
			}, day.day);
		})
	});
}
var DEFAULT_ACCENT_HSL = {
	h: 344,
	s: 98,
	l: 62
};
function rgbToHsl(r, g, b) {
	const rn = r / 255;
	const gn = g / 255;
	const bn = b / 255;
	const max = Math.max(rn, gn, bn);
	const min = Math.min(rn, gn, bn);
	const delta = max - min;
	let h = 0;
	let s = 0;
	const l = (max + min) / 2;
	if (delta !== 0) {
		s = delta / (1 - Math.abs(2 * l - 1));
		switch (max) {
			case rn:
				h = ((gn - bn) / delta + (gn < bn ? 6 : 0)) * 60;
				break;
			case gn:
				h = ((bn - rn) / delta + 2) * 60;
				break;
			default:
				h = ((rn - gn) / delta + 4) * 60;
				break;
		}
	}
	return {
		h: Math.round(h),
		s: Math.round(s * 100),
		l: Math.round(l * 100)
	};
}
function resolveCssColor(color, context) {
	if (typeof document === "undefined") return color;
	const probe = document.createElement("span");
	probe.style.color = color;
	probe.style.display = "none";
	(context ?? document.documentElement).appendChild(probe);
	const resolved = getComputedStyle(probe).color;
	probe.remove();
	return resolved;
}
function parseCssAccentToHsl(color, context) {
	const match = resolveCssColor(color, context).match(/rgba?\(\s*([\d.]+)[,\s]+([\d.]+)[,\s]+([\d.]+)/i);
	if (!match) return DEFAULT_ACCENT_HSL;
	const r = Number(match[1]);
	const g = Number(match[2]);
	const b = Number(match[3]);
	if ([
		r,
		g,
		b
	].some((channel) => Number.isNaN(channel))) return DEFAULT_ACCENT_HSL;
	return rgbToHsl(r, g, b);
}
function accentHslToCss({ h, s, l }) {
	return `hsl(${h}, ${s}%, ${l}%)`;
}
var ROWS = 13;
var LOW_POWER_ROWS = 9;
var FROM = "center";
var PARTICLE_BRIGHTNESS = .8;
function getBrandPink() {
	if (typeof document === "undefined") return "#ff8d11";
	return getComputedStyle(document.documentElement).getPropertyValue("--brand-cta").trim() || "#ff8d11";
}
function isDarkChrome(resolvedTheme) {
	if (resolvedTheme === "light") return false;
	if (resolvedTheme === "dark") return true;
	if (!resolvedTheme) return isHtmlDarkChrome();
	if (isResolvedThemeDarkChrome(resolvedTheme)) return true;
	return isHtmlDarkChrome();
}
function dim(value) {
	return value * PARTICLE_BRIGHTNESS;
}
function dimBrandPink(color) {
	return `color-mix(in srgb, ${color} ${PARTICLE_BRIGHTNESS * 100}%, transparent)`;
}
function getParticleTheme(isDark) {
	if (isDark) return {
		opacityRange: [dim(1), dim(.5)],
		lightnessRange: [dim(80), dim(50)],
		shadowRange: [dim(5), dim(1)],
		pulseOpacity: dim(1)
	};
	return {
		opacityRange: [.95, .6],
		lightnessRange: [72, 56],
		shadowRange: [8, 2],
		pulseOpacity: 1
	};
}
function applyParticleTheme(runtime, particleTheme, isDark, accent, lowPower) {
	const { particleEls, scaleStagger, grid, from } = runtime;
	const accentCss = accentHslToCss(accent);
	const opacityStagger = stagger(particleTheme.opacityRange, {
		grid,
		from
	});
	const shadowColor = isDark ? dimBrandPink(accentCss) : `color-mix(in srgb, ${accentCss} 90%, transparent)`;
	const lightnessStagger = stagger(particleTheme.lightnessRange, {
		grid,
		from
	});
	const shadowStagger = stagger(particleTheme.shadowRange, {
		grid,
		from
	});
	const backgroundStagger = (target, index, targets, prevTween, tl) => {
		const lightness = lightnessStagger(target, index, targets, prevTween, tl);
		return `hsl(${accent.h}, ${accent.s}%, ${lightness}%)`;
	};
	const boxShadowStagger = (target, index, targets, prevTween, tl) => {
		const shadow = shadowStagger(target, index, targets, prevTween, tl);
		return `0px 0px ${utils.round(shadow, 0)}em 0px ${shadowColor}`;
	};
	utils.set(particleEls, {
		scale: scaleStagger,
		opacity: opacityStagger,
		background: backgroundStagger,
		boxShadow: lowPower ? "none" : boxShadowStagger
	});
	return {
		opacityStagger,
		accent
	};
}
function InitHeroBackground({ containerRef, compact = false, active = true, bare = false, accentColor, particleIsDark, particleMotionSpeed = 1, onInteractionStart, onInteractionEnd, keepAliveWhenHidden = false }) {
	const creatureRef = useRef(null);
	const runtimeRef = useRef(null);
	const themeStateRef = useRef(null);
	const compactRef = useRef(compact);
	const activeRef = useRef(active);
	const canRunRef = useRef(true);
	const keepAliveWhenHiddenRef = useRef(keepAliveWhenHidden);
	const onInteractionStartRef = useRef(onInteractionStart);
	const onInteractionEndRef = useRef(onInteractionEnd);
	const { resolvedTheme } = useTheme();
	const isDark = particleIsDark ?? isDarkChrome(resolvedTheme);
	const accentColorRef = useRef(accentColor);
	const animationDecision = useInitLowPowerAnimationDecision();
	const lowPower = animationDecision.enabled;
	compactRef.current = compact;
	activeRef.current = active;
	accentColorRef.current = accentColor;
	keepAliveWhenHiddenRef.current = keepAliveWhenHidden;
	onInteractionStartRef.current = onInteractionStart;
	onInteractionEndRef.current = onInteractionEnd;
	useEffect(() => {
		const runtime = runtimeRef.current;
		if (!runtime) return;
		if (active && canRunRef.current) runtime.resume();
		else runtime.pause();
	}, [active]);
	useEffect(() => {
		const container = containerRef.current;
		const creatureEl = creatureRef.current;
		if (!container || !creatureEl) return;
		if (!animationDecision.joolAnimationEnabled) return;
		const rows = lowPower ? LOW_POWER_ROWS : ROWS;
		const grid = [rows, rows];
		const from = FROM;
		const motion$1 = Math.max(.12, Math.min(1, particleMotionSpeed));
		const dur = (ms) => ms / motion$1;
		const timeScale = (factor) => factor * motion$1;
		const scaleStagger = stagger(lowPower ? [1.6, 3.6] : [2, 5], {
			ease: "inQuad",
			grid,
			from
		});
		const darkChrome = particleIsDark ?? isDarkChrome(resolvedTheme);
		const particleTheme = getParticleTheme(darkChrome);
		const accent = parseCssAccentToHsl(accentColorRef.current ?? getBrandPink(), container);
		for (let i = 0; i < rows * rows; i++) {
			const particle = document.createElement("div");
			particle.className = "init-hero-particle";
			creatureEl.appendChild(particle);
		}
		const particleEls = creatureEl.querySelectorAll(".init-hero-particle");
		const syncLayout = () => {
			const isCompact = compactRef.current;
			const multiplier = isCompact ? .0045 : .0025;
			const minSize = isCompact ? .75 : 1;
			creatureEl.style.fontSize = `${Math.max(container.clientHeight * multiplier, minSize)}px`;
		};
		syncLayout();
		utils.set(creatureEl, {
			width: `${rows * 10}em`,
			height: `${rows * 10}em`
		});
		const { opacityStagger, accent: appliedAccent } = applyParticleTheme({
			particleEls,
			scaleStagger,
			grid,
			from
		}, particleTheme, darkChrome, accent, lowPower);
		themeStateRef.current = {
			particleTheme,
			opacityStagger,
			accent: appliedAccent,
			lowPower
		};
		utils.set(particleEls, {
			x: 0,
			y: 0,
			zIndex: stagger([rows * rows, 1], {
				grid,
				from,
				modifier: utils.round(0)
			})
		});
		const getViewport = () => {
			const isCompact = compactRef.current;
			return {
				w: container.clientWidth * (isCompact ? .32 : .5),
				h: container.clientHeight * (isCompact ? .9 : .5)
			};
		};
		let viewport = getViewport();
		const cursor = {
			x: 0,
			y: 0
		};
		const pulse = () => {
			if (lowPower) return;
			const themeState = themeStateRef.current;
			if (!themeState) return;
			animate(particleEls, { keyframes: [{
				scale: 4,
				opacity: themeState.particleTheme.pulseOpacity,
				delay: stagger(dur(90), {
					start: dur(1650),
					grid,
					from
				}),
				duration: dur(150)
			}, {
				scale: scaleStagger,
				opacity: themeState.opacityStagger,
				ease: "inOutQuad",
				duration: dur(600)
			}] });
		};
		const mainLoop = createTimer({
			frameRate: lowPower ? 8 : 15,
			onUpdate: () => {
				if (!canRunRef.current || !activeRef.current) return;
				animate(particleEls, {
					x: cursor.x,
					y: cursor.y,
					delay: stagger(lowPower ? dur(70) : dur(40), {
						grid,
						from
					}),
					duration: stagger(lowPower ? dur(180) : dur(120), {
						start: lowPower ? dur(950) : dur(750),
						ease: "inQuad",
						grid,
						from
					}),
					ease: "inOut",
					composition: "blend"
				});
			}
		});
		const autoMove = createTimeline().add(cursor, {
			x: [-viewport.w * .45, viewport.w * .45],
			modifier: (x) => x + Math.sin(mainLoop.currentTime * timeScale(7e-4)) * viewport.w * .5,
			duration: dur(3e3),
			ease: "inOutExpo",
			alternate: true,
			loop: true,
			onBegin: pulse,
			onLoop: pulse
		}, 0).add(cursor, {
			y: [-viewport.h * .45, viewport.h * .45],
			modifier: (y) => y + Math.cos(mainLoop.currentTime * timeScale(12e-5)) * viewport.h * .5,
			duration: dur(1e3),
			ease: "inOutQuad",
			alternate: true,
			loop: true
		}, 0);
		const manualMovementTimeout = createTimer({
			duration: dur(1500),
			onComplete: () => {
				onInteractionEndRef.current?.();
				if (canRunRef.current && activeRef.current) autoMove.play();
			}
		});
		const pause = () => {
			mainLoop.pause();
			autoMove.pause();
			manualMovementTimeout.pause();
		};
		const resume = () => {
			if (!canRunRef.current || !activeRef.current) return;
			if (document.visibilityState === "hidden" && !keepAliveWhenHiddenRef.current) return;
			mainLoop.play();
			autoMove.play();
		};
		const followPointer = (event) => {
			if (!canRunRef.current || !activeRef.current) return;
			const rect = container.getBoundingClientRect();
			const point = event.type === "touchmove" ? event.touches[0] : event;
			if (!point) return;
			onInteractionStartRef.current?.();
			cursor.x = point.clientX - rect.left - viewport.w;
			cursor.y = point.clientY - rect.top - viewport.h;
			autoMove.pause();
			manualMovementTimeout.restart();
		};
		const endPointerInteraction = () => {
			onInteractionEndRef.current?.();
		};
		const handleResize = () => {
			viewport = getViewport();
			syncLayout();
		};
		const handleVisibility = () => {
			if (document.visibilityState === "hidden") {
				if (keepAliveWhenHiddenRef.current) return;
				pause();
				return;
			}
			if (canRunRef.current && activeRef.current) resume();
		};
		const intersectionObserver = new IntersectionObserver(([entry]) => {
			canRunRef.current = entry?.isIntersecting ?? true;
			if (canRunRef.current && activeRef.current && (document.visibilityState === "visible" || keepAliveWhenHiddenRef.current)) resume();
			else if (!keepAliveWhenHiddenRef.current) pause();
		}, { threshold: .05 });
		const resizeObserver = new ResizeObserver(handleResize);
		resizeObserver.observe(container);
		intersectionObserver.observe(container);
		container.addEventListener("mousemove", followPointer);
		container.addEventListener("touchmove", followPointer, { passive: true });
		container.addEventListener("mouseleave", endPointerInteraction);
		container.addEventListener("touchend", endPointerInteraction);
		document.addEventListener("visibilitychange", handleVisibility);
		if (activeRef.current) resume();
		runtimeRef.current = {
			particleEls,
			scaleStagger,
			grid,
			from,
			mainLoop,
			autoMove,
			manualMovementTimeout,
			syncLayout,
			pause,
			resume
		};
		return () => {
			resizeObserver.disconnect();
			intersectionObserver.disconnect();
			container.removeEventListener("mousemove", followPointer);
			container.removeEventListener("touchmove", followPointer);
			container.removeEventListener("mouseleave", endPointerInteraction);
			container.removeEventListener("touchend", endPointerInteraction);
			document.removeEventListener("visibilitychange", handleVisibility);
			pause();
			creatureEl.replaceChildren();
			runtimeRef.current = null;
			themeStateRef.current = null;
		};
	}, [
		containerRef,
		particleIsDark,
		particleMotionSpeed,
		lowPower,
		resolvedTheme,
		animationDecision.joolAnimationEnabled
	]);
	useEffect(() => {
		runtimeRef.current?.syncLayout();
	}, [compact]);
	useEffect(() => {
		if (!runtimeRef.current) return;
		let cancelled = false;
		let outerFrame = 0;
		let innerFrame = 0;
		outerFrame = requestAnimationFrame(() => {
			innerFrame = requestAnimationFrame(() => {
				if (cancelled || !runtimeRef.current) return;
				const container = containerRef.current;
				const darkChrome = particleIsDark ?? isDarkChrome(resolvedTheme);
				const particleTheme = getParticleTheme(darkChrome);
				const accent = parseCssAccentToHsl(accentColorRef.current ?? getBrandPink(), container);
				const { opacityStagger, accent: appliedAccent } = applyParticleTheme(runtimeRef.current, particleTheme, darkChrome, accent, lowPower);
				themeStateRef.current = {
					particleTheme,
					opacityStagger,
					accent: appliedAccent,
					lowPower
				};
			});
		});
		return () => {
			cancelled = true;
			cancelAnimationFrame(outerFrame);
			cancelAnimationFrame(innerFrame);
		};
	}, [
		resolvedTheme,
		accentColor,
		particleIsDark,
		containerRef,
		lowPower
	]);
	const creatureClassName = isDark ? "flex flex-wrap items-center justify-center [&>.init-hero-particle]:relative [&>.init-hero-particle]:m-[3em] [&>.init-hero-particle]:size-[4em] [&>.init-hero-particle]:rounded-[2em] [&>.init-hero-particle]:[mix-blend-mode:plus-lighter] [&>.init-hero-particle]:will-change-transform [&>.init-hero-particle]:[transform-style:preserve-3d]" : "flex flex-wrap items-center justify-center [&>.init-hero-particle]:relative [&>.init-hero-particle]:m-[3em] [&>.init-hero-particle]:size-[4em] [&>.init-hero-particle]:rounded-[2em] [&>.init-hero-particle]:mix-blend-normal [&>.init-hero-particle]:will-change-transform [&>.init-hero-particle]:[transform-style:preserve-3d]";
	return /* @__PURE__ */ jsxs(Fragment, { children: [
		!bare ? /* @__PURE__ */ jsx("div", {
			className: cn("absolute inset-0", compact ? "bg-transparent" : "bg-background"),
			"aria-hidden": true
		}) : null,
		!bare ? /* @__PURE__ */ jsx("div", {
			className: cn("absolute inset-0 bg-[radial-gradient(circle,var(--border)_1px,transparent_1px)] bg-[length:18px_18px] transition-opacity duration-500", compact && "opacity-0 [mask-image:linear-gradient(to_left,transparent,black_35%)]"),
			"aria-hidden": true
		}) : null,
		/* @__PURE__ */ jsx("div", {
			className: cn("pointer-events-none absolute inset-0 flex overflow-hidden transition-[justify-content,padding] duration-500 ease-[cubic-bezier(0.4,0,0.2,1)]", compact ? "items-center justify-end pe-2 sm:pe-6" : "items-center justify-center"),
			"aria-hidden": true,
			children: /* @__PURE__ */ jsx("div", {
				ref: creatureRef,
				className: creatureClassName,
				style: {
					width: "150em",
					height: "150em"
				}
			})
		})
	] });
}
var InitTicketVideoRecordingContext = createContext(null);
function InitTicketVideoRecordingProvider({ children }) {
	const [isCapturing, setIsCapturing] = useState(false);
	useEffect(() => {
		if (isCapturing) document.documentElement.dataset.initTicketVideoCapturing = "";
		else delete document.documentElement.dataset.initTicketVideoCapturing;
		return () => {
			delete document.documentElement.dataset.initTicketVideoCapturing;
		};
	}, [isCapturing]);
	const value = useMemo(() => ({
		isCapturing,
		setIsCapturing
	}), [isCapturing]);
	return /* @__PURE__ */ jsx(InitTicketVideoRecordingContext.Provider, {
		value,
		children
	});
}
function useInitTicketVideoRecording() {
	const context = useContext(InitTicketVideoRecordingContext);
	if (!context) return {
		isCapturing: false,
		setIsCapturing: () => void 0
	};
	return context;
}
function CollapsedHeroBar({ event, visible, liveBanner, bounds, dayNumbers, activeDay, particlesActive, days }) {
	const barRef = useRef(null);
	const showDayNav = dayNumbers.length > 0;
	const { setTransientActivity } = useInitPresenceActivity();
	const handleJoolInteractionStart = useCallback(() => {
		setTransientActivity(buildInitPlayingWithJoolActivity());
	}, [setTransientActivity]);
	const handleJoolInteractionEnd = useCallback(() => {
		setTransientActivity(null);
	}, [setTransientActivity]);
	return /* @__PURE__ */ jsxs("div", {
		ref: barRef,
		"aria-hidden": !visible,
		className: cn("fixed z-[15] overflow-hidden border-b border-border", "bg-background/95 backdrop-blur-sm supports-[backdrop-filter]:bg-background/80", "transition-[transform,opacity] duration-300 ease-out", visible ? "translate-y-0 opacity-100" : "pointer-events-none -translate-y-full opacity-0"),
		style: {
			top: bounds?.top ?? 0,
			left: bounds?.left ?? 0,
			width: bounds?.width ?? "100%",
			height: 56
		},
		children: [visible ? /* @__PURE__ */ jsx(InitHeroBackground, {
			containerRef: barRef,
			compact: true,
			active: particlesActive,
			onInteractionStart: handleJoolInteractionStart,
			onInteractionEnd: handleJoolInteractionEnd
		}) : null, /* @__PURE__ */ jsx("div", {
			className: cn("absolute inset-0 z-20 flex items-center", visible && "pointer-events-auto"),
			children: showDayNav ? /* @__PURE__ */ jsxs("div", {
				className: "relative mx-auto flex w-full max-w-7xl items-center px-4 sm:px-6",
				children: [
					/* @__PURE__ */ jsxs("div", {
						className: "relative z-10 hidden min-w-0 shrink-0 items-center gap-2 sm:flex sm:gap-3",
						children: [/* @__PURE__ */ jsx(InitWordmark, { className: "shrink-0 text-[20px] text-foreground sm:text-[22px]" }), /* @__PURE__ */ jsx("span", {
							className: "hidden truncate text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground sm:inline",
							children: event.dateRangeLabel
						})]
					}),
					/* @__PURE__ */ jsx("div", {
						className: cn("pointer-events-none absolute inset-0 flex items-center justify-center px-4", liveBanner ? "pe-[5.75rem] sm:px-28 sm:pe-36 md:px-36" : "sm:px-28 md:px-36"),
						children: /* @__PURE__ */ jsx("div", {
							className: "pointer-events-auto w-full max-w-full sm:max-w-none",
							children: /* @__PURE__ */ jsx(InitCollapsedDayNav, {
								days,
								activeDay,
								currentDay: event.currentDay
							})
						})
					}),
					/* @__PURE__ */ jsx("div", {
						className: "relative z-10 ms-auto flex shrink-0 justify-end",
						children: liveBanner ? /* @__PURE__ */ jsxs("div", {
							className: "flex items-center gap-1.5 sm:gap-2",
							children: [/* @__PURE__ */ jsx(Badge, {
								variant: "error",
								className: "text-[10px] shrink-0",
								children: "Live"
							}), liveBanner.href ? /* @__PURE__ */ jsx(Button, {
								variant: "outline",
								size: "sm",
								className: "h-7 shrink-0 px-2 text-[12px] sm:px-3",
								asChild: true,
								children: /* @__PURE__ */ jsxs("a", {
									href: liveBanner.href,
									...isExternalInitHref(liveBanner.href) ? {
										target: "_blank",
										rel: "noopener noreferrer"
									} : {},
									children: ["Watch", /* @__PURE__ */ jsx(ChevronRight, { className: "size-3.5" })]
								})
							}) : /* @__PURE__ */ jsx("span", {
								className: "hidden max-w-[120px] truncate text-[12px] font-medium text-foreground sm:inline",
								children: liveBanner.title
							})]
						}) : null
					})
				]
			}) : /* @__PURE__ */ jsxs("div", {
				className: "mx-auto flex w-full max-w-7xl items-center gap-3 px-4 sm:gap-4 sm:px-6",
				children: [/* @__PURE__ */ jsx(InitWordmark, { className: "shrink-0 text-[22px] text-foreground sm:text-[24px]" }), liveBanner ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsxs("div", {
					className: "flex min-w-0 flex-1 items-center gap-2",
					children: [/* @__PURE__ */ jsx(Badge, {
						variant: "error",
						className: "text-[10px] shrink-0",
						children: "Live now"
					}), /* @__PURE__ */ jsx("span", {
						className: "truncate text-[13px] font-medium text-foreground",
						children: liveBanner.title
					})]
				}), liveBanner.href ? /* @__PURE__ */ jsx(Button, {
					variant: "outline",
					size: "sm",
					className: "h-7 shrink-0 text-[12px]",
					asChild: true,
					children: /* @__PURE__ */ jsxs("a", {
						href: liveBanner.href,
						...isExternalInitHref(liveBanner.href) ? {
							target: "_blank",
							rel: "noopener noreferrer"
						} : {},
						children: ["Watch", /* @__PURE__ */ jsx(ChevronRight, { className: "size-3.5" })]
					})
				}) : null] }) : /* @__PURE__ */ jsx("span", {
					className: "hidden text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground sm:inline",
					children: event.dateRangeLabel
				})]
			})
		})]
	});
}
function EventHero({ event, headerAddon, liveBanner }) {
	const { data: account } = useQuery(consoleAccountQueryOptions());
	const heroRef = useRef(null);
	const sentinelRef = useRef(null);
	const [isCollapsed, setIsCollapsed] = useState(false);
	const [collapseEnabled, setCollapseEnabled] = useState(true);
	const [mainBounds, setMainBounds] = useState(null);
	const dayNumbers = useMemo(() => event.days.map((day) => day.day), [event.days]);
	const activeDay = useInitScrollSpyDay(dayNumbers);
	const { isCapturing: isTicketVideoCapturing } = useInitTicketVideoRecording();
	const { setTransientActivity } = useInitPresenceActivity();
	const handleJoolInteractionStart = useCallback(() => {
		setTransientActivity(buildInitPlayingWithJoolActivity());
	}, [setTransientActivity]);
	const handleJoolInteractionEnd = useCallback(() => {
		setTransientActivity(null);
	}, [setTransientActivity]);
	useEffect(() => {
		const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
		setCollapseEnabled(!prefersReducedMotion);
	}, []);
	useEffect(() => {
		const main = document.getElementById("main-content");
		if (!main) return;
		let frame = 0;
		const updateBounds = () => {
			cancelAnimationFrame(frame);
			frame = requestAnimationFrame(() => {
				const rect = main.getBoundingClientRect();
				setMainBounds({
					left: rect.left,
					width: rect.width,
					top: rect.top
				});
			});
		};
		updateBounds();
		const resizeObserver = new ResizeObserver(updateBounds);
		resizeObserver.observe(main);
		window.addEventListener("resize", updateBounds);
		main.addEventListener("scroll", updateBounds, { passive: true });
		return () => {
			cancelAnimationFrame(frame);
			resizeObserver.disconnect();
			window.removeEventListener("resize", updateBounds);
			main.removeEventListener("scroll", updateBounds);
		};
	}, []);
	useEffect(() => {
		if (!collapseEnabled || isTicketVideoCapturing) return;
		const sentinel = sentinelRef.current;
		const main = document.getElementById("main-content");
		if (!sentinel || !main) return;
		const observer = new IntersectionObserver(([entry]) => {
			if (!entry.isIntersecting) {
				setIsCollapsed(true);
				return;
			}
			if (entry.boundingClientRect.top >= -1) setIsCollapsed(false);
		}, {
			root: main,
			threshold: 0
		});
		observer.observe(sentinel);
		return () => observer.disconnect();
	}, [collapseEnabled, isTicketVideoCapturing]);
	return /* @__PURE__ */ jsxs(Fragment, { children: [
		collapseEnabled ? /* @__PURE__ */ jsx(CollapsedHeroBar, {
			event,
			visible: isCollapsed && !isTicketVideoCapturing,
			liveBanner,
			bounds: mainBounds,
			dayNumbers,
			activeDay,
			particlesActive: !isTicketVideoCapturing,
			days: event.days
		}) : null,
		/* @__PURE__ */ jsxs("div", {
			ref: heroRef,
			className: "relative min-h-[420px] overflow-hidden border-b border-border bg-background sm:min-h-[480px]",
			children: [/* @__PURE__ */ jsx(InitHeroBackground, {
				containerRef: heroRef,
				active: !isTicketVideoCapturing,
				onInteractionStart: handleJoolInteractionStart,
				onInteractionEnd: handleJoolInteractionEnd
			}), /* @__PURE__ */ jsxs("div", {
				className: "relative z-10 mx-auto flex min-h-[420px] w-full max-w-7xl flex-col items-center justify-center px-4 py-12 text-center sm:min-h-[480px] sm:px-6 sm:py-16",
				children: [
					headerAddon,
					/* @__PURE__ */ jsx("p", {
						className: cn("text-[11px] font-semibold uppercase tracking-[0.25em] text-foreground", headerAddon && "mt-4"),
						children: event.dateRangeLabel
					}),
					/* @__PURE__ */ jsx(InitWordmark, { className: "mt-6 text-[clamp(48px,12vw,96px)] text-foreground" }),
					/* @__PURE__ */ jsx("p", {
						className: "mx-auto mt-4 max-w-xl text-[14px] leading-relaxed text-muted-foreground",
						children: event.description
					}),
					!event.isRecapMode ? /* @__PURE__ */ jsx("div", {
						className: "mt-6 flex flex-wrap items-center justify-center gap-2",
						children: !account ? /* @__PURE__ */ jsx(EventCtaButton, {
							cta: event.primaryCta,
							variant: "brandCta",
							size: "lg"
						}) : event.prizes ? /* @__PURE__ */ jsx(Button, {
							variant: "brandCta",
							size: "lg",
							className: "h-10 text-[14px]",
							asChild: true,
							children: /* @__PURE__ */ jsx("a", {
								href: `#${INIT_PRIZES_SECTION_ID}`,
								children: event.giveaway?.ctaLabel ?? "View prizes"
							})
						}) : null
					}) : null,
					!event.isRecapMode && event.presenceEnabled ? /* @__PURE__ */ jsx("div", {
						className: "mt-8 flex min-h-8 items-center justify-center gap-3",
						children: event.onlineCount > 0 ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
							className: "flex -space-x-2",
							children: event.onlineUsers.slice(0, 4).map((user) => /* @__PURE__ */ jsx(InitialsAvatar, {
								name: user.name,
								size: "sm",
								className: "ring-2 ring-background"
							}, user.id))
						}), /* @__PURE__ */ jsxs("span", {
							className: "text-[12px] text-muted-foreground",
							children: [event.onlineCount.toLocaleString(), " online now"]
						})] }) : null
					}) : null
				]
			})]
		}),
		/* @__PURE__ */ jsx("div", {
			ref: sentinelRef,
			className: "h-px w-full shrink-0",
			"aria-hidden": true
		})
	] });
}
var CARD_SHELL$4 = "rounded-xl border border-border bg-card/50 overflow-hidden";
function LockedScheduleDayCard({ day, eventStartDate, currentDay, fullWidth = false }) {
	const { setTransientActivity } = useInitPresenceActivity();
	return /* @__PURE__ */ jsxs("button", {
		type: "button",
		onClick: () => scrollToInitDayCard(day.day),
		onMouseEnter: () => setTransientActivity(buildInitDayPreviewActivity(day, currentDay)),
		onMouseLeave: () => setTransientActivity(null),
		onFocus: () => setTransientActivity(buildInitDayPreviewActivity(day, currentDay)),
		onBlur: () => setTransientActivity(null),
		className: cn("flex flex-col rounded-xl border border-dashed border-border bg-muted/20 p-4 text-start transition-colors", "cursor-pointer hover:bg-muted/30 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background", fullWidth ? "min-w-0 w-full" : "min-w-[200px] max-w-[220px] shrink-0"),
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "flex items-start justify-between gap-2",
				children: [/* @__PURE__ */ jsxs("p", {
					className: "text-[10px] font-semibold uppercase tracking-wider text-muted-foreground",
					children: [
						"Day ",
						day.day,
						" · ",
						day.dateLabel
					]
				}), /* @__PURE__ */ jsx(Lock, {
					className: "size-3.5 shrink-0 text-muted-foreground",
					"aria-hidden": true
				})]
			}),
			/* @__PURE__ */ jsx("p", {
				className: "mt-3 text-[13px] font-medium text-muted-foreground",
				children: "Coming soon"
			}),
			/* @__PURE__ */ jsx(InitDayCountdown, {
				eventStartDate,
				dayNumber: day.day,
				size: "sm",
				className: "mt-2"
			}),
			/* @__PURE__ */ jsxs("p", {
				className: "mt-2 text-[12px] leading-normal text-muted-foreground/80",
				children: ["Unlocks on ", day.dateLabel]
			})
		]
	});
}
function ScheduleDayCard({ day, currentDay, isRecapMode = false, fullWidth = false }) {
	const { setTransientActivity } = useInitPresenceActivity();
	return /* @__PURE__ */ jsxs("button", {
		type: "button",
		onClick: () => scrollToInitDayCard(day.day),
		onMouseEnter: () => setTransientActivity(buildInitDayPreviewActivity(day, currentDay)),
		onMouseLeave: () => setTransientActivity(null),
		onFocus: () => setTransientActivity(buildInitDayPreviewActivity(day, currentDay)),
		onBlur: () => setTransientActivity(null),
		className: cn("flex flex-col rounded-xl border bg-card/50 p-4 text-start transition-colors", "cursor-pointer hover:bg-accent/50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background", fullWidth ? "min-w-0 w-full" : "min-w-[200px] max-w-[220px] shrink-0", !isRecapMode && day.isLive && "border-[color-mix(in_srgb,var(--brand-cta)_45%,var(--border))]", isRecapMode || !day.isLive ? "border-border hover:border-border" : void 0),
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "flex items-start justify-between gap-2",
				children: [/* @__PURE__ */ jsxs("p", {
					className: "text-[10px] font-semibold uppercase tracking-wider text-muted-foreground",
					children: [
						"Day ",
						day.day,
						" · ",
						day.dateLabel
					]
				}), !isRecapMode && day.isLive ? /* @__PURE__ */ jsx(Badge, {
					variant: "error",
					className: "text-[10px] shrink-0",
					children: "Live"
				}) : null]
			}),
			/* @__PURE__ */ jsx("h4", {
				className: "mt-3 text-[15px] font-semibold leading-none text-foreground",
				children: day.title
			}),
			/* @__PURE__ */ jsx("p", {
				className: "mt-3 min-h-[2lh] line-clamp-2 text-[12px] leading-normal text-muted-foreground",
				children: day.description
			})
		]
	});
}
function DayPickerCard({ day, eventStartDate, currentDay, isRecapMode = false, fullWidth = false }) {
	if (isLaunchEventDayLocked(day)) return /* @__PURE__ */ jsx(LockedScheduleDayCard, {
		day,
		eventStartDate,
		currentDay,
		fullWidth
	});
	return /* @__PURE__ */ jsx(ScheduleDayCard, {
		day,
		currentDay,
		isRecapMode,
		fullWidth
	});
}
function EventSchedule({ event, fullWidth = false }) {
	if (event.days.length === 0) return /* @__PURE__ */ jsx("div", {
		className: CARD_SHELL$4,
		children: /* @__PURE__ */ jsx("div", {
			className: "px-6 py-8 text-center",
			children: /* @__PURE__ */ jsx("p", {
				className: "text-[13px] text-muted-foreground",
				children: "The schedule will be announced closer to the event."
			})
		})
	});
	if (fullWidth) return /* @__PURE__ */ jsx("section", { children: /* @__PURE__ */ jsx("div", {
		className: "grid w-full grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-5",
		children: event.days.map((day) => /* @__PURE__ */ jsx(DayPickerCard, {
			day,
			eventStartDate: event.startDate,
			currentDay: event.currentDay,
			isRecapMode: event.isRecapMode,
			fullWidth: true
		}, day.day))
	}) });
	return /* @__PURE__ */ jsx("section", { children: /* @__PURE__ */ jsx("div", {
		className: "-mx-1 overflow-x-auto px-1 pb-1",
		children: /* @__PURE__ */ jsx("div", {
			className: "flex gap-3",
			children: event.days.map((day) => /* @__PURE__ */ jsx(DayPickerCard, {
				day,
				eventStartDate: event.startDate,
				currentDay: event.currentDay,
				isRecapMode: event.isRecapMode
			}, day.day))
		})
	}) });
}
var SAVE_DEBOUNCE_MS = 600;
function useInitTicketPrefs(eventId, account) {
	const userId = account?.$id;
	const [prefs, setPrefs] = useState(DEFAULT_INIT_TICKET_PREFS);
	const saveTimerRef = useRef(null);
	useEffect(() => {
		if (!userId) {
			setPrefs(readInitTicketGuestPrefsFromStorage(eventId) ?? DEFAULT_INIT_TICKET_PREFS);
			return;
		}
		const fromAccount = readInitTicketPrefsFromAccountPrefs(account?.prefs, eventId);
		const fromStorage = readInitTicketPrefsFromStorage(eventId, userId);
		setPrefs(fromAccount ?? fromStorage ?? DEFAULT_INIT_TICKET_PREFS);
	}, [
		account?.prefs,
		eventId,
		userId
	]);
	const persistPrefs = useCallback((next) => {
		if (!userId) {
			writeInitTicketGuestPrefsToStorage(eventId, next);
			return;
		}
		writeInitTicketPrefsToStorage(eventId, userId, next);
		if (saveTimerRef.current) clearTimeout(saveTimerRef.current);
		saveTimerRef.current = setTimeout(() => {
			sdk.forConsole.account.updatePrefs({ prefs: mergeInitTicketPrefsIntoAccountPrefs(account?.prefs, eventId, next) }).catch(() => {});
		}, SAVE_DEBOUNCE_MS);
	}, [
		account?.prefs,
		eventId,
		userId
	]);
	const updatePrefs = useCallback((patch) => {
		setPrefs((current) => {
			const next = {
				...current,
				...patch
			};
			persistPrefs(next);
			return next;
		});
	}, [persistPrefs]);
	useEffect(() => {
		if (!userId || typeof window === "undefined") return;
		const handlePrefsChange = (event) => {
			const detail = event.detail;
			if (detail.eventId !== eventId || detail.userId !== userId) return;
			setPrefs(detail.prefs);
		};
		window.addEventListener(INIT_TICKET_PREFS_CHANGE_EVENT, handlePrefsChange);
		return () => {
			window.removeEventListener(INIT_TICKET_PREFS_CHANGE_EVENT, handlePrefsChange);
		};
	}, [eventId, userId]);
	useEffect(() => {
		return () => {
			if (saveTimerRef.current) clearTimeout(saveTimerRef.current);
		};
	}, []);
	return {
		prefs,
		updatePrefs
	};
}
function buildInitTicketImageSignature(data) {
	return JSON.stringify({
		holderName: data.holderName,
		githubUsername: data.githubUsername ?? null,
		stack: data.prefs.stack,
		holderTitle: data.prefs.holderTitle ?? null,
		ticketTypeId: data.ticketAppearance.typeId,
		usesDarkChrome: data.ticketAppearance.usesDarkChrome,
		ticketNumber: data.ticketNumber
	});
}
function buildInitTicketImageUrl(params) {
	const { renderData } = params;
	const search = new URLSearchParams();
	search.set("name", renderData.holderName);
	if (renderData.prefs.holderTitle?.trim()) search.set("title", renderData.prefs.holderTitle.trim());
	if (renderData.prefs.stack.length > 0) search.set("stack", renderData.prefs.stack.join(","));
	search.set("type", renderData.ticketAppearance.typeId);
	if (params.themeUsesDarkImage) search.set("theme", "dark");
	search.set("ticketNumber", renderData.ticketNumber);
	if (params.consoleUserId) search.set("userId", params.consoleUserId);
	if (renderData.githubUsername) search.set("github", renderData.githubUsername);
	return `/init/ticket/${params.eventSlug}?${search.toString()}`;
}
function buildInitTicketFilePermissions() {
	return [Permission.read(Role.any())];
}
var client = null;
var storage = null;
function getInitTicketStorageSdk() {
	if (!storage) {
		const config = getInitTicketStorageConfig();
		client = new Client();
		client.setEndpoint(config.endpoint).setProject(config.projectId);
		storage = new Storage(client);
	}
	return storage;
}
async function uploadInitTicketImage(params) {
	const signature = buildInitTicketImageSignature(params.renderData);
	const imageUrl = buildInitTicketImageUrl({
		eventSlug: params.eventSlug,
		renderData: params.renderData,
		themeUsesDarkImage: params.themeUsesDarkImage,
		consoleUserId: params.consoleUserId
	});
	const imageResponse = await fetch(imageUrl, { signal: params.signal });
	if (!imageResponse.ok) throw new Error(`Init ticket image render failed (${imageResponse.status})`);
	const blob = await imageResponse.blob();
	const { bucketId } = getInitTicketStorageConfig();
	return {
		fileId: (await getInitTicketStorageSdk().createFile({
			bucketId,
			fileId: ID.unique(),
			file: new File([blob], "init-ticket.png", { type: "image/png" }),
			permissions: buildInitTicketFilePermissions()
		})).$id,
		signature
	};
}
async function syncInitTicketImage(params) {
	if (!isInitTicketStorageConfigured()) throw new Error("Init ticket storage is not configured");
	return uploadInitTicketImage(params);
}
var SYNC_DEBOUNCE_MS = 1200;
function useSyncInitTicketImage(params) {
	const { eventSlug, account, prefs, renderData, themeUsesDarkImage, updatePrefs } = params;
	const updatePrefsRef = useRef(updatePrefs);
	updatePrefsRef.current = updatePrefs;
	useEffect(() => {
		if (!isInitTicketStorageConfigured()) return;
		const signature = buildInitTicketImageSignature(renderData);
		if (prefs.imageFileId && prefs.imageSignature === signature) return;
		const controller = new AbortController();
		const timer = window.setTimeout(() => {
			syncInitTicketImage({
				eventSlug,
				renderData,
				themeUsesDarkImage,
				consoleUserId: account?.$id,
				signal: controller.signal
			}).then((result) => {
				if (prefs.imageFileId === result.fileId && prefs.imageSignature === result.signature) return;
				updatePrefsRef.current({
					imageFileId: result.fileId,
					imageSignature: result.signature
				});
			}).catch(() => {});
		}, SYNC_DEBOUNCE_MS);
		return () => {
			window.clearTimeout(timer);
			controller.abort();
		};
	}, [
		account?.$id,
		eventSlug,
		prefs.imageFileId,
		prefs.imageSignature,
		renderData,
		themeUsesDarkImage
	]);
}
var BETA_SENSITIVITY = .55;
var GAMMA_SENSITIVITY = .65;
function clamp(value, min, max) {
	return Math.min(max, Math.max(min, value));
}
function isInitTicketDeviceTiltSupported() {
	if (typeof window === "undefined") return false;
	return "DeviceOrientationEvent" in window || "DeviceMotionEvent" in window;
}
function prefersFinePointer() {
	if (typeof window === "undefined") return true;
	return window.matchMedia("(pointer: fine)").matches;
}
function isIOSDevice() {
	if (typeof navigator === "undefined") return false;
	return /iPad|iPhone|iPod/.test(navigator.userAgent) || navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1;
}
function prefersInitTicketDeviceTilt() {
	if (typeof window === "undefined") return false;
	if (isIOSDevice()) return true;
	if (prefersFinePointer()) return false;
	return window.matchMedia("(pointer: coarse)").matches || window.matchMedia("(hover: none)").matches;
}
function requestInitTicketDeviceOrientationAccessFromGesture() {
	if (!isInitTicketDeviceTiltSupported()) return Promise.resolve(false);
	const Orientation = DeviceOrientationEvent;
	if (typeof Orientation.requestPermission !== "function") return Promise.resolve(true);
	try {
		return Orientation.requestPermission().then((state) => state === "granted");
	} catch {
		return Promise.resolve(false);
	}
}
async function requestInitTicketDeviceOrientationAccess() {
	return requestInitTicketDeviceOrientationAccessFromGesture();
}
function tiltFromGravity(x, y, z) {
	return {
		beta: Math.atan2(y, Math.hypot(x, z)) * 180 / Math.PI,
		gamma: Math.atan2(x, z) * 180 / Math.PI
	};
}
function useInitTicketDeviceTilt({ enabled, maxTiltX, maxTiltY, onTiltChange }) {
	const baselineRef = useRef(null);
	const orientationActiveRef = useRef(false);
	const listeningRef = useRef(false);
	const [listening, setListening] = useState(false);
	const onTiltChangeRef = useRef(onTiltChange);
	useEffect(() => {
		onTiltChangeRef.current = onTiltChange;
	}, [onTiltChange]);
	const isSupported = isInitTicketDeviceTiltSupported();
	const applyTiltFromAngles = useCallback((beta, gamma) => {
		if (!baselineRef.current) {
			baselineRef.current = {
				beta,
				gamma
			};
			return;
		}
		const deltaBeta = beta - baselineRef.current.beta;
		const deltaGamma = gamma - baselineRef.current.gamma;
		onTiltChangeRef.current({
			x: clamp(-deltaBeta * BETA_SENSITIVITY, -maxTiltX, maxTiltX),
			y: clamp(deltaGamma * GAMMA_SENSITIVITY, -maxTiltY, maxTiltY)
		});
	}, [maxTiltX, maxTiltY]);
	const stopListening = useCallback(() => {
		listeningRef.current = false;
		baselineRef.current = null;
		orientationActiveRef.current = false;
		setListening(false);
		onTiltChangeRef.current({
			x: 0,
			y: 0
		});
	}, []);
	const startListening = useCallback(async (options) => {
		if (!enabled || !isSupported || listeningRef.current) return listeningRef.current;
		if (!options?.skipPermission) {
			if (!await requestInitTicketDeviceOrientationAccess()) return false;
		}
		baselineRef.current = null;
		orientationActiveRef.current = false;
		listeningRef.current = true;
		setListening(true);
		return true;
	}, [enabled, isSupported]);
	useEffect(() => {
		if (!enabled || !listening) return;
		const handleOrientation = (event) => {
			const { beta, gamma } = event;
			if (beta == null || gamma == null) return;
			orientationActiveRef.current = true;
			applyTiltFromAngles(beta, gamma);
		};
		const handleMotion = (event) => {
			if (orientationActiveRef.current) return;
			const gravity = event.accelerationIncludingGravity;
			if (!gravity || gravity.x == null || gravity.y == null || gravity.z == null) return;
			const { beta, gamma } = tiltFromGravity(gravity.x, gravity.y, gravity.z);
			applyTiltFromAngles(beta, gamma);
		};
		window.addEventListener("deviceorientation", handleOrientation, { passive: true });
		window.addEventListener("devicemotion", handleMotion, { passive: true });
		return () => {
			window.removeEventListener("deviceorientation", handleOrientation);
			window.removeEventListener("devicemotion", handleMotion);
		};
	}, [
		applyTiltFromAngles,
		enabled,
		listening
	]);
	useEffect(() => {
		if (!enabled) stopListening();
	}, [enabled, stopListening]);
	return {
		isSupported,
		listening,
		startListening,
		stopListening
	};
}
var TILT_MAX_X = 22;
var TILT_MAX_Y = 32;
var TILT_DURATION_MS = 140;
var FLIP_DURATION_MS = 720;
var RESET_DURATION_MS = 520;
var FLIP_DRAG_THRESHOLD_PX = 10;
function TicketGitHubBadge({ username, usesDarkImage }) {
	return /* @__PURE__ */ jsxs("div", {
		className: cn("flex items-center gap-1.5", usesDarkImage ? "text-white/75" : "text-neutral-600"),
		children: [/* @__PURE__ */ jsx("img", {
			src: "/icons/github.svg",
			alt: "",
			"aria-hidden": true,
			className: cn("size-3.5 shrink-0 object-contain", usesDarkImage ? "brightness-0 invert" : "brightness-0")
		}), /* @__PURE__ */ jsxs("span", {
			className: "truncate text-[clamp(10px,1.8vw,12px)] font-medium",
			children: ["@", username]
		})]
	});
}
function getInitTicketStackIconSrc(iconKey, usesDarkImage) {
	if (iconKey === "appwrite") return `/icons/${usesDarkImage ? "appwrite-white.svg" : "appwrite.svg"}`;
	const iconFile = getFrameworkIconFile(iconKey);
	return iconFile ? `/icons/${iconFile}` : null;
}
function TicketStackIcons({ stack, usesDarkImage }) {
	if (!stack.length) return null;
	return /* @__PURE__ */ jsx("div", {
		className: "flex flex-wrap gap-x-3.5 gap-y-3",
		children: stack.map((id) => {
			const option = getInitTicketStackOption(id);
			if (!option) return null;
			const iconSrc = getInitTicketStackIconSrc(option.iconKey, usesDarkImage);
			return /* @__PURE__ */ jsx("span", {
				className: "flex items-center justify-center",
				title: option.label,
				children: iconSrc ? /* @__PURE__ */ jsx("img", {
					src: iconSrc,
					alt: "",
					"aria-hidden": true,
					className: cn("size-5 object-contain", option.iconKey !== "appwrite" && (usesDarkImage ? "brightness-0 invert" : "brightness-0"))
				}) : /* @__PURE__ */ jsx(Globe, {
					className: cn("size-5", usesDarkImage ? "text-white" : "text-neutral-900"),
					"aria-hidden": true
				})
			}, id);
		})
	});
}
function TicketStubContent({ ticketNumber, holderName, holderTitle, passLabel, dateRangeLabel, accentColor, usesDarkImage }) {
	const mutedClass = usesDarkImage ? "text-white/55" : "text-neutral-500";
	const labelClass = usesDarkImage ? "text-white/70" : "text-neutral-600";
	const { left, right, bottom } = INIT_TICKET_STUB_LABEL_INSET;
	return /* @__PURE__ */ jsx("div", {
		className: "relative h-full min-w-0",
		"aria-hidden": true,
		children: /* @__PURE__ */ jsx("div", {
			className: "absolute flex items-end justify-start",
			style: {
				left: `${left}%`,
				right: `${right}%`,
				bottom: `${bottom}%`
			},
			children: /* @__PURE__ */ jsxs("div", {
				className: "flex origin-bottom-start -rotate-90 flex-col items-start gap-1 whitespace-nowrap text-start",
				children: [
					/* @__PURE__ */ jsx(InitWordmark, {
						accentColor,
						className: cn("text-[clamp(10px,1.8vw,14px)]", usesDarkImage ? "text-white" : "text-neutral-900")
					}),
					/* @__PURE__ */ jsx("p", {
						className: "font-mono text-[clamp(9px,1.6vw,12px)] font-semibold tabular-nums tracking-wide",
						style: { color: accentColor },
						children: ticketNumber
					}),
					/* @__PURE__ */ jsx("p", {
						className: cn("text-[clamp(11px,2vw,16px)] font-normal", labelClass),
						children: holderName
					}),
					/* @__PURE__ */ jsx("p", {
						className: cn("font-medium leading-none", initTicketStubTitleClass(holderTitle), mutedClass),
						children: holderTitle
					}),
					/* @__PURE__ */ jsx("p", {
						className: cn("text-[7px] font-semibold uppercase tracking-[0.22em]", mutedClass),
						children: passLabel
					}),
					/* @__PURE__ */ jsx("p", {
						className: cn("text-[7px] font-semibold uppercase tracking-[0.14em]", mutedClass),
						children: dateRangeLabel
					})
				]
			})
		})
	});
}
function TicketFaceShell({ ticketBgSrc, inset, usesDarkImage, isBack, children }) {
	const [loadedBackgroundSrc, setLoadedBackgroundSrc] = useState(null);
	const backgroundLoaded = loadedBackgroundSrc === ticketBgSrc;
	return /* @__PURE__ */ jsxs("div", {
		className: cn("absolute inset-0 w-full [backface-visibility:hidden] [transform-style:preserve-3d]", isBack && "[transform:rotateY(180deg)]"),
		children: [/* @__PURE__ */ jsx("img", {
			src: ticketBgSrc,
			alt: "",
			className: cn("pointer-events-none absolute inset-0 size-full object-contain object-top", backgroundLoaded ? "opacity-100" : "opacity-0"),
			"aria-hidden": true,
			draggable: false,
			onLoad: () => setLoadedBackgroundSrc(ticketBgSrc)
		}, ticketBgSrc), /* @__PURE__ */ jsx("div", {
			className: cn("absolute inset-0 overflow-visible", backgroundLoaded ? "opacity-100" : "opacity-0", usesDarkImage ? "text-white" : "text-neutral-900"),
			style: inset,
			children
		})]
	});
}
function TicketFrontFace(props) {
	const { dateRangeLabel, holderName, githubUsername, ticketNumber, prefs, passLabel, holderTitle, accentColor, usesDarkImage } = props;
	const contentGrid = initTicketContentGridStyle();
	return /* @__PURE__ */ jsx(TicketFaceShell, {
		...props,
		children: /* @__PURE__ */ jsxs("div", {
			className: "grid h-full min-h-0 overflow-visible",
			style: contentGrid,
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex min-w-0 flex-col justify-between pe-[8%]",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "space-y-2",
					children: [/* @__PURE__ */ jsx(InitWordmark, {
						accentColor,
						className: cn("text-[clamp(24px,5vw,38px)]", usesDarkImage ? "text-white" : "text-neutral-900")
					}), /* @__PURE__ */ jsx("p", {
						className: cn("text-[clamp(10px,2vw,13px)] font-semibold uppercase tracking-[0.16em]", usesDarkImage ? "text-white/60" : "text-neutral-500"),
						children: dateRangeLabel
					})]
				}), /* @__PURE__ */ jsxs("div", {
					className: "space-y-4",
					children: [/* @__PURE__ */ jsx(TicketStackIcons, {
						stack: prefs.stack,
						usesDarkImage
					}), /* @__PURE__ */ jsxs("div", {
						className: cn("space-y-2.5 border-t border-dashed pt-4", usesDarkImage ? "border-white/20" : "border-neutral-900/15"),
						children: [
							/* @__PURE__ */ jsx("p", {
								className: cn("truncate font-normal leading-[1.02] tracking-tight", initTicketHolderNameFontSizeClass(holderName), usesDarkImage ? "text-white" : "text-neutral-900"),
								children: holderName
							}),
							/* @__PURE__ */ jsx("p", {
								className: cn("truncate text-[clamp(12px,2.2vw,16px)] font-medium leading-relaxed", usesDarkImage ? "text-white/70" : "text-neutral-600"),
								children: holderTitle
							}),
							githubUsername ? /* @__PURE__ */ jsx(TicketGitHubBadge, {
								username: githubUsername,
								usesDarkImage
							}) : null,
							/* @__PURE__ */ jsx("p", {
								className: cn("text-[9px] font-semibold uppercase tracking-[0.2em]", usesDarkImage ? "text-white/55" : "text-neutral-500"),
								children: passLabel
							}),
							/* @__PURE__ */ jsx("p", {
								className: "font-mono text-[9px] font-medium tabular-nums sm:text-[10px]",
								style: { color: accentColor },
								children: ticketNumber
							})
						]
					})]
				})]
			}), /* @__PURE__ */ jsx("div", {
				className: "min-w-0",
				children: /* @__PURE__ */ jsx(TicketStubContent, {
					ticketNumber,
					holderName,
					holderTitle,
					passLabel,
					dateRangeLabel,
					accentColor,
					usesDarkImage
				})
			})]
		})
	});
}
function TicketBackFace(props) {
	const { dateRangeLabel, holderName, githubUsername, ticketNumber, prefs, passLabel, holderTitle, accentColor, usesDarkImage } = props;
	const mutedClass = usesDarkImage ? "text-white/55" : "text-neutral-500";
	const contentGrid = initTicketContentGridStyle();
	return /* @__PURE__ */ jsx(TicketFaceShell, {
		...props,
		isBack: true,
		children: /* @__PURE__ */ jsxs("div", {
			className: "grid h-full min-h-0 overflow-visible",
			style: contentGrid,
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex min-w-0 flex-col justify-between pe-[8%]",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "space-y-2",
					children: [
						/* @__PURE__ */ jsx("p", {
							className: cn("text-[9px] font-semibold uppercase tracking-[0.24em]", mutedClass),
							children: "Official pass"
						}),
						/* @__PURE__ */ jsx(InitWordmark, {
							accentColor,
							className: cn("text-[clamp(22px,4.5vw,34px)]", usesDarkImage ? "text-white" : "text-neutral-900")
						}),
						/* @__PURE__ */ jsx("p", {
							className: "font-mono text-[11px] font-semibold tabular-nums sm:text-[12px]",
							style: { color: accentColor },
							children: ticketNumber
						})
					]
				}), /* @__PURE__ */ jsxs("div", {
					className: "space-y-3",
					children: [/* @__PURE__ */ jsx("div", {
						className: "flex h-10 items-end justify-start gap-0.5 overflow-hidden",
						"aria-hidden": true,
						children: Array.from({ length: 24 }).map((_, index) => /* @__PURE__ */ jsx("span", {
							className: cn("w-0.5 rounded-full", usesDarkImage ? "bg-white/30" : "bg-neutral-900/25"),
							style: { height: `${28 + index * 17 % 40}%` }
						}, index))
					}), /* @__PURE__ */ jsxs("div", {
						className: cn("space-y-1 border-t border-dashed pt-3", usesDarkImage ? "border-white/20" : "border-neutral-900/15"),
						children: [
							/* @__PURE__ */ jsx("p", {
								className: cn("text-[9px] font-semibold uppercase tracking-[0.2em]", mutedClass),
								children: "Valid for Init week"
							}),
							/* @__PURE__ */ jsx("p", {
								className: cn("text-[12px] font-medium", usesDarkImage ? "text-white/90" : "text-neutral-800"),
								children: dateRangeLabel
							}),
							/* @__PURE__ */ jsx("p", {
								className: cn("truncate text-[clamp(11px,1.9vw,15px)] font-normal leading-tight", usesDarkImage ? "text-white/90" : "text-neutral-800"),
								children: holderName
							}),
							/* @__PURE__ */ jsx("p", {
								className: cn("truncate text-[11px] font-medium", usesDarkImage ? "text-white/65" : "text-neutral-500"),
								children: holderTitle
							}),
							/* @__PURE__ */ jsx("p", {
								className: cn("text-[9px] font-semibold uppercase tracking-[0.2em]", mutedClass),
								children: passLabel
							}),
							githubUsername ? /* @__PURE__ */ jsx(TicketGitHubBadge, {
								username: githubUsername,
								usesDarkImage
							}) : null,
							/* @__PURE__ */ jsx(TicketStackIcons, {
								stack: prefs.stack,
								usesDarkImage
							})
						]
					})]
				})]
			}), /* @__PURE__ */ jsx("div", { "aria-hidden": true })]
		})
	});
}
const InitTicketCard = forwardRef(function InitTicketCard$1({ dateRangeLabel, holderName, githubUsername, ticketNumber, prefs, ticketAppearance, blurred = false, previewOnly = false, captureMode = false, className }, ref) {
	const captureRootRef = useRef(null);
	const ticketFrameRef = useRef(null);
	const interactive = !blurred && !previewOnly && !captureMode;
	const { backgroundSrc: ticketBgSrc, usesDarkChrome: usesDarkImage, shadowClassName, shadowOffsetY, passLabel, holderTitle: defaultHolderTitle, accentColor } = ticketAppearance;
	const holderTitle = getInitTicketHolderTitle(prefs, defaultHolderTitle);
	const displayHolderName = useMemo(() => blurred ? scrambleSensitiveText(holderName) : holderName, [blurred, holderName]);
	const displayGithubUsername = useMemo(() => {
		if (!githubUsername) return void 0;
		return blurred ? scrambleSensitiveText(githubUsername) : githubUsername;
	}, [blurred, githubUsername]);
	const displayTicketNumber = useMemo(() => blurred ? scrambleSensitiveText(ticketNumber) : ticketNumber, [blurred, ticketNumber]);
	const sceneRef = useRef(null);
	const flipperRef = useRef(null);
	const shadowRef = useRef(null);
	const tiltRef = useRef({
		x: 0,
		y: 0
	});
	const pointerTiltRef = useRef({
		x: 0,
		y: 0
	});
	const deviceTiltRef = useRef({
		x: 0,
		y: 0
	});
	const pointerTiltActiveRef = useRef(false);
	const flippedRef = useRef(false);
	const animatingRef = useRef(false);
	const tiltRafRef = useRef(0);
	const shadowOffsetYRef = useRef(shadowOffsetY);
	const pointerStartRef = useRef(null);
	const didDragRef = useRef(false);
	const touchActiveRef = useRef(false);
	const [isFlipped, setIsFlipped] = useState(false);
	const [reducedMotion, setReducedMotion] = useState(false);
	const deviceTiltEnabled = interactive && !reducedMotion && prefersInitTicketDeviceTilt();
	useEffect(() => {
		shadowOffsetYRef.current = shadowOffsetY;
	}, [shadowOffsetY]);
	const faceProps = {
		dateRangeLabel,
		holderName: displayHolderName,
		githubUsername: displayGithubUsername,
		ticketNumber: displayTicketNumber,
		prefs,
		passLabel,
		holderTitle,
		accentColor,
		usesDarkImage,
		ticketBgSrc,
		inset: initTicketInsetStyle()
	};
	useEffect(() => {
		const media = window.matchMedia("(prefers-reduced-motion: reduce)");
		const update = () => setReducedMotion(media.matches);
		update();
		media.addEventListener("change", update);
		return () => media.removeEventListener("change", update);
	}, []);
	const applyTransform = useCallback((duration = TILT_DURATION_MS) => {
		const flipper = flipperRef.current;
		const shadow = shadowRef.current;
		if (!flipper || reducedMotion) return;
		let rotateY = (flippedRef.current ? 180 : 0) + tiltRef.current.y;
		let rotateX = tiltRef.current.x;
		if (captureMode && duration === 0) {
			rotateY = Math.round(rotateY * 4) / 4;
			rotateX = Math.round(rotateX * 4) / 4;
		}
		const transition = duration === 0 ? "none" : `transform ${duration}ms ${duration >= FLIP_DURATION_MS ? "cubic-bezier(0.16, 1, 0.3, 1)" : "cubic-bezier(0.25, 0.46, 0.45, 0.94)"}`;
		flipper.style.transition = transition;
		flipper.style.transform = `rotateX(${rotateX}deg) rotateY(${rotateY}deg)`;
		if (shadow) {
			const translateX = tiltRef.current.y * .55;
			const translateY = shadowOffsetYRef.current + Math.abs(tiltRef.current.x) * .08;
			const scale = 1 - Math.abs(tiltRef.current.x) * .008;
			shadow.style.transition = transition;
			shadow.style.transform = `translate3d(${translateX}px, ${translateY}px, 0) scale(${scale})`;
		}
	}, [captureMode, reducedMotion]);
	const scheduleTiltApply = useCallback((duration = TILT_DURATION_MS) => {
		if (tiltRafRef.current) cancelAnimationFrame(tiltRafRef.current);
		tiltRafRef.current = window.requestAnimationFrame(() => {
			tiltRafRef.current = 0;
			applyTransform(duration);
		});
	}, [applyTransform]);
	const syncEffectiveTilt = useCallback((duration = TILT_DURATION_MS) => {
		tiltRef.current = pointerTiltActiveRef.current ? pointerTiltRef.current : deviceTiltRef.current;
		scheduleTiltApply(duration);
	}, [scheduleTiltApply]);
	const { startListening: startDeviceTilt, listening: deviceTiltListening } = useInitTicketDeviceTilt({
		enabled: deviceTiltEnabled,
		maxTiltX: TILT_MAX_X,
		maxTiltY: TILT_MAX_Y,
		onTiltChange: (tilt) => {
			deviceTiltRef.current = tilt;
			if (!pointerTiltActiveRef.current && !animatingRef.current) syncEffectiveTilt(TILT_DURATION_MS);
		}
	});
	useEffect(() => {
		if (!deviceTiltEnabled) return;
		if (typeof DeviceOrientationEvent.requestPermission !== "function") startDeviceTilt();
	}, [deviceTiltEnabled, startDeviceTilt]);
	const computeTiltAtClientCoords = useCallback((clientX, clientY) => {
		const scene = sceneRef.current;
		if (!scene) return null;
		const rect = scene.getBoundingClientRect();
		if (rect.width <= 0 || rect.height <= 0) return null;
		const px = (clientX - rect.left) / rect.width - .5;
		return {
			x: -((clientY - rect.top) / rect.height - .5) * TILT_MAX_X,
			y: px * TILT_MAX_Y
		};
	}, []);
	const applyMouseHoverTilt = useCallback((clientX, clientY) => {
		if (deviceTiltEnabled) return;
		const tilt = computeTiltAtClientCoords(clientX, clientY);
		if (!tilt) return;
		pointerTiltActiveRef.current = false;
		tiltRef.current = tilt;
		scheduleTiltApply(TILT_DURATION_MS);
	}, [
		computeTiltAtClientCoords,
		deviceTiltEnabled,
		scheduleTiltApply
	]);
	const applyPointerTilt = useCallback((clientX, clientY) => {
		if (deviceTiltEnabled) return;
		if (!computeTiltAtClientCoords(clientX, clientY)) return;
		applyMouseHoverTilt(clientX, clientY);
	}, [
		applyMouseHoverTilt,
		computeTiltAtClientCoords,
		deviceTiltEnabled
	]);
	const updateTiltFromPointer = useCallback((event) => {
		applyPointerTilt(event.clientX, event.clientY);
	}, [applyPointerTilt]);
	const releaseMouseHoverTilt = useCallback(() => {
		pointerTiltActiveRef.current = false;
		pointerTiltRef.current = {
			x: 0,
			y: 0
		};
		if (deviceTiltEnabled && deviceTiltListening) {
			syncEffectiveTilt(TILT_DURATION_MS);
			return;
		}
		tiltRef.current = {
			x: 0,
			y: 0
		};
		scheduleTiltApply(RESET_DURATION_MS);
	}, [
		deviceTiltEnabled,
		deviceTiltListening,
		scheduleTiltApply,
		syncEffectiveTilt
	]);
	const releasePointerTilt = releaseMouseHoverTilt;
	const applyMouseHoverTiltRef = useRef(applyMouseHoverTilt);
	const releaseMouseHoverTiltRef = useRef(releaseMouseHoverTilt);
	useEffect(() => {
		applyMouseHoverTiltRef.current = applyMouseHoverTilt;
	}, [applyMouseHoverTilt]);
	useEffect(() => {
		releaseMouseHoverTiltRef.current = releaseMouseHoverTilt;
	}, [releaseMouseHoverTilt]);
	useEffect(() => {
		if (!interactive || reducedMotion) return;
		let mouseHovering = false;
		const isPointerOverScene = (clientX, clientY) => {
			const scene = sceneRef.current;
			if (!scene) return false;
			const target = document.elementFromPoint(clientX, clientY);
			if (target && (target === scene || scene.contains(target))) return true;
			const rect = scene.getBoundingClientRect();
			return clientX >= rect.left && clientX <= rect.right && clientY >= rect.top && clientY <= rect.bottom;
		};
		const handleWindowMouseMove = (event) => {
			if (animatingRef.current || touchActiveRef.current) return;
			if (!isPointerOverScene(event.clientX, event.clientY)) {
				if (mouseHovering) {
					mouseHovering = false;
					pointerStartRef.current = null;
					releaseMouseHoverTiltRef.current();
				}
				return;
			}
			mouseHovering = true;
			applyMouseHoverTiltRef.current(event.clientX, event.clientY);
		};
		window.addEventListener("mousemove", handleWindowMouseMove, { passive: true });
		return () => window.removeEventListener("mousemove", handleWindowMouseMove);
	}, [interactive, reducedMotion]);
	const handleSceneMouseMove = useCallback((event) => {
		if (!interactive || reducedMotion || animatingRef.current || touchActiveRef.current) return;
		applyMouseHoverTilt(event.clientX, event.clientY);
	}, [
		applyMouseHoverTilt,
		interactive,
		reducedMotion
	]);
	const handleSceneMouseLeave = useCallback(() => {
		if (!interactive || reducedMotion || touchActiveRef.current) return;
		pointerStartRef.current = null;
		releaseMouseHoverTilt();
	}, [
		interactive,
		reducedMotion,
		releaseMouseHoverTilt
	]);
	const handlePointerMove = useCallback((event) => {
		if (!interactive || reducedMotion || animatingRef.current) return;
		if (event.pointerType === "mouse") return;
		const isTouch = event.pointerType === "touch";
		const isHoverPointer = event.pointerType !== "touch" && event.buttons === 0;
		if (!isTouch && !isHoverPointer && event.buttons === 0) return;
		if (pointerStartRef.current) {
			const dx = event.clientX - pointerStartRef.current.x;
			const dy = event.clientY - pointerStartRef.current.y;
			if (Math.hypot(dx, dy) > FLIP_DRAG_THRESHOLD_PX) didDragRef.current = true;
		}
		updateTiltFromPointer(event);
	}, [
		interactive,
		reducedMotion,
		updateTiltFromPointer
	]);
	useEffect(() => {
		return () => {
			if (tiltRafRef.current) window.cancelAnimationFrame(tiltRafRef.current);
		};
	}, []);
	const endTouchInteraction = useCallback((event) => {
		if (event.currentTarget.hasPointerCapture(event.pointerId)) event.currentTarget.releasePointerCapture(event.pointerId);
		touchActiveRef.current = false;
		pointerStartRef.current = null;
		releasePointerTilt();
	}, [releasePointerTilt]);
	const handlePointerDown = useCallback((event) => {
		if (!interactive || reducedMotion) return;
		pointerStartRef.current = {
			x: event.clientX,
			y: event.clientY
		};
		didDragRef.current = false;
		if (deviceTiltEnabled) startDeviceTilt();
		if (event.pointerType === "touch") {
			touchActiveRef.current = true;
			event.currentTarget.setPointerCapture(event.pointerId);
			if (!deviceTiltEnabled) updateTiltFromPointer(event);
		}
	}, [
		deviceTiltEnabled,
		interactive,
		reducedMotion,
		startDeviceTilt,
		updateTiltFromPointer
	]);
	const handlePointerUp = useCallback((event) => {
		if (!interactive || reducedMotion) return;
		if (event.pointerType === "touch") endTouchInteraction(event);
	}, [
		endTouchInteraction,
		interactive,
		reducedMotion
	]);
	const handlePointerCancel = useCallback((event) => {
		if (!interactive || reducedMotion) return;
		if (event.pointerType === "touch") endTouchInteraction(event);
	}, [
		endTouchInteraction,
		interactive,
		reducedMotion
	]);
	const handlePointerLeave = useCallback((event) => {
		if (!interactive || reducedMotion || touchActiveRef.current) return;
		if (event.pointerType === "touch" || event.pointerType === "mouse") return;
		pointerStartRef.current = null;
		releasePointerTilt();
	}, [
		interactive,
		reducedMotion,
		releasePointerTilt
	]);
	const handleFlip = useCallback(() => {
		if (!interactive || reducedMotion) return;
		const nextFlipped = !flippedRef.current;
		flippedRef.current = nextFlipped;
		setIsFlipped(nextFlipped);
		animatingRef.current = true;
		applyTransform(FLIP_DURATION_MS);
		window.setTimeout(() => {
			animatingRef.current = false;
		}, FLIP_DURATION_MS);
	}, [
		applyTransform,
		interactive,
		reducedMotion
	]);
	const handleClick = useCallback(() => {
		if (!interactive || reducedMotion || didDragRef.current) return;
		handleFlip();
	}, [
		handleFlip,
		interactive,
		reducedMotion
	]);
	const handleKeyDown = useCallback((event) => {
		if (event.key === "Enter" || event.key === " ") {
			event.preventDefault();
			handleFlip();
		}
	}, [handleFlip]);
	useImperativeHandle(ref, () => ({
		prepareForVideoCapture() {
			flippedRef.current = false;
			setIsFlipped(false);
			pointerTiltActiveRef.current = false;
			tiltRef.current = {
				x: 0,
				y: 0
			};
			applyTransform(0);
		},
		setCaptureTilt(x, y) {
			pointerTiltActiveRef.current = false;
			tiltRef.current = {
				x,
				y
			};
			applyTransform(0);
		},
		resetCaptureTilt() {
			pointerTiltActiveRef.current = false;
			tiltRef.current = {
				x: 0,
				y: 0
			};
			applyTransform(0);
		}
	}), [applyTransform]);
	return /* @__PURE__ */ jsx("div", {
		ref: captureRootRef,
		className: cn("relative mx-auto w-full select-none", blurred && "blur-[6px]", captureMode && "pointer-events-none overflow-visible antialiased", className),
		style: { maxWidth: 820 },
		"aria-hidden": blurred,
		children: /* @__PURE__ */ jsxs("div", {
			ref: ticketFrameRef,
			className: cn("relative w-full", captureMode && "overflow-visible"),
			style: { aspectRatio: initTicketDisplayAspectRatio() },
			children: [/* @__PURE__ */ jsx("div", {
				ref: shadowRef,
				"data-init-ticket-capture-exclude": true,
				className: cn("absolute inset-x-10 bottom-0 h-6 -translate-y-0.5 rounded-full blur-3xl", shadowClassName),
				"aria-hidden": true
			}), /* @__PURE__ */ jsx("div", {
				ref: sceneRef,
				className: cn("absolute inset-x-0 top-0 w-full [perspective:1000px]", captureMode && "overflow-visible", interactive && !reducedMotion && "cursor-pointer touch-none"),
				style: {
					aspectRatio: INIT_TICKET_ASPECT_RATIO,
					...captureMode ? {} : { clipPath: `inset(0 0 5% 0)` }
				},
				onMouseMove: interactive && !reducedMotion ? handleSceneMouseMove : void 0,
				onMouseLeave: interactive && !reducedMotion ? handleSceneMouseLeave : void 0,
				onPointerDown: interactive && !reducedMotion ? handlePointerDown : void 0,
				onPointerMove: interactive && !reducedMotion ? handlePointerMove : void 0,
				onPointerUp: interactive && !reducedMotion ? handlePointerUp : void 0,
				onPointerCancel: interactive && !reducedMotion ? handlePointerCancel : void 0,
				onPointerLeave: interactive && !reducedMotion ? handlePointerLeave : void 0,
				onClick: interactive && !reducedMotion ? handleClick : void 0,
				onKeyDown: interactive && !reducedMotion ? handleKeyDown : void 0,
				role: interactive && !reducedMotion ? "button" : void 0,
				tabIndex: interactive && !reducedMotion ? 0 : void 0,
				"aria-pressed": interactive && !reducedMotion ? isFlipped : void 0,
				"aria-label": interactive && !reducedMotion ? isFlipped ? "Init ticket back, click to flip to front" : "Init ticket front, click to flip to back" : void 0,
				children: /* @__PURE__ */ jsxs("div", {
					ref: flipperRef,
					className: cn("absolute inset-0 [transform-style:preserve-3d]", captureMode && "overflow-visible"),
					children: [/* @__PURE__ */ jsx(TicketFrontFace, { ...faceProps }), !captureMode ? /* @__PURE__ */ jsx(TicketBackFace, { ...faceProps }) : null]
				})
			})]
		})
	});
});
const INIT_TICKET_VIDEO_SURFACE_CLASS = "bg-muted/40 dark:bg-background";
const INIT_TICKET_VIDEO_DOT_PATTERN_CLASS = "bg-[radial-gradient(circle,var(--border)_1px,transparent_1px)] bg-[length:18px_18px]";
const INIT_TICKET_VIDEO_CAPTURE_PROGRESS_WEIGHT = .72;
const INIT_TICKET_VIDEO_HERO_MOTION_SPEED = .38;
var DEFAULT_TICK_INTERVAL_MS = 16;
function sleep(ms) {
	return new Promise((resolve) => {
		window.setTimeout(resolve, ms);
	});
}
function isDocumentHidden() {
	return typeof document !== "undefined" && document.visibilityState === "hidden";
}
async function runWallClockLoop({ durationMs, onTick, tickIntervalMs = DEFAULT_TICK_INTERVAL_MS }) {
	const startTime = performance.now();
	return new Promise((resolve) => {
		const tick = () => {
			const elapsed = performance.now() - startTime;
			if (elapsed >= durationMs) {
				onTick(1);
				resolve();
				return;
			}
			onTick(Math.min(1, elapsed / durationMs));
			window.setTimeout(tick, tickIntervalMs);
		};
		tick();
	});
}
async function runVisibilityPausedWallClockLoop({ durationMs, onTick, onPause, onResume, tickIntervalMs = DEFAULT_TICK_INTERVAL_MS }) {
	let elapsedMs = 0;
	let lastSample = performance.now();
	let paused = isDocumentHidden();
	if (paused) onPause?.();
	return new Promise((resolve) => {
		const handleVisibility = () => {
			const now = performance.now();
			const hidden = isDocumentHidden();
			if (hidden && !paused) {
				elapsedMs += now - lastSample;
				paused = true;
				onPause?.();
			} else if (!hidden && paused) {
				paused = false;
				onResume?.();
			}
			lastSample = now;
		};
		const tick = () => {
			const now = performance.now();
			if (!paused) elapsedMs += now - lastSample;
			lastSample = now;
			if (elapsedMs >= durationMs) {
				onTick(1);
				document.removeEventListener("visibilitychange", handleVisibility);
				resolve();
				return;
			}
			onTick(Math.min(1, elapsedMs / durationMs));
			window.setTimeout(tick, tickIntervalMs);
		};
		document.addEventListener("visibilitychange", handleVisibility);
		tick();
	});
}
async function withInitTicketCaptureWakeLock(task) {
	let wakeLock = null;
	const requestWakeLock = async () => {
		if (typeof navigator === "undefined" || !("wakeLock" in navigator)) return;
		if (isDocumentHidden()) return;
		try {
			wakeLock = await navigator.wakeLock.request("screen");
		} catch {
			wakeLock = null;
		}
	};
	const handleVisibility = () => {
		if (document.visibilityState === "visible") requestWakeLock();
	};
	try {
		await requestWakeLock();
		document.addEventListener("visibilitychange", handleVisibility);
		return await task();
	} finally {
		document.removeEventListener("visibilitychange", handleVisibility);
		await wakeLock?.release().catch(() => void 0);
	}
}
var VIDEO_BITRATE$2 = 35e6;
async function seekVideoTo(video, time) {
	if (Math.abs(video.currentTime - time) < .001) return;
	await new Promise((resolve, reject) => {
		const onSeeked = () => {
			cleanup();
			resolve();
		};
		const onError = () => {
			cleanup();
			reject(/* @__PURE__ */ new Error("Could not seek video"));
		};
		const cleanup = () => {
			video.removeEventListener("seeked", onSeeked);
			video.removeEventListener("error", onError);
		};
		video.addEventListener("seeked", onSeeked);
		video.addEventListener("error", onError);
		video.currentTime = time;
	});
}
async function compressInitTicketRecordingToClip({ blob, mimeType, fileExtension, sourceDurationSec = 40, onProgress }) {
	const clipDurationSec = 10;
	const targetFps = 60;
	const totalFrames = Math.max(1, Math.round(clipDurationSec * targetFps));
	const frameIntervalMs = 1e3 / targetFps;
	const video = document.createElement("video");
	video.muted = true;
	video.playsInline = true;
	video.preload = "auto";
	const url = URL.createObjectURL(blob);
	try {
		video.src = url;
		await new Promise((resolve, reject) => {
			video.addEventListener("loadedmetadata", () => resolve(), { once: true });
			video.addEventListener("error", () => reject(/* @__PURE__ */ new Error("Could not load recording")), { once: true });
		});
		const metadataDuration = Number.isFinite(video.duration) && video.duration > 0 ? video.duration : sourceDurationSec;
		const canvas = document.createElement("canvas");
		canvas.width = video.videoWidth || 1920;
		canvas.height = video.videoHeight || 1080;
		const ctx = canvas.getContext("2d", { alpha: false });
		if (!ctx) throw new Error("Could not create encode canvas");
		const stream = canvas.captureStream(targetFps);
		const recorder = new MediaRecorder(stream, {
			mimeType,
			videoBitsPerSecond: VIDEO_BITRATE$2
		});
		const chunks = [];
		recorder.ondataavailable = (event) => {
			if (event.data.size > 0) chunks.push(event.data);
		};
		const recordingFinished = new Promise((resolve, reject) => {
			recorder.onstop = () => {
				stream.getTracks().forEach((track) => track.stop());
				resolve({
					blob: new Blob(chunks, { type: mimeType }),
					fileExtension
				});
			};
			recorder.onerror = () => {
				reject(recorder.error ?? /* @__PURE__ */ new Error("Could not encode video"));
			};
		});
		recorder.start(100);
		const encodeStart = performance.now();
		for (let frameIndex = 0; frameIndex < totalFrames; frameIndex += 1) {
			const outputProgress = totalFrames <= 1 ? 1 : frameIndex / (totalFrames - 1);
			await seekVideoTo(video, Math.min(metadataDuration, Math.max(0, outputProgress * metadataDuration)));
			ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
			onProgress?.(INIT_TICKET_VIDEO_CAPTURE_PROGRESS_WEIGHT + outputProgress * (1 - INIT_TICKET_VIDEO_CAPTURE_PROGRESS_WEIGHT));
			const targetTime = encodeStart + (frameIndex + 1) * frameIntervalMs;
			const waitMs = Math.max(0, targetTime - performance.now());
			if (waitMs > 0) await sleep(waitMs);
		}
		onProgress?.(1);
		await sleep(120);
		if (recorder.state !== "inactive") recorder.stop();
		return recordingFinished;
	} finally {
		URL.revokeObjectURL(url);
		video.removeAttribute("src");
		video.load();
	}
}
var CAPTURE_WALL_CLOCK_MS$1 = 40 * 1e3;
var VIDEO_BITRATE$1 = 35e6;
function getCropTargetConstructor() {
	return globalThis.CropTarget;
}
function canRecordInitTicketViaElementCapture() {
	if (typeof navigator === "undefined") return false;
	if (typeof window !== "undefined" && !window.isSecureContext) return false;
	if (typeof navigator.mediaDevices?.getDisplayMedia !== "function") return false;
	if (typeof getCropTargetConstructor()?.fromElement !== "function") return false;
	if (typeof MediaStreamTrack === "undefined") return false;
	return "cropTo" in MediaStreamTrack.prototype;
}
async function apply60FpsVideoConstraints(track) {
	try {
		await track.applyConstraints({ frameRate: {
			ideal: 60,
			min: 30,
			max: 60
		} });
	} catch {}
}
async function recordWallClockCapture({ captureElement, mimeType, setTilt, getTiltForProgress, onProgress }) {
	captureElement.scrollIntoView({
		block: "center",
		behavior: "instant"
	});
	await sleep(80);
	let stream;
	try {
		stream = await navigator.mediaDevices.getDisplayMedia({
			video: {
				frameRate: {
					ideal: 60,
					min: 30,
					max: 60
				},
				width: { ideal: 1920 },
				height: { ideal: 1080 }
			},
			audio: false,
			preferCurrentTab: true,
			selfBrowserSurface: "include",
			surfaceSwitching: "exclude"
		});
	} catch (error) {
		if (error instanceof DOMException && error.name === "NotAllowedError") throw new Error("Tab capture was cancelled");
		throw error;
	}
	const [videoTrack] = stream.getVideoTracks();
	if (!videoTrack?.cropTo) {
		stream.getTracks().forEach((track) => track.stop());
		throw new Error("Element crop is not supported in this browser");
	}
	try {
		const cropTarget = await getCropTargetConstructor().fromElement(captureElement);
		await videoTrack.cropTo(cropTarget);
		await apply60FpsVideoConstraints(videoTrack);
	} catch (error) {
		stream.getTracks().forEach((track) => track.stop());
		throw error;
	}
	const recorder = new MediaRecorder(stream, {
		mimeType,
		videoBitsPerSecond: VIDEO_BITRATE$1
	});
	const chunks = [];
	recorder.ondataavailable = (event) => {
		if (event.data.size > 0) chunks.push(event.data);
	};
	const recordingFinished = new Promise((resolve, reject) => {
		recorder.onstop = () => {
			stream.getTracks().forEach((track) => track.stop());
			resolve(new Blob(chunks, { type: mimeType }));
		};
		recorder.onerror = () => {
			stream.getTracks().forEach((track) => track.stop());
			reject(recorder.error ?? /* @__PURE__ */ new Error("Recording failed"));
		};
	});
	setTilt(0, 0);
	await sleep(32);
	recorder.start(100);
	const pauseRecorder = () => {
		if (recorder.state === "recording") recorder.pause();
	};
	const resumeRecorder = () => {
		if (recorder.state === "paused") recorder.resume();
	};
	await runVisibilityPausedWallClockLoop({
		durationMs: CAPTURE_WALL_CLOCK_MS$1,
		onPause: pauseRecorder,
		onResume: resumeRecorder,
		onTick: (progress) => {
			onProgress?.(progress * INIT_TICKET_VIDEO_CAPTURE_PROGRESS_WEIGHT);
			const { x, y } = getTiltForProgress(progress);
			setTilt(x, y);
		}
	});
	await sleep(200);
	if (recorder.state !== "inactive") recorder.stop();
	return recordingFinished;
}
async function recordInitTicketVideoViaElementCapture({ captureElement, mimeType, fileExtension, setTilt, resetTilt, getTiltForProgress, onProgress, onVisibleCaptureComplete }) {
	const rawBlob = await recordWallClockCapture({
		captureElement,
		mimeType,
		setTilt,
		getTiltForProgress,
		onProgress
	});
	onVisibleCaptureComplete?.();
	resetTilt();
	return compressInitTicketRecordingToClip({
		blob: rawBlob,
		mimeType,
		fileExtension,
		onProgress
	});
}
var INIT_TICKET_CAPTURE_FONT_SMOOTHING_CSS = `
* {
  -webkit-font-smoothing: antialiased !important;
  -moz-osx-font-smoothing: grayscale !important;
  text-rendering: geometricPrecision !important;
}
`;
function buildInitTicketCaptureFontEmbedCss() {
	return INIT_TICKET_CAPTURE_FONT_SMOOTHING_CSS;
}
async function preloadInitTicketCaptureFonts() {
	if (typeof document === "undefined") return;
	await document.fonts.ready;
	await document.fonts.load("400 16px system-ui").catch(() => void 0);
	await document.fonts.load("600 12px ui-monospace").catch(() => void 0);
	await document.fonts.ready;
}
var CAPTURE_WALL_CLOCK_MS = 40 * 1e3;
var FALLBACK_CAPTURE_PIXEL_RATIO = 1.5;
var VIDEO_BITRATE = 2e7;
var FRAME_PADDING_X_RATIO = .1;
var FRAME_PADDING_Y_RATIO = .015;
function canRecordInitTicketViaCanvas() {
	return typeof HTMLCanvasElement !== "undefined" && typeof HTMLCanvasElement.prototype.captureStream === "function";
}
function isCaptureExcludedNode(node) {
	return node instanceof HTMLElement && node.dataset.initTicketCaptureExclude !== void 0;
}
function resolveCaptureSize(element, fallbackWidth) {
	const rect = element.getBoundingClientRect();
	return {
		width: rect.width > 0 ? Math.round(rect.width) : fallbackWidth,
		height: rect.height > 0 ? Math.round(rect.height) : Math.round(fallbackWidth / INIT_TICKET_ASPECT_RATIO)
	};
}
function cloneCanvasFrame(source) {
	const clone = document.createElement("canvas");
	clone.width = source.width;
	clone.height = source.height;
	const ctx = clone.getContext("2d", { alpha: false });
	if (!ctx) throw new Error("Could not clone capture frame");
	ctx.drawImage(source, 0, 0);
	return clone;
}
function pickSnapshotForProgress(snapshots, progress) {
	if (snapshots.length === 0) throw new Error("No frames were captured");
	let best = snapshots[0];
	for (const snapshot of snapshots) {
		if (snapshot.progress <= progress) {
			best = snapshot;
			continue;
		}
		break;
	}
	return best.canvas;
}
async function preloadImages(element) {
	const images = element.querySelectorAll("img");
	await Promise.all(Array.from(images).map((img) => new Promise((resolve) => {
		if (img.complete && img.naturalWidth > 0) {
			resolve();
			return;
		}
		const done = () => resolve();
		img.addEventListener("load", done, { once: true });
		img.addEventListener("error", done, { once: true });
	})));
}
async function prepareCapturePipeline() {
	await preloadInitTicketCaptureFonts();
	return buildInitTicketCaptureFontEmbedCss();
}
async function warmUpFontCapture(captureElement, captureOptions) {
	try {
		await toCanvas(captureElement, captureOptions);
		await document.fonts.ready;
		await waitForNextPaint$1();
	} catch {}
}
function waitForNextPaint$1() {
	if (typeof document !== "undefined" && document.visibilityState === "hidden") return sleep(32);
	return new Promise((resolve) => {
		requestAnimationFrame(() => requestAnimationFrame(() => resolve()));
	});
}
async function gatherSnapshots({ captureElement, captureOptions, compositeCtx, outputCtx, outputWidth, outputHeight, sourceX, sourceY, compositeWidth, compositeHeight, bg, setTilt, getTiltForProgress, onProgress }) {
	const snapshots = [];
	const startTime = performance.now();
	let stopped = false;
	let captureChain = Promise.resolve();
	const wallClockLoop = runWallClockLoop({
		durationMs: CAPTURE_WALL_CLOCK_MS,
		onTick: (progress) => {
			onProgress?.(progress * INIT_TICKET_VIDEO_CAPTURE_PROGRESS_WEIGHT);
			const { x, y } = getTiltForProgress(progress);
			setTilt(x, y);
		}
	});
	const runCaptureLoop = () => {
		if (stopped || performance.now() - startTime >= CAPTURE_WALL_CLOCK_MS) return;
		captureChain = captureChain.then(async () => {
			if (stopped || performance.now() - startTime >= CAPTURE_WALL_CLOCK_MS) return;
			const elapsed = performance.now() - startTime;
			const progress = Math.min(1, elapsed / CAPTURE_WALL_CLOCK_MS);
			const { x, y } = getTiltForProgress(progress);
			setTilt(x, y);
			await waitForNextPaint$1();
			await document.fonts.ready;
			const frameCanvas = await toCanvas(captureElement, captureOptions);
			compositeCtx.clearRect(0, 0, compositeWidth, compositeHeight);
			compositeCtx.fillStyle = bg;
			compositeCtx.fillRect(0, 0, compositeWidth, compositeHeight);
			compositeCtx.drawImage(frameCanvas, sourceX, sourceY, outputWidth, outputHeight);
			outputCtx.clearRect(0, 0, outputWidth, outputHeight);
			outputCtx.drawImage(compositeCtx.canvas, sourceX, sourceY, outputWidth, outputHeight, 0, 0, outputWidth, outputHeight);
			snapshots.push({
				canvas: cloneCanvasFrame(outputCtx.canvas),
				progress
			});
		}).catch(() => void 0).finally(() => {
			if (!stopped && performance.now() - startTime < CAPTURE_WALL_CLOCK_MS) window.setTimeout(runCaptureLoop, 0);
		});
	};
	runCaptureLoop();
	await wallClockLoop;
	stopped = true;
	await captureChain;
	if (snapshots.length === 0) throw new Error("No frames were captured");
	return snapshots;
}
async function replaySnapshotsToStream({ snapshots, outputCtx, outputWidth, outputHeight, onProgress }) {
	const totalFrames = Math.max(1, Math.round(600));
	const frameIntervalMs = 1e3 / 60;
	const playbackStart = performance.now();
	for (let frameIndex = 0; frameIndex < totalFrames; frameIndex += 1) {
		const progress = totalFrames <= 1 ? 1 : frameIndex / (totalFrames - 1);
		const snapshot = pickSnapshotForProgress(snapshots, progress);
		outputCtx.clearRect(0, 0, outputWidth, outputHeight);
		outputCtx.drawImage(snapshot, 0, 0, outputWidth, outputHeight);
		onProgress?.(INIT_TICKET_VIDEO_CAPTURE_PROGRESS_WEIGHT + progress * (1 - INIT_TICKET_VIDEO_CAPTURE_PROGRESS_WEIGHT));
		const targetTime = playbackStart + (frameIndex + 1) * frameIntervalMs;
		const waitMs = Math.max(0, targetTime - performance.now());
		if (waitMs > 0) await sleep(waitMs);
	}
}
async function recordInitTicketVideoViaCanvas({ captureElement, mimeType, fileExtension, exportWidthPx, setTilt, resetTilt, getTiltForProgress, backgroundColor, onProgress, onVisibleCaptureComplete }) {
	const { width: captureWidth, height: captureHeight } = resolveCaptureSize(captureElement, exportWidthPx);
	const padX = Math.ceil(captureWidth * FRAME_PADDING_X_RATIO);
	const padY = Math.ceil(captureHeight * FRAME_PADDING_Y_RATIO);
	const outputWidth = Math.round(captureWidth * FALLBACK_CAPTURE_PIXEL_RATIO);
	const outputHeight = Math.round(captureHeight * FALLBACK_CAPTURE_PIXEL_RATIO);
	const compositeWidth = Math.round((captureWidth + padX * 2) * FALLBACK_CAPTURE_PIXEL_RATIO);
	const compositeHeight = Math.round((captureHeight + padY * 2) * FALLBACK_CAPTURE_PIXEL_RATIO);
	const sourceX = Math.round(padX * FALLBACK_CAPTURE_PIXEL_RATIO);
	const sourceY = Math.round(padY * FALLBACK_CAPTURE_PIXEL_RATIO);
	const compositeCanvas = document.createElement("canvas");
	compositeCanvas.width = compositeWidth;
	compositeCanvas.height = compositeHeight;
	const compositeCtx = compositeCanvas.getContext("2d", { alpha: false });
	if (!compositeCtx) throw new Error("Could not create composite canvas");
	const outputCanvas = document.createElement("canvas");
	outputCanvas.width = outputWidth;
	outputCanvas.height = outputHeight;
	const outputCtx = outputCanvas.getContext("2d", { alpha: false });
	if (!outputCtx) throw new Error("Could not create output canvas");
	const bg = backgroundColor ?? getComputedStyle(captureElement).backgroundColor ?? getComputedStyle(document.documentElement).backgroundColor ?? "#09090b";
	const fontEmbedCSS = await prepareCapturePipeline();
	await preloadImages(captureElement);
	const captureOptions = {
		width: captureWidth,
		height: captureHeight,
		canvasWidth: captureWidth,
		canvasHeight: captureHeight,
		pixelRatio: FALLBACK_CAPTURE_PIXEL_RATIO,
		cacheBust: false,
		includeQueryParams: false,
		skipAutoScale: true,
		backgroundColor: bg,
		preferredFontFormat: "woff2",
		fontEmbedCSS,
		filter: (node) => !isCaptureExcludedNode(node),
		fetchRequestInit: { cache: "force-cache" }
	};
	captureElement.scrollIntoView({
		block: "center",
		behavior: "instant"
	});
	setTilt(0, 0);
	await waitForNextPaint$1();
	await warmUpFontCapture(captureElement, captureOptions);
	const snapshots = await gatherSnapshots({
		captureElement,
		captureOptions,
		compositeCtx,
		outputCtx,
		outputWidth,
		outputHeight,
		sourceX,
		sourceY,
		compositeWidth,
		compositeHeight,
		bg,
		setTilt,
		getTiltForProgress,
		onProgress
	});
	onVisibleCaptureComplete?.();
	resetTilt();
	const stream = outputCanvas.captureStream(60);
	const recorder = new MediaRecorder(stream, {
		mimeType,
		videoBitsPerSecond: VIDEO_BITRATE
	});
	const chunks = [];
	recorder.ondataavailable = (event) => {
		if (event.data.size > 0) chunks.push(event.data);
	};
	const recordingFinished = new Promise((resolve, reject) => {
		recorder.onstop = () => {
			stream.getTracks().forEach((t) => t.stop());
			resolve({
				blob: new Blob(chunks, { type: mimeType }),
				fileExtension
			});
		};
		recorder.onerror = () => {
			reject(recorder.error ?? /* @__PURE__ */ new Error("Recording failed"));
		};
	});
	recorder.start(100);
	await replaySnapshotsToStream({
		snapshots,
		outputCtx,
		outputWidth,
		outputHeight,
		onProgress
	});
	onProgress?.(1);
	await sleep(120);
	if (recorder.state !== "inactive") recorder.stop();
	return recordingFinished;
}
var CAPTURE_TILT_MAX_X = 22;
var CAPTURE_TILT_MAX_Y = 16;
var EXPORT_WIDTH_PX = 820;
var VIDEO_FORMAT_CANDIDATES = [
	{
		mimeType: "video/mp4;codecs=avc1",
		fileExtension: "mp4"
	},
	{
		mimeType: "video/mp4;codecs=\"avc1.42E01E,mp4a.40.2\"",
		fileExtension: "mp4"
	},
	{
		mimeType: "video/mp4",
		fileExtension: "mp4"
	},
	{
		mimeType: "video/webm;codecs=vp9",
		fileExtension: "webm"
	},
	{
		mimeType: "video/webm;codecs=vp8",
		fileExtension: "webm"
	},
	{
		mimeType: "video/webm",
		fileExtension: "webm"
	}
];
function getPreferredVideoFormat() {
	if (typeof MediaRecorder === "undefined") return void 0;
	return VIDEO_FORMAT_CANDIDATES.find((format) => MediaRecorder.isTypeSupported(format.mimeType));
}
function supportsInitTicket60FpsVideoCapture() {
	return canRecordInitTicketViaElementCapture();
}
function isInitTicketVideoExportSupported() {
	if (typeof window === "undefined") return false;
	if (!getPreferredVideoFormat()) return false;
	return canRecordInitTicketViaElementCapture() || canRecordInitTicketViaCanvas();
}
function waitForNextPaint() {
	if (typeof document !== "undefined" && document.visibilityState === "hidden") return sleep(32);
	return new Promise((resolve) => {
		requestAnimationFrame(() => {
			requestAnimationFrame(() => resolve());
		});
	});
}
function getInitTicketTiltForProgress(t) {
	const angle = t * Math.PI * 2 * 2;
	return {
		x: Math.sin(angle) * CAPTURE_TILT_MAX_X * .92,
		y: Math.sin(angle * .92 + .45) * CAPTURE_TILT_MAX_Y
	};
}
function downloadInitTicketVideo(blob, filename) {
	const url = URL.createObjectURL(blob);
	const anchor = document.createElement("a");
	anchor.href = url;
	anchor.download = filename;
	anchor.rel = "noopener";
	anchor.click();
	URL.revokeObjectURL(url);
}
async function recordInitTicketVideo({ captureElement, setTilt, resetTilt, backgroundColor, onProgress, onVisibleCaptureComplete }) {
	const format = getPreferredVideoFormat();
	if (!format) throw new Error("Video recording is not supported in this browser");
	const tiltHandlers = {
		setTilt,
		resetTilt,
		getTiltForProgress: getInitTicketTiltForProgress,
		onProgress
	};
	await preloadInitTicketCaptureFonts();
	return withInitTicketCaptureWakeLock(async () => {
		if (canRecordInitTicketViaElementCapture()) return recordInitTicketVideoViaElementCapture({
			captureElement,
			mimeType: format.mimeType,
			fileExtension: format.fileExtension,
			...tiltHandlers,
			onVisibleCaptureComplete
		});
		if (canRecordInitTicketViaCanvas()) return recordInitTicketVideoViaCanvas({
			captureElement,
			mimeType: format.mimeType,
			fileExtension: format.fileExtension,
			exportWidthPx: EXPORT_WIDTH_PX,
			backgroundColor,
			...tiltHandlers,
			onVisibleCaptureComplete
		});
		throw new Error("Video recording is not supported in this browser");
	});
}
function getInitTicketScaledDimensions(widthPx) {
	const scale = widthPx / 820;
	return {
		widthPx,
		heightPx: widthPx / initTicketDisplayAspectRatio(),
		scale
	};
}
function ScaledTicketShell({ widthPx, pointerEventsNone, overflowVisible, className, children }) {
	const { heightPx, scale } = getInitTicketScaledDimensions(widthPx);
	return /* @__PURE__ */ jsx("div", {
		className: cn("relative shrink-0 touch-none", overflowVisible ? "overflow-visible" : "overflow-hidden", className),
		style: {
			width: widthPx,
			height: heightPx,
			touchAction: "none"
		},
		children: /* @__PURE__ */ jsx("div", {
			className: cn("absolute start-0 top-0 origin-top-start will-change-transform", pointerEventsNone && "pointer-events-none"),
			style: {
				width: 820,
				transform: `scale(${scale})`
			},
			children
		})
	});
}
function InitTicketScaledFrame({ children, className, widthPx: fixedWidthPx, measureContainer = false, pointerEventsNone = false, overflowVisible = false }) {
	const measureRef = useRef(null);
	const [measuredWidthPx, setMeasuredWidthPx] = useState(fixedWidthPx ?? 820);
	useEffect(() => {
		if (!measureContainer || fixedWidthPx !== void 0) return;
		const el = measureRef.current;
		if (!el) return;
		const update = () => {
			const width = el.getBoundingClientRect().width;
			if (width > 0) setMeasuredWidthPx(Math.min(820, width));
		};
		update();
		const observer = new ResizeObserver(update);
		observer.observe(el);
		return () => observer.disconnect();
	}, [fixedWidthPx, measureContainer]);
	useEffect(() => {
		if (fixedWidthPx !== void 0) setMeasuredWidthPx(fixedWidthPx);
	}, [fixedWidthPx]);
	const widthPx = fixedWidthPx ?? measuredWidthPx;
	if (measureContainer) return /* @__PURE__ */ jsx("div", {
		ref: measureRef,
		className: cn("w-full max-w-[820px]", className),
		children: /* @__PURE__ */ jsx(ScaledTicketShell, {
			widthPx,
			pointerEventsNone,
			overflowVisible,
			className: "mx-auto",
			children
		})
	});
	return /* @__PURE__ */ jsx(ScaledTicketShell, {
		widthPx,
		pointerEventsNone,
		overflowVisible,
		className,
		children
	});
}
const InitTicketVideoCaptureStage = forwardRef(function InitTicketVideoCaptureStage$1({ children, className, showRecordingChrome = false, showHeroAnimation = false, ticketAccentColor, ticketUsesDarkChrome = false }, ref) {
	const viewportRef = useRef(null);
	useImperativeHandle(ref, () => viewportRef.current);
	const isRecording = showRecordingChrome || showHeroAnimation;
	return /* @__PURE__ */ jsx("div", {
		className: cn("relative mx-auto w-full max-w-[820px]", className),
		children: /* @__PURE__ */ jsxs("div", {
			className: "relative w-full aspect-video",
			children: [/* @__PURE__ */ jsx(InitTicketRecordingChrome, { visible: showRecordingChrome }), /* @__PURE__ */ jsxs("div", {
				ref: viewportRef,
				"data-init-ticket-video-capture": true,
				className: cn("absolute inset-0 overflow-hidden rounded-2xl transition-[box-shadow] duration-200", showRecordingChrome && "shadow-[0_0_0_2px_color-mix(in_srgb,var(--destructive)_55%,transparent),0_0_0_1px_color-mix(in_srgb,var(--destructive)_25%,transparent),0_0_32px_-8px_color-mix(in_srgb,var(--destructive)_35%,transparent)]"),
				children: [
					showHeroAnimation ? /* @__PURE__ */ jsx("div", {
						className: "absolute inset-0 z-0 overflow-hidden",
						children: /* @__PURE__ */ jsx(InitHeroBackground, {
							containerRef: viewportRef,
							compact: false,
							active: showHeroAnimation,
							bare: true,
							accentColor: ticketAccentColor,
							particleIsDark: ticketUsesDarkChrome,
							particleMotionSpeed: INIT_TICKET_VIDEO_HERO_MOTION_SPEED,
							keepAliveWhenHidden: showHeroAnimation
						})
					}) : null,
					/* @__PURE__ */ jsx("div", {
						className: cn("absolute inset-0 z-[1]", isRecording ? showHeroAnimation ? "bg-muted/30 dark:bg-background/40" : INIT_TICKET_VIDEO_SURFACE_CLASS : "bg-transparent"),
						"aria-hidden": true
					}),
					isRecording ? /* @__PURE__ */ jsx("div", {
						className: cn("pointer-events-none absolute inset-0 z-[2]", INIT_TICKET_VIDEO_DOT_PATTERN_CLASS),
						"aria-hidden": true
					}) : null,
					/* @__PURE__ */ jsx("div", {
						className: "relative z-10 flex h-full w-full items-center justify-center px-4 py-0.5 sm:px-6 sm:py-1",
						children: /* @__PURE__ */ jsx("div", {
							className: "w-full origin-center scale-[0.94] sm:scale-[0.98]",
							style: { maxWidth: 820 },
							children
						})
					})
				]
			})]
		})
	});
});
function InitTicketRecordingChrome({ visible }) {
	return /* @__PURE__ */ jsx("div", {
		className: cn("pointer-events-none absolute inset-0 z-30 rounded-2xl transition-opacity duration-200", visible ? "opacity-100" : "opacity-0"),
		"aria-hidden": !visible,
		children: /* @__PURE__ */ jsxs("div", {
			className: cn("absolute start-3 top-3 flex items-center gap-2 rounded-full bg-red-500/10 px-3 py-1.5 text-[12px] font-semibold tracking-wide text-red-600 shadow-md backdrop-blur-sm sm:start-4 sm:top-4", visible ? "opacity-100" : "opacity-0"),
			children: [/* @__PURE__ */ jsxs("span", {
				className: "relative flex size-2 shrink-0",
				"aria-hidden": true,
				children: [/* @__PURE__ */ jsx("span", { className: cn("absolute inline-flex size-full rounded-full bg-red-500 opacity-70", visible && "animate-ping") }), /* @__PURE__ */ jsx("span", { className: "relative inline-flex size-2 rounded-full bg-red-500" })]
			}), /* @__PURE__ */ jsx("span", { children: "Capturing frames" })]
		})
	});
}
function InitTicketCustomizeDrawer({ open, onOpenChange, prefs, updatePrefs, account, defaultHolderTitle }) {
	const accountName = account?.name?.trim() || account?.email?.split("@")[0] || void 0;
	const toggleStackItem = (id) => {
		const current = prefs.stack ?? [];
		if (current.includes(id)) {
			updatePrefs({ stack: current.filter((item) => item !== id) });
			return;
		}
		if (current.length >= 6) {
			toast.error(`Choose up to 6 technologies`);
			return;
		}
		updatePrefs({ stack: [...current, id] });
	};
	return /* @__PURE__ */ jsxs(BaseDrawer, {
		open,
		onOpenChange,
		title: "Customize ticket",
		description: "Personalize your Init pass before you share it.",
		maxWidth: "sm:max-w-md",
		children: [/* @__PURE__ */ jsx("div", { className: "border-t border-border" }), /* @__PURE__ */ jsxs("div", {
			className: "flex-1 overflow-y-auto px-6 py-6 space-y-6",
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "space-y-2",
					children: [/* @__PURE__ */ jsx(Label, {
						htmlFor: "init-ticket-display-name",
						className: "text-[13px]",
						children: "Name on ticket"
					}), /* @__PURE__ */ jsx(Input, {
						id: "init-ticket-display-name",
						value: prefs.displayName ?? accountName ?? "",
						placeholder: accountName ?? "Your name",
						onChange: (e) => updatePrefs({ displayName: e.target.value }),
						className: "h-9 text-[13px]"
					})]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "space-y-2",
					children: [/* @__PURE__ */ jsx(Label, {
						htmlFor: "init-ticket-holder-title",
						className: "text-[13px]",
						children: "Title on ticket"
					}), /* @__PURE__ */ jsx(Input, {
						id: "init-ticket-holder-title",
						value: prefs.holderTitle ?? "",
						placeholder: defaultHolderTitle,
						onChange: (e) => {
							const value = e.target.value;
							updatePrefs({ holderTitle: value.trim() ? value : void 0 });
						},
						className: "h-9 text-[13px]"
					})]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "space-y-3",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "space-y-1",
						children: [/* @__PURE__ */ jsx("p", {
							className: "text-[13px] font-medium text-foreground",
							children: "Your stack"
						}), /* @__PURE__ */ jsxs("p", {
							className: "text-[12px] text-muted-foreground",
							children: [
								"Pick up to ",
								6,
								" technologies you build with. Icons appear on your ticket."
							]
						})]
					}), /* @__PURE__ */ jsx("div", {
						className: "grid grid-cols-3 gap-2 sm:grid-cols-4",
						children: INIT_TICKET_STACK_OPTIONS.map((option) => {
							const selected = prefs.stack.includes(option.id);
							return /* @__PURE__ */ jsxs("button", {
								type: "button",
								onClick: () => toggleStackItem(option.id),
								className: cn("flex flex-col items-center gap-1.5 rounded-lg border px-2 py-2.5 text-[11px] font-medium transition-colors", selected ? "border-foreground/30 bg-accent text-foreground" : "border-border bg-background text-muted-foreground hover:bg-accent/50 hover:text-foreground"),
								"aria-pressed": selected,
								children: [/* @__PURE__ */ jsx(FrameworkIcon, {
									framework: option.iconKey,
									size: "sm",
									className: "shrink-0"
								}), /* @__PURE__ */ jsx("span", {
									className: "line-clamp-2 text-center leading-tight",
									children: option.label
								})]
							}, option.id);
						})
					})]
				})
			]
		})]
	});
}
function buildFallbackShareUrl() {
	if (typeof window === "undefined") return `${getDefaultSiteOrigin()}/init`;
	return `${window.location.origin}/init`;
}
function buildTicketShareUrl(ticketId) {
	if (!ticketId) return buildFallbackShareUrl();
	if (typeof window === "undefined") return buildInitTicketShareUrl(ticketId, getDefaultSiteOrigin());
	return buildInitTicketShareUrl(ticketId);
}
var shareMenuItemClass = "flex w-full items-center gap-2 rounded-md px-2 py-2 text-start text-[13px] text-foreground transition-colors hover:bg-accent";
function ShareActions({ compact = false, isAuthenticated, isRecapMode = false, shareButtonLabel, isSharing, shareOpen, hasShareLink, shareDisabledTooltip, onShareOpenChange, onNativeShare, onCopyShareMessage, onCustomize, canNativeShare, twitterShareHref, linkedInShareHref, onShareMenuClose }) {
	const buttonSizeClass = compact ? "h-8 text-[12px]" : "h-10 text-[13px]";
	const primaryButtonClass = cn(compact ? "h-8 px-2.5 text-[12px] has-[>svg]:px-2" : "h-10 px-3 text-[13px] has-[>svg]:px-2.5");
	const iconSizeClass = compact ? "me-1 size-3.5" : "me-1.5 size-4";
	const actionsAlignClass = compact ? "justify-start" : "justify-center";
	const primaryShareLabel = shareButtonLabel ?? (isRecapMode ? "Share ticket" : "Share and win");
	if (!isAuthenticated && !hasShareLink) return /* @__PURE__ */ jsxs("div", {
		className: cn("flex flex-wrap items-center gap-2", actionsAlignClass),
		children: [/* @__PURE__ */ jsx(Button, {
			className: primaryButtonClass,
			asChild: true,
			children: /* @__PURE__ */ jsxs(Link, {
				to: "/sign-up",
				search: { redirect: "/init" },
				...analyticsAttrs("init-claim-ticket"),
				children: [/* @__PURE__ */ jsx(Ticket, { className: iconSizeClass }), "Claim your ticket"]
			})
		}), /* @__PURE__ */ jsx(Button, {
			variant: "outline",
			className: buttonSizeClass,
			asChild: true,
			children: /* @__PURE__ */ jsx(Link, {
				to: "/sign-in",
				search: { redirect: "/init" },
				children: "Sign in"
			})
		})]
	});
	return /* @__PURE__ */ jsxs("div", {
		className: cn("flex flex-wrap items-center gap-2", actionsAlignClass),
		children: [
			!isAuthenticated ? /* @__PURE__ */ jsx(Button, {
				className: primaryButtonClass,
				asChild: true,
				children: /* @__PURE__ */ jsxs(Link, {
					to: "/sign-up",
					search: { redirect: "/init" },
					...analyticsAttrs("init-claim-ticket"),
					children: [/* @__PURE__ */ jsx(Ticket, { className: iconSizeClass }), "Claim your ticket"]
				})
			}) : null,
			/* @__PURE__ */ jsxs(Popover, {
				open: shareOpen,
				onOpenChange: onShareOpenChange,
				children: [/* @__PURE__ */ jsx(PopoverTrigger, {
					asChild: true,
					children: /* @__PURE__ */ jsxs(Button, {
						className: primaryButtonClass,
						disabled: isSharing || !hasShareLink,
						title: !hasShareLink ? shareDisabledTooltip : void 0,
						children: [/* @__PURE__ */ jsx(Trophy, { className: iconSizeClass }), primaryShareLabel]
					})
				}), /* @__PURE__ */ jsxs(PopoverContent, {
					align: compact ? "start" : "center",
					className: "w-52 p-1",
					children: [
						canNativeShare ? /* @__PURE__ */ jsxs("button", {
							type: "button",
							className: shareMenuItemClass,
							disabled: isSharing,
							onClick: () => void onNativeShare(),
							children: [/* @__PURE__ */ jsx(Share2, { className: "size-4 shrink-0 text-muted-foreground" }), "Share…"]
						}) : null,
						/* @__PURE__ */ jsxs("a", {
							href: twitterShareHref,
							target: "_blank",
							rel: "noopener noreferrer",
							className: shareMenuItemClass,
							onClick: onShareMenuClose,
							children: [/* @__PURE__ */ jsx("span", {
								className: "flex size-4 shrink-0 items-center justify-center text-[11px] font-bold text-muted-foreground",
								children: "X"
							}), "Share on X"]
						}),
						/* @__PURE__ */ jsxs("a", {
							href: linkedInShareHref,
							target: "_blank",
							rel: "noopener noreferrer",
							className: shareMenuItemClass,
							onClick: onShareMenuClose,
							children: [/* @__PURE__ */ jsx(Linkedin, { className: "size-4 shrink-0 text-muted-foreground" }), "LinkedIn"]
						}),
						/* @__PURE__ */ jsxs("button", {
							type: "button",
							className: cn(shareMenuItemClass, "mt-1 border-t border-border pt-2"),
							onClick: () => void onCopyShareMessage(),
							children: [/* @__PURE__ */ jsx(Copy, { className: "size-4 shrink-0 text-muted-foreground" }), "Copy message"]
						})
					]
				})]
			}),
			isAuthenticated ? /* @__PURE__ */ jsxs(Button, {
				type: "button",
				variant: "outline",
				className: buttonSizeClass,
				onClick: onCustomize,
				children: [/* @__PURE__ */ jsx(SlidersHorizontal, { className: iconSizeClass }), "Customize"]
			}) : null
		]
	});
}
function InitTicketSection({ event, account }) {
	const isAuthenticated = Boolean(account);
	const themeUsesDarkImage = useInitThemeUsesDarkImage();
	const { mockInitTicketType } = useDebugOverrides();
	const { data: identitiesData } = useQuery({
		...accountIdentitiesQueryOptions(),
		enabled: isAuthenticated
	});
	const { prefs, updatePrefs } = useInitTicketPrefs(event.id, account);
	const collapsed = prefs.sectionCollapsed ?? false;
	const [shareOpen, setShareOpen] = useState(false);
	const [customizeOpen, setCustomizeOpen] = useState(false);
	const [isSharing, setIsSharing] = useState(false);
	const [isCapturingVideo, setIsCapturingVideo] = useState(false);
	const [isExportingVideo, setIsExportingVideo] = useState(false);
	const [videoExportProgress, setVideoExportProgress] = useState(null);
	const isVideoBusy = isCapturingVideo || isExportingVideo;
	const { setIsCapturing: setPageVideoCapturing } = useInitTicketVideoRecording();
	const { setPriorityActivity, setTransientActivity } = useInitPresenceActivity();
	const ticketCardRef = useRef(null);
	useEffect(() => {
		setPageVideoCapturing(isCapturingVideo);
		return () => setPageVideoCapturing(false);
	}, [isCapturingVideo, setPageVideoCapturing]);
	useEffect(() => {
		if (!isAuthenticated) return;
		if (isVideoBusy) {
			setPriorityActivity(buildInitRecordingTicketActivity());
			return;
		}
		if (customizeOpen) {
			setPriorityActivity(buildInitCustomizingTicketActivity());
			return;
		}
		setPriorityActivity(null);
	}, [
		customizeOpen,
		isAuthenticated,
		isVideoBusy,
		setPriorityActivity
	]);
	const videoCaptureStageRef = useRef(null);
	const canExportTicketVideo = isInitTicketVideoExportSupported();
	const canExport60FpsVideo = supportsInitTicket60FpsVideoCapture();
	const ticketRenderData = useMemo(() => buildInitTicketRenderData({
		event,
		account,
		identities: isAuthenticated ? identitiesData?.identities : void 0,
		prefs,
		themeUsesDarkImage,
		mockTypeId: mockInitTicketType,
		fallbackHolderName: isAuthenticated ? "Console user" : "Your name"
	}), [
		account,
		event,
		identitiesData?.identities,
		isAuthenticated,
		mockInitTicketType,
		prefs,
		themeUsesDarkImage
	]);
	const { holderName, ticketAppearance } = ticketRenderData;
	useSyncInitTicketImage({
		eventSlug: event.slug,
		account,
		prefs,
		renderData: ticketRenderData,
		themeUsesDarkImage,
		updatePrefs
	});
	const shareUrl = buildTicketShareUrl(prefs.imageFileId);
	const hasShareLink = Boolean(prefs.imageFileId);
	const shareDisabledTooltip = hasShareLink ? void 0 : "Your ticket image is still generating";
	const shareMessage = useMemo(() => buildInitTicketShareMessage({
		eventName: event.name,
		dateRangeLabel: event.dateRangeLabel,
		holderName,
		shareUrl
	}), [
		event.dateRangeLabel,
		event.name,
		holderName,
		shareUrl
	]);
	const twitterShareHref = `https://twitter.com/intent/tweet?text=${encodeURIComponent(shareMessage)}`;
	const linkedInShareHref = `https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(shareUrl)}`;
	const canNativeShare = typeof navigator !== "undefined" && typeof navigator.share === "function";
	const handleNativeShare = async () => {
		if (!hasShareLink) return;
		setIsSharing(true);
		try {
			await navigator.share({
				title: `${event.name} Init ticket`,
				text: shareMessage,
				url: shareUrl
			});
			setShareOpen(false);
		} catch (error) {
			if (error instanceof DOMException && error.name === "AbortError") return;
			toast.error("Could not share your ticket");
		} finally {
			setIsSharing(false);
		}
	};
	const handleCopyShareMessage = async () => {
		try {
			await navigator.clipboard.writeText(shareMessage);
			toast.success("Share message copied");
			setShareOpen(false);
		} catch {
			toast.error("Could not copy to clipboard");
		}
	};
	const handleDownloadTicketVideo = async () => {
		if (!isAuthenticated) return;
		const handle = ticketCardRef.current;
		const captureElement = videoCaptureStageRef.current;
		if (!handle || !captureElement) return;
		setIsCapturingVideo(true);
		setIsExportingVideo(true);
		setVideoExportProgress(0);
		try {
			await waitForNextPaint();
			await waitForNextPaint();
			await new Promise((resolve) => {
				window.setTimeout(resolve, 550);
			});
			handle.prepareForVideoCapture();
			await waitForNextPaint();
			const { blob, fileExtension } = await recordInitTicketVideo({
				captureElement,
				setTilt: handle.setCaptureTilt,
				resetTilt: handle.resetCaptureTilt,
				onProgress: setVideoExportProgress,
				onVisibleCaptureComplete: () => {
					setIsCapturingVideo(false);
					handle.resetCaptureTilt();
				}
			});
			downloadInitTicketVideo(blob, `${event.name.trim().toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "init"}-ticket.${fileExtension}`);
			toast.success("Ticket video downloaded");
		} catch (error) {
			if (error instanceof Error && error.message === "Tab capture was cancelled") toast.error("Video capture was cancelled");
			else toast.error("Could not generate ticket video");
		} finally {
			setIsCapturingVideo(false);
			setIsExportingVideo(false);
			setVideoExportProgress(null);
		}
	};
	const ticketCopy = event.isRecapMode ? event.recap?.ticket : void 0;
	const shareActionsProps = {
		compact: collapsed,
		isAuthenticated,
		isRecapMode: event.isRecapMode,
		shareButtonLabel: ticketCopy?.shareButtonLabel,
		isSharing,
		shareOpen,
		hasShareLink,
		shareDisabledTooltip,
		onShareOpenChange: setShareOpen,
		onNativeShare: () => void handleNativeShare(),
		onCopyShareMessage: () => void handleCopyShareMessage(),
		onCustomize: () => setCustomizeOpen(true),
		canNativeShare,
		twitterShareHref,
		linkedInShareHref,
		onShareMenuClose: () => setShareOpen(false)
	};
	const ticketCardProps = ticketRenderData;
	const sectionTitle = isAuthenticated ? ticketCopy?.titleAuthenticated ?? "Share to enter the giveaway" : ticketCopy?.titleGuest ?? "Claim your Init ticket";
	const sectionDescriptionExpanded = isAuthenticated ? ticketCopy?.descriptionAuthenticated ?? "Post your ticket on socials during Init week. One ticket holder wins the exclusive giveaway on day 5. Sharing is how you enter." : ticketCopy?.descriptionGuest ?? "Create a free account to unlock your personalized pass, customize it with your stack, and share for a chance to win exclusive Init swag.";
	const sectionDescriptionCollapsed = isAuthenticated ? ticketCopy?.descriptionCollapsedAuthenticated ?? "Share your ticket on socials for a chance to win exclusive Init swag." : ticketCopy?.descriptionCollapsedGuest ?? "Sign up to claim your pass and enter the day 5 giveaway.";
	let sectionBody;
	if (collapsed) sectionBody = /* @__PURE__ */ jsxs("div", {
		className: "flex items-center gap-3 py-1 pe-10 sm:gap-5 sm:py-2",
		children: [/* @__PURE__ */ jsx(InitTicketScaledFrame, {
			widthPx: 240,
			pointerEventsNone: true,
			children: /* @__PURE__ */ jsx(InitTicketCard, {
				...ticketCardProps,
				previewOnly: true
			})
		}), /* @__PURE__ */ jsxs("div", {
			className: "min-w-0 flex-1 space-y-2 text-start",
			children: [
				/* @__PURE__ */ jsx("h2", {
					className: "text-[14px] font-semibold leading-tight tracking-tight text-foreground sm:text-[15px]",
					children: sectionTitle
				}),
				/* @__PURE__ */ jsx("p", {
					className: "hidden text-[12px] leading-relaxed text-muted-foreground sm:block",
					children: sectionDescriptionCollapsed
				}),
				/* @__PURE__ */ jsx(ShareActions, { ...shareActionsProps })
			]
		})]
	});
	else sectionBody = /* @__PURE__ */ jsxs("div", {
		className: "mx-auto flex w-full max-w-[820px] flex-col items-center pt-8",
		children: [/* @__PURE__ */ jsx(InitTicketVideoCaptureStage, {
			ref: videoCaptureStageRef,
			showRecordingChrome: isCapturingVideo,
			showHeroAnimation: isCapturingVideo,
			ticketAccentColor: ticketAppearance.accentColor,
			ticketUsesDarkChrome: ticketAppearance.usesDarkChrome,
			children: /* @__PURE__ */ jsx(InitTicketScaledFrame, {
				measureContainer: true,
				className: "w-full",
				overflowVisible: true,
				children: /* @__PURE__ */ jsx(InitTicketCard, {
					ref: ticketCardRef,
					captureMode: isCapturingVideo,
					...ticketCardProps
				})
			})
		}), /* @__PURE__ */ jsxs("div", {
			className: "mt-0 flex w-full flex-col items-center gap-3 pb-5 text-center sm:pb-6",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "max-w-md space-y-2",
				children: [/* @__PURE__ */ jsx("h2", {
					className: "text-[18px] font-semibold leading-tight tracking-tight text-foreground",
					children: sectionTitle
				}), /* @__PURE__ */ jsx("p", {
					className: "text-[13px] leading-relaxed text-muted-foreground",
					children: sectionDescriptionExpanded
				})]
			}), /* @__PURE__ */ jsx(ShareActions, { ...shareActionsProps })]
		})]
	});
	return /* @__PURE__ */ jsxs("section", {
		id: INIT_TICKET_SECTION_ID,
		className: "relative w-full scroll-mt-28",
		"aria-label": "Init ticket",
		onMouseEnter: () => {
			if (!isAuthenticated || customizeOpen || isVideoBusy) return;
			setTransientActivity(buildInitViewingTicketActivity());
		},
		onMouseLeave: () => setTransientActivity(null),
		children: [
			!collapsed && isAuthenticated ? /* @__PURE__ */ jsx(Button, {
				type: "button",
				variant: "outline",
				size: "sm",
				className: "absolute start-0 top-0 z-20 shrink-0 text-[13px]",
				disabled: !canExportTicketVideo || isVideoBusy,
				title: !canExportTicketVideo ? "Video export is not supported in this browser" : isVideoBusy ? void 0 : canExport60FpsVideo ? "Records a ~10s 60fps clip via tab share (Chrome or Edge)" : "Records a ~10s clip (use Chrome or Edge for 60fps)",
				"aria-busy": isVideoBusy,
				onClick: () => void handleDownloadTicketVideo(),
				children: isVideoBusy ? /* @__PURE__ */ jsxs(Fragment, { children: [
					/* @__PURE__ */ jsx(Loader2, { className: "me-1.5 size-4 animate-spin" }),
					isCapturingVideo ? "Recording" : "Processing",
					" ",
					Math.round((videoExportProgress ?? 0) * 100),
					"%"
				] }) : /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(Video, { className: "me-1.5 size-4" }), canExport60FpsVideo ? "Download 60fps video" : "Download ticket video"] })
			}) : null,
			/* @__PURE__ */ jsx(Button, {
				type: "button",
				variant: "outline",
				size: "icon",
				className: "absolute end-0 top-0 z-20 size-8 shrink-0",
				onClick: () => updatePrefs({ sectionCollapsed: !collapsed }),
				"aria-expanded": !collapsed,
				"aria-label": collapsed ? "Expand ticket section" : "Collapse ticket section",
				children: /* @__PURE__ */ jsx(ChevronDown, { className: cn("size-4 transition-transform duration-200", collapsed && "rotate-180") })
			}),
			sectionBody,
			/* @__PURE__ */ jsx(InitTicketCustomizeDrawer, {
				open: customizeOpen,
				onOpenChange: setCustomizeOpen,
				prefs,
				updatePrefs,
				account,
				defaultHolderTitle: ticketAppearance.holderTitle
			})
		]
	});
}
var PRIZE_ZOOM_EASE = "ease-[cubic-bezier(0.33,0,0.2,1)]";
var PRIZE_ZOOM_EASE_COMPACT = "ease-[cubic-bezier(0.22,1,0.36,1)]";
const PRIZE_IMAGE_HOVER_ZOOM = `transition-transform duration-[1600ms] ${PRIZE_ZOOM_EASE} will-change-transform group-hover:scale-[1.03]`;
const PRIZE_IMAGE_HOVER_ZOOM_COMPACT = `transition-transform duration-[2200ms] ${PRIZE_ZOOM_EASE_COMPACT} will-change-transform group-hover:scale-[1.02]`;
const PRIZE_SWAG_IMAGE_OPACITY = `opacity-[0.85] transition-opacity duration-[1600ms] ${PRIZE_ZOOM_EASE} group-hover:opacity-100`;
const PRIZE_SWAG_IMAGE_OPACITY_COMPACT = `opacity-[0.88] transition-opacity duration-[2200ms] ${PRIZE_ZOOM_EASE_COMPACT} group-hover:opacity-100`;
const PRIZE_IMAGE_INSET = "p-3 sm:p-4";
const PRIZE_IMAGE_FRAME = "relative overflow-hidden rounded-lg";
const PRIZE_CARD_BG = "bg-card";
function GiveawayPromoCard({ giveaway }) {
	const imageSrc = useInitThemeImageSrc(giveaway.imageSrcLight, giveaway.imageSrcDark);
	const ctaLabel = giveaway.ctaLabel ?? "View all prizes";
	const ctaHref = giveaway.ctaHref ?? `#prizes`;
	return /* @__PURE__ */ jsxs("section", {
		className: cn("group overflow-hidden rounded-xl border border-border", PRIZE_CARD_BG),
		"aria-labelledby": "init-giveaway-heading",
		children: [/* @__PURE__ */ jsx("div", {
			className: cn("w-full shrink-0 border-b border-border", PRIZE_IMAGE_INSET),
			children: /* @__PURE__ */ jsx("div", {
				className: cn(PRIZE_IMAGE_FRAME, "aspect-[3/2]"),
				children: /* @__PURE__ */ jsx("img", {
					src: imageSrc,
					alt: giveaway.imageAlt,
					className: cn("absolute inset-0 size-full object-cover object-center", PRIZE_IMAGE_HOVER_ZOOM_COMPACT),
					loading: "lazy",
					decoding: "async"
				})
			})
		}), /* @__PURE__ */ jsxs("div", {
			className: "flex flex-col gap-4 px-6 py-5",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "space-y-3",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "flex items-center gap-2",
					children: [/* @__PURE__ */ jsx("span", {
						className: "flex size-8 shrink-0 items-center justify-center rounded-lg bg-[color-mix(in_srgb,var(--brand-cta)_12%,transparent)] text-[var(--brand-cta)]",
						children: /* @__PURE__ */ jsx(Gift, {
							className: "size-3.5",
							"aria-hidden": true
						})
					}), /* @__PURE__ */ jsx("p", {
						className: "text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground",
						children: "Giveaways"
					})]
				}), /* @__PURE__ */ jsxs("div", {
					className: "space-y-2",
					children: [/* @__PURE__ */ jsx("h3", {
						id: "init-giveaway-heading",
						className: "text-[17px] font-semibold leading-snug tracking-tight text-foreground",
						children: giveaway.title
					}), /* @__PURE__ */ jsx("p", {
						className: "text-[13px] leading-relaxed text-muted-foreground",
						children: giveaway.description
					})]
				})]
			}), /* @__PURE__ */ jsx(Button, {
				variant: "outline",
				size: "sm",
				className: "h-9 w-full text-[13px]",
				asChild: true,
				children: /* @__PURE__ */ jsxs("a", {
					href: ctaHref,
					children: [ctaLabel, /* @__PURE__ */ jsx(ChevronRight, {
						className: "size-4",
						"aria-hidden": true
					})]
				})
			})]
		})]
	});
}
function resolveInvolvementItem(item, isAuthenticated) {
	if (item.id !== "ticket" || !isAuthenticated) return item;
	return {
		...item,
		title: "Share ticket",
		href: `#${INIT_TICKET_SECTION_ID}`
	};
}
function GetInvolvedCards({ event, account }) {
	const { setTransientActivity } = useInitPresenceActivity();
	const isAuthenticated = Boolean(account);
	const items = event.getInvolved.map((item) => resolveInvolvementItem(item, isAuthenticated));
	if (items.length === 0) return null;
	const sectionTitle = event.isRecapMode ? event.recap?.getInvolvedSectionTitle ?? "Keep exploring" : "Ways to get involved";
	const itemCount = items.length;
	return /* @__PURE__ */ jsxs("section", {
		id: INIT_GET_INVOLVED_SECTION_ID,
		className: "space-y-4",
		children: [/* @__PURE__ */ jsx("h3", {
			className: "text-[15px] font-semibold text-foreground",
			children: sectionTitle
		}), /* @__PURE__ */ jsx("div", {
			className: cn("grid gap-3", itemCount === 1 && "max-w-xl grid-cols-1", itemCount === 2 && "grid-cols-1 sm:grid-cols-2", itemCount >= 3 && "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3"),
			children: items.map((item) => {
				const Icon$1 = item.icon;
				const inner = /* @__PURE__ */ jsxs(Fragment, { children: [
					/* @__PURE__ */ jsx("div", {
						className: "flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-muted text-muted-foreground",
						children: item.iconSrc ? /* @__PURE__ */ jsx("img", {
							src: item.iconSrc,
							alt: "",
							className: "size-5 dark:invert-0",
							"aria-hidden": true
						}) : Icon$1 ? /* @__PURE__ */ jsx(Icon$1, {
							className: "size-5",
							"aria-hidden": true
						}) : null
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "min-w-0 flex-1",
						children: [/* @__PURE__ */ jsx("p", {
							className: "text-[13px] font-medium text-foreground",
							children: item.title
						}), /* @__PURE__ */ jsx("p", {
							className: "mt-0.5 text-[12px] text-muted-foreground",
							children: item.description
						})]
					}),
					/* @__PURE__ */ jsx(ChevronRight, {
						className: "size-4 shrink-0 text-[var(--brand-cta)]",
						"aria-hidden": true
					})
				] });
				const className = cn("group flex w-full items-center gap-3 rounded-xl border border-border bg-card/50 p-4 text-start transition-colors", item.href && "hover:border-border hover:bg-accent/50");
				const presenceHandlers = {
					onMouseEnter: () => setTransientActivity(buildInitExploringActivity(item.title)),
					onMouseLeave: () => setTransientActivity(null),
					onFocus: () => setTransientActivity(buildInitExploringActivity(item.title)),
					onBlur: () => setTransientActivity(null)
				};
				const isExternalLink = item.href ? isExternalInitHref(item.href) : false;
				if (item.href) return /* @__PURE__ */ jsx("a", {
					href: item.href,
					...isExternalLink ? {
						target: "_blank",
						rel: "noopener noreferrer"
					} : {},
					className,
					...presenceHandlers,
					children: inner
				}, item.id);
				return /* @__PURE__ */ jsx("div", {
					className,
					...presenceHandlers,
					children: inner
				}, item.id);
			})
		})]
	});
}
var WHEEL_SIZE = 360;
var WHEEL_RADIUS = WHEEL_SIZE / 2 - 6;
var SPIN_DURATION = 4.2;
var SPIN_EASE = [
	.12,
	.82,
	.18,
	1
];
var SEGMENT_FILLS = ["color-mix(in srgb, var(--brand-cta) 18%, var(--card))", "color-mix(in srgb, var(--brand-cta) 8%, var(--muted))"];
function polarToCartesian(angleDeg, radius) {
	const angleRad = (angleDeg - 90) * Math.PI / 180;
	return {
		x: radius * Math.cos(angleRad),
		y: radius * Math.sin(angleRad)
	};
}
function buildSegmentPath(index, count, radius) {
	const segmentAngle = 360 / count;
	const startAngle = index * segmentAngle;
	const endAngle = (index + 1) * segmentAngle;
	const start = polarToCartesian(startAngle, radius);
	const end = polarToCartesian(endAngle, radius);
	const largeArc = segmentAngle > 180 ? 1 : 0;
	return [
		"M 0 0",
		`L ${start.x} ${start.y}`,
		`A ${radius} ${radius} 0 ${largeArc} 1 ${end.x} ${end.y}`,
		"Z"
	].join(" ");
}
function firstName(name) {
	const token = name.trim().split(/\s+/)[0] || name;
	return token.charAt(0).toUpperCase() + token.slice(1);
}
function computeRaffleWheelRotation(winnerIndex, participantCount, currentRotation) {
	if (participantCount <= 0) return currentRotation;
	const segmentAngle = 360 / participantCount;
	const segmentCenter = (winnerIndex + .5) * segmentAngle;
	const normalized = (currentRotation % 360 + 360) % 360;
	const delta = (360 - segmentCenter - normalized + 360) % 360;
	return currentRotation + 360 * 6 + delta;
}
function InitGiveawayRaffleWheel({ participants, rotation, className }) {
	const reduceMotion = useReducedMotion();
	const clipId = `raffle-wheel-${useId().replace(/:/g, "")}`;
	const count = participants.length;
	const center = WHEEL_SIZE / 2;
	const segments = useMemo(() => {
		if (count === 0) return [];
		const segmentAngle = 360 / count;
		const labelRadius = WHEEL_RADIUS * .58;
		return participants.map((participant, index) => {
			const midAngle = index * segmentAngle + segmentAngle / 2;
			const labelPosition = polarToCartesian(midAngle, labelRadius);
			return {
				participant,
				path: buildSegmentPath(index, count, WHEEL_RADIUS),
				fill: SEGMENT_FILLS[index % SEGMENT_FILLS.length],
				labelPosition,
				labelRotation: midAngle
			};
		});
	}, [count, participants]);
	if (count === 0) return /* @__PURE__ */ jsx("div", {
		className: cn("flex items-center justify-center rounded-full border border-dashed border-border bg-muted/20", className),
		style: {
			width: WHEEL_SIZE,
			height: WHEEL_SIZE
		},
		children: /* @__PURE__ */ jsx("p", {
			className: "max-w-[200px] text-center text-[13px] text-muted-foreground",
			children: "No one is online right now."
		})
	});
	return /* @__PURE__ */ jsxs("div", {
		className: cn("relative shrink-0", className),
		style: {
			width: WHEEL_SIZE,
			height: WHEEL_SIZE
		},
		children: [
			/* @__PURE__ */ jsx("div", {
				className: "pointer-events-none absolute start-1/2 top-0 z-20 -translate-x-1/2",
				"aria-hidden": true,
				children: /* @__PURE__ */ jsx("div", { className: "size-0 border-x-[11px] border-x-transparent border-t-[18px] border-t-[var(--brand-cta)] drop-shadow-sm" })
			}),
			/* @__PURE__ */ jsx("div", {
				className: "absolute inset-[10px] overflow-hidden rounded-full border border-border bg-card shadow-lg",
				children: /* @__PURE__ */ jsx(motion.div, {
					className: "size-full origin-center",
					animate: { rotate: rotation },
					transition: reduceMotion ? { duration: 0 } : {
						duration: SPIN_DURATION,
						ease: SPIN_EASE
					},
					children: /* @__PURE__ */ jsxs("svg", {
						width: WHEEL_SIZE,
						height: WHEEL_SIZE,
						viewBox: `0 0 ${WHEEL_SIZE} ${WHEEL_SIZE}`,
						className: "size-full",
						role: "img",
						"aria-label": "Giveaway raffle wheel",
						children: [/* @__PURE__ */ jsx("defs", { children: /* @__PURE__ */ jsx("clipPath", {
							id: clipId,
							children: /* @__PURE__ */ jsx("circle", {
								cx: center,
								cy: center,
								r: WHEEL_RADIUS
							})
						}) }), /* @__PURE__ */ jsx("g", {
							clipPath: `url(#${clipId})`,
							children: /* @__PURE__ */ jsx("g", {
								transform: `translate(${center}, ${center})`,
								children: segments.map(({ participant, path, fill, labelPosition, labelRotation }) => /* @__PURE__ */ jsxs("g", { children: [/* @__PURE__ */ jsx("path", {
									d: path,
									fill,
									stroke: "var(--border)",
									strokeWidth: 1
								}), /* @__PURE__ */ jsx("text", {
									x: labelPosition.x,
									y: labelPosition.y,
									fill: "var(--foreground)",
									fontSize: count > 12 ? 9 : count > 6 ? 10 : 12,
									fontWeight: 600,
									textAnchor: "middle",
									dominantBaseline: "middle",
									transform: `rotate(${labelRotation}, ${labelPosition.x}, ${labelPosition.y})`,
									children: firstName(participant.name).slice(0, count > 14 ? 6 : 12)
								})] }, participant.id))
							})
						})]
					})
				})
			}),
			/* @__PURE__ */ jsx("div", {
				className: "pointer-events-none absolute start-1/2 top-1/2 z-10 size-11 -translate-x-1/2 -translate-y-1/2 rounded-full border border-border bg-card shadow-sm",
				"aria-hidden": true
			})
		]
	});
}
const INIT_GIVEAWAY_RAFFLE_SPIN_MS = SPIN_DURATION * 1e3;
var RAFFLE_PARTICIPANT_LIMIT = 200;
async function fetchInitRaffleParticipants(event) {
	const allowlist = buildInitPresenceActivityAllowlist(event);
	return mapPresencesToOnlineUsers(await listInitPresences(event.id, "online", RAFFLE_PARTICIPANT_LIMIT), allowlist);
}
var GIVEAWAY_RAFFLE_DAYS = [
	1,
	2,
	3,
	4
];
var GRAND_PRIZE_REVEAL_DAY = 5;
function parseGiveawayHostShortcut(event) {
	if (!event.shiftKey || !event.ctrlKey) return null;
	if (event.altKey || event.metaKey) return null;
	const dayMatch = event.code.match(/^Digit([1-5])$/);
	if (!dayMatch) return null;
	return Number(dayMatch[1]);
}
function useInitGiveawayRaffle(event, dailyGiveaways, grandPrize, onClose) {
	const [activeDay, setActiveDay] = useState(null);
	const [isGrandPrizeRevealOpen, setIsGrandPrizeRevealOpen] = useState(false);
	const [participants, setParticipants] = useState([]);
	const [loadingParticipants, setLoadingParticipants] = useState(false);
	const enabled = Boolean(event.presenceEnabled && !event.isRecapMode && dailyGiveaways.length > 0);
	const close = useCallback(() => {
		setActiveDay(null);
		setIsGrandPrizeRevealOpen(false);
		setParticipants([]);
		setLoadingParticipants(false);
		onClose?.();
	}, [onClose]);
	const loadParticipants = useCallback(async () => {
		setLoadingParticipants(true);
		try {
			setParticipants(await fetchInitRaffleParticipants(event));
		} catch {
			setParticipants([]);
		} finally {
			setLoadingParticipants(false);
		}
	}, [event]);
	const openForDay = useCallback(async (day) => {
		if (!isInitDailyPrizeRevealed(event.currentDay, day)) return;
		if (!dailyGiveaways.find((entry) => entry.day === day)) return;
		if (activeDay === day && !isGrandPrizeRevealOpen) {
			close();
			return;
		}
		setIsGrandPrizeRevealOpen(false);
		setActiveDay(day);
		await loadParticipants();
	}, [
		activeDay,
		close,
		dailyGiveaways,
		event.currentDay,
		isGrandPrizeRevealOpen,
		loadParticipants
	]);
	const openGrandPrizeReveal = useCallback(async () => {
		if (!grandPrize) return;
		if (!isInitDailyPrizeRevealed(event.currentDay, grandPrize.day)) return;
		if (isGrandPrizeRevealOpen) {
			close();
			return;
		}
		setActiveDay(null);
		setIsGrandPrizeRevealOpen(true);
		await loadParticipants();
	}, [
		close,
		event.currentDay,
		grandPrize,
		isGrandPrizeRevealOpen,
		loadParticipants
	]);
	const activeGiveaway = activeDay == null ? void 0 : dailyGiveaways.find((entry) => entry.day === activeDay);
	useEffect(() => {
		if (!enabled) return;
		const handleKeyDown = (keyboardEvent) => {
			if (shouldSuppressGlobalShortcuts(document.activeElement)) return;
			if (keyboardEvent.key === "Escape" && (activeDay != null || isGrandPrizeRevealOpen)) {
				keyboardEvent.preventDefault();
				close();
				return;
			}
			const day = parseGiveawayHostShortcut(keyboardEvent);
			if (!day) return;
			if (day === GRAND_PRIZE_REVEAL_DAY) {
				if (!grandPrize) return;
				keyboardEvent.preventDefault();
				keyboardEvent.stopPropagation();
				openGrandPrizeReveal();
				return;
			}
			if (!dailyGiveaways.some((entry) => entry.day === day)) return;
			keyboardEvent.preventDefault();
			keyboardEvent.stopPropagation();
			openForDay(day);
		};
		window.addEventListener("keydown", handleKeyDown, true);
		return () => window.removeEventListener("keydown", handleKeyDown, true);
	}, [
		activeDay,
		close,
		dailyGiveaways,
		enabled,
		grandPrize,
		isGrandPrizeRevealOpen,
		openForDay,
		openGrandPrizeReveal
	]);
	return {
		enabled,
		activeDay,
		activeGiveaway,
		isGrandPrizeRevealOpen,
		grandPrize,
		participants,
		loadingParticipants,
		openForDay,
		openGrandPrizeReveal,
		close,
		raffleDays: GIVEAWAY_RAFFLE_DAYS
	};
}
var InitGiveawayRaffleContext = createContext(null);
function InitGiveawayRaffleProvider({ event, children }) {
	const [raffleWinnerId, setRaffleWinnerId] = useState(null);
	const [raffleWinnerPulse, setRaffleWinnerPulse] = useState(0);
	const clearRaffleWinner = useCallback(() => {
		setRaffleWinnerId(null);
	}, []);
	const raffle = useInitGiveawayRaffle(event, event.prizes?.dailyGiveaways ?? [], event.prizes?.grandPrize, clearRaffleWinner);
	const { setPriorityActivity } = useInitPresenceActivity();
	useEffect(() => {
		if (raffle.isGrandPrizeRevealOpen) {
			setPriorityActivity(buildInitRunningGrandPrizeRevealActivity());
			return () => setPriorityActivity(null);
		}
		if (raffle.activeDay == null) {
			setPriorityActivity(null);
			return;
		}
		setPriorityActivity(buildInitRunningGiveawayRaffleActivity(raffle.activeDay));
		return () => setPriorityActivity(null);
	}, [
		raffle.activeDay,
		raffle.isGrandPrizeRevealOpen,
		setPriorityActivity
	]);
	const celebrateRaffleWinner = useCallback((user) => {
		setRaffleWinnerId(user.id);
		setRaffleWinnerPulse((pulse) => pulse + 1);
	}, []);
	const value = useMemo(() => ({
		...raffle,
		raffleWinnerId,
		raffleWinnerPulse,
		celebrateRaffleWinner,
		clearRaffleWinner
	}), [
		celebrateRaffleWinner,
		clearRaffleWinner,
		raffle,
		raffleWinnerId,
		raffleWinnerPulse
	]);
	return /* @__PURE__ */ jsx(InitGiveawayRaffleContext.Provider, {
		value,
		children
	});
}
function useInitGiveawayRaffleContext() {
	return useContext(InitGiveawayRaffleContext);
}
function InitGiveawayRaffleBack({ giveaway, participants, loadingParticipants, onClose }) {
	const prizeImageSrc = useInitThemeImageSrc(giveaway.visual?.imageSrcLight ?? "", giveaway.visual?.imageSrcDark ?? "");
	const hasPrizeImage = Boolean(giveaway.visual?.imageSrcLight && giveaway.visual?.imageSrcDark);
	const [rotation, setRotation] = useState(0);
	const [isSpinning, setIsSpinning] = useState(false);
	const [winner, setWinner] = useState(null);
	const raffleContext = useInitGiveawayRaffleContext();
	const { setTransientActivity } = useInitPresenceActivity();
	useEffect(() => {
		if (!isSpinning) {
			setTransientActivity(null);
			return;
		}
		setTransientActivity(buildInitSpinningGiveawayRaffleActivity());
		return () => setTransientActivity(null);
	}, [isSpinning, setTransientActivity]);
	const canRaffle = participants.length > 0 && !isSpinning && !loadingParticipants;
	const handleRaffle = useCallback(() => {
		if (!canRaffle) return;
		const winnerIndex = Math.floor(Math.random() * participants.length);
		const nextWinner = participants[winnerIndex];
		const nextRotation = computeRaffleWheelRotation(winnerIndex, participants.length, rotation);
		setWinner(null);
		raffleContext?.clearRaffleWinner();
		setIsSpinning(true);
		setRotation(nextRotation);
		window.setTimeout(() => {
			setWinner(nextWinner);
			setIsSpinning(false);
			raffleContext?.celebrateRaffleWinner(nextWinner);
		}, INIT_GIVEAWAY_RAFFLE_SPIN_MS);
	}, [
		canRaffle,
		participants,
		raffleContext,
		rotation
	]);
	return /* @__PURE__ */ jsxs("div", {
		className: cn("flex h-full min-h-[min(100%,640px)] flex-col overflow-hidden rounded-xl border border-border", PRIZE_CARD_BG),
		children: [/* @__PURE__ */ jsxs("div", {
			className: "flex items-start justify-between gap-4 border-b border-border px-5 py-4 sm:px-6",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "min-w-0 space-y-1",
				children: [
					/* @__PURE__ */ jsxs("p", {
						className: "text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground",
						children: [
							"Day ",
							giveaway.day,
							" giveaway · ",
							giveaway.dateLabel
						]
					}),
					/* @__PURE__ */ jsx("h4", {
						className: "text-[18px] font-semibold tracking-tight text-foreground",
						children: "Live giveaway draw"
					}),
					/* @__PURE__ */ jsx("p", {
						className: "text-[13px] text-muted-foreground",
						children: "One random winner from everyone here live on Init takes home today's prize."
					})
				]
			}), /* @__PURE__ */ jsx(Button, {
				type: "button",
				variant: "ghost",
				size: "icon",
				className: "size-8 shrink-0",
				onClick: onClose,
				"aria-label": "Close raffle",
				children: /* @__PURE__ */ jsx(X, { className: "size-4" })
			})]
		}), /* @__PURE__ */ jsxs("div", {
			className: "grid flex-1 gap-6 p-5 sm:p-6 lg:grid-cols-[minmax(0,1fr)_280px] xl:grid-cols-[minmax(0,1fr)_320px] lg:items-center",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex flex-col items-center justify-center gap-5 py-2",
				children: [
					loadingParticipants ? /* @__PURE__ */ jsx("div", {
						className: "flex items-center justify-center rounded-full border border-border bg-muted/20",
						style: {
							width: 360,
							height: 360
						},
						children: /* @__PURE__ */ jsx(Loader2, {
							className: "size-6 animate-spin text-muted-foreground",
							"aria-hidden": true
						})
					}) : /* @__PURE__ */ jsx(InitGiveawayRaffleWheel, {
						participants,
						rotation
					}),
					/* @__PURE__ */ jsx("p", {
						className: "text-center text-[12px] text-muted-foreground",
						children: loadingParticipants ? "Loading online participants…" : `${participants.length} participant${participants.length === 1 ? "" : "s"} online`
					}),
					/* @__PURE__ */ jsx(Button, {
						type: "button",
						className: "h-10 min-w-[180px] text-[13px]",
						disabled: !canRaffle,
						onClick: handleRaffle,
						children: isSpinning ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(Loader2, {
							className: "size-4 animate-spin",
							"aria-hidden": true
						}), "Spinning…"] }) : winner ? "Raffle again" : "Raffle winner"
					})
				]
			}), /* @__PURE__ */ jsxs("div", {
				className: "flex flex-col gap-4",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "overflow-hidden rounded-xl border border-border bg-card",
					children: [hasPrizeImage ? /* @__PURE__ */ jsx("div", {
						className: "relative aspect-[3/2] overflow-hidden bg-muted/20",
						children: /* @__PURE__ */ jsx("img", {
							src: prizeImageSrc,
							alt: giveaway.visual?.imageAlt ?? giveaway.prizeDescription,
							className: "absolute inset-0 size-full object-cover object-center"
						})
					}) : null, /* @__PURE__ */ jsxs("div", {
						className: "space-y-2 p-4",
						children: [
							/* @__PURE__ */ jsx("p", {
								className: "text-[10px] font-semibold uppercase tracking-[0.18em] text-muted-foreground",
								children: "Today's prize"
							}),
							/* @__PURE__ */ jsx("p", {
								className: "text-[15px] font-semibold text-foreground",
								children: giveaway.prizeDescription
							}),
							/* @__PURE__ */ jsx("p", {
								className: "text-[13px] text-muted-foreground",
								children: giveaway.sessionTitle
							})
						]
					})]
				}), /* @__PURE__ */ jsx("div", {
					className: cn("rounded-xl border border-border bg-muted/20 p-4 transition-colors", winner && "border-[color-mix(in_srgb,var(--brand-cta)_35%,var(--border))] bg-[color-mix(in_srgb,var(--brand-cta)_8%,transparent)]"),
					children: winner ? /* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-3",
						children: [/* @__PURE__ */ jsx(InitialsAvatar, {
							name: winner.name,
							size: "md",
							className: "rounded-full"
						}), /* @__PURE__ */ jsxs("div", {
							className: "min-w-0",
							children: [/* @__PURE__ */ jsxs("div", {
								className: "flex items-center gap-1.5 text-[10px] font-semibold uppercase tracking-[0.16em] text-[var(--brand-cta)]",
								children: [/* @__PURE__ */ jsx(Trophy, {
									className: "size-3.5",
									"aria-hidden": true
								}), "Winner"]
							}), /* @__PURE__ */ jsx("p", {
								className: "truncate text-[15px] font-semibold text-foreground",
								children: winner.name
							})]
						})]
					}) : /* @__PURE__ */ jsxs("div", {
						className: "flex items-start gap-3 text-muted-foreground",
						children: [/* @__PURE__ */ jsx("span", {
							className: "flex size-9 shrink-0 items-center justify-center rounded-lg bg-background/70",
							children: /* @__PURE__ */ jsx(Sparkles, {
								className: "size-4",
								"aria-hidden": true
							})
						}), /* @__PURE__ */ jsx("p", {
							className: "text-[13px] leading-relaxed",
							children: isSpinning ? "Hold tight - the wheel is spinning." : "Press raffle winner when you are ready to draw."
						})]
					})
				})]
			})]
		})]
	});
}
var PLATFORM_META$1 = {
	youtube: {
		label: "YouTube",
		icon: "/icons/youtube.svg"
	},
	discord: {
		label: "Discord",
		icon: "/icons/discord-simple.svg"
	},
	reddit: {
		label: "Reddit",
		icon: "/icons/reddit.svg"
	}
};
function InitGrandPrizeRevealBack({ grandPrize, participants, loadingParticipants, onClose }) {
	const prizeImageSrc = useInitThemeImageSrc(grandPrize.visual?.imageSrcLight ?? "", grandPrize.visual?.imageSrcDark ?? "");
	const hasPrizeImage = Boolean(grandPrize.visual?.imageSrcLight && grandPrize.visual?.imageSrcDark);
	const platformMeta = grandPrize.platform ? PLATFORM_META$1[grandPrize.platform] : null;
	const [winner, setWinner] = useState(null);
	const raffleContext = useInitGiveawayRaffleContext();
	const canReveal = participants.length > 0 && !loadingParticipants;
	const handleReveal = useCallback(() => {
		if (!canReveal) return;
		const nextWinner = participants[Math.floor(Math.random() * participants.length)];
		setWinner(nextWinner);
		raffleContext?.celebrateRaffleWinner(nextWinner);
	}, [
		canReveal,
		participants,
		raffleContext
	]);
	const handleRevealAgain = useCallback(() => {
		if (!canReveal) return;
		const nextWinner = participants[Math.floor(Math.random() * participants.length)];
		raffleContext?.clearRaffleWinner();
		setWinner(nextWinner);
		raffleContext?.celebrateRaffleWinner(nextWinner);
	}, [
		canReveal,
		participants,
		raffleContext
	]);
	return /* @__PURE__ */ jsxs("div", {
		className: cn("flex h-full min-h-[min(100%,640px)] flex-col overflow-hidden rounded-xl border border-border", PRIZE_CARD_BG),
		children: [/* @__PURE__ */ jsxs("div", {
			className: "flex items-start justify-between gap-4 border-b border-border px-5 py-4 sm:px-6",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "min-w-0 space-y-1",
				children: [
					/* @__PURE__ */ jsxs("p", {
						className: "text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground",
						children: [
							"Grand prize · Day ",
							grandPrize.day,
							" · ",
							grandPrize.dateLabel
						]
					}),
					/* @__PURE__ */ jsx("h4", {
						className: "text-[18px] font-semibold tracking-tight text-foreground",
						children: "Live winner reveal"
					}),
					/* @__PURE__ */ jsx("p", {
						className: "text-[13px] text-muted-foreground",
						children: grandPrize.sessionTitle ? `Announced during ${grandPrize.sessionTitle.toLowerCase()}.` : "Announced live during Init day 5."
					})
				]
			}), /* @__PURE__ */ jsx(Button, {
				type: "button",
				variant: "ghost",
				size: "icon",
				className: "size-8 shrink-0",
				onClick: onClose,
				"aria-label": "Close winner reveal",
				children: /* @__PURE__ */ jsx(X, { className: "size-4" })
			})]
		}), /* @__PURE__ */ jsxs("div", {
			className: "grid flex-1 gap-6 p-5 sm:p-6 lg:grid-cols-[minmax(0,1fr)_280px] xl:grid-cols-[minmax(0,1fr)_320px] lg:items-center",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex flex-col items-center justify-center gap-5 py-2",
				children: [
					/* @__PURE__ */ jsxs("div", {
						className: cn("w-full max-w-md overflow-hidden rounded-xl border border-border bg-card", winner && "border-[color-mix(in_srgb,var(--brand-cta)_35%,var(--border))]"),
						children: [hasPrizeImage ? /* @__PURE__ */ jsx("div", {
							className: "relative aspect-[4/3] overflow-hidden bg-muted/20",
							children: /* @__PURE__ */ jsx("img", {
								src: prizeImageSrc,
								alt: grandPrize.visual?.imageAlt ?? grandPrize.title,
								className: "absolute inset-0 size-full object-cover object-center"
							})
						}) : null, /* @__PURE__ */ jsxs("div", {
							className: "space-y-2 p-4",
							children: [
								/* @__PURE__ */ jsx("p", {
									className: "text-[10px] font-semibold uppercase tracking-[0.18em] text-muted-foreground",
									children: "Grand prize"
								}),
								/* @__PURE__ */ jsx("p", {
									className: "text-[15px] font-semibold text-foreground",
									children: grandPrize.title
								}),
								/* @__PURE__ */ jsx("p", {
									className: "text-[13px] text-muted-foreground",
									children: grandPrize.description
								}),
								grandPrize.sessionTitle && platformMeta ? /* @__PURE__ */ jsxs("div", {
									className: "flex min-w-0 items-center gap-1.5 pt-1 text-[11px] text-muted-foreground",
									children: [/* @__PURE__ */ jsx("img", {
										src: platformMeta.icon,
										alt: "",
										className: "size-3 shrink-0 opacity-70",
										"aria-hidden": true
									}), /* @__PURE__ */ jsxs("span", {
										className: "truncate",
										children: [
											platformMeta.label,
											" · ",
											grandPrize.sessionTitle,
											" · ",
											grandPrize.timeLabel
										]
									})]
								}) : null
							]
						})]
					}),
					/* @__PURE__ */ jsx("p", {
						className: "text-center text-[12px] text-muted-foreground",
						children: loadingParticipants ? "Loading online participants…" : `${participants.length} participant${participants.length === 1 ? "" : "s"} online`
					}),
					/* @__PURE__ */ jsx(Button, {
						type: "button",
						className: "h-10 min-w-[180px] text-[13px]",
						disabled: !canReveal,
						onClick: winner ? handleRevealAgain : handleReveal,
						children: winner ? "Reveal again" : "Reveal winner"
					})
				]
			}), /* @__PURE__ */ jsx("div", {
				className: cn("rounded-xl border border-border bg-muted/20 p-4 transition-colors", winner && "border-[color-mix(in_srgb,var(--brand-cta)_35%,var(--border))] bg-[color-mix(in_srgb,var(--brand-cta)_8%,transparent)]"),
				children: winner ? /* @__PURE__ */ jsxs("div", {
					className: "flex items-center gap-3",
					children: [/* @__PURE__ */ jsx(InitialsAvatar, {
						name: winner.name,
						size: "md",
						className: "rounded-full"
					}), /* @__PURE__ */ jsxs("div", {
						className: "min-w-0",
						children: [/* @__PURE__ */ jsxs("div", {
							className: "flex items-center gap-1.5 text-[10px] font-semibold uppercase tracking-[0.16em] text-[var(--brand-cta)]",
							children: [/* @__PURE__ */ jsx(Trophy, {
								className: "size-3.5",
								"aria-hidden": true
							}), "Grand prize winner"]
						}), /* @__PURE__ */ jsx("p", {
							className: "truncate text-[15px] font-semibold text-foreground",
							children: winner.name
						})]
					})]
				}) : /* @__PURE__ */ jsxs("div", {
					className: "flex items-start gap-3 text-muted-foreground",
					children: [/* @__PURE__ */ jsx("span", {
						className: "flex size-9 shrink-0 items-center justify-center rounded-lg bg-background/70",
						children: /* @__PURE__ */ jsx(Sparkles, {
							className: "size-4",
							"aria-hidden": true
						})
					}), /* @__PURE__ */ jsx("p", {
						className: "text-[13px] leading-relaxed",
						children: "Press reveal winner when you are ready to announce the grand prize."
					})]
				})
			})]
		})]
	});
}
var DAILY_IMAGE_ASPECT = "aspect-[3/2]";
var GRAND_IMAGE_ASPECT = "aspect-[4/3]";
var PRIZE_LINE = "min-w-0 truncate";
var GRID_FLIP_TRANSITION = "transition-transform duration-700 ease-in-out [transform-style:preserve-3d]";
function usePrizePresenceHandlers(activity) {
	const { setTransientActivity } = useInitPresenceActivity();
	return {
		onMouseEnter: () => setTransientActivity(activity),
		onMouseLeave: () => setTransientActivity(null),
		onFocus: () => setTransientActivity(activity),
		onBlur: () => setTransientActivity(null)
	};
}
var PLATFORM_META = {
	youtube: {
		label: "YouTube",
		icon: "/icons/youtube.svg"
	},
	discord: {
		label: "Discord",
		icon: "/icons/discord-simple.svg"
	},
	reddit: {
		label: "Reddit",
		icon: "/icons/reddit.svg"
	}
};
var DAILY_PLACEMENT = [
	"border-b border-e border-border sm:col-start-1 sm:row-start-2 lg:col-start-1 lg:row-start-1",
	"border-b border-border sm:col-start-2 sm:row-start-2 lg:col-start-2 lg:row-start-1",
	"border-b border-e border-border sm:col-start-1 sm:row-start-3 lg:col-start-1 lg:row-start-2 lg:border-b-0",
	"border-b border-border sm:col-start-2 sm:row-start-3 lg:col-start-2 lg:row-start-2 lg:border-b-0"
];
function PrizeImage({ visual, placeholderIcon: PlaceholderIcon, subdued = false, compact = false, className }) {
	const imageSrc = useInitThemeImageSrc(visual?.imageSrcLight ?? "", visual?.imageSrcDark ?? "");
	return /* @__PURE__ */ jsx(Fragment, { children: Boolean(visual?.imageSrcLight && visual?.imageSrcDark) && visual ? /* @__PURE__ */ jsx("img", {
		src: imageSrc,
		alt: visual.imageAlt,
		className: cn("absolute inset-0 size-full object-cover object-center", compact ? PRIZE_IMAGE_HOVER_ZOOM_COMPACT : PRIZE_IMAGE_HOVER_ZOOM, subdued && (compact ? PRIZE_SWAG_IMAGE_OPACITY_COMPACT : PRIZE_SWAG_IMAGE_OPACITY), className),
		loading: "lazy",
		decoding: "async"
	}) : /* @__PURE__ */ jsx("div", {
		className: cn("absolute inset-0 flex items-center justify-center", className),
		children: /* @__PURE__ */ jsx("span", {
			className: "flex size-10 items-center justify-center rounded-full border border-border/60 bg-background/50",
			children: /* @__PURE__ */ jsx(PlaceholderIcon, {
				className: "size-4 text-muted-foreground/60",
				"aria-hidden": true
			})
		})
	}) });
}
function DailyPrizeCell({ giveaway, className }) {
	const meta = PLATFORM_META[giveaway.platform];
	const presenceHandlers = usePrizePresenceHandlers(buildInitViewingDailyPrizeActivity(giveaway.day, giveaway.prizeDescription));
	const shellClass = cn("group flex h-full min-h-0 min-w-0 flex-col transition-colors hover:bg-accent/15", PRIZE_CARD_BG, className);
	const content = /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
		className: cn("w-full shrink-0 border-b border-border", PRIZE_IMAGE_INSET),
		children: /* @__PURE__ */ jsx("div", {
			className: cn(PRIZE_IMAGE_FRAME, DAILY_IMAGE_ASPECT),
			children: /* @__PURE__ */ jsx(PrizeImage, {
				visual: giveaway.visual,
				placeholderIcon: Gift,
				subdued: true,
				compact: true
			})
		})
	}), /* @__PURE__ */ jsxs("div", {
		className: "flex flex-1 flex-col justify-between gap-3 p-4",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "flex min-w-0 items-start justify-between gap-3",
			children: [/* @__PURE__ */ jsxs("p", {
				className: cn(PRIZE_LINE, "text-[10px] font-semibold uppercase tracking-[0.18em] text-muted-foreground"),
				children: [
					"Day ",
					giveaway.day,
					/* @__PURE__ */ jsx("span", {
						className: "mx-1.5 text-border",
						children: "·"
					}),
					giveaway.dateLabel
				]
			}), giveaway.href ? /* @__PURE__ */ jsx(ArrowUpRight, {
				className: "size-3.5 shrink-0 text-muted-foreground/70 transition-colors group-hover:text-foreground",
				"aria-hidden": true
			}) : null]
		}), /* @__PURE__ */ jsxs("div", {
			className: "space-y-2",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "min-w-0 space-y-1.5",
				children: [/* @__PURE__ */ jsx("h4", {
					className: cn(PRIZE_LINE, "text-[13px] font-medium text-foreground"),
					title: giveaway.sessionTitle,
					children: giveaway.sessionTitle
				}), /* @__PURE__ */ jsxs("div", {
					className: "flex min-w-0 items-center gap-1.5 text-[11px] text-muted-foreground",
					children: [/* @__PURE__ */ jsx("img", {
						src: meta.icon,
						alt: "",
						className: "size-3 shrink-0 opacity-70",
						"aria-hidden": true
					}), /* @__PURE__ */ jsxs("span", {
						className: PRIZE_LINE,
						children: [
							meta.label,
							" · ",
							giveaway.timeLabel
						]
					})]
				})]
			}), /* @__PURE__ */ jsx("p", {
				className: cn(PRIZE_LINE, "text-[12px] text-foreground/90"),
				title: giveaway.prizeDescription,
				children: giveaway.prizeDescription
			})]
		})]
	})] });
	if (giveaway.href) {
		const external = isExternalInitHref(giveaway.href);
		return /* @__PURE__ */ jsx("a", {
			href: giveaway.href,
			...external ? {
				target: "_blank",
				rel: "noopener noreferrer"
			} : {},
			className: shellClass,
			...presenceHandlers,
			children: content
		});
	}
	return /* @__PURE__ */ jsx("article", {
		className: shellClass,
		...presenceHandlers,
		children: content
	});
}
function GrandPrizeCell({ grandPrize, className }) {
	const platformMeta = grandPrize.platform ? PLATFORM_META[grandPrize.platform] : null;
	const presenceHandlers = usePrizePresenceHandlers(buildInitViewingGrandPrizeActivity(grandPrize.title));
	return /* @__PURE__ */ jsxs("article", {
		className: cn("group flex h-full min-h-0 min-w-0 flex-col transition-colors hover:bg-accent/15", PRIZE_CARD_BG, className),
		...presenceHandlers,
		children: [/* @__PURE__ */ jsx("div", {
			className: cn("w-full shrink-0 border-b border-border", PRIZE_IMAGE_INSET),
			children: /* @__PURE__ */ jsx("div", {
				className: cn(PRIZE_IMAGE_FRAME, GRAND_IMAGE_ASPECT),
				children: /* @__PURE__ */ jsx(PrizeImage, {
					visual: grandPrize.visual,
					placeholderIcon: Sparkles
				})
			})
		}), /* @__PURE__ */ jsx("div", {
			className: "flex min-h-0 flex-1 flex-col justify-center px-5 py-5 sm:px-6 sm:py-6",
			children: /* @__PURE__ */ jsxs("div", {
				className: "min-w-0 space-y-3",
				children: [
					/* @__PURE__ */ jsxs("p", {
						className: cn(PRIZE_LINE, "text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground"),
						children: [
							"Grand prize · Day ",
							grandPrize.day,
							/* @__PURE__ */ jsx("span", {
								className: "mx-1.5 text-border",
								children: "·"
							}),
							grandPrize.dateLabel
						]
					}),
					/* @__PURE__ */ jsx("h4", {
						className: cn(PRIZE_LINE, "font-aeonik-pro text-[clamp(18px,2.2vw,24px)] font-normal tracking-tight text-foreground"),
						title: grandPrize.title,
						children: grandPrize.title
					}),
					grandPrize.sessionTitle && platformMeta ? /* @__PURE__ */ jsxs("div", {
						className: "flex min-w-0 items-center gap-1.5 text-[11px] text-muted-foreground",
						children: [/* @__PURE__ */ jsx("img", {
							src: platformMeta.icon,
							alt: "",
							className: "size-3 shrink-0 opacity-70",
							"aria-hidden": true
						}), /* @__PURE__ */ jsxs("span", {
							className: PRIZE_LINE,
							children: [
								platformMeta.label,
								" · ",
								grandPrize.sessionTitle,
								" · ",
								grandPrize.timeLabel
							]
						})]
					}) : null,
					/* @__PURE__ */ jsx("p", {
						className: cn(PRIZE_LINE, "text-[12px] text-muted-foreground"),
						title: grandPrize.eligibility,
						children: grandPrize.eligibility
					})
				]
			})
		})]
	});
}
function PrizesGrid({ prizes }) {
	return /* @__PURE__ */ jsx("div", {
		className: cn("overflow-hidden rounded-xl border border-border", PRIZE_CARD_BG),
		children: /* @__PURE__ */ jsxs("div", {
			className: "grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 lg:grid-rows-2 lg:items-stretch",
			children: [/* @__PURE__ */ jsx(GrandPrizeCell, {
				grandPrize: prizes.grandPrize,
				className: "order-first border-b border-border sm:col-span-2 sm:row-start-1 lg:col-span-2 lg:col-start-3 lg:row-span-2 lg:row-start-1 lg:border-b-0 lg:border-s"
			}), prizes.dailyGiveaways.map((giveaway, index) => /* @__PURE__ */ jsx(DailyPrizeCell, {
				giveaway,
				className: DAILY_PLACEMENT[index] ?? "border-b border-border"
			}, giveaway.scheduleItemId))]
		})
	});
}
function InitPrizesSection({ event }) {
	const prizes = event.prizes;
	const raffle = useInitGiveawayRaffleContext();
	if (!prizes || event.isRecapMode || !raffle) return null;
	const sectionTitle = prizes.sectionTitle ?? "Prizes and giveaways";
	const [isGridFlipped, setIsGridFlipped] = useState(false);
	const isRevealOpen = Boolean(raffle.activeGiveaway || raffle.isGrandPrizeRevealOpen);
	useEffect(() => {
		if (isRevealOpen) {
			let flipFrame = 0;
			const mountFrame = requestAnimationFrame(() => {
				flipFrame = requestAnimationFrame(() => setIsGridFlipped(true));
			});
			return () => {
				cancelAnimationFrame(mountFrame);
				cancelAnimationFrame(flipFrame);
			};
		}
		setIsGridFlipped(false);
	}, [isRevealOpen]);
	return /* @__PURE__ */ jsxs(Fragment, { children: [
		/* @__PURE__ */ jsx("div", {
			className: "border-t border-border",
			"aria-hidden": true
		}),
		/* @__PURE__ */ jsx("div", {
			className: "py-8",
			children: /* @__PURE__ */ jsx("div", {
				className: "mx-auto w-full max-w-7xl px-4 sm:px-6",
				children: /* @__PURE__ */ jsxs("section", {
					id: INIT_PRIZES_SECTION_ID,
					className: "scroll-mt-28 space-y-4",
					"aria-labelledby": "prizes-heading",
					children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h3", {
						id: "prizes-heading",
						className: "text-[15px] font-semibold text-foreground",
						children: sectionTitle
					}), prizes.sectionDescription ? /* @__PURE__ */ jsx("p", {
						className: "mt-2 max-w-2xl text-[13px] leading-relaxed text-muted-foreground",
						children: prizes.sectionDescription
					}) : null] }), /* @__PURE__ */ jsx("div", {
						className: "[perspective:1400px]",
						children: /* @__PURE__ */ jsxs("div", {
							className: cn("relative", GRID_FLIP_TRANSITION, isGridFlipped && "[transform:rotateY(180deg)]"),
							children: [/* @__PURE__ */ jsx("div", {
								className: "[backface-visibility:hidden]",
								children: /* @__PURE__ */ jsx(PrizesGrid, { prizes })
							}), raffle.activeGiveaway ? /* @__PURE__ */ jsx("div", {
								className: cn("absolute inset-0 min-h-full [backface-visibility:hidden] [transform:rotateY(180deg)]"),
								children: /* @__PURE__ */ jsx(InitGiveawayRaffleBack, {
									giveaway: raffle.activeGiveaway,
									participants: raffle.participants,
									loadingParticipants: raffle.loadingParticipants,
									onClose: raffle.close
								}, raffle.activeGiveaway.day)
							}) : raffle.isGrandPrizeRevealOpen && raffle.grandPrize ? /* @__PURE__ */ jsx("div", {
								className: cn("absolute inset-0 min-h-full [backface-visibility:hidden] [transform:rotateY(180deg)]"),
								children: /* @__PURE__ */ jsx(InitGrandPrizeRevealBack, {
									grandPrize: raffle.grandPrize,
									participants: raffle.participants,
									loadingParticipants: raffle.loadingParticipants,
									onClose: raffle.close
								})
							}) : null]
						})
					})]
				})
			})
		}),
		/* @__PURE__ */ jsx("div", {
			className: "border-t border-border",
			"aria-hidden": true
		})
	] });
}
var APPWRITE_PRESENCES_DOCS_URL = "/docs/apis/realtime/presences";
var ACETERNITY_GLOBE_URL = "https://ui.aceternity.com/components/github-globe";
var CREDIT_LINE_CLASS = "text-[12px] leading-relaxed text-muted-foreground";
var CREDIT_LINK_CLASS = "link-neutral";
function InitPageCredits({ showPresenceCredit = false, showGlobeCredit = false }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-2 text-center",
		children: [
			/* @__PURE__ */ jsxs("p", {
				className: "flex items-center justify-center gap-1.5 text-[13px] text-muted-foreground",
				children: [
					"Built with love",
					/* @__PURE__ */ jsx(Heart, {
						className: "size-3.5 shrink-0 fill-current text-muted-foreground",
						"aria-hidden": true
					}),
					"for the Appwrite community"
				]
			}),
			showPresenceCredit ? /* @__PURE__ */ jsxs("p", {
				className: CREDIT_LINE_CLASS,
				children: [
					"Realtime powered by",
					" ",
					/* @__PURE__ */ jsx(DocsRouteLink, {
						href: APPWRITE_PRESENCES_DOCS_URL,
						className: CREDIT_LINK_CLASS,
						children: "Appwrite Presences"
					})
				]
			}) : null,
			/* @__PURE__ */ jsxs("p", {
				className: CREDIT_LINE_CLASS,
				children: [
					showGlobeCredit ? /* @__PURE__ */ jsxs(Fragment, { children: [
						"Community globe by",
						" ",
						/* @__PURE__ */ jsx("a", {
							href: ACETERNITY_GLOBE_URL,
							target: "_blank",
							rel: "noopener noreferrer",
							className: CREDIT_LINK_CLASS,
							children: "Aceternity UI"
						}),
						/* @__PURE__ */ jsx("span", {
							className: "text-muted-foreground",
							children: " · "
						})
					] }) : null,
					"Jool animation by",
					" ",
					/* @__PURE__ */ jsx("a", {
						href: "https://animejs.com/",
						target: "_blank",
						rel: "noopener noreferrer",
						className: CREDIT_LINK_CLASS,
						children: "Anime.js"
					})
				]
			})
		]
	});
}
const COUNTRY_COORDINATES = {
	AD: [1.601554, 42.546245],
	AE: [53.847818, 23.424076],
	AF: [67.709953, 33.93911],
	AG: [-61.796428, 17.060816],
	AL: [20.168331, 41.153332],
	AM: [45.038189, 40.069099],
	AO: [17.873887, -11.202692],
	AR: [-63.616672, -38.416097],
	AT: [14.550072, 47.516231],
	AU: [133.775136, -25.274398],
	AZ: [47.576927, 40.143105],
	BA: [17.679076, 43.915886],
	BD: [90.356331, 23.684994],
	BE: [4.469936, 50.503887],
	BF: [-1.561593, 12.238333],
	BG: [25.48583, 42.733883],
	BH: [50.637772, 25.930414],
	BI: [29.918886, -3.373056],
	BJ: [2.315834, 9.30769],
	BN: [114.727669, 4.535277],
	BO: [-63.588653, -16.290154],
	BR: [-51.92528, -14.235004],
	BS: [-77.39628, 25.03428],
	BT: [90.433601, 27.514162],
	BW: [24.684866, -22.328474],
	BY: [27.953389, 53.709807],
	BZ: [-88.49765, 17.189877],
	CA: [-106.346771, 56.130366],
	CD: [21.758664, -4.038333],
	CF: [20.939444, 6.611111],
	CG: [15.827659, -.228021],
	CH: [8.227512, 46.818188],
	CI: [-5.54708, 7.539989],
	CL: [-71.542969, -35.675147],
	CM: [12.354722, 7.369722],
	CN: [104.195397, 35.86166],
	CO: [-74.297333, 4.570868],
	CR: [-83.753428, 9.748917],
	CU: [-77.781167, 21.521757],
	CV: [-24.013197, 16.002082],
	CY: [33.429859, 35.126413],
	CZ: [15.472962, 49.817492],
	DE: [10.451526, 51.165691],
	DJ: [42.590275, 11.825138],
	DK: [9.501785, 56.26392],
	DO: [-70.162651, 18.735693],
	DZ: [1.659626, 28.033886],
	EC: [-78.183406, -1.831239],
	EE: [25.013607, 58.595272],
	EG: [30.802498, 26.820553],
	ER: [39.782334, 15.179384],
	ES: [-3.74922, 40.463667],
	ET: [40.489673, 9.145],
	FI: [25.748151, 61.92411],
	FJ: [178.065032, -17.713371],
	FR: [2.213749, 46.227638],
	GA: [11.609444, -.803689],
	GB: [-3.435973, 55.378051],
	GE: [43.356892, 42.315407],
	GH: [-1.023194, 7.946527],
	GM: [-15.310139, 13.443182],
	GN: [-9.696645, 9.945587],
	GQ: [10.267895, 1.650801],
	GR: [21.824312, 39.074208],
	GT: [-90.230759, 15.783471],
	GW: [-15.180413, 11.803749],
	GY: [-58.93018, 4.860416],
	HK: [114.109497, 22.396428],
	HN: [-86.241905, 15.199999],
	HR: [15.2, 45.1],
	HT: [-72.285215, 18.971187],
	HU: [19.503304, 47.162494],
	ID: [113.921327, -.789275],
	IE: [-8.24389, 53.41291],
	IL: [34.851612, 31.046051],
	IN: [78.96288, 20.593684],
	IQ: [43.679291, 33.223191],
	IR: [53.688046, 32.427908],
	IS: [-19.020835, 64.963051],
	IT: [12.56738, 41.87194],
	JM: [-77.297508, 18.109581],
	JO: [36.238414, 30.585164],
	JP: [138.252924, 36.204824],
	KE: [37.906193, -.023559],
	KG: [74.766098, 41.20438],
	KH: [104.990963, 12.565679],
	KR: [127.766922, 35.907757],
	KW: [47.481766, 29.31166],
	KZ: [66.923684, 48.019573],
	LA: [102.495496, 19.85627],
	LB: [35.862285, 33.854721],
	LK: [80.771797, 7.873054],
	LR: [-9.429499, 6.428055],
	LS: [28.233608, -29.609988],
	LT: [23.881275, 55.169438],
	LU: [6.129583, 49.815273],
	LV: [24.603189, 56.879635],
	LY: [17.228331, 26.3351],
	MA: [-7.09262, 31.791702],
	MD: [28.369885, 47.411631],
	ME: [19.37439, 42.708678],
	MG: [46.869107, -18.766947],
	MK: [21.745275, 41.608635],
	ML: [-3.996166, 17.570692],
	MM: [95.956223, 21.913965],
	MN: [103.846656, 46.862496],
	MO: [113.543873, 22.198745],
	MR: [-10.940835, 21.00789],
	MT: [14.375416, 35.937496],
	MU: [57.552152, -20.348404],
	MV: [73.22068, 3.202778],
	MW: [34.301525, -13.254308],
	MX: [-102.552784, 23.634501],
	MY: [101.975766, 4.210484],
	MZ: [35.529562, -18.665695],
	NA: [18.49041, -22.95764],
	NE: [8.081666, 17.607789],
	NG: [8.675277, 9.081999],
	NI: [-85.207229, 12.865416],
	NL: [5.291266, 52.132633],
	NO: [8.468946, 60.472024],
	NP: [84.124008, 28.394857],
	NZ: [174.885971, -40.900557],
	OM: [55.923255, 21.512583],
	PA: [-80.782127, 8.537981],
	PE: [-75.015152, -9.189967],
	PG: [143.95555, -6.314993],
	PH: [121.774017, 12.879721],
	PK: [69.345116, 30.375321],
	PL: [19.145136, 51.919438],
	PR: [-66.590149, 18.220833],
	PS: [35.233154, 31.952162],
	PT: [-8.224454, 39.399872],
	PY: [-58.443832, -23.442503],
	QA: [51.183884, 25.354826],
	RO: [24.96676, 45.943161],
	RS: [21.005859, 44.016521],
	RU: [105.318756, 61.52401],
	RW: [29.873888, -1.940278],
	SA: [45.079162, 23.885942],
	SD: [30.217636, 12.862807],
	SE: [18.643501, 60.128161],
	SG: [103.819836, 1.352083],
	SI: [14.995463, 46.151241],
	SK: [19.699024, 48.669026],
	SL: [-11.779889, 8.460555],
	SN: [-14.452362, 14.497401],
	SO: [46.199616, 5.152149],
	SR: [-56.027783, 3.919305],
	SV: [-88.89653, 13.794185],
	SY: [38.996815, 34.802075],
	SZ: [31.465866, -26.522503],
	TD: [18.732207, 15.454166],
	TG: [.824782, 8.619543],
	TH: [100.992541, 15.870032],
	TJ: [71.276093, 38.861034],
	TM: [59.556278, 38.969719],
	TN: [9.537499, 33.886917],
	TR: [35.243322, 38.963745],
	TT: [-61.222503, 10.691803],
	TW: [120.960515, 23.69781],
	TZ: [34.888822, -6.369028],
	UA: [31.16558, 48.379433],
	UG: [32.290275, 1.373333],
	US: [-95.712891, 37.09024],
	UY: [-55.765835, -32.522779],
	UZ: [64.585262, 41.377491],
	VE: [-66.58973, 6.42375],
	VN: [108.277199, 14.058324],
	YE: [48.516388, 15.552727],
	ZA: [22.937506, -30.559482],
	ZM: [27.849332, -13.133897],
	ZW: [29.154857, -19.015438]
};
function getCountryCoordinates(countryCode) {
	if (!countryCode) return null;
	return COUNTRY_COORDINATES[countryCode.trim().toUpperCase()] ?? null;
}
function buildArcColors(brandColor) {
	return [
		brandColor,
		withAlpha(brandColor, .75),
		withAlpha(brandColor, .55)
	];
}
function toLatLng(code) {
	const coords = getCountryCoordinates(code);
	if (!coords) return null;
	const [lng, lat] = coords;
	return {
		lat,
		lng
	};
}
function pushArc(arcs, order, start, end, arcAlt, colorIndex, arcColors) {
	arcs.push({
		order,
		startLat: start.lat,
		startLng: start.lng,
		endLat: end.lat,
		endLng: end.lng,
		arcAlt,
		color: arcColors[colorIndex % arcColors.length]
	});
}
function buildMarkerVisualWeights(count, maxCount) {
	const MIN_POINT = 1.25;
	const MAX_POINT = 4.5;
	const MIN_RING = 2;
	const MAX_RING = 7;
	const weight = maxCount <= 1 ? 1 : Math.sqrt(count / maxCount);
	return {
		pointRadius: MIN_POINT + weight * (MAX_POINT - MIN_POINT),
		ringMaxRadius: MIN_RING + weight * (MAX_RING - MIN_RING)
	};
}
function buildInitGlobePresenceData(countries, brandColor = "rgb(253, 54, 110)") {
	const arcColors = buildArcColors(brandColor);
	const positions = countries.map((country) => {
		const point = toLatLng(country.code);
		if (!point) return null;
		return {
			...point,
			count: country.count,
			code: country.code
		};
	}).filter((entry) => entry !== null);
	if (positions.length === 0) return {
		arcs: [],
		markers: []
	};
	const maxCount = Math.max(...positions.map((position) => position.count));
	const markers = positions.map((position, index) => {
		const { pointRadius, ringMaxRadius } = buildMarkerVisualWeights(position.count, maxCount);
		return {
			lat: position.lat,
			lng: position.lng,
			color: arcColors[index % arcColors.length],
			count: position.count,
			pointRadius,
			ringMaxRadius
		};
	});
	if (positions.length < 2) return {
		arcs: [],
		markers
	};
	const arcs = [];
	let order = 1;
	for (let index = 0; index < positions.length; index += 1) {
		const current = positions[index];
		const arcCount = Math.min(current.count, 2);
		for (let offset = 1; offset <= arcCount; offset += 1) {
			const target = positions[(index + offset) % positions.length];
			if (current.code === target.code) continue;
			pushArc(arcs, order, current, target, .12 + (offset - 1) * .05, order, arcColors);
			order += 1;
		}
	}
	return {
		arcs: arcs.slice(0, 40),
		markers
	};
}
function serializeCommunityCountries(countries) {
	return countries.map((country) => `${country.code}:${country.count}`).join("|");
}
function InitGlobePresenceStats({ countries, developerCount, isLive, className }) {
	const countryCount = countries.length;
	const developers = developerCount > 0 ? developerCount : countries.reduce((total, country) => total + country.count, 0);
	if (countryCount === 0 && developers === 0) return null;
	return /* @__PURE__ */ jsxs("div", {
		className: cn("pointer-events-none flex items-center gap-2 rounded-lg border border-border/80 bg-background/85 px-2.5 py-1.5 shadow-sm backdrop-blur-sm", className),
		children: [
			isLive ? /* @__PURE__ */ jsxs("span", {
				className: "relative flex size-2 shrink-0",
				"aria-hidden": true,
				children: [/* @__PURE__ */ jsx("span", { className: "absolute inline-flex size-full animate-ping rounded-full bg-green-500/60 opacity-75" }), /* @__PURE__ */ jsx("span", { className: "relative inline-flex size-2 rounded-full bg-green-500" })]
			}) : null,
			/* @__PURE__ */ jsxs("p", {
				className: "text-[12px] tabular-nums text-foreground",
				children: [
					/* @__PURE__ */ jsx("span", {
						className: "font-semibold",
						children: countryCount.toLocaleString()
					}),
					/* @__PURE__ */ jsxs("span", {
						className: "text-muted-foreground",
						children: [" ", countryCount === 1 ? "country" : "countries"]
					}),
					developers > 0 ? /* @__PURE__ */ jsxs(Fragment, { children: [
						/* @__PURE__ */ jsx("span", {
							className: "text-muted-foreground",
							children: " · "
						}),
						/* @__PURE__ */ jsx("span", {
							className: "font-semibold",
							children: developers.toLocaleString()
						}),
						/* @__PURE__ */ jsx("span", {
							className: "text-muted-foreground",
							children: " online"
						})
					] }) : null
				]
			}),
			isLive ? /* @__PURE__ */ jsx("span", {
				className: "sr-only",
				children: "Live presence"
			}) : null
		]
	});
}
function InitCommunityGlobe({ countries, developerCount, isLive, className }) {
	const { config: globeConfig, themeKey } = useGlobeThemeConfig();
	const countriesKey = useMemo(() => serializeCommunityCountries(countries), [countries]);
	const brandRgb = useMemo(() => globeConfig ? getInitGlobeBrandRgb() : "rgb(253, 54, 110)", [globeConfig, themeKey]);
	const globePresence = useMemo(() => buildInitGlobePresenceData(countries, brandRgb), [brandRgb, countriesKey]);
	return /* @__PURE__ */ jsxs("div", {
		className: cn("relative mx-auto w-full max-w-[min(100%,44rem)] overflow-hidden aspect-[100/55] sm:max-w-[min(100%,52rem)] lg:max-w-[min(100%,60rem)] xl:max-w-[min(100%,68rem)]", className),
		children: [/* @__PURE__ */ jsx("div", {
			className: "absolute inset-x-0 top-0 aspect-square w-full",
			children: globeConfig ? /* @__PURE__ */ jsx(World, {
				globeConfig,
				data: globePresence.arcs,
				markers: globePresence.markers
			}, themeKey) : /* @__PURE__ */ jsx("div", {
				className: "flex h-full w-full items-center justify-center",
				children: /* @__PURE__ */ jsx("div", { className: "size-10 animate-spin rounded-full border-2 border-border border-t-[var(--brand-cta)]" })
			})
		}), /* @__PURE__ */ jsx(InitGlobePresenceStats, {
			countries,
			developerCount,
			isLive,
			className: "absolute bottom-4 start-3 z-30 sm:bottom-5 sm:start-4"
		})]
	});
}
function InitGlobalCommunitySection({ countries, developerCount, isLive, isAuthenticated }) {
	const { setTransientActivity } = useInitPresenceActivity();
	const globeActivity = buildInitExploringGlobeActivity();
	return /* @__PURE__ */ jsxs("div", {
		className: "relative",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "relative overflow-hidden",
			children: [/* @__PURE__ */ jsxs("section", {
				id: INIT_GLOBE_SECTION_ID,
				className: "relative px-4 pt-6 sm:px-6 sm:pt-8",
				onMouseEnter: useCallback(() => {
					setTransientActivity(globeActivity);
				}, [globeActivity, setTransientActivity]),
				onMouseLeave: useCallback(() => {
					setTransientActivity(null);
				}, [setTransientActivity]),
				children: [/* @__PURE__ */ jsxs("div", {
					className: "relative z-10 mx-auto flex w-full max-w-2xl flex-col items-center space-y-3 text-center",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "space-y-2",
						children: [/* @__PURE__ */ jsx("h3", {
							className: "text-[15px] font-semibold text-foreground",
							children: "Developers joining from every corner of the world"
						}), /* @__PURE__ */ jsx("p", {
							className: "mx-auto max-w-xl text-[13px] leading-relaxed text-muted-foreground",
							children: "Init brings developers together to connect through a shared passion for code and open source. Watch the globe light up as builders tune in live from around the globe."
						})]
					}), !isAuthenticated ? /* @__PURE__ */ jsx("p", {
						className: "text-[12px] text-muted-foreground",
						children: "Sign in to share your location and appear on the globe."
					}) : null]
				}), /* @__PURE__ */ jsx(InitCommunityGlobe, {
					countries,
					developerCount,
					isLive,
					className: "relative z-20 -mt-6 w-full sm:-mt-10 lg:-mt-14"
				})]
			}), /* @__PURE__ */ jsx("div", {
				className: "pointer-events-none absolute inset-x-0 bottom-0 z-10",
				"aria-hidden": true,
				children: /* @__PURE__ */ jsx("div", { className: "h-32 w-full bg-gradient-to-b from-transparent to-background sm:h-40 lg:h-48" })
			})]
		}), /* @__PURE__ */ jsx("div", {
			className: "w-full border-t border-border bg-background",
			"aria-hidden": true
		})]
	});
}
function buildBaselineAnchors(options) {
	const anchors = [{
		zone: { kind: "ticket" },
		id: INIT_TICKET_SECTION_ID
	}];
	for (const day of [...options.dayNumbers].sort((a, b) => a - b)) anchors.push({
		zone: {
			kind: "day",
			day
		},
		id: getInitDayCardId(day)
	});
	if (options.hasPrizes) anchors.push({
		zone: { kind: "prizes" },
		id: INIT_PRIZES_SECTION_ID
	});
	if (options.hasGlobe) anchors.push({
		zone: { kind: "globe" },
		id: INIT_GLOBE_SECTION_ID
	});
	anchors.push({
		zone: { kind: "get-involved" },
		id: INIT_GET_INVOLVED_SECTION_ID
	});
	return anchors;
}
function resolveInitPresenceBaselineZone(scrollRoot, options) {
	const anchorY = scrollRoot.getBoundingClientRect().top + INIT_DAY_SCROLL_SPY_OFFSET_PX;
	let zone = { kind: "default" };
	for (const entry of buildBaselineAnchors(options)) {
		const element = document.getElementById(entry.id);
		if (!element) continue;
		if (element.getBoundingClientRect().top <= anchorY) zone = entry.zone;
	}
	return zone;
}
function resolveInitPresenceBaselineActivity(event, zone, seed) {
	switch (zone.kind) {
		case "globe": return buildInitExploringGlobeActivity();
		case "prizes": return buildInitExploringActivity(event.prizes?.sectionTitle ?? "Prizes and giveaways");
		case "ticket": return buildInitViewingTicketActivity();
		case "get-involved": return buildInitExploringActivity(event.isRecapMode ? event.recap?.getInvolvedSectionTitle ?? "Keep exploring" : "Ways to get involved");
		case "day": {
			const dayView = resolveInitScrollSpyBaselineDay(event.days, zone.day, event.currentDay);
			if (!dayView) return event.currentDay <= 0 ? buildInitPreEventBaselineActivity(seed) : INIT_PRESENCE_ACTIVITY_ON_INIT;
			return buildInitDayReadingActivity(dayView, event.currentDay, `${seed}:day-${dayView.day}`);
		}
		case "default":
		default: return event.currentDay <= 0 ? buildInitPreEventBaselineActivity(seed) : INIT_PRESENCE_ACTIVITY_ON_INIT;
	}
}
function useInitScrollSpyBaselineZone(event) {
	const dayNumbers = useMemo(() => event.days.map((day) => day.day), [event.days]);
	const hasPrizes = Boolean(event.prizes && !event.isRecapMode);
	const hasGlobe = Boolean(event.presenceEnabled && !event.isRecapMode);
	const [zone, setZone] = useState({ kind: "default" });
	useEffect(() => {
		setZone({ kind: "default" });
	}, [
		dayNumbers,
		hasGlobe,
		hasPrizes
	]);
	useEffect(() => {
		const main = document.getElementById("main-content");
		if (!main) return;
		let frame = 0;
		const update = () => {
			cancelAnimationFrame(frame);
			frame = requestAnimationFrame(() => {
				setZone(resolveInitPresenceBaselineZone(main, {
					dayNumbers,
					hasPrizes,
					hasGlobe
				}));
			});
		};
		update();
		main.addEventListener("scroll", update, { passive: true });
		window.addEventListener("resize", update);
		const resizeObserver = new ResizeObserver(update);
		resizeObserver.observe(main);
		return () => {
			cancelAnimationFrame(frame);
			main.removeEventListener("scroll", update);
			window.removeEventListener("resize", update);
			resizeObserver.disconnect();
		};
	}, [
		dayNumbers,
		hasGlobe,
		hasPrizes
	]);
	return zone;
}
function useInitPresenceBaseline(event, accountUserId) {
	const zone = useInitScrollSpyBaselineZone(event);
	const { setBaselineActivity } = useInitPresenceActivity();
	useEffect(() => {
		setBaselineActivity(resolveInitPresenceBaselineActivity(event, zone, accountUserId ?? "guest"));
	}, [
		accountUserId,
		event,
		setBaselineActivity,
		zone
	]);
}
function InitPresenceBaselineSync({ event }) {
	const { data: account } = useQuery(consoleAccountQueryOptions());
	useInitPresenceBaseline(event, account?.$id);
	return null;
}
var COMMUNITY_REFRESH_MS = 3e4;
function useInitCommunityGlobeData(event, options) {
	const presence = useInitPresence();
	const enabled = Boolean(event?.presenceEnabled && !event.isRecapMode && options?.enabled !== false);
	const { data: polledCountries } = useQuery({
		queryKey: [
			"init",
			"community-countries",
			event?.id
		],
		queryFn: async () => {
			if (!event) return [];
			return aggregateInitCommunityCountries(await listInitPresences(event.id, "online", 100), buildInitPresenceActivityAllowlist(event));
		},
		enabled: enabled && (!presence.isReady || presence.communityCountries.length === 0),
		refetchInterval: COMMUNITY_REFRESH_MS,
		staleTime: 15e3
	});
	const countries = presence.communityCountries.length > 0 ? presence.communityCountries : polledCountries ?? [];
	return {
		countries,
		developerCount: presence.communityDeveloperCount > 0 ? presence.communityDeveloperCount : countries.reduce((total, country) => total + country.count, 0),
		isLive: presence.isReady && presence.communityCountries.length > 0
	};
}
var CONFETTI_PARTICLE_COUNT$1 = 72;
var CONFETTI_EASE$1 = [
	.22,
	1,
	.36,
	1
];
var CONFETTI_COLORS = [
	"color-mix(in srgb, var(--foreground) 70%, transparent)",
	"color-mix(in srgb, var(--foreground) 45%, transparent)",
	"color-mix(in srgb, var(--muted-foreground) 80%, transparent)",
	"color-mix(in srgb, var(--foreground) 28%, transparent)",
	"color-mix(in srgb, var(--muted-foreground) 55%, transparent)",
	"color-mix(in srgb, var(--border) 85%, var(--foreground) 15%)"
];
function createConfettiParticles$1(burstKey) {
	return Array.from({ length: CONFETTI_PARTICLE_COUNT$1 }, (_, index) => {
		const seed = burstKey + index * 991;
		const rand = (offset) => {
			const value = Math.sin(seed + offset * 12.9898) * 43758.5453;
			return value - Math.floor(value);
		};
		const angle = rand(1) * Math.PI * 2;
		const distance = 120 + rand(2) * 520;
		const drift = (rand(3) - .5) * 180;
		return {
			id: index,
			x: Math.cos(angle) * distance + drift,
			y: Math.sin(angle) * distance * .75 + 120 + rand(4) * 280,
			width: 7 + rand(5) * 10,
			height: 4 + rand(6) * 8,
			delay: rand(7) * .18,
			duration: 1.2 + rand(8) * 1.1,
			spin: (rand(9) - .5) * 720,
			color: CONFETTI_COLORS[index % CONFETTI_COLORS.length],
			round: rand(10) > .55
		};
	});
}
function GiveawayRaffleConfettiBurst({ burstKey, onComplete }) {
	const reduceMotion = useReducedMotion();
	const particles = useMemo(() => createConfettiParticles$1(burstKey), [burstKey]);
	useEffect(() => {
		if (reduceMotion) onComplete();
	}, [onComplete, reduceMotion]);
	if (reduceMotion) return null;
	return /* @__PURE__ */ jsx("div", {
		className: "pointer-events-none fixed inset-0 z-[125] overflow-hidden",
		"aria-hidden": true,
		children: /* @__PURE__ */ jsx("div", {
			className: "absolute start-1/2 top-[38%] h-0 w-0 -translate-x-1/2 -translate-y-1/2",
			children: particles.map((particle, index) => /* @__PURE__ */ jsx(motion.span, {
				className: "absolute block",
				style: {
					width: particle.width,
					height: particle.height,
					backgroundColor: particle.color,
					borderRadius: particle.round ? "9999px" : "2px"
				},
				initial: {
					x: 0,
					y: 0,
					opacity: 0,
					scale: 0,
					rotate: 0
				},
				animate: {
					x: particle.x,
					y: particle.y,
					opacity: [
						0,
						1,
						1,
						.85,
						0
					],
					scale: [
						0,
						1.15,
						1,
						.9,
						.45
					],
					rotate: particle.spin
				},
				transition: {
					duration: particle.duration,
					delay: particle.delay,
					ease: CONFETTI_EASE$1
				},
				onAnimationComplete: index === particles.length - 1 ? onComplete : void 0
			}, `${burstKey}-${particle.id}`))
		})
	});
}
function InitGiveawayRaffleConfetti({ pulse }) {
	const reduceMotion = useReducedMotion();
	const [burstKey, setBurstKey] = useState(null);
	useEffect(() => {
		if (reduceMotion || pulse <= 0) return;
		setBurstKey(pulse);
	}, [pulse, reduceMotion]);
	if (!burstKey) return null;
	return /* @__PURE__ */ jsx(GiveawayRaffleConfettiBurst, {
		burstKey,
		onComplete: () => setBurstKey(null)
	});
}
var CONFETTI_PARTICLE_COUNT = 52;
var CONFETTI_EASE = [
	.22,
	1,
	.36,
	1
];
var CONFETTI_EVAL_INTERVAL_MS = 1e3;
function createConfettiParticles(burstKey) {
	return Array.from({ length: CONFETTI_PARTICLE_COUNT }, (_, index) => {
		const seed = burstKey + index * 997;
		const rand = (offset) => {
			const value = Math.sin(seed + offset * 12.9898) * 43758.5453;
			return value - Math.floor(value);
		};
		const angle = Math.PI / 12 + rand(1) * (Math.PI * .48);
		const distance = 180 + rand(2) * 420;
		const wobble = (rand(3) - .5) * 140;
		return {
			id: index,
			x: Math.cos(angle) * distance + wobble,
			y: -Math.sin(angle) * distance - rand(4) * 90,
			size: 12 + rand(5) * 16,
			delay: rand(6) * .14,
			duration: 1.05 + rand(7) * .85,
			spin: (rand(8) - .5) * 540
		};
	});
}
function ReactionConfettiBurst({ reactionId, burstKey, onComplete }) {
	const reduceMotion = useReducedMotion();
	const reaction = getInitReactionById(reactionId);
	const particles = useMemo(() => createConfettiParticles(burstKey), [burstKey]);
	useEffect(() => {
		if (reduceMotion || !reaction) onComplete();
	}, [
		onComplete,
		reaction,
		reduceMotion
	]);
	if (!reaction || reduceMotion) return null;
	const Icon$1 = reaction.icon;
	return /* @__PURE__ */ jsx("div", {
		className: "pointer-events-none fixed bottom-0 start-0 z-[120] h-0 w-0 overflow-visible",
		"aria-hidden": true,
		children: particles.map((particle, index) => /* @__PURE__ */ jsx(motion.span, {
			className: "absolute text-muted-foreground",
			initial: {
				x: 0,
				y: 0,
				opacity: 0,
				scale: 0,
				rotate: 0
			},
			animate: {
				x: particle.x,
				y: particle.y,
				opacity: [
					0,
					1,
					1,
					.85,
					0
				],
				scale: [
					0,
					1.25,
					1,
					.85,
					.5
				],
				rotate: particle.spin
			},
			transition: {
				duration: particle.duration,
				delay: particle.delay,
				ease: CONFETTI_EASE
			},
			onAnimationComplete: index === particles.length - 1 ? onComplete : void 0,
			children: /* @__PURE__ */ jsx(Icon$1, {
				style: {
					width: particle.size,
					height: particle.size
				},
				"aria-hidden": true
			})
		}, `${burstKey}-${particle.id}`))
	});
}
function evaluateConfettiBurst(options) {
	const now = options.now ?? Date.now();
	const counts = getInitReactionCounts(options.onlineUsers);
	for (const reactionId of options.lastBurstAt.keys()) if ((counts.get(reactionId) ?? 0) < options.confettiThreshold) options.lastBurstAt.delete(reactionId);
	for (const [reactionId, count] of counts) {
		if (count < options.confettiThreshold) continue;
		const lastBurst = options.lastBurstAt.get(reactionId) ?? 0;
		if (now - lastBurst < 6e3) continue;
		const previous = options.previousCounts.get(reactionId) ?? 0;
		if (previous < options.confettiThreshold || count > previous || lastBurst > 0) {
			options.lastBurstAt.set(reactionId, now);
			options.previousCounts.clear();
			for (const [id, value] of counts) options.previousCounts.set(id, value);
			return {
				reactionId,
				key: now
			};
		}
	}
	options.previousCounts.clear();
	for (const [id, value] of counts) options.previousCounts.set(id, value);
	return null;
}
function InitReactionConfetti({ onlineUsers }) {
	const reduceMotion = useReducedMotion();
	const { previewInitReactionConfetti } = useDebugOverrides();
	const [burst, setBurst] = useState(null);
	const previousCountsRef = useRef(/* @__PURE__ */ new Map());
	const lastBurstAtRef = useRef(/* @__PURE__ */ new Map());
	const onlineUsersRef = useRef(onlineUsers);
	onlineUsersRef.current = onlineUsers;
	const confettiThreshold = previewInitReactionConfetti ? 1 : 5;
	useEffect(() => {
		previousCountsRef.current = /* @__PURE__ */ new Map();
		lastBurstAtRef.current = /* @__PURE__ */ new Map();
	}, [previewInitReactionConfetti]);
	useEffect(() => {
		if (reduceMotion) return void 0;
		const runEvaluation = () => {
			const nextBurst = evaluateConfettiBurst({
				onlineUsers: onlineUsersRef.current,
				confettiThreshold,
				previousCounts: previousCountsRef.current,
				lastBurstAt: lastBurstAtRef.current
			});
			if (nextBurst) setBurst(nextBurst);
		};
		runEvaluation();
		const interval = window.setInterval(runEvaluation, CONFETTI_EVAL_INTERVAL_MS);
		return () => window.clearInterval(interval);
	}, [
		confettiThreshold,
		onlineUsers,
		reduceMotion
	]);
	return /* @__PURE__ */ jsx(AnimatePresence, { children: burst ? /* @__PURE__ */ jsx(ReactionConfettiBurst, {
		reactionId: burst.reactionId,
		burstKey: burst.key,
		onComplete: () => setBurst(null)
	}, burst.key) : null });
}
function LiveBannerBar({ liveBanner }) {
	return /* @__PURE__ */ jsx("div", {
		className: "shrink-0 border-b border-border bg-background",
		children: /* @__PURE__ */ jsxs("div", {
			className: "mx-auto flex max-w-7xl items-center justify-between gap-3 px-4 py-2.5 sm:px-6",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex min-w-0 items-center gap-2",
				children: [/* @__PURE__ */ jsx(Badge, {
					variant: "error",
					className: "text-[10px] shrink-0",
					children: "Live now"
				}), /* @__PURE__ */ jsx("span", {
					className: "truncate text-[13px] font-medium text-foreground",
					children: liveBanner.title
				})]
			}), liveBanner.href ? /* @__PURE__ */ jsx(Button, {
				variant: "outline",
				size: "sm",
				className: "h-7 shrink-0 text-[12px]",
				asChild: true,
				children: /* @__PURE__ */ jsxs("a", {
					href: liveBanner.href,
					...isExternalInitHref(liveBanner.href) ? {
						target: "_blank",
						rel: "noopener noreferrer"
					} : {},
					children: ["Watch", /* @__PURE__ */ jsx(ChevronRight, { className: "size-3.5" })]
				})
			}) : null]
		})
	});
}
function InitRecapBanner({ event }) {
	if (!event.isRecapMode) return null;
	const message = event.recap?.bannerMessage ?? `${event.dateRangeLabel} has ended. Browse the full recap below.`;
	return /* @__PURE__ */ jsx("div", {
		className: "shrink-0 border-b border-border bg-background",
		children: /* @__PURE__ */ jsxs("div", {
			className: "mx-auto flex max-w-7xl items-center gap-2 px-4 py-2.5 sm:px-6",
			children: [/* @__PURE__ */ jsx(Badge, {
				variant: "info",
				className: "text-[10px] shrink-0",
				children: "Recap"
			}), /* @__PURE__ */ jsx("span", {
				className: "text-[13px] text-muted-foreground",
				children: message
			})]
		})
	});
}
var CARD_SHELL$3 = "rounded-xl border border-border bg-card/50 overflow-hidden";
function InitRecapIntro({ event }) {
	if (!event.isRecapMode) return null;
	const title = event.recap?.introTitle ?? "Everything we shipped";
	const description = event.recap?.introDescription ?? "Explore the full launch timeline, blog posts, and session replays from Init week.";
	return /* @__PURE__ */ jsx("section", {
		className: CARD_SHELL$3,
		children: /* @__PURE__ */ jsxs("div", {
			className: "px-6 py-4",
			children: [/* @__PURE__ */ jsx("h2", {
				className: "text-[15px] font-semibold text-foreground",
				children: title
			}), /* @__PURE__ */ jsx("p", {
				className: "mt-2 text-[13px] leading-relaxed text-muted-foreground",
				children: description
			})]
		})
	});
}
function Appwrite2DayVisual() {
	return /* @__PURE__ */ jsx("div", {
		className: "absolute inset-0 flex flex-col overflow-hidden",
		children: /* @__PURE__ */ jsxs("div", {
			className: cn("relative mx-auto flex h-full min-h-0 w-full max-w-[22rem] flex-col items-center justify-center overflow-hidden", productBentoContainer.shell),
			children: [
				/* @__PURE__ */ jsx("div", {
					className: "pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_50%_42%,color-mix(in_srgb,var(--brand-cta)_16%,transparent),transparent_65%)] opacity-60 transition-opacity duration-500 group-hover:opacity-100 motion-reduce:group-hover:opacity-60",
					"aria-hidden": true
				}),
				/* @__PURE__ */ jsx("div", {
					className: "pointer-events-none absolute inset-0 opacity-25 [background-image:radial-gradient(circle,color-mix(in_srgb,var(--border)_80%,transparent)_1px,transparent_1px)] [background-size:14px_14px] transition-opacity duration-500 group-hover:opacity-40 motion-reduce:group-hover:opacity-25",
					"aria-hidden": true
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "relative z-10 flex flex-col items-center px-6 py-4 text-center",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "flex items-center justify-center gap-3.5 sm:gap-4",
						children: [/* @__PURE__ */ jsx("span", {
							className: cn("flex size-14 shrink-0 items-center justify-center rounded-xl border border-border bg-background/80 shadow-sm transition-[border-color,background-color,transform,box-shadow] duration-500 group-hover:scale-105 group-hover:border-[color-mix(in_srgb,var(--brand-cta)_35%,var(--border))] group-hover:bg-background group-hover:shadow-[0_0_28px_color-mix(in_srgb,var(--brand-cta)_20%,transparent)] motion-reduce:group-hover:scale-100"),
							children: /* @__PURE__ */ jsx("img", {
								src: "/icons/appwrite.svg",
								alt: "",
								className: cn("size-7 transition-[filter,opacity] duration-500", "opacity-70 grayscale group-hover:opacity-100 group-hover:grayscale-0 motion-reduce:group-hover:opacity-70 motion-reduce:group-hover:grayscale"),
								"aria-hidden": true
							})
						}), /* @__PURE__ */ jsxs("div", {
							className: "flex items-baseline",
							children: [/* @__PURE__ */ jsx("span", {
								className: cn("font-aeonik-pro text-[clamp(3rem,10vw,4rem)] font-normal leading-none tracking-tight transition-colors duration-500", productBentoIdle.text),
								children: "2.0"
							}), /* @__PURE__ */ jsx("span", {
								className: cn("font-aeonik-pro text-[clamp(3rem,10vw,4rem)] font-normal leading-none text-muted-foreground/40 transition-colors duration-500 group-hover:text-[var(--brand-cta)] motion-reduce:group-hover:text-muted-foreground/40"),
								"aria-hidden": true,
								children: "_"
							})]
						})]
					}), /* @__PURE__ */ jsx("p", {
						className: "mt-4 max-w-[12rem] text-[10px] leading-snug text-muted-foreground sm:text-[11px]",
						children: "The next chapter of Appwrite"
					})]
				})
			]
		})
	});
}
function PostgresDayVisual() {
	return /* @__PURE__ */ jsx("div", {
		className: "absolute inset-0 flex flex-col overflow-hidden",
		children: /* @__PURE__ */ jsxs("div", {
			className: cn("mx-auto flex h-full min-h-0 w-full max-w-[22rem] flex-col", productBentoContainer.shell),
			children: [
				/* @__PURE__ */ jsx("div", {
					className: cn(productBentoContainer.header, "px-3 py-2"),
					children: /* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-2",
						children: [/* @__PURE__ */ jsx("span", {
							className: "flex size-7 shrink-0 items-center justify-center rounded-md border border-border bg-muted/40",
							children: /* @__PURE__ */ jsx(PostgresElephantIcon, {
								className: "size-4 text-muted-foreground",
								"aria-hidden": true
							})
						}), /* @__PURE__ */ jsxs("div", {
							className: "min-w-0 flex-1",
							children: [/* @__PURE__ */ jsx("p", {
								className: cn("truncate text-[11px] font-medium sm:text-[12px]", productBentoIdle.text),
								children: "race_analytics"
							}), /* @__PURE__ */ jsx("p", {
								className: "truncate text-[10px] text-muted-foreground",
								children: "PostgreSQL · us-east"
							})]
						})]
					})
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "min-h-0 flex-1 overflow-hidden p-2.5 font-mono text-[10px] leading-relaxed sm:text-[11px]",
					children: [/* @__PURE__ */ jsx("div", {
						className: "group-hover:hidden motion-reduce:hidden",
						children: /* @__PURE__ */ jsx(PostgresIdleSql, {})
					}), /* @__PURE__ */ jsx("div", {
						className: "hidden group-hover:block motion-reduce:block",
						children: /* @__PURE__ */ jsx(PostgresTypedSql, { baseDelayMs: 80 })
					})]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "flex items-center justify-between gap-2 border-t border-border bg-muted/8 px-3 py-1.5",
					children: [/* @__PURE__ */ jsxs("p", {
						className: "text-[9px] text-muted-foreground transition-colors duration-300 group-hover:text-foreground sm:text-[10px]",
						children: [/* @__PURE__ */ jsx("span", {
							className: "font-medium tabular-nums",
							children: "5"
						}), " rows"]
					}), /* @__PURE__ */ jsxs("span", {
						className: "flex items-center gap-1 text-[9px] text-muted-foreground sm:text-[10px]",
						children: [
							/* @__PURE__ */ jsx(Loader2, {
								className: "size-3 animate-spin group-hover:hidden motion-reduce:hidden",
								"aria-hidden": true
							}),
							/* @__PURE__ */ jsx(CheckCircle2, {
								className: cn("hidden size-3 group-hover:inline motion-reduce:inline", productBentoIdle.emeraldIcon),
								"aria-hidden": true
							}),
							/* @__PURE__ */ jsx("span", {
								className: "transition-colors duration-300 group-hover:text-foreground",
								children: "12ms"
							})
						]
					})]
				})
			]
		})
	});
}
var COMPATIBLE_TOOLS = [
	{
		id: "rclone",
		name: "rclone",
		iconSrc: "/icons/rclone.svg"
	},
	{
		id: "terraform",
		name: "Terraform",
		iconSrc: "/icons/terraform.svg"
	},
	{
		id: "aws-cli",
		name: "AWS CLI",
		iconSrc: "/icons/amazon.svg"
	},
	{
		id: "cyberduck",
		name: "Cyberduck",
		iconSrc: "/icons/cyberduck.svg"
	}
];
function MockCredentialRow({ label, value, masked = false, delayMs = 0 }) {
	return /* @__PURE__ */ jsxs("div", {
		className: cn("rounded-md border border-border/70 px-2.5 py-2 transition-[border-color,background-color] duration-300", productBentoContainer.panelMd, "group-hover:border-[color-mix(in_srgb,var(--brand-cta)_22%,var(--border))] group-hover:bg-background motion-reduce:group-hover:border-border/70 motion-reduce:group-hover:bg-card/70"),
		style: { transitionDelay: `${delayMs}ms` },
		children: [/* @__PURE__ */ jsx("p", {
			className: "text-[9px] uppercase tracking-wider text-muted-foreground sm:text-[10px]",
			children: label
		}), /* @__PURE__ */ jsxs("div", {
			className: "mt-1 flex items-center gap-1.5",
			children: [/* @__PURE__ */ jsx("p", {
				className: cn("min-w-0 flex-1 truncate font-mono text-[10px] sm:text-[11px]", productBentoIdle.text),
				children: masked ? value.replace(/./g, "•") : value
			}), /* @__PURE__ */ jsx(Copy, {
				className: "size-3 shrink-0 text-muted-foreground",
				"aria-hidden": true
			})]
		})]
	});
}
function S3StorageDayVisual() {
	return /* @__PURE__ */ jsx("div", {
		className: "absolute inset-0 flex flex-col overflow-hidden",
		children: /* @__PURE__ */ jsxs("div", {
			className: cn("mx-auto flex h-full min-h-0 w-full max-w-[22rem] flex-col", productBentoContainer.shell),
			children: [/* @__PURE__ */ jsx("div", {
				className: cn(productBentoContainer.header, "px-3 py-2.5"),
				children: /* @__PURE__ */ jsxs("div", {
					className: "flex items-start justify-between gap-2",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "flex min-w-0 items-start gap-2",
						children: [/* @__PURE__ */ jsx("span", {
							className: "flex size-7 shrink-0 items-center justify-center rounded-md border border-border bg-background",
							children: /* @__PURE__ */ jsx(Server, {
								className: "size-3.5 text-muted-foreground",
								"aria-hidden": true
							})
						}), /* @__PURE__ */ jsxs("div", {
							className: "min-w-0",
							children: [/* @__PURE__ */ jsx("p", {
								className: cn("text-[11px] font-medium sm:text-[12px]", productBentoIdle.text),
								children: "S3-compatible access"
							}), /* @__PURE__ */ jsx("p", {
								className: "mt-0.5 text-[10px] text-muted-foreground sm:text-[11px]",
								children: "Project-scoped endpoint with SigV4 signing"
							})]
						})]
					}), /* @__PURE__ */ jsx(Badge, {
						variant: "inactive",
						className: "h-5 shrink-0 px-1.5 text-[9px] transition-[color,background-color,border-color] duration-300 group-hover:border-[var(--brand-cta)]/25 group-hover:bg-[var(--brand-cta)]/10 group-hover:text-[var(--brand-cta)] sm:text-[10px]",
						children: "SigV4"
					})]
				})
			}), /* @__PURE__ */ jsxs("div", {
				className: "space-y-1.5 overflow-hidden p-2.5",
				children: [
					/* @__PURE__ */ jsx(MockCredentialRow, {
						label: "Endpoint",
						value: "https://cloud.appwrite.io/v1/s3",
						delayMs: 0
					}),
					/* @__PURE__ */ jsx(MockCredentialRow, {
						label: "Access key",
						value: "AKIA4EXAMPLEKEY",
						masked: true,
						delayMs: 40
					}),
					/* @__PURE__ */ jsx(MockCredentialRow, {
						label: "Secret key",
						value: "wJalrXUtnFEMIEXAMPLE",
						masked: true,
						delayMs: 80
					}),
					/* @__PURE__ */ jsxs("div", {
						style: { transitionDelay: "120ms" },
						children: [/* @__PURE__ */ jsx("p", {
							className: "mb-1.5 text-[9px] font-semibold uppercase tracking-wider text-muted-foreground sm:text-[10px]",
							children: "Works with"
						}), /* @__PURE__ */ jsx("div", {
							className: "grid grid-cols-2 gap-1.5",
							children: COMPATIBLE_TOOLS.map((tool, index) => /* @__PURE__ */ jsxs("div", {
								className: cn("flex min-w-0 items-center gap-1.5 rounded-md border border-border/70 px-2 py-1.5 transition-[border-color,background-color] duration-300", productBentoContainer.panelMd, "group-hover:border-[color-mix(in_srgb,var(--brand-cta)_18%,var(--border))] group-hover:bg-background motion-reduce:group-hover:border-border/70 motion-reduce:group-hover:bg-card/70"),
								style: { transitionDelay: `${120 + index * 30}ms` },
								children: [/* @__PURE__ */ jsx("span", {
									className: "flex size-5 shrink-0 items-center justify-center rounded border border-border bg-muted/30",
									children: /* @__PURE__ */ jsx("img", {
										src: tool.iconSrc,
										alt: "",
										className: "size-3",
										"aria-hidden": true
									})
								}), /* @__PURE__ */ jsx("span", {
									className: cn("min-w-0 truncate text-[9px] font-medium sm:text-[10px]", productBentoIdle.text),
									children: tool.name
								})]
							}, tool.id))
						})]
					})
				]
			})]
		})
	});
}
var INIT_DAY_MOCK_VISUALS = {
	"appwrite-2": Appwrite2DayVisual,
	postgres: PostgresDayVisual,
	databases: DatabasesProductVisual,
	"s3-storage": S3StorageDayVisual,
	firewall: FirewallProductVisual
};
function InitDayMockVisual({ mockVisualId, label, className }) {
	const Visual = INIT_DAY_MOCK_VISUALS[mockVisualId];
	return /* @__PURE__ */ jsxs("div", {
		className: cn("group relative w-full overflow-hidden rounded-lg border border-border bg-muted/20", className),
		style: { aspectRatio: "3 / 2" },
		role: "img",
		"aria-label": label,
		children: [/* @__PURE__ */ jsx(ProductBentoSoftLights, {}), /* @__PURE__ */ jsx("div", {
			className: "absolute inset-0 p-2 sm:p-2.5",
			children: /* @__PURE__ */ jsx("div", {
				className: "relative h-full min-h-0 w-full opacity-[0.94] transition-opacity duration-300 group-hover:opacity-100 motion-reduce:opacity-100",
				children: /* @__PURE__ */ jsx(Visual, {})
			})
		})]
	});
}
function InitDayCardHeaderNav({ day }) {
	const hash = `#${getInitDayCardId(day.day)}`;
	const handleClick = (event) => {
		event.preventDefault();
		scrollToInitDayCard(day.day);
		window.history.replaceState(null, "", hash);
	};
	return /* @__PURE__ */ jsx(Button, {
		asChild: true,
		variant: "ghost",
		size: "icon",
		className: "size-6 shrink-0 text-muted-foreground",
		children: /* @__PURE__ */ jsx("a", {
			href: hash,
			"aria-label": `Link to day ${day.day}`,
			onClick: handleClick,
			children: /* @__PURE__ */ jsx(Link2, {
				className: "size-3.5",
				"aria-hidden": true
			})
		})
	});
}
function InitScheduleCalendarButton({ event, item }) {
	return /* @__PURE__ */ jsxs(DropdownMenu, { children: [/* @__PURE__ */ jsx(DropdownMenuTrigger, {
		asChild: true,
		children: /* @__PURE__ */ jsx("button", {
			type: "button",
			className: "flex shrink-0 cursor-pointer items-center text-muted-foreground transition-colors hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background",
			"aria-label": `Add ${item.title} to calendar`,
			children: /* @__PURE__ */ jsx(CalendarPlus, {
				className: "size-3.5",
				"aria-hidden": true
			})
		})
	}), /* @__PURE__ */ jsxs(DropdownMenuContent, {
		align: "end",
		className: "min-w-[220px]",
		children: [/* @__PURE__ */ jsx(DropdownMenuItem, {
			onClick: () => openGoogleCalendarEventUrl(buildGoogleCalendarScheduleItemEventUrl(event, item)),
			children: "Google Calendar"
		}), /* @__PURE__ */ jsxs(DropdownMenuItem, {
			onClick: () => downloadInitScheduleItemCalendar(event, item),
			children: ["Apple & Outlook (.ics)", /* @__PURE__ */ jsx(Download, { className: "ms-auto size-4" })]
		})]
	})] });
}
const INIT_SCHEDULE_PLATFORM_META = {
	youtube: {
		label: "YouTube",
		icon: "/icons/youtube.svg"
	},
	discord: {
		label: "Discord",
		icon: "/icons/discord-simple.svg"
	},
	reddit: {
		label: "Reddit",
		icon: "/icons/reddit.svg"
	}
};
var INIT_YOUTUBE_FALLBACK_HREF = "https://www.youtube.com/@Appwrite";
function InitScheduleRow({ event, item, isRecapMode = false, inlineWhenWide = false }) {
	const meta = INIT_SCHEDULE_PLATFORM_META[item.platform];
	const { setTransientActivity } = useInitPresenceActivity();
	const sessionActivity = buildInitCheckingScheduleActivity(item.day);
	const actionHref = item.href ?? (item.platform === "youtube" ? event.liveBanner?.href ?? INIT_YOUTUBE_FALLBACK_HREF : void 0);
	const actionLabel = item.platform === "youtube" ? "Watch" : "Join event";
	const actionExternal = actionHref ? isExternalInitHref(actionHref) : false;
	const badge = /* @__PURE__ */ jsx("span", {
		className: cn("flex shrink-0", inlineWhenWide ? "w-[92px]" : "w-6"),
		children: /* @__PURE__ */ jsxs(Badge, {
			variant: "secondary",
			className: cn("rounded-md bg-muted text-[10px] font-semibold uppercase tracking-wider text-muted-foreground", inlineWhenWide ? "gap-1.5 px-2 py-0.5" : "size-6 justify-center p-0"),
			children: [/* @__PURE__ */ jsx("img", {
				src: meta.icon,
				alt: "",
				className: cn("size-3", PUBLIC_ICON_MUTED_CLASSES),
				"aria-hidden": true
			}), inlineWhenWide ? meta.label : null]
		})
	});
	const details = /* @__PURE__ */ jsxs("div", {
		className: cn("min-w-0 flex-1 space-y-1", inlineWhenWide && "sm:flex sm:items-center sm:gap-3 sm:space-y-0"),
		children: [/* @__PURE__ */ jsxs("div", {
			className: "flex min-w-0 flex-wrap items-center gap-2",
			children: [/* @__PURE__ */ jsx("p", {
				className: "min-w-0 truncate text-[13px] font-medium text-foreground",
				children: item.title
			}), !isRecapMode && item.isLive ? /* @__PURE__ */ jsx(Badge, {
				variant: "error",
				className: "text-[10px] shrink-0",
				children: "Live"
			}) : null]
		}), /* @__PURE__ */ jsx("p", {
			className: cn("text-[11px] text-muted-foreground", inlineWhenWide && "sm:shrink-0"),
			children: item.timeLabel
		})]
	});
	const mainContent = !inlineWhenWide && actionHref ? /* @__PURE__ */ jsxs("a", {
		href: actionHref,
		...actionExternal ? {
			target: "_blank",
			rel: "noopener noreferrer"
		} : {},
		className: "flex min-w-0 flex-1 items-start gap-4",
		children: [badge, details]
	}) : /* @__PURE__ */ jsxs(Fragment, { children: [badge, details] });
	return /* @__PURE__ */ jsxs("li", {
		className: cn("group flex items-start gap-4 px-6 py-3.5", inlineWhenWide && "sm:items-center", !isRecapMode && item.isLive && "bg-[color-mix(in_srgb,var(--brand-cta)_5%,transparent)]"),
		onMouseEnter: () => setTransientActivity(sessionActivity),
		onMouseLeave: () => setTransientActivity(null),
		onFocus: () => setTransientActivity(sessionActivity),
		onBlur: () => setTransientActivity(null),
		children: [mainContent, /* @__PURE__ */ jsxs("div", {
			className: "flex shrink-0 items-center gap-3",
			children: [!isRecapMode ? /* @__PURE__ */ jsx(InitScheduleCalendarButton, {
				event,
				item
			}) : null, inlineWhenWide && actionHref ? /* @__PURE__ */ jsxs(Fragment, { children: [!isRecapMode ? /* @__PURE__ */ jsx("span", {
				"aria-hidden": true,
				className: "size-1 rounded-full bg-muted-foreground/40"
			}) : null, /* @__PURE__ */ jsxs("a", {
				href: actionHref,
				...actionExternal ? {
					target: "_blank",
					rel: "noopener noreferrer"
				} : {},
				className: "flex shrink-0 items-center gap-1 text-[12px] text-muted-foreground transition-colors group-hover:text-foreground hover:text-foreground",
				children: [actionLabel, /* @__PURE__ */ jsx(ArrowUpRight, {
					className: "size-3.5",
					"aria-hidden": true
				})]
			})] }) : null]
		})]
	});
}
var CARD_SHELL$2 = "rounded-xl border border-border bg-card/50 overflow-hidden";
function VideoThumbnail({ label, href, className }) {
	const inner = /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsxs("div", {
		className: cn("relative flex aspect-video items-center justify-center overflow-hidden rounded-lg border border-border bg-muted/40", className),
		children: [/* @__PURE__ */ jsx("div", { className: "absolute inset-0 bg-[radial-gradient(circle_at_30%_20%,color-mix(in_srgb,var(--brand-cta)_12%,transparent),transparent_55%)]" }), /* @__PURE__ */ jsx("span", {
			className: "relative flex size-9 items-center justify-center rounded-full bg-background/80 text-foreground shadow-sm backdrop-blur-sm",
			children: /* @__PURE__ */ jsx(Play, {
				className: "ms-0.5 size-4 fill-current",
				"aria-hidden": true
			})
		})]
	}), /* @__PURE__ */ jsx("p", {
		className: "mt-2 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground",
		children: label
	})] });
	if (href) return /* @__PURE__ */ jsx("a", {
		href,
		...isExternalInitHref(href) ? {
			target: "_blank",
			rel: "noopener noreferrer"
		} : {},
		className: "group block transition-opacity hover:opacity-90",
		children: inner
	});
	return /* @__PURE__ */ jsx("div", { children: inner });
}
function DayVisual({ day }) {
	const Icon$1 = day.icon;
	if (day.visual) return /* @__PURE__ */ jsx(InitDayMockVisual, {
		mockVisualId: day.visual.mockVisualId,
		label: day.visual.imageAlt
	});
	return /* @__PURE__ */ jsxs("div", {
		"aria-hidden": true,
		className: "relative flex min-h-[200px] flex-1 items-center justify-center overflow-hidden rounded-lg border border-border bg-muted/30 lg:min-h-[240px]",
		children: [
			/* @__PURE__ */ jsx("div", { className: "absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,color-mix(in_srgb,var(--brand-cta)_10%,transparent),transparent_65%)]" }),
			/* @__PURE__ */ jsx("div", { className: "absolute inset-0 opacity-30 [background-image:radial-gradient(circle,color-mix(in_srgb,var(--border)_80%,transparent)_1px,transparent_1px)] [background-size:16px_16px]" }),
			/* @__PURE__ */ jsx("div", {
				className: "relative flex size-24 items-center justify-center rounded-full border border-[color-mix(in_srgb,var(--brand-cta)_25%,var(--border))] bg-background/60 shadow-sm backdrop-blur-sm",
				children: /* @__PURE__ */ jsx(Icon$1, { className: "size-10 text-muted-foreground" })
			}),
			/* @__PURE__ */ jsx("div", { className: "absolute end-[18%] top-[28%] size-3 rounded-full bg-[var(--brand-cta)] shadow-[0_0_12px_color-mix(in_srgb,var(--brand-cta)_60%,transparent)]" })
		]
	});
}
var TITLE_CLASS = "font-aeonik-pro text-[clamp(32px,4.5vw,42px)] font-normal leading-none tracking-tight";
function DayTitle({ title }) {
	const lastSpace = title.lastIndexOf(" ");
	const leading = lastSpace === -1 ? "" : title.slice(0, lastSpace + 1);
	const lastWord = lastSpace === -1 ? title : title.slice(lastSpace + 1);
	return /* @__PURE__ */ jsxs("h3", {
		className: cn(TITLE_CLASS, "text-foreground"),
		children: [leading ? /* @__PURE__ */ jsx("span", { children: leading }) : null, /* @__PURE__ */ jsxs("span", {
			className: "whitespace-nowrap",
			children: [lastWord, /* @__PURE__ */ jsx("span", {
				className: "text-[var(--brand-cta)]",
				children: "_"
			})]
		})]
	});
}
function DayResourceRow({ resource }) {
	const ResourceIcon = resource.typeLabel.toLowerCase() === "docs" ? BookOpen : FileText;
	const isBlogLink = Boolean(parseBlogPagePath(resource.href));
	const isDocsLink = Boolean(parseDocsPagePath(resource.href));
	const rowClassName = "group flex items-center gap-4 px-6 py-3.5 transition-colors hover:bg-accent/30";
	const rowContent = /* @__PURE__ */ jsxs(Fragment, { children: [
		/* @__PURE__ */ jsx("span", {
			className: "flex w-[92px] shrink-0",
			children: /* @__PURE__ */ jsxs(Badge, {
				variant: "secondary",
				className: "gap-1.5 rounded-md bg-muted px-2 py-0.5 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground",
				children: [/* @__PURE__ */ jsx(ResourceIcon, {
					className: "size-3 text-muted-foreground",
					"aria-hidden": true
				}), resource.typeLabel]
			})
		}),
		/* @__PURE__ */ jsx("span", {
			className: "min-w-0 flex-1 text-[13px] font-medium text-foreground",
			children: resource.title
		}),
		/* @__PURE__ */ jsxs("span", {
			className: "flex shrink-0 items-center gap-1 text-[12px] text-muted-foreground transition-colors group-hover:text-foreground",
			children: [resource.actionLabel, /* @__PURE__ */ jsx(ArrowUpRight, {
				className: "size-3.5",
				"aria-hidden": true
			})]
		})
	] });
	return /* @__PURE__ */ jsx("li", { children: isBlogLink ? /* @__PURE__ */ jsx(BlogPageAnchor, {
		href: resource.href,
		className: rowClassName,
		children: rowContent
	}) : isDocsLink ? /* @__PURE__ */ jsx(DocsRouteLink, {
		href: resource.href,
		className: rowClassName,
		children: rowContent
	}) : /* @__PURE__ */ jsx("a", {
		href: resource.href,
		...isExternalInitHref(resource.href) ? {
			target: "_blank",
			rel: "noopener noreferrer"
		} : {},
		className: rowClassName,
		children: rowContent
	}) });
}
function DayDetailCard({ event, day, scheduleItems, currentDay, isRecapMode = false }) {
	const { setTransientActivity } = useInitPresenceActivity();
	const blogResources = day.resources.filter((resource) => resource.typeLabel.toLowerCase() === "blog");
	const otherResources = day.resources.filter((resource) => resource.typeLabel.toLowerCase() !== "blog");
	const hasListContent = blogResources.length > 0 || scheduleItems.length > 0 || otherResources.length > 0;
	const footerVideos = day.footerVideos ?? [];
	return /* @__PURE__ */ jsxs("article", {
		id: getInitDayCardId(day.day),
		className: cn(CARD_SHELL$2, "scroll-mt-28"),
		onMouseEnter: () => setTransientActivity(buildInitDayPreviewActivity(day, currentDay)),
		onMouseLeave: () => setTransientActivity(null),
		children: [
			/* @__PURE__ */ jsxs("header", {
				className: "grid grid-cols-[1fr_auto_1fr] items-center border-b border-border px-6 py-3",
				children: [
					/* @__PURE__ */ jsx("span", { "aria-hidden": true }),
					/* @__PURE__ */ jsxs("p", {
						className: "text-center text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground",
						children: [
							"Day ",
							day.day,
							" / ",
							day.weekdayLabel
						]
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "flex items-center justify-end gap-2",
						children: [!isRecapMode && day.isLive ? /* @__PURE__ */ jsx(Badge, {
							variant: "error",
							className: "text-[10px] shrink-0",
							children: "Live"
						}) : null, /* @__PURE__ */ jsx(InitDayCardHeaderNav, { day })]
					})
				]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid lg:grid-cols-2",
				children: [/* @__PURE__ */ jsx("div", {
					className: "space-y-5 border-b border-border px-6 py-6 lg:border-b-0 lg:border-e",
					children: /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(DayTitle, { title: day.title }), /* @__PURE__ */ jsx("p", {
						className: "mt-4 text-[13px] leading-relaxed text-muted-foreground",
						children: day.longDescription
					})] })
				}), /* @__PURE__ */ jsx("div", {
					className: "flex items-center p-6",
					children: /* @__PURE__ */ jsx(DayVisual, { day })
				})]
			}),
			hasListContent ? /* @__PURE__ */ jsxs("ul", {
				className: "divide-y divide-border border-t border-border",
				children: [
					blogResources.map((resource) => /* @__PURE__ */ jsx(DayResourceRow, { resource }, resource.id)),
					scheduleItems.map((item) => /* @__PURE__ */ jsx(InitScheduleRow, {
						event,
						item,
						isRecapMode,
						inlineWhenWide: true
					}, item.id)),
					otherResources.map((resource) => /* @__PURE__ */ jsx(DayResourceRow, { resource }, resource.id))
				]
			}) : null,
			footerVideos.length > 0 ? /* @__PURE__ */ jsx("div", {
				className: "grid gap-6 border-t border-border px-6 py-6 sm:grid-cols-2",
				children: footerVideos.map((video) => /* @__PURE__ */ jsx(VideoThumbnail, {
					label: video.label,
					href: video.href
				}, video.id))
			}) : null
		]
	});
}
var CARD_SHELL$1 = "rounded-xl border border-border bg-card/50 overflow-hidden";
function LockedDayDetailCard({ day, eventStartDate, currentDay }) {
	const { setTransientActivity } = useInitPresenceActivity();
	return /* @__PURE__ */ jsxs("article", {
		id: getInitDayCardId(day.day),
		className: cn(CARD_SHELL$1, "scroll-mt-28"),
		"aria-label": `Day ${day.day} locked`,
		onMouseEnter: () => setTransientActivity(buildInitDayPreviewActivity(day, currentDay)),
		onMouseLeave: () => setTransientActivity(null),
		children: [/* @__PURE__ */ jsxs("header", {
			className: "grid grid-cols-[1fr_auto_1fr] items-center border-b border-border px-6 py-3",
			children: [
				/* @__PURE__ */ jsx("span", { "aria-hidden": true }),
				/* @__PURE__ */ jsxs("p", {
					className: "text-center text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground",
					children: [
						"Day ",
						day.day,
						" / ",
						day.weekdayLabel
					]
				}),
				/* @__PURE__ */ jsx("div", {
					className: "flex justify-end",
					children: /* @__PURE__ */ jsx(InitDayCardHeaderNav, { day })
				})
			]
		}), /* @__PURE__ */ jsxs("div", {
			className: "flex flex-col items-center justify-center px-6 py-16 text-center",
			children: [
				/* @__PURE__ */ jsx("span", {
					className: "flex size-12 items-center justify-center rounded-full border border-border bg-muted/40 text-muted-foreground",
					children: /* @__PURE__ */ jsx(Lock, {
						className: "size-5",
						"aria-hidden": true
					})
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-4 text-[15px] font-semibold text-foreground",
					children: "Coming soon"
				}),
				/* @__PURE__ */ jsx(InitDayCountdown, {
					eventStartDate,
					dayNumber: day.day,
					className: "mt-3"
				}),
				/* @__PURE__ */ jsxs("p", {
					className: "mt-3 max-w-sm text-[13px] text-muted-foreground",
					children: [
						"This launch unlocks on ",
						day.dateLabel,
						". Check back then for announcements, resources, and sessions."
					]
				})
			]
		})]
	});
}
function isOptionUnlocked(event, optionUnlockDay) {
	return event.isRecapMode || event.currentDay >= optionUnlockDay;
}
function ReleaseOptionLink({ option, children, className, locked = false }) {
	if (locked) return /* @__PURE__ */ jsx("div", {
		className,
		"aria-disabled": true,
		children
	});
	const isDocsLink = Boolean(parseDocsPagePath(option.href));
	const external = option.external ?? isExternalInitHref(option.href);
	if (isDocsLink) return /* @__PURE__ */ jsx(DocsRouteLink, {
		href: option.href,
		className,
		children
	});
	return /* @__PURE__ */ jsx("a", {
		href: option.href,
		...external ? {
			target: "_blank",
			rel: "noopener noreferrer"
		} : {},
		className,
		children
	});
}
function InitReleaseAvailabilityStage({ event }) {
	const release = event.releaseAvailability;
	if (!release) return null;
	const cloudUnlockDay = release.cloud.unlockDay ?? 1;
	const selfHostUnlockDay = release.selfHosted.unlockDay ?? release.unlockDay;
	const cloudUnlocked = isOptionUnlocked(event, cloudUnlockDay);
	const selfHostedUnlocked = isOptionUnlocked(event, selfHostUnlockDay);
	const fullReleaseUnlocked = selfHostedUnlocked;
	const options = [{
		option: release.cloud,
		icon: Cloud,
		label: "Cloud",
		unlocked: cloudUnlocked,
		unlockDay: cloudUnlockDay
	}, {
		option: release.selfHosted,
		icon: Server,
		label: "Self-host",
		unlocked: selfHostedUnlocked,
		unlockDay: selfHostUnlockDay
	}];
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
		className: "border-t border-border",
		"aria-hidden": true
	}), /* @__PURE__ */ jsx("div", {
		className: "py-8",
		children: /* @__PURE__ */ jsx("div", {
			className: "mx-auto w-full max-w-7xl px-4 sm:px-6",
			children: /* @__PURE__ */ jsxs("section", {
				id: INIT_RELEASE_AVAILABILITY_SECTION_ID,
				className: "scroll-mt-28 flex flex-col items-center",
				children: [
					/* @__PURE__ */ jsxs("div", {
						className: "max-w-2xl text-center",
						children: [/* @__PURE__ */ jsx("h3", {
							className: "text-[15px] font-semibold text-foreground",
							children: fullReleaseUnlocked ? release.sectionTitle : release.lockedTitle
						}), fullReleaseUnlocked ? release.sectionDescription ? /* @__PURE__ */ jsx("p", {
							className: "mt-2 text-[13px] leading-relaxed text-muted-foreground",
							children: release.sectionDescription
						}) : null : /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("p", {
							className: "mt-2 text-[13px] leading-relaxed text-muted-foreground",
							children: release.lockedDescription
						}), /* @__PURE__ */ jsx(InitDayCountdown, {
							eventStartDate: event.startDate,
							dayNumber: selfHostUnlockDay,
							className: "mt-3"
						})] })]
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "mt-6 flex w-full max-w-4xl flex-col items-center",
						"aria-hidden": true,
						children: [
							/* @__PURE__ */ jsx("div", { className: "h-6 w-px bg-border" }),
							/* @__PURE__ */ jsx("div", {
								className: "relative flex w-full justify-center",
								children: /* @__PURE__ */ jsx("div", { className: "h-px w-full max-w-xl bg-gradient-to-r from-transparent via-border to-transparent" })
							}),
							/* @__PURE__ */ jsxs("div", {
								className: "grid w-full max-w-xl grid-cols-2",
								children: [/* @__PURE__ */ jsx("div", { className: "h-5 border-e border-border" }), /* @__PURE__ */ jsx("div", { className: "h-5 flex-1 border-s border-border" })]
							})
						]
					}),
					/* @__PURE__ */ jsx("div", {
						className: "w-full max-w-4xl rounded-2xl border border-border bg-muted/40 p-1.5",
						children: /* @__PURE__ */ jsx("div", {
							className: "grid gap-1.5 sm:grid-cols-2",
							children: options.map(({ option, icon: Icon$1, label, unlocked, unlockDay }) => /* @__PURE__ */ jsxs(ReleaseOptionLink, {
								option,
								locked: !unlocked,
								className: cn("group relative flex items-start gap-3 rounded-xl px-5 py-5 pt-5 text-start transition-all", unlocked ? "bg-background shadow-sm hover:bg-accent/50" : "border border-dashed border-border/80 bg-background/40 opacity-90"),
								children: [
									/* @__PURE__ */ jsx("span", {
										className: cn("absolute -top-2.5 left-1/2 -translate-x-1/2 rounded-full border px-2 py-px text-[10px] font-semibold uppercase tracking-[0.2em]", unlocked ? "border-border bg-background text-muted-foreground" : "border-dashed border-border bg-muted/50 text-muted-foreground/70"),
										children: label
									}),
									/* @__PURE__ */ jsx("div", {
										className: cn("flex size-10 shrink-0 items-center justify-center rounded-lg", unlocked ? "bg-[color-mix(in_srgb,var(--brand-cta)_12%,transparent)] text-[var(--brand-cta)]" : "bg-muted text-muted-foreground/60"),
										children: /* @__PURE__ */ jsx(Icon$1, {
											className: "size-5",
											"aria-hidden": true
										})
									}),
									/* @__PURE__ */ jsxs("div", {
										className: "min-w-0 flex-1",
										children: [
											/* @__PURE__ */ jsx("p", {
												className: cn("text-[13px] font-medium", unlocked ? "text-foreground" : "text-muted-foreground"),
												children: option.title
											}),
											option.availabilityLabel ? /* @__PURE__ */ jsx("p", {
												className: cn("mt-1 text-[10px] font-semibold uppercase tracking-[0.2em]", unlocked ? "text-[var(--brand-cta)]" : "text-muted-foreground/70"),
												children: option.availabilityLabel
											}) : null,
											/* @__PURE__ */ jsx("p", {
												className: "mt-2 line-clamp-2 text-[13px] leading-snug text-muted-foreground",
												children: option.description
											}),
											/* @__PURE__ */ jsx("p", {
												className: "mt-2 text-[13px] font-medium text-muted-foreground transition-colors group-hover:text-foreground",
												children: unlocked ? option.ctaLabel : unlockDay > 1 ? `Unlocks on day ${unlockDay}` : "Coming soon"
											})
										]
									}),
									unlocked ? /* @__PURE__ */ jsx(ChevronRight, {
										className: "size-4 shrink-0 text-muted-foreground transition-colors group-hover:text-[var(--brand-cta)]",
										"aria-hidden": true
									}) : /* @__PURE__ */ jsx(Lock, {
										className: "size-4 shrink-0 text-muted-foreground/60",
										"aria-hidden": true
									})
								]
							}, option.title))
						})
					})
				]
			})
		})
	})] });
}
var CARD_SHELL = "rounded-xl border border-border bg-card/50 overflow-hidden";
function EventSchedulePanel({ event, embedded = false }) {
	const scheduleDays = useMemo(() => {
		return event.days.map((day) => day.day).sort((a, b) => a - b);
	}, [event.days]);
	const defaultDay = event.currentDay > 0 && scheduleDays.includes(event.currentDay) ? event.currentDay : scheduleDays[0] ?? 1;
	const [selectedDay, setSelectedDay] = useState(defaultDay);
	const { setTransientActivity } = useInitPresenceActivity();
	useEffect(() => {
		setSelectedDay(defaultDay);
	}, [defaultDay]);
	const selectedDayInfo = event.days.find((day) => day.day === selectedDay);
	const dayEvents = event.schedule.filter((item) => item.day === selectedDay);
	const selectedIndex = scheduleDays.indexOf(selectedDay);
	const canGoPrevious = selectedIndex > 0;
	const canGoNext = selectedIndex >= 0 && selectedIndex < scheduleDays.length - 1;
	if (event.schedule.length === 0 || scheduleDays.length === 0) return null;
	const selectedTitle = selectedDayInfo && !isLaunchEventDayLocked(selectedDayInfo) ? selectedDayInfo.title : null;
	return /* @__PURE__ */ jsxs("div", {
		className: cn(CARD_SHELL, !embedded && "lg:sticky lg:self-start lg:top-20"),
		onMouseEnter: () => setTransientActivity(buildInitCheckingScheduleActivity(selectedDay)),
		onMouseLeave: () => setTransientActivity(null),
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "px-6 py-4",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "flex items-start justify-between gap-3",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "min-w-0",
						children: [/* @__PURE__ */ jsx("h3", {
							className: "text-[15px] font-semibold text-foreground",
							children: event.isRecapMode ? "Session replays" : "Schedule"
						}), /* @__PURE__ */ jsx("p", {
							className: "mt-1 text-[13px] text-muted-foreground",
							children: event.isRecapMode ? "Session replays and community hangouts" : selectedDayInfo && !isLaunchEventDayLocked(selectedDayInfo) && selectedDayInfo.isLive ? "Today's sessions" : "YouTube streams and Reddit AMAs"
						})]
					}), scheduleDays.length > 1 ? /* @__PURE__ */ jsxs("div", {
						className: "flex shrink-0 items-center gap-0.5",
						children: [/* @__PURE__ */ jsx(Button, {
							type: "button",
							variant: "ghost",
							size: "icon",
							className: "size-7",
							disabled: !canGoPrevious,
							"aria-label": "Previous day",
							onClick: () => {
								if (!canGoPrevious) return;
								setSelectedDay(scheduleDays[selectedIndex - 1]);
							},
							children: /* @__PURE__ */ jsx(ChevronLeft, { className: "size-4" })
						}), /* @__PURE__ */ jsx(Button, {
							type: "button",
							variant: "ghost",
							size: "icon",
							className: "size-7",
							disabled: !canGoNext,
							"aria-label": "Next day",
							onClick: () => {
								if (!canGoNext) return;
								setSelectedDay(scheduleDays[selectedIndex + 1]);
							},
							children: /* @__PURE__ */ jsx(ChevronRight, { className: "size-4" })
						})]
					}) : null]
				}), /* @__PURE__ */ jsxs("p", {
					className: "mt-3 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground",
					children: [
						"Day ",
						selectedDay,
						selectedDayInfo && !isLaunchEventDayLocked(selectedDayInfo) ? ` · ${selectedDayInfo.dateLabel}` : null,
						selectedTitle ? ` · ${selectedTitle}` : null
					]
				})]
			}),
			/* @__PURE__ */ jsx("div", { className: "border-t border-border" }),
			dayEvents.length > 0 ? /* @__PURE__ */ jsx("ol", {
				className: "divide-y divide-border",
				children: dayEvents.map((item) => /* @__PURE__ */ jsx(InitScheduleRow, {
					event,
					item,
					isRecapMode: event.isRecapMode
				}, item.id))
			}) : /* @__PURE__ */ jsx("div", {
				className: "px-6 py-8 text-center",
				children: /* @__PURE__ */ jsx("p", {
					className: "text-[13px] text-muted-foreground",
					children: "No sessions scheduled for this day."
				})
			})
		]
	});
}
function InitPresenceReactionSpark({ index, animationKey, sparkCount = 6 }) {
	const phase = index / sparkCount * Math.PI * 2;
	const radius = 8 + index % 3 * 2.5;
	const steps = 5;
	const xKeyframes = Array.from({ length: steps }, (_, step) => {
		const angle = phase + step / (steps - 1) * Math.PI * 2;
		return Math.cos(angle) * radius;
	});
	const yKeyframes = Array.from({ length: steps }, (_, step) => {
		const angle = phase + step / (steps - 1) * Math.PI * 2;
		return Math.sin(angle) * radius;
	});
	return /* @__PURE__ */ jsx(motion.span, {
		className: cn("absolute start-1/2 top-1/2 size-1 -translate-x-1/2 -translate-y-1/2 rounded-full", "bg-muted-foreground/50 shadow-[0_0_4px_color-mix(in_srgb,var(--muted-foreground)_35%,transparent)]", index % 2 === 0 && "size-[3px] rounded-[1px] rotate-45"),
		initial: {
			x: 0,
			y: 0,
			opacity: 0,
			scale: 0
		},
		animate: {
			x: [...xKeyframes, xKeyframes[0]],
			y: [...yKeyframes, yKeyframes[0]],
			opacity: [
				.2,
				.75,
				.45,
				.85,
				.35,
				.2
			],
			scale: [
				.4,
				1,
				.65,
				.95,
				.5,
				.4
			]
		},
		transition: {
			duration: 1.6 + index * .12,
			repeat: Infinity,
			ease: "linear",
			delay: index * .08
		},
		"aria-hidden": true
	}, `${animationKey}-spark-${index}`);
}
var REACTION_BUTTON_EASE = [
	.34,
	1.56,
	.64,
	1
];
var REACTIONS_SECTION_CLASS = "shrink-0 border-t border-border bg-background px-3 py-2 sm:px-3";
function InitPresenceReactionsSkeleton({ isMobile = false }) {
	return /* @__PURE__ */ jsx("div", {
		className: cn(REACTIONS_SECTION_CLASS, isMobile && "px-4"),
		"aria-busy": "true",
		"aria-label": "Loading reactions",
		children: /* @__PURE__ */ jsx("div", {
			className: "grid w-full grid-cols-6 items-center gap-0",
			children: INIT_REACTIONS.map((reaction) => /* @__PURE__ */ jsx("div", {
				className: "flex justify-center",
				children: /* @__PURE__ */ jsx(Skeleton, { className: "size-7 rounded-md bg-muted/80 dark:bg-muted/40" })
			}, reaction.id))
		})
	});
}
function ReactionButton({ reaction, disabled, burstKey, onClick }) {
	const reduceMotion = useReducedMotion();
	const Icon$1 = reaction.icon;
	return /* @__PURE__ */ jsxs(Button, {
		type: "button",
		variant: "ghost",
		size: "icon",
		disabled,
		className: "relative size-7 shrink-0 overflow-visible rounded-md text-muted-foreground",
		"aria-label": reaction.label,
		onClick,
		children: [/* @__PURE__ */ jsx(motion.span, {
			className: "inline-flex",
			initial: reduceMotion ? false : {
				scale: 1,
				rotate: 0
			},
			animate: reduceMotion || burstKey === 0 ? void 0 : {
				scale: [
					1,
					1.45,
					.88,
					1.12,
					1
				],
				rotate: [
					0,
					-16,
					14,
					-8,
					0
				]
			},
			transition: {
				duration: .45,
				ease: REACTION_BUTTON_EASE
			},
			children: /* @__PURE__ */ jsx(Icon$1, {
				className: "size-3.5",
				"aria-hidden": true
			})
		}, burstKey), !reduceMotion && burstKey > 0 ? /* @__PURE__ */ jsx(motion.span, {
			className: "pointer-events-none absolute inset-0 rounded-md ring-2 ring-[var(--brand-cta)]",
			initial: {
				scale: .85,
				opacity: .7
			},
			animate: {
				scale: 1.55,
				opacity: 0
			},
			transition: {
				duration: .45,
				ease: "easeOut"
			},
			"aria-hidden": true
		}, `ring-${burstKey}`) : null]
	});
}
function InitPresenceReactions({ eventId, collapsed = false, isMobile = false, onReactionPulse }) {
	const { isReady, participantStatus } = useInitPresence();
	const { setTransientActivity } = useInitPresenceActivity();
	const [burstKeys, setBurstKeys] = useState({});
	const resetTimeoutRef = useRef(null);
	const eventIdRef = useRef(eventId);
	eventIdRef.current = eventId;
	const clearReaction = useCallback(() => {
		if (resetTimeoutRef.current) {
			window.clearTimeout(resetTimeoutRef.current);
			resetTimeoutRef.current = null;
		}
		writeStoredInitReaction(eventIdRef.current, null);
		setTransientActivity(null);
	}, [setTransientActivity]);
	const scheduleReactionReset = useCallback((delay = INIT_REACTION_DURATION_MS) => {
		if (resetTimeoutRef.current) window.clearTimeout(resetTimeoutRef.current);
		resetTimeoutRef.current = window.setTimeout(() => {
			resetTimeoutRef.current = null;
			clearReaction();
		}, delay);
	}, [clearReaction]);
	const applyReaction = useCallback((reactionId) => {
		writeStoredInitReaction(eventIdRef.current, reactionId);
		setTransientActivity(buildInitReactingActivity(reactionId));
		scheduleReactionReset(INIT_REACTION_DURATION_MS);
	}, [scheduleReactionReset, setTransientActivity]);
	useEffect(() => {
		const stored = readStoredInitReaction(eventId);
		if (!stored) return;
		setTransientActivity(buildInitReactingActivity(stored.reactionId));
		scheduleReactionReset(Math.max(0, stored.expiresAt - Date.now()));
		return () => {
			if (resetTimeoutRef.current) {
				window.clearTimeout(resetTimeoutRef.current);
				resetTimeoutRef.current = null;
			}
		};
	}, [
		eventId,
		scheduleReactionReset,
		setTransientActivity
	]);
	if (collapsed && !isMobile) return null;
	if (!isReady) return /* @__PURE__ */ jsx(InitPresenceReactionsSkeleton, { isMobile });
	const isOnline = participantStatus === "online";
	const handleReaction = (reactionId) => {
		onReactionPulse?.();
		setBurstKeys((previous) => ({
			...previous,
			[reactionId]: (previous[reactionId] ?? 0) + 1
		}));
		applyReaction(reactionId);
	};
	return /* @__PURE__ */ jsx("div", {
		className: cn(REACTIONS_SECTION_CLASS, isMobile && "px-4"),
		children: /* @__PURE__ */ jsx("div", {
			className: "grid w-full grid-cols-6 items-center gap-0",
			role: "group",
			"aria-label": "React to Init",
			children: INIT_REACTIONS.map((reaction) => /* @__PURE__ */ jsx("div", {
				className: "flex justify-center",
				children: /* @__PURE__ */ jsx(ReactionButton, {
					reaction,
					disabled: !isOnline,
					burstKey: burstKeys[reaction.id] ?? 0,
					onClick: () => handleReaction(reaction.id)
				})
			}, reaction.id))
		})
	});
}
var PRESENCE_STATUS_PULSE_TRANSITION = {
	duration: .42,
	ease: [
		.22,
		1,
		.36,
		1
	]
};
var ONLINE_USER_TOOLTIP_CLASS$1 = "max-w-[min(280px,calc(100dvw-5rem))] border border-border bg-popover px-3 py-2.5 text-popover-foreground shadow-md [&_svg]:!hidden";
function StatusDot({ online, className }) {
	const reduceMotion = useReducedMotion();
	return /* @__PURE__ */ jsx(motion.span, {
		className: cn("size-2 shrink-0 rounded-full", online ? "bg-emerald-500 shadow-[0_0_0_2px_color-mix(in_srgb,var(--background)_70%,transparent)]" : "bg-muted-foreground/45", className),
		animate: reduceMotion ? void 0 : { scale: [
			1,
			1.25,
			1
		] },
		transition: PRESENCE_STATUS_PULSE_TRANSITION,
		"aria-hidden": true
	}, online ? "online" : "offline");
}
function PresenceStatusToggle({ participantStatus, disabled, onStatusChange, className }) {
	return /* @__PURE__ */ jsxs(ToggleGroup, {
		type: "single",
		variant: "outline",
		size: "sm",
		value: participantStatus,
		onValueChange: (value) => {
			if (value === "online" || value === "offline") onStatusChange(value);
		},
		disabled,
		className: cn("grid w-full grid-cols-2", className),
		"aria-label": "Your Init presence status",
		children: [/* @__PURE__ */ jsx(ToggleGroupItem, {
			value: "online",
			className: "h-8 cursor-pointer text-[12px] font-medium disabled:cursor-not-allowed data-[state=on]:bg-emerald-500/10 data-[state=on]:text-emerald-700 dark:data-[state=on]:text-emerald-400",
			children: "Online"
		}), /* @__PURE__ */ jsx(ToggleGroupItem, {
			value: "offline",
			className: "h-8 cursor-pointer text-[12px] font-medium disabled:cursor-not-allowed data-[state=on]:bg-muted data-[state=on]:text-foreground",
			children: "Offline"
		})]
	});
}
function PresenceStatusPanel({ name, participantStatus, isUpdating, onStatusChange }) {
	const isOnline = participantStatus === "online";
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-3",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "flex items-start gap-2.5",
			children: [
				/* @__PURE__ */ jsx(InitialsAvatar, {
					name,
					size: "sm",
					className: "mt-0.5 shrink-0 rounded-full"
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "min-w-0 flex-1",
					children: [/* @__PURE__ */ jsx("p", {
						className: "truncate text-[13px] font-medium text-foreground",
						children: name
					}), /* @__PURE__ */ jsxs("div", {
						className: "mt-0.5 flex items-start gap-1.5",
						children: [/* @__PURE__ */ jsx(StatusDot, {
							online: isOnline,
							className: "mt-1"
						}), /* @__PURE__ */ jsx("p", {
							className: "min-h-[2lh] text-[11px] leading-normal text-muted-foreground",
							children: isOnline ? "Visible to others on Init" : "Hidden from the online list"
						})]
					})]
				}),
				/* @__PURE__ */ jsx("div", {
					className: "flex size-3.5 shrink-0 items-center justify-center",
					"aria-hidden": !isUpdating,
					children: isUpdating ? /* @__PURE__ */ jsx(Loader2, { className: "size-3.5 animate-spin text-muted-foreground" }) : null
				})
			]
		}), /* @__PURE__ */ jsx(PresenceStatusToggle, {
			participantStatus,
			disabled: isUpdating,
			onStatusChange
		})]
	});
}
function InitPresenceStatusSkeleton({ collapsed = false, isMobile = false }) {
	if (collapsed && !isMobile) return /* @__PURE__ */ jsx("div", {
		className: "flex justify-center py-2",
		"aria-busy": "true",
		"aria-label": "Loading your status",
		children: /* @__PURE__ */ jsx(Skeleton, { className: "size-8 rounded-full bg-muted/80 dark:bg-muted/40" })
	});
	return /* @__PURE__ */ jsxs("div", {
		className: cn("space-y-3 px-2.5 py-3", isMobile && "px-0 py-0"),
		"aria-busy": "true",
		"aria-label": "Loading your status",
		children: [
			/* @__PURE__ */ jsx(Skeleton, { className: "h-2.5 w-14 bg-muted/80 dark:bg-muted/40" }),
			/* @__PURE__ */ jsxs("div", {
				className: "flex items-start gap-2.5",
				children: [
					/* @__PURE__ */ jsx(Skeleton, { className: "mt-0.5 size-8 shrink-0 rounded-full bg-muted/80 dark:bg-muted/40" }),
					/* @__PURE__ */ jsxs("div", {
						className: "min-w-0 flex-1 space-y-2",
						children: [/* @__PURE__ */ jsx(Skeleton, { className: "h-3.5 w-[58%] bg-muted/80 dark:bg-muted/40" }), /* @__PURE__ */ jsx(Skeleton, { className: "min-h-[2lh] w-full bg-muted/80 dark:bg-muted/40" })]
					}),
					/* @__PURE__ */ jsx("span", {
						className: "size-3.5 shrink-0",
						"aria-hidden": true
					})
				]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid grid-cols-2 gap-1.5",
				children: [/* @__PURE__ */ jsx(Skeleton, { className: "h-8 rounded-md bg-muted/80 dark:bg-muted/40" }), /* @__PURE__ */ jsx(Skeleton, { className: "h-8 rounded-md bg-muted/80 dark:bg-muted/40" })]
			})
		]
	});
}
function InitPresenceStatusControl({ collapsed = false, isMobile = false }) {
	const { data: account } = useQuery(consoleAccountQueryOptions());
	const { participantStatus, isParticipantStatusUpdating, isReady, setParticipantStatus } = useInitPresence();
	const reduceMotion = useReducedMotion();
	const name = account?.name?.trim() || account?.email?.split("@")[0]?.trim() || "You";
	const handleStatusChange = (status) => {
		setParticipantStatus(status);
	};
	if (!account || !isReady) return /* @__PURE__ */ jsx(InitPresenceStatusSkeleton, {
		collapsed,
		isMobile
	});
	if (collapsed && !isMobile) {
		const isOnline = participantStatus === "online";
		return /* @__PURE__ */ jsxs(Popover, { children: [/* @__PURE__ */ jsxs(Tooltip, {
			delayDuration: 200,
			children: [/* @__PURE__ */ jsx(TooltipTrigger, {
				asChild: true,
				children: /* @__PURE__ */ jsx(PopoverTrigger, {
					asChild: true,
					children: /* @__PURE__ */ jsxs("button", {
						type: "button",
						className: cn("relative flex w-full items-center justify-center rounded-md py-2", "transition-colors hover:bg-accent/50", "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background"),
						"aria-label": `Your status: ${isOnline ? "Online" : "Offline"}. Open status settings`,
						children: [/* @__PURE__ */ jsxs("span", {
							className: "relative",
							children: [/* @__PURE__ */ jsx(InitialsAvatar, {
								name,
								size: "sm",
								className: "rounded-full"
							}), /* @__PURE__ */ jsx(motion.span, {
								className: cn("absolute -bottom-0.5 -end-0.5 size-2.5 rounded-full ring-2 ring-background", isOnline ? "bg-emerald-500" : "bg-muted-foreground/50"),
								animate: reduceMotion ? void 0 : { scale: [
									1,
									1.25,
									1
								] },
								transition: PRESENCE_STATUS_PULSE_TRANSITION,
								"aria-hidden": true
							}, isOnline ? "online" : "offline")]
						}), isParticipantStatusUpdating ? /* @__PURE__ */ jsx(Loader2, { className: "absolute end-1 top-1 size-3 animate-spin text-muted-foreground" }) : null]
					})
				})
			}), /* @__PURE__ */ jsxs(TooltipContent, {
				side: "right",
				sideOffset: 12,
				className: ONLINE_USER_TOOLTIP_CLASS$1,
				children: [/* @__PURE__ */ jsx("p", {
					className: "text-[13px] font-semibold text-foreground",
					children: isOnline ? "You are online" : "You are offline"
				}), /* @__PURE__ */ jsx("p", {
					className: "text-[12px] leading-relaxed text-muted-foreground/70",
					children: "Click to change your Init visibility"
				})]
			})]
		}), /* @__PURE__ */ jsx(PopoverContent, {
			side: "right",
			align: "end",
			sideOffset: 12,
			className: "w-[240px] border border-border bg-popover p-3 shadow-md",
			children: /* @__PURE__ */ jsx(PresenceStatusPanel, {
				name,
				participantStatus,
				isUpdating: isParticipantStatusUpdating,
				onStatusChange: handleStatusChange
			})
		})] });
	}
	return /* @__PURE__ */ jsxs("div", {
		className: cn("px-2.5 py-3", isMobile && "px-0 py-0"),
		children: [/* @__PURE__ */ jsx("p", {
			className: "mb-2 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground/70",
			children: "Your status"
		}), /* @__PURE__ */ jsx(PresenceStatusPanel, {
			name,
			participantStatus,
			isUpdating: isParticipantStatusUpdating,
			onStatusChange: handleStatusChange
		})]
	});
}
function InitPresenceThemeBar({ light, dark, collapsed = false, isMobile = false, reserveSpace = false }) {
	const { resolvedTheme, setTheme } = useTheme();
	const { syncPresenceTheme } = useInitPresence();
	const total = light + dark;
	if (collapsed && !isMobile) return null;
	if (total === 0 && !reserveSpace) return null;
	const shellClassName = cn("shrink-0 px-3 py-3", isMobile && "px-4");
	if (total === 0 && reserveSpace) return /* @__PURE__ */ jsx("div", {
		className: shellClassName,
		"aria-hidden": true,
		children: /* @__PURE__ */ jsx("div", {
			className: "pointer-events-none invisible rounded-lg border border-border bg-card/50 px-1.5 py-1.5",
			children: /* @__PURE__ */ jsxs("div", {
				className: "flex h-6 items-center gap-1",
				children: [
					/* @__PURE__ */ jsx("span", { className: "size-6 shrink-0" }),
					/* @__PURE__ */ jsx("span", { className: "h-2 min-w-0 flex-1 rounded-full" }),
					/* @__PURE__ */ jsx("span", { className: "size-6 shrink-0" })
				]
			})
		})
	});
	const lightPercent = light / total * 100;
	const darkPercent = 100 - lightPercent;
	const scoreLabel = `${Math.round(lightPercent)}% light · ${Math.round(darkPercent)}% dark (${light} · ${dark})`;
	const activeTheme = resolveInitPresenceTheme(resolvedTheme);
	const handleThemeChange = (theme) => {
		setTheme(theme);
		syncPresenceTheme(theme);
	};
	return /* @__PURE__ */ jsx("div", {
		className: shellClassName,
		children: /* @__PURE__ */ jsx("div", {
			className: "rounded-lg border border-border bg-card/50 px-1.5 py-1.5",
			children: /* @__PURE__ */ jsxs("div", {
				className: "flex items-center gap-1",
				children: [
					/* @__PURE__ */ jsx(Button, {
						type: "button",
						variant: "ghost",
						size: "icon",
						className: cn("size-6 shrink-0 rounded-md border border-border text-muted-foreground", activeTheme === "light" && "border-border bg-accent text-foreground"),
						"aria-label": "Light theme",
						"aria-pressed": activeTheme === "light",
						onClick: () => handleThemeChange("light"),
						children: /* @__PURE__ */ jsx(Sun, {
							className: "size-3",
							"aria-hidden": true
						})
					}),
					/* @__PURE__ */ jsxs(Tooltip, {
						delayDuration: 200,
						children: [/* @__PURE__ */ jsx(TooltipTrigger, {
							asChild: true,
							children: /* @__PURE__ */ jsxs("div", {
								className: "flex h-2 min-w-0 flex-1 cursor-default overflow-hidden rounded-full border border-border/80 outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background",
								role: "img",
								"aria-label": scoreLabel,
								tabIndex: 0,
								children: [/* @__PURE__ */ jsx("div", {
									className: "h-full bg-[#f4f4f5] transition-[width] duration-300 ease-out",
									style: { width: `${lightPercent}%` }
								}), /* @__PURE__ */ jsx("div", { className: "h-full flex-1 bg-[#27272a] transition-[width] duration-300 ease-out" })]
							})
						}), /* @__PURE__ */ jsx(TooltipContent, {
							side: "top",
							className: "text-[12px]",
							children: scoreLabel
						})]
					}),
					/* @__PURE__ */ jsx(Button, {
						type: "button",
						variant: "ghost",
						size: "icon",
						className: cn("size-6 shrink-0 rounded-md border border-border text-muted-foreground", activeTheme === "dark" && "border-border bg-accent text-foreground"),
						"aria-label": "Dark theme",
						"aria-pressed": activeTheme === "dark",
						onClick: () => handleThemeChange("dark"),
						children: /* @__PURE__ */ jsx(Moon, {
							className: "size-3",
							"aria-hidden": true
						})
					})
				]
			})
		})
	});
}
var REACTION_ICON_POP_TRANSITION = {
	duration: .5,
	ease: [
		.34,
		1.56,
		.64,
		1
	]
};
var REACTION_ICON_IDLE_TRANSITION = {
	duration: .55,
	ease: [
		.22,
		1,
		.36,
		1
	]
};
function OnlineUserReactionBadge({ reaction, animationKey }) {
	const reduceMotion = useReducedMotion();
	const Icon$1 = reaction.icon;
	return /* @__PURE__ */ jsxs(motion.span, {
		className: "reaction-badge relative ms-auto inline-flex size-5 shrink-0 items-center justify-center text-muted-foreground",
		initial: reduceMotion ? false : {
			scale: 0,
			opacity: 0,
			rotate: -28,
			y: 3
		},
		animate: reduceMotion ? {
			scale: 1,
			opacity: 1,
			rotate: 0,
			y: 0
		} : {
			scale: [
				0,
				1.35,
				.92,
				1.06,
				1
			],
			opacity: [
				0,
				1,
				1,
				1,
				1
			],
			rotate: [
				-28,
				14,
				-8,
				4,
				0
			],
			y: [
				3,
				-2,
				1,
				0,
				0
			]
		},
		exit: reduceMotion ? void 0 : {
			scale: 0,
			opacity: 0,
			rotate: 16,
			transition: { duration: .14 }
		},
		transition: REACTION_ICON_POP_TRANSITION,
		"aria-hidden": true,
		children: [!reduceMotion ? /* @__PURE__ */ jsx("span", {
			className: "pointer-events-none absolute inset-[-6px]",
			"aria-hidden": true,
			children: Array.from({ length: 6 }, (_, index) => /* @__PURE__ */ jsx(InitPresenceReactionSpark, {
				index,
				animationKey
			}, index))
		}) : null, /* @__PURE__ */ jsx(motion.span, {
			className: "relative z-[1]",
			animate: reduceMotion ? void 0 : {
				scale: [
					1,
					1.12,
					1
				],
				rotate: [
					0,
					-6,
					6,
					0
				]
			},
			transition: {
				...REACTION_ICON_IDLE_TRANSITION,
				repeat: Infinity,
				repeatDelay: 2.2
			},
			children: /* @__PURE__ */ jsx(Icon$1, { className: "size-3.5" })
		})]
	}, animationKey);
}
function OnlineUserReactionBadgeSlot({ reaction, animationKey }) {
	return /* @__PURE__ */ jsx(AnimatePresence, {
		initial: false,
		children: reaction && animationKey ? /* @__PURE__ */ jsx(OnlineUserReactionBadge, {
			reaction,
			animationKey
		}) : null
	});
}
var PRESENCE_LIST_EASE = [
	.22,
	1,
	.36,
	1
];
var PRESENCE_LIST_TRANSITION = {
	duration: .22,
	ease: PRESENCE_LIST_EASE
};
var PRESENCE_ACTIVITY_ENTER_TRANSITION = {
	duration: .22,
	ease: PRESENCE_LIST_EASE
};
var PRESENCE_ACTIVITY_EXIT_TRANSITION = {
	duration: .14,
	ease: PRESENCE_LIST_EASE
};
var PRESENCE_RING_PULSE_TRANSITION = {
	duration: .42,
	ease: PRESENCE_LIST_EASE
};
function buildPresenceStatusKey(user, presence) {
	return `${presence}:${user.isLive ? "live" : "idle"}`;
}
var ONLINE_USER_TOOLTIP_CLASS = "max-w-[min(280px,calc(100dvw-5rem))] border border-border bg-popover px-3 py-2.5 text-popover-foreground shadow-md [&_svg]:!hidden [&_.reaction-badge_svg]:!inline-block";
var ONLINE_USERS_LIST_CLASS = "cursor-default select-none";
var RAFFLE_WINNER_SURFACE_CLASS = "bg-[color-mix(in_srgb,var(--brand-cta)_14%,var(--card))]";
var RAFFLE_WINNER_AVATAR_CLASS = "bg-[color-mix(in_srgb,var(--brand-cta)_14%,var(--card))] text-foreground";
var PRESENCE_AVATAR_SHELL_CLASS = "relative z-[1] inline-flex shrink-0 rounded-full [transform:translateZ(0)] [backface-visibility:hidden]";
function sortUsersWithRaffleWinner(users, raffleWinnerId) {
	if (!raffleWinnerId) return users;
	const winner = users.find((user) => user.id === raffleWinnerId);
	if (!winner) return users;
	return [winner, ...users.filter((user) => user.id !== raffleWinnerId)];
}
function RaffleWinnerSparkles() {
	const reduceMotion = useReducedMotion();
	const sparkAnchors = useMemo(() => [
		{
			id: "a",
			left: "10%",
			top: "28%",
			sparkIndex: 0
		},
		{
			id: "b",
			left: "24%",
			top: "68%",
			sparkIndex: 2
		},
		{
			id: "c",
			left: "38%",
			top: "34%",
			sparkIndex: 4
		},
		{
			id: "d",
			left: "52%",
			top: "62%",
			sparkIndex: 1
		},
		{
			id: "e",
			left: "66%",
			top: "30%",
			sparkIndex: 3
		},
		{
			id: "f",
			left: "80%",
			top: "70%",
			sparkIndex: 5
		},
		{
			id: "g",
			left: "92%",
			top: "38%",
			sparkIndex: 0
		},
		{
			id: "h",
			left: "46%",
			top: "48%",
			sparkIndex: 2
		}
	], []);
	if (reduceMotion) return null;
	return /* @__PURE__ */ jsx("div", {
		className: "pointer-events-none absolute inset-0 overflow-hidden rounded-md",
		"aria-hidden": true,
		children: sparkAnchors.map((anchor) => /* @__PURE__ */ jsx("span", {
			className: "absolute size-7 -translate-x-1/2 -translate-y-1/2",
			style: {
				left: anchor.left,
				top: anchor.top
			},
			children: /* @__PURE__ */ jsx(InitPresenceReactionSpark, {
				index: anchor.sparkIndex,
				animationKey: `winner-${anchor.id}`
			})
		}, anchor.id))
	});
}
function OnlineUserTooltipDetails({ name, activity, selfUserId, userId, reactionPulse }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-1 text-start",
		children: [/* @__PURE__ */ jsx(OnlineUserName, {
			name,
			activity,
			reactionPulse,
			isSelf: Boolean(selfUserId && userId === selfUserId)
		}), /* @__PURE__ */ jsx(OnlineUserActivity, {
			activity,
			className: "text-muted-foreground/70"
		})]
	});
}
function OnlineUserName({ name, activity, reactionPulse, isSelf = false }) {
	const reaction = parseInitReactingActivity(activity);
	const animationKey = reaction ? isSelf && reactionPulse !== void 0 ? `${reaction.id}-${reactionPulse}` : reaction.id : null;
	return /* @__PURE__ */ jsxs("p", {
		className: "flex min-h-[18px] min-w-0 items-center gap-2 text-[13px] font-medium leading-snug text-foreground",
		children: [/* @__PURE__ */ jsx("span", {
			className: "min-w-0 flex-1 truncate",
			children: name
		}), /* @__PURE__ */ jsx("span", {
			className: "inline-flex size-5 shrink-0 items-center justify-center",
			children: /* @__PURE__ */ jsx(OnlineUserReactionBadgeSlot, {
				reaction,
				animationKey
			})
		})]
	});
}
function PresenceAvatar({ user, presence, size = "sm", isRaffleWinner = false }) {
	const reduceMotion = useReducedMotion();
	const statusKey = buildPresenceStatusKey(user, presence);
	return /* @__PURE__ */ jsx(motion.span, {
		className: PRESENCE_AVATAR_SHELL_CLASS,
		animate: reduceMotion ? void 0 : { scale: [
			1,
			1.14,
			1
		] },
		transition: PRESENCE_RING_PULSE_TRANSITION,
		"aria-hidden": true,
		children: /* @__PURE__ */ jsx(InitialsAvatar, {
			name: user.name,
			size,
			className: cn("rounded-full", isRaffleWinner && RAFFLE_WINNER_AVATAR_CLASS)
		})
	}, isRaffleWinner ? `winner-${statusKey}` : statusKey);
}
var ONLINE_USER_ACTIVITY_CLASS = "truncate text-[12px] font-normal leading-[16px] text-muted-foreground";
function OnlineUserActivity({ activity, className }) {
	const reduceMotion = useReducedMotion();
	const displayActivity = formatInitPresenceActivityDisplay(activity);
	const textClassName = cn(ONLINE_USER_ACTIVITY_CLASS, className);
	if (reduceMotion) return /* @__PURE__ */ jsx("p", {
		className: cn("h-4", textClassName),
		title: displayActivity,
		children: displayActivity
	});
	return /* @__PURE__ */ jsx("div", {
		className: "relative h-4 overflow-hidden",
		"aria-live": "polite",
		"aria-atomic": "true",
		children: /* @__PURE__ */ jsx(AnimatePresence, {
			initial: false,
			children: /* @__PURE__ */ jsx(motion.p, {
				initial: { opacity: 0 },
				animate: { opacity: 1 },
				exit: {
					opacity: 0,
					transition: PRESENCE_ACTIVITY_EXIT_TRANSITION
				},
				transition: PRESENCE_ACTIVITY_ENTER_TRANSITION,
				className: cn("absolute inset-x-0 top-0", textClassName),
				title: displayActivity,
				children: displayActivity
			}, displayActivity)
		})
	});
}
function OnlineUserRow({ user, presence, collapsed, isMobile = false, selfUserId, reactionPulse = 0, isRaffleWinner = false }) {
	const reduceMotion = useReducedMotion();
	const avatarSize = isMobile ? "md" : "sm";
	const isSelf = Boolean(selfUserId && user.id === selfUserId);
	const displayActivity = formatInitPresenceActivityDisplay(user.activity);
	const row = /* @__PURE__ */ jsxs("div", {
		className: cn("relative w-full rounded-md px-2.5 py-1.5 text-[13px] font-medium transition-colors duration-300", ONLINE_USERS_LIST_CLASS, isRaffleWinner ? cn(RAFFLE_WINNER_SURFACE_CLASS, "text-foreground") : "text-muted-foreground hover:bg-accent/50 hover:text-foreground", collapsed && !isMobile ? SECONDARY_SIDEBAR_NAV_LINK_COLLAPSED_CLASS : SECONDARY_SIDEBAR_NAV_LINK_GRID_CLASS, isMobile && "gap-x-3 px-3 py-2.5 text-[14px]"),
		children: [
			isRaffleWinner ? /* @__PURE__ */ jsx(RaffleWinnerSparkles, {}) : null,
			/* @__PURE__ */ jsx(PresenceAvatar, {
				user,
				presence,
				size: avatarSize,
				isRaffleWinner
			}),
			(!collapsed || isMobile) && /* @__PURE__ */ jsxs("div", {
				className: "relative z-[1] min-w-0 flex-1 text-start",
				children: [/* @__PURE__ */ jsx(OnlineUserName, {
					name: user.name,
					activity: user.activity,
					reactionPulse,
					isSelf
				}), /* @__PURE__ */ jsx("div", {
					className: "min-w-0 flex-1",
					children: /* @__PURE__ */ jsx(OnlineUserActivity, {
						activity: isRaffleWinner ? "Winner!" : user.activity,
						className: isRaffleWinner ? "text-muted-foreground" : void 0
					})
				})]
			})
		]
	});
	return /* @__PURE__ */ jsx(motion.div, {
		layout: !reduceMotion,
		initial: reduceMotion ? false : { opacity: 0 },
		animate: { opacity: 1 },
		exit: reduceMotion ? void 0 : { opacity: 0 },
		transition: {
			...PRESENCE_LIST_TRANSITION,
			layout: reduceMotion ? void 0 : {
				type: "spring",
				stiffness: 420,
				damping: 34,
				mass: .85
			}
		},
		children: collapsed && !isMobile ? /* @__PURE__ */ jsxs(Tooltip, {
			delayDuration: 200,
			children: [/* @__PURE__ */ jsx(TooltipTrigger, {
				asChild: true,
				children: /* @__PURE__ */ jsxs("button", {
					type: "button",
					className: cn("relative flex w-full cursor-default select-none items-center justify-center rounded-md px-0 py-1.5", "transition-colors duration-300", isRaffleWinner ? RAFFLE_WINNER_SURFACE_CLASS : "hover:bg-accent/50", "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background"),
					"aria-label": `${user.name}. ${isRaffleWinner ? "Winner!" : displayActivity}`,
					children: [isRaffleWinner ? /* @__PURE__ */ jsx(RaffleWinnerSparkles, {}) : null, /* @__PURE__ */ jsx(PresenceAvatar, {
						user,
						presence,
						size: avatarSize,
						isRaffleWinner
					})]
				})
			}), /* @__PURE__ */ jsx(TooltipContent, {
				side: "right",
				sideOffset: 12,
				className: ONLINE_USER_TOOLTIP_CLASS,
				children: /* @__PURE__ */ jsx(OnlineUserTooltipDetails, {
					name: user.name,
					activity: user.activity,
					userId: user.id,
					selfUserId,
					reactionPulse
				})
			})]
		}) : row
	});
}
function UserCategoryCount({ count }) {
	const reduceMotion = useReducedMotion();
	const countClassName = "shrink-0 text-[11px] font-medium tabular-nums text-muted-foreground/60";
	if (reduceMotion) return /* @__PURE__ */ jsx("span", {
		className: countClassName,
		children: count
	});
	return /* @__PURE__ */ jsx(motion.span, {
		initial: { opacity: 0 },
		animate: { opacity: 1 },
		transition: PRESENCE_ACTIVITY_ENTER_TRANSITION,
		className: countClassName,
		children: count
	}, count);
}
function UserCategory({ label, users, presence, collapsed, isMobile = false, selfUserId, reactionPulse, raffleWinnerId = null, infoTooltip }) {
	const t = useT();
	const reduceMotion = useReducedMotion();
	const sortedUsers = useMemo(() => sortUsersWithRaffleWinner(users, raffleWinnerId), [raffleWinnerId, users]);
	return /* @__PURE__ */ jsx(AnimatePresence, {
		initial: false,
		children: users.length > 0 ? /* @__PURE__ */ jsxs(motion.div, {
			initial: reduceMotion ? false : { opacity: 0 },
			animate: { opacity: 1 },
			exit: reduceMotion ? void 0 : { opacity: 0 },
			transition: PRESENCE_LIST_TRANSITION,
			className: "space-y-0.5",
			children: [(!collapsed || isMobile) && /* @__PURE__ */ jsxs("div", {
				className: cn("mb-1.5 flex items-center justify-between gap-2 px-2.5", isMobile && "px-3"),
				children: [/* @__PURE__ */ jsxs("div", {
					className: "flex min-w-0 items-center gap-1",
					children: [/* @__PURE__ */ jsx("p", {
						className: "text-[11px] font-medium uppercase tracking-wider text-muted-foreground/60",
						children: t(label)
					}), infoTooltip ? /* @__PURE__ */ jsxs(Tooltip, { children: [/* @__PURE__ */ jsx(TooltipTrigger, {
						asChild: true,
						children: /* @__PURE__ */ jsx("button", {
							type: "button",
							className: "inline-flex shrink-0 items-center justify-center rounded-sm text-muted-foreground/60 transition-colors hover:text-muted-foreground focus:outline-none focus-visible:ring-2 focus-visible:ring-ring",
							"aria-label": t(infoTooltip),
							children: /* @__PURE__ */ jsx(HelpCircle, { className: "size-3" })
						})
					}), /* @__PURE__ */ jsx(TooltipContent, {
						side: "top",
						sideOffset: 4,
						className: "max-w-[220px] text-[12px]",
						children: t(infoTooltip)
					})] }) : null]
				}), /* @__PURE__ */ jsx(UserCategoryCount, { count: users.length })]
			}), /* @__PURE__ */ jsx(AnimatePresence, {
				initial: false,
				mode: "popLayout",
				children: sortedUsers.map((user) => /* @__PURE__ */ jsx(OnlineUserRow, {
					user,
					presence,
					collapsed,
					isMobile,
					selfUserId,
					reactionPulse,
					isRaffleWinner: Boolean(raffleWinnerId && user.id === raffleWinnerId)
				}, user.id))
			})]
		}, label) : null
	});
}
function SkeletonPulse({ className, style }) {
	return /* @__PURE__ */ jsx(Skeleton, {
		className: cn("bg-muted/80 dark:bg-muted/40", className),
		style
	});
}
function OnlineUserRowSkeleton({ collapsed, isMobile = false, nameWidth, activityWidth, index = 0 }) {
	const reduceMotion = useReducedMotion();
	const avatarSize = isMobile ? "size-10" : "size-8";
	const staggerMs = index * 55;
	const content = collapsed && !isMobile ? /* @__PURE__ */ jsx("div", {
		className: "flex justify-center py-1.5",
		children: /* @__PURE__ */ jsx(SkeletonPulse, {
			className: cn("rounded-full", avatarSize),
			style: reduceMotion ? void 0 : { animationDelay: `${staggerMs}ms` }
		})
	}) : /* @__PURE__ */ jsxs("div", {
		className: cn("flex items-center gap-2.5 rounded-md px-2.5 py-1.5", isMobile && "gap-3 px-3 py-2.5"),
		children: [/* @__PURE__ */ jsx(SkeletonPulse, {
			className: cn("shrink-0 rounded-full ring-2 ring-background", avatarSize),
			style: reduceMotion ? void 0 : { animationDelay: `${staggerMs}ms` }
		}), /* @__PURE__ */ jsxs("div", {
			className: "min-w-0 flex-1 space-y-1.5",
			children: [/* @__PURE__ */ jsx(SkeletonPulse, {
				className: cn("h-3.5", nameWidth),
				style: reduceMotion ? void 0 : { animationDelay: `${staggerMs + 40}ms` }
			}), /* @__PURE__ */ jsx(SkeletonPulse, {
				className: cn("h-3", activityWidth),
				style: reduceMotion ? void 0 : { animationDelay: `${staggerMs + 80}ms` }
			})]
		})]
	});
	if (reduceMotion) return content;
	return /* @__PURE__ */ jsx("div", { children: content });
}
function UserCategorySkeleton({ labelWidth, rowCount, collapsed, isMobile = false, rowOffset = 0 }) {
	const nameWidths = [
		"w-[72%]",
		"w-[58%]",
		"w-[64%]",
		"w-[52%]"
	];
	const activityWidths = [
		"w-[88%]",
		"w-[76%]",
		"w-[82%]",
		"w-[70%]"
	];
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-0.5",
		children: [(!collapsed || isMobile) && /* @__PURE__ */ jsxs("div", {
			className: cn("mb-1.5 flex items-center justify-between gap-2 px-2.5", isMobile && "px-3"),
			children: [/* @__PURE__ */ jsx(SkeletonPulse, { className: cn("h-2.5", labelWidth) }), /* @__PURE__ */ jsx(SkeletonPulse, { className: "h-2.5 w-4 shrink-0 rounded-sm" })]
		}), Array.from({ length: rowCount }, (_, index) => /* @__PURE__ */ jsx(OnlineUserRowSkeleton, {
			collapsed,
			isMobile,
			nameWidth: nameWidths[(rowOffset + index) % nameWidths.length],
			activityWidth: activityWidths[(rowOffset + index) % activityWidths.length],
			index: rowOffset + index
		}, index))]
	});
}
function OnlineUsersListSkeletonView({ collapsed, isMobile = false }) {
	return /* @__PURE__ */ jsxs("div", {
		"aria-busy": "true",
		"aria-label": "Loading online participants",
		children: [collapsed && !isMobile ? /* @__PURE__ */ jsx("div", {
			className: "space-y-0.5",
			children: Array.from({ length: 5 }, (_, index) => /* @__PURE__ */ jsx(OnlineUserRowSkeleton, {
				collapsed: true,
				index
			}, index))
		}) : /* @__PURE__ */ jsxs("div", {
			className: "space-y-6",
			children: [/* @__PURE__ */ jsx(UserCategorySkeleton, {
				labelWidth: "w-16",
				rowCount: 4,
				collapsed,
				isMobile,
				rowOffset: 0
			}), /* @__PURE__ */ jsx(UserCategorySkeleton, {
				labelWidth: "w-24",
				rowCount: 2,
				collapsed,
				isMobile,
				rowOffset: 4
			})]
		}), /* @__PURE__ */ jsx("span", {
			className: "sr-only",
			children: "Loading online participants"
		})]
	});
}
var ONLINE_USERS_LIST_MIN_HEIGHT = "min-h-[240px]";
function OnlineUsersNavContent({ event, collapsed, isMobile = false, showPanel = false, isLoading = false, isAuthenticated = false, selfUserId, reactionPulse, raffleWinnerId = null }) {
	const t = useT();
	const hasUsers = event.onlineUsers.length > 0 || event.recentlyOnlineUsers.length > 0;
	const emptyMessage = isAuthenticated ? t("No one else online yet. You are connected.") : t("Sign in to join the event and see who is online.");
	return /* @__PURE__ */ jsx("div", {
		className: cn("relative", ONLINE_USERS_LIST_MIN_HEIGHT),
		children: showPanel && isLoading ? /* @__PURE__ */ jsx(OnlineUsersListSkeletonView, {
			collapsed,
			isMobile
		}) : showPanel && !hasUsers ? /* @__PURE__ */ jsx("p", {
			className: cn("px-2.5 text-[13px] text-muted-foreground", isMobile && "px-0", collapsed && !isMobile && "text-center text-[12px]"),
			children: collapsed && !isMobile ? "…" : emptyMessage
		}) : /* @__PURE__ */ jsxs("div", {
			className: cn("space-y-6", ONLINE_USERS_LIST_CLASS),
			children: [/* @__PURE__ */ jsx(UserCategory, {
				label: "Online now",
				users: event.onlineUsers,
				presence: "online",
				collapsed,
				isMobile,
				selfUserId,
				reactionPulse,
				raffleWinnerId,
				infoTooltip: "This feature is powered by Appwrite Realtime and Appwrite Presences."
			}), /* @__PURE__ */ jsx(UserCategory, {
				label: "Recently online",
				users: event.recentlyOnlineUsers,
				presence: "recent",
				collapsed,
				isMobile,
				selfUserId,
				reactionPulse
			})]
		})
	});
}
function OnlineUsersLoginCta({ collapsed = false, isMobile = false }) {
	const t = useT();
	if (collapsed && !isMobile) return /* @__PURE__ */ jsx("div", {
		className: "flex justify-center py-2",
		children: /* @__PURE__ */ jsxs(Tooltip, { children: [/* @__PURE__ */ jsx(TooltipTrigger, {
			asChild: true,
			children: /* @__PURE__ */ jsx(Button, {
				variant: "brandCta",
				size: "icon",
				className: "size-9",
				asChild: true,
				children: /* @__PURE__ */ jsx(Link, {
					to: "/sign-in",
					search: { redirect: "/init" },
					"aria-label": t("Sign in to join the event"),
					children: /* @__PURE__ */ jsx(LogIn, {
						className: "size-4",
						"aria-hidden": true
					})
				})
			})
		}), /* @__PURE__ */ jsx(TooltipContent, {
			side: "right",
			className: "text-[12px]",
			children: t("Sign in to join the event")
		})] })
	});
	return /* @__PURE__ */ jsxs("div", {
		className: cn("space-y-2 px-2.5 py-3", isMobile && "px-0 py-0"),
		children: [/* @__PURE__ */ jsx("p", {
			className: "text-[12px] leading-snug text-muted-foreground",
			children: t("Sign in to join the event")
		}), /* @__PURE__ */ jsx(Button, {
			variant: "brandCta",
			size: "sm",
			className: "h-8 w-full text-[12px]",
			asChild: true,
			children: /* @__PURE__ */ jsx(Link, {
				to: "/sign-in",
				search: { redirect: "/init" },
				children: t("Sign in")
			})
		})]
	});
}
function hasOnlineUsersNav(event, options) {
	if (options?.presenceEnabled) return true;
	return event.onlineUsers.length > 0 || event.recentlyOnlineUsers.length > 0;
}
function OnlineUsersNav({ event, mobileOpen, onMobileClose, showPanel = false }) {
	const [collapsed, setCollapsed] = useState(false);
	const [reactionPulse, setReactionPulse] = useState(0);
	const { data: account } = useQuery(consoleAccountQueryOptions());
	const { isReady: isPresenceReady, onlineThemeCounts } = useInitPresence();
	const raffle = useInitGiveawayRaffleContext();
	const isAuthenticated = Boolean(account);
	const isLoadingPresence = showPanel && isAuthenticated && !isPresenceReady;
	const selfUserId = account?.$id;
	if (!showPanel && !hasOnlineUsersNav(event)) return null;
	return /* @__PURE__ */ jsxs(TooltipProvider, {
		delayDuration: 200,
		skipDelayDuration: 0,
		children: [/* @__PURE__ */ jsxs("div", {
			className: cn("relative z-20 hidden h-full flex-shrink-0 @[1024px]:block", "transition-[width] duration-150 ease-out", collapsed ? "w-[60px]" : "w-[220px]"),
			children: [/* @__PURE__ */ jsxs("aside", {
				className: cn("flex h-full w-full flex-col overflow-hidden border-e border-border bg-background", "[transform:translateZ(0)] [backface-visibility:hidden]"),
				children: [/* @__PURE__ */ jsxs("div", {
					className: "flex min-h-0 flex-1 flex-col",
					children: [
						/* @__PURE__ */ jsx("nav", {
							className: cn("min-h-0 flex-1 overflow-y-auto px-3 py-4", ONLINE_USERS_LIST_CLASS),
							role: "navigation",
							"aria-label": "Online participants",
							children: /* @__PURE__ */ jsx(OnlineUsersNavContent, {
								event,
								collapsed,
								showPanel,
								isLoading: isLoadingPresence,
								isAuthenticated,
								selfUserId,
								reactionPulse,
								raffleWinnerId: raffle?.raffleWinnerId ?? null
							})
						}),
						showPanel && isAuthenticated ? /* @__PURE__ */ jsx(InitPresenceReactions, {
							eventId: event.id,
							collapsed,
							onReactionPulse: () => setReactionPulse((count) => count + 1)
						}) : null,
						showPanel && isAuthenticated ? /* @__PURE__ */ jsx(InitPresenceThemeBar, {
							light: onlineThemeCounts.light,
							dark: onlineThemeCounts.dark,
							collapsed,
							reserveSpace: !isPresenceReady
						}) : null
					]
				}), showPanel ? /* @__PURE__ */ jsx("div", {
					className: "shrink-0 border-t border-border bg-muted/20",
					children: isAuthenticated ? /* @__PURE__ */ jsx(InitPresenceStatusControl, { collapsed }) : /* @__PURE__ */ jsx(OnlineUsersLoginCta, { collapsed })
				}) : null]
			}), /* @__PURE__ */ jsx("button", {
				type: "button",
				onClick: () => setCollapsed(!collapsed),
				className: cn("absolute end-0 top-1/2 z-10 flex h-6 w-6 shrink-0 -translate-y-1/2 cursor-pointer items-center justify-center rounded-md border border-border bg-card text-muted-foreground transition-colors hover:bg-accent hover:text-foreground focus:outline-none focus-visible:ring-2 focus-visible:ring-ring", SIDEBAR_EDGE_TOGGLE_OVERFLOW),
				"aria-label": collapsed ? "Expand online panel" : "Collapse online panel",
				children: /* @__PURE__ */ jsx(ChevronLeft, { className: cn("h-3.5 w-3.5 transition-transform duration-200", collapsed && "rotate-180") })
			})]
		}), /* @__PURE__ */ jsxs("aside", {
			className: cn("fixed start-0 top-0 z-[130] flex h-[100dvh] max-h-[100dvh] w-[280px] flex-col overflow-hidden border-e border-border bg-background", "transition-transform duration-200 ease-out [backface-visibility:hidden]", "@[1024px]:hidden", mobileOpen ? "translate-x-0" : OFFCANVAS_START_CLOSED),
			role: "dialog",
			"aria-modal": "true",
			"aria-label": "Online participants",
			inert: !mobileOpen ? true : void 0,
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "flex h-14 items-center justify-between border-b border-border px-4",
					children: [/* @__PURE__ */ jsx("p", {
						className: "text-[14px] font-semibold text-foreground",
						children: "Who's online"
					}), /* @__PURE__ */ jsx("button", {
						type: "button",
						onClick: onMobileClose,
						className: "flex h-8 w-8 cursor-pointer items-center justify-center rounded-md text-muted-foreground transition-colors hover:bg-accent hover:text-foreground focus:outline-none focus-visible:ring-2 focus-visible:ring-ring",
						"aria-label": "Close online panel",
						children: /* @__PURE__ */ jsx(X, { className: "h-5 w-5" })
					})]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "flex min-h-0 flex-1 flex-col",
					children: [
						/* @__PURE__ */ jsx("nav", {
							className: cn("min-h-0 flex-1 overflow-y-auto px-4 py-4", ONLINE_USERS_LIST_CLASS),
							role: "navigation",
							"aria-label": "Mobile online participants",
							children: /* @__PURE__ */ jsx(OnlineUsersNavContent, {
								event,
								collapsed: false,
								isMobile: true,
								showPanel,
								isLoading: isLoadingPresence,
								isAuthenticated,
								selfUserId,
								reactionPulse,
								raffleWinnerId: raffle?.raffleWinnerId ?? null
							})
						}),
						showPanel && isAuthenticated ? /* @__PURE__ */ jsx(InitPresenceReactions, {
							eventId: event.id,
							isMobile: true,
							onReactionPulse: () => setReactionPulse((count) => count + 1)
						}) : null,
						showPanel && isAuthenticated ? /* @__PURE__ */ jsx(InitPresenceThemeBar, {
							light: onlineThemeCounts.light,
							dark: onlineThemeCounts.dark,
							isMobile: true,
							reserveSpace: !isPresenceReady
						}) : null
					]
				}),
				showPanel ? /* @__PURE__ */ jsx("div", {
					className: "shrink-0 border-t border-border bg-muted/20 px-4 py-3",
					children: isAuthenticated ? /* @__PURE__ */ jsx(InitPresenceStatusControl, { isMobile: true }) : /* @__PURE__ */ jsx(OnlineUsersLoginCta, { isMobile: true })
				}) : null
			]
		})]
	});
}
function InitPageContent({ baseEvent, account }) {
	const [commandCenterOpen, setCommandCenterOpen] = useState(false);
	const [onlineNavOpen, setOnlineNavOpen] = useState(false);
	const presence = useInitPresence();
	const raffle = useInitGiveawayRaffleContext();
	const communityGlobe = useInitCommunityGlobeData(baseEvent, { enabled: Boolean(baseEvent.presenceEnabled && !baseEvent.isRecapMode) });
	const event = useMemo(() => {
		if (!baseEvent.presenceEnabled) return baseEvent;
		return {
			...baseEvent,
			onlineUsers: presence.onlineUsers,
			recentlyOnlineUsers: presence.recentlyOnlineUsers,
			onlineCount: presence.onlineCount,
			othersOnlineCount: presence.othersOnlineCount
		};
	}, [baseEvent, presence]);
	useKeyboardShortcut("meta+k", () => setCommandCenterOpen(true), OPEN_COMMAND_CENTER_SHORTCUT_OPTIONS);
	useKeyboardShortcut("control+k", () => setCommandCenterOpen(true), OPEN_COMMAND_CENTER_SHORTCUT_OPTIONS);
	useEffect(() => {
		const frame = requestAnimationFrame(() => scrollToInitDayFromHash());
		return () => cancelAnimationFrame(frame);
	}, [event.days.length]);
	const showOnlineNav = !event.isRecapMode && hasOnlineUsersNav(event, { presenceEnabled: baseEvent.presenceEnabled });
	return /* @__PURE__ */ jsxs(Fragment, { children: [
		baseEvent.presenceEnabled && !baseEvent.isRecapMode && account ? /* @__PURE__ */ jsx(InitPresenceBaselineSync, { event: baseEvent }) : null,
		/* @__PURE__ */ jsx(ConsoleLayout, {
			header: { onCommandCenterOpen: () => setCommandCenterOpen(true) },
			leftSidebar: showOnlineNav ? {
				mobileOpen: onlineNavOpen,
				onMobileClose: () => setOnlineNavOpen(false),
				onMenuClick: () => setOnlineNavOpen(true),
				content: /* @__PURE__ */ jsx(OnlineUsersNav, {
					event,
					showPanel: true,
					mobileOpen: onlineNavOpen,
					onMobileClose: () => setOnlineNavOpen(false)
				})
			} : void 0,
			showFooter: true,
			children: /* @__PURE__ */ jsxs(InitTicketVideoRecordingProvider, { children: [
				event.isRecapMode ? /* @__PURE__ */ jsx(InitRecapBanner, { event }) : event.liveBanner ? /* @__PURE__ */ jsx(LiveBannerBar, { liveBanner: event.liveBanner }) : null,
				/* @__PURE__ */ jsx(EventHero, {
					event,
					liveBanner: event.liveBanner
				}),
				/* @__PURE__ */ jsx("div", {
					className: "mx-auto w-full max-w-7xl px-4 pb-8 pt-8 sm:px-6",
					children: /* @__PURE__ */ jsx(EventSchedule, {
						event,
						fullWidth: true
					})
				}),
				/* @__PURE__ */ jsx("div", {
					className: "border-t border-border",
					"aria-hidden": true
				}),
				/* @__PURE__ */ jsxs("div", {
					className: cn("relative overflow-hidden", INIT_TICKET_VIDEO_SURFACE_CLASS),
					children: [/* @__PURE__ */ jsx("div", {
						className: cn("pointer-events-none absolute inset-0", INIT_TICKET_VIDEO_DOT_PATTERN_CLASS),
						"aria-hidden": true
					}), /* @__PURE__ */ jsx("div", {
						className: "relative z-10 mx-auto w-full max-w-7xl px-4 pb-4 pt-2 sm:px-6 sm:pb-6",
						children: /* @__PURE__ */ jsx(InitTicketSection, {
							event,
							account
						})
					})]
				}),
				/* @__PURE__ */ jsx("div", {
					className: "border-t border-border",
					"aria-hidden": true
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "mx-auto w-full max-w-7xl space-y-8 px-4 pb-8 pt-8 sm:px-6 sm:pb-10",
					children: [/* @__PURE__ */ jsx(InitRecapIntro, { event }), /* @__PURE__ */ jsxs("div", {
						className: "grid gap-8 lg:grid-cols-[minmax(0,1fr)_320px] xl:grid-cols-[minmax(0,1fr)_360px]",
						children: [/* @__PURE__ */ jsx("div", {
							className: "space-y-6",
							children: event.days.map((day) => isLaunchEventDayLocked(day) ? /* @__PURE__ */ jsx(LockedDayDetailCard, {
								day,
								eventStartDate: event.startDate,
								currentDay: event.currentDay
							}, day.day) : /* @__PURE__ */ jsx(DayDetailCard, {
								event,
								day,
								scheduleItems: event.schedule.filter((item) => item.day === day.day),
								currentDay: event.currentDay,
								isRecapMode: event.isRecapMode
							}, day.day))
						}), /* @__PURE__ */ jsxs("div", {
							className: "flex flex-col gap-6 lg:sticky lg:top-20 lg:self-start",
							children: [/* @__PURE__ */ jsx(EventSchedulePanel, {
								event,
								embedded: true
							}), event.giveaway && !event.isRecapMode ? /* @__PURE__ */ jsx(GiveawayPromoCard, { giveaway: event.giveaway }) : null]
						})]
					})]
				}),
				/* @__PURE__ */ jsx(InitReleaseAvailabilityStage, { event }),
				/* @__PURE__ */ jsx(InitPrizesSection, { event }),
				event.presenceEnabled && !event.isRecapMode ? /* @__PURE__ */ jsx(InitGlobalCommunitySection, {
					countries: communityGlobe.countries,
					developerCount: communityGlobe.developerCount,
					isLive: communityGlobe.isLive,
					isAuthenticated: Boolean(account)
				}) : null,
				/* @__PURE__ */ jsxs("div", {
					className: "mx-auto w-full max-w-7xl space-y-8 px-4 pb-8 pt-8 sm:px-6 sm:pb-10",
					children: [/* @__PURE__ */ jsx(GetInvolvedCards, {
						event,
						account
					}), /* @__PURE__ */ jsx(InitPageCredits, {
						showPresenceCredit: Boolean(event.presenceEnabled && account),
						showGlobeCredit: Boolean(event.presenceEnabled && !event.isRecapMode)
					})]
				})
			] })
		}),
		event.presenceEnabled && account && !event.isRecapMode ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(InitReactionConfetti, { onlineUsers: event.onlineUsers }), raffle ? /* @__PURE__ */ jsx(InitGiveawayRaffleConfetti, { pulse: raffle.raffleWinnerPulse }) : null] }) : null,
		/* @__PURE__ */ jsx(CommandCenter, {
			open: commandCenterOpen,
			onOpenChange: setCommandCenterOpen,
			context: "account"
		})
	] });
}
function View() {
	const { mockInitCurrentDay } = useDebugOverrides();
	const baseEvent = useMemo(() => {
		const active = getActiveLaunchEvent();
		if (!active) return void 0;
		return applyInitEventVisibility(active, { mockCurrentDay: mockInitCurrentDay });
	}, [mockInitCurrentDay]);
	const { data: account, isSuccess: isAccountSuccess, isError: isAccountError } = useQuery(consoleAccountQueryOptions());
	const isAccountReady = isAccountSuccess || isAccountError || !hasLikelyConsoleSession();
	if (!baseEvent) return /* @__PURE__ */ jsx(InitEmptyState, {});
	return /* @__PURE__ */ jsx(InitPresenceProvider, {
		event: baseEvent,
		enabled: Boolean(baseEvent.presenceEnabled && !baseEvent.isRecapMode && isAccountReady),
		children: /* @__PURE__ */ jsx(InitGiveawayRaffleProvider, {
			event: baseEvent,
			children: /* @__PURE__ */ jsx(InitPageContent, {
				baseEvent,
				account
			})
		})
	});
}
function InitEmptyState() {
	const [commandCenterOpen, setCommandCenterOpen] = useState(false);
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(ConsoleLayout, {
		header: { onCommandCenterOpen: () => setCommandCenterOpen(true) },
		showFooter: true,
		children: /* @__PURE__ */ jsxs("div", {
			className: "mx-auto flex w-full max-w-7xl flex-col items-center justify-center px-4 py-24 sm:px-6",
			children: [/* @__PURE__ */ jsx("p", {
				className: "text-[15px] font-semibold text-foreground",
				children: "No launch events scheduled"
			}), /* @__PURE__ */ jsx("p", {
				className: "mt-2 text-[13px] text-muted-foreground",
				children: "Check back soon for the next Appwrite product launch week."
			})]
		})
	}), /* @__PURE__ */ jsx(CommandCenter, {
		open: commandCenterOpen,
		onOpenChange: setCommandCenterOpen,
		context: "account"
	})] });
}
function InitPage() {
	return /* @__PURE__ */ jsx(View, {});
}
export { InitPage as component };
