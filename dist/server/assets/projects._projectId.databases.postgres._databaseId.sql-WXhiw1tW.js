import { t as cn } from "./utils-DoqqkI3X.js";
import "./runtime-config-DK7G0iKr.js";
import "./debug-endpoint-BvungD5q.js";
import "./sdk-DjIJ_hjn.js";
import "./console-session-cookie-7RV5Zfr4.js";
import "./i18n-Db4baE06.js";
import "./ticket-layout-B97VGq99.js";
import "./ticket-types-BpqSrvYB.js";
import { n as useT } from "./translate-DZcqveGn.js";
import { i as getErrorMessage } from "./error-formatting-CL2hjGy5.js";
import "./console-projects-C0b0tMaH.js";
import "./constants-CL7SLzjY.js";
import "./constants-Dd6QzW31.js";
import { f as ROWS_DEFAULT_PAGE_SIZE } from "./constants-BDeF927R.js";
import { Pt as useOrganizationScopes } from "./organizations-BKtnlNrj.js";
import "./console-profiles-D__E5Kgi.js";
import { Ah as fetchPostgresTableAutocompleteColumns, Ig as usePostgresTableRows, Rh as fetchPostgresTablesPage, Xh as postgresTableAutocompleteColumnsQueryOptions, bg as usePostgresSavedQueries, kh as fetchPostgresSchemasPage, mg as useExplainPostgresSql, pg as useExecutePostgresSql } from "./hooks-BONwG3Mt.js";
import { Q as usePostgresSqlEditorHeight, Wt as MAX_SAVED_POSTGRES_QUERY_NAME_LENGTH, zt as MAX_POSTGRES_SQL_EDITOR_TAB_TITLE_LENGTH } from "./auth-BPuxYQAc.js";
import { R as useProject } from "./projects-BaTJenfQ.js";
import "./appwrite-id-L15yEGeF.js";
import "./databases-Dh0pwZ6h.js";
import "./database-specs-CBc802K0.js";
import { d as postgresNav, i as normalizePostgresTableRouteId, s as parsePostgresTableId } from "./postgres-database-routes-CyTsPbzl.js";
import { G as executionResultRows, K as formatPostgresQueryDurationMs, j as POSTGRES_SIDEBAR_LIST_PAGE_SIZE, q as formatPostgresSql } from "./database-row-inline-edits-CdyGeTxj.js";
import "./form-field-type-badge-C7qMzJo0.js";
import { F as POSTGRES_SQL_EDITOR_MIN_HEIGHT_PX, I as POSTGRES_SQL_RESULTS_MIN_HEIGHT_PX, N as POSTGRES_SQL_EDITOR_DEFAULT_HEIGHT_PX, P as POSTGRES_SQL_EDITOR_MAX_HEIGHT_PX, W as clampPostgresSqlEditorHeightPx } from "./resizable-layout-BVnWw80t.js";
import "./chart-interval-Dbrn19qD.js";
import "./is-marketing-page-dgx45Oqy.js";
import "./use-console-impersonation-revision-BiI0c7pX.js";
import "./affiliates-BOg1SHC6.js";
import "./social-stats-X1CQqP0k.js";
import "./cimd-CRIktQxf.js";
import "./account-applications-Bo8bAeE0.js";
import "./date-format-BD1j7PxK.js";
import "./invite-url-B18y3cBt.js";
import "./format-metric-6jsfxd5f.js";
import "./domains-Bfw8HsXF.js";
import "./parse-spec-DW3UGcrS.js";
import "./page-direction-CnacIIOa.js";
import { t as Button } from "./button-Bnm2QhOm.js";
import { t as Input } from "./input-yKHNPhDZ.js";
import "./select-BYGLGp-f.js";
import { a as openInNewWindow, i as openInNewTab, o as toPrettyJson, r as copyToClipboard, t as buildConsoleUrl } from "./context-menu-D55xedo-.js";
import "./CodeBlock-BGAzMP_K.js";
import "./WizardLayout-DWqXFGuX.js";
import { r as useAuth } from "./RequireAuth-DahioVQe.js";
import "./HeaderAlertBar-CK7Gy4sE.js";
import { t as Badge } from "./badge-L9aO6DfA.js";
import { t as Label } from "./label-D8nNLJBa.js";
import "./modal-auto-focus-BO41bKmA.js";
import { a as DialogHeader, n as DialogContent, o as DialogTitle, r as DialogDescription, t as Dialog } from "./dialog-CFWmsJbI.js";
import "./EmptyState-DZDwceHm.js";
import "./dropdown-menu-DH51wH-m.js";
import { t as Pagination } from "./Pagination-BDei8M4v.js";
import { n as AlertDescription, r as AlertTitle, t as Alert } from "./alert-BTaNwkUC.js";
import { a as ContextMenuSub, c as ContextMenuTrigger, i as ContextMenuSeparator, n as ContextMenuContent, o as ContextMenuSubContent, r as ContextMenuItem, s as ContextMenuSubTrigger, t as ContextMenu } from "./context-menu-Ca6WjjAw.js";
import { t as ContextMenuIcon } from "./ContextMenuIcon-DPnw7e0V.js";
import { n as usePlatform, t as useKeyboardShortcut } from "./use-keyboard-shortcuts-C2m0wYFf.js";
import { i as formatDisplayKeys } from "./display-DbRQIyxk.js";
import { n as ToggleGroupItem, t as ToggleGroup } from "./toggle-group-qQyCSBun.js";
import { i as TooltipTrigger, n as TooltipContent, r as TooltipProvider, t as Tooltip } from "./tooltip-DUssQZhw.js";
import { t as useConsoleProfile } from "./use-console-profile-DiUZxZ_6.js";
import { m as setBodyResizeDragActive } from "./horizontal-resize-BcegzCwH.js";
import { n as ConnectCodePanel } from "./ConnectCodeExample-Ujo3XkoF.js";
import { b as canSaveTeamFilters } from "./console-access-checks-BTMEOKcL.js";
import { t as CodeEditor } from "./CodeEditor-Z8DByNgB.js";
import { a as registerPostgresSqlEditorActions, i as openPostgresSqlJumpToTabPicker, r as isPostgresSqlMonacoFocused, t as getPostgresSqlEditorActions } from "./postgres-sql-editor-actions-C3EmLCXJ.js";
import { _ as POSTGRES_SQL_RUN_SHORTCUT_COMBOS, a as POSTGRES_SQL_EXPLAIN_SHORTCUT_RAW, b as POSTGRES_SQL_SAVE_SHORTCUT_RAW, c as POSTGRES_SQL_JUMP_TAB_PICKER_SHORTCUT_COMBOS, f as POSTGRES_SQL_NEXT_TAB_SHORTCUT_COMBOS, g as POSTGRES_SQL_REDO_SHORTCUT_RAW, i as POSTGRES_SQL_EXPLAIN_SHORTCUT_COMBOS, m as POSTGRES_SQL_PREV_TAB_SHORTCUT_COMBOS, o as POSTGRES_SQL_FORMAT_SHORTCUT_COMBOS, s as POSTGRES_SQL_FORMAT_SHORTCUT_RAW, t as POSTGRES_SQL_CLOSE_TAB_SHORTCUT_COMBOS, u as POSTGRES_SQL_NEW_TAB_SHORTCUT_COMBOS, v as POSTGRES_SQL_RUN_SHORTCUT_RAW, x as POSTGRES_SQL_UNDO_SHORTCUT_RAW, y as POSTGRES_SQL_SAVE_SHORTCUT_COMBOS } from "./postgres-sql-editor-shortcuts-CXAI529H.js";
import "./postgres-tab-route-loader-B--X8VvC.js";
import { t as Route$1 } from "./projects._projectId.databases.postgres._databaseId.sql-D-zMPyxu.js";
import "./RefreshButton-BA9lQ7jC.js";
import { t as ServiceHeader } from "./ServiceHeader-C9TuKD27.js";
import { r as useDatabaseOperationsLock } from "./DatabaseOperationsLockContext-B72U5wFZ.js";
import { t as ReadOnlyDataSpreadsheet } from "./ReadOnlyDataSpreadsheet-BZQrrEaV.js";
import { n as sortableAxisTransform, t as getAxisRestrictedDragModifiers } from "./dnd-modifiers-CiFEVwzL.js";
import { i as usePostgresSidebar, r as queryPreviewLabel } from "./PostgresSidebarContext-CiaD4ogj.js";
import { r as POSTGRES_SQL_EDITOR_SURFACE_CLASS, t as POSTGRES_RUN_QUERY_PLAY_ICON_CLASS } from "./postgres-chrome-B-StjUdY.js";
import { n as SqlWorkbenchPanelEmptyState, t as PostgresTableRowsEmptyState } from "./PostgresTableRowsEmptyState-CoN8qkVW.js";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useNavigate, useParams } from "@tanstack/react-router";
import { forwardRef, useCallback, useEffect, useImperativeHandle, useLayoutEffect, useMemo, useRef, useState } from "react";
import { useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { AlertCircle, Bookmark, Braces, ChevronUp, Copy, ExternalLink, FileJson, Key, LayoutGrid, ListTree, Loader2, Play, Plus, Redo2, Rows3, Settings, Square, Undo2, X } from "lucide-react";
import { DndContext, DragOverlay, KeyboardSensor, PointerSensor, closestCenter, useSensor, useSensors } from "@dnd-kit/core";
import { SortableContext, horizontalListSortingStrategy, sortableKeyboardCoordinates, useSortable } from "@dnd-kit/sortable";
function PostgresQueryResultsMeta({ title, rowCount, countLabel = "row", durationMs, truncated }) {
	const t = useT();
	const countText = `${rowCount.toLocaleString()} ${countLabel}${rowCount === 1 ? "" : "s"}`;
	return /* @__PURE__ */ jsxs("div", {
		className: "flex min-w-0 items-center justify-between gap-3",
		children: [/* @__PURE__ */ jsx("h3", {
			className: "min-w-0 truncate text-[13px] font-semibold text-foreground",
			children: title
		}), /* @__PURE__ */ jsxs("div", {
			className: "flex shrink-0 items-center gap-2",
			children: [/* @__PURE__ */ jsxs("p", {
				className: "flex items-center gap-x-2 text-[12px] text-muted-foreground",
				children: [/* @__PURE__ */ jsx("span", {
					className: "tabular-nums text-foreground/90",
					children: countText
				}), durationMs != null ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("span", {
					"aria-hidden": true,
					className: "text-muted-foreground/40",
					children: "·"
				}), /* @__PURE__ */ jsx("span", {
					className: "tabular-nums",
					children: formatPostgresQueryDurationMs(durationMs)
				})] }) : null]
			}), truncated ? /* @__PURE__ */ jsx(Badge, {
				variant: "warning",
				className: "text-[10px]",
				children: t("Truncated")
			}) : null]
		})]
	});
}
var WORKBENCH_SHORTCUT_OPTIONS = {
	ignoreInputs: false,
	capture: true,
	preventDefault: false
};
function useWorkbenchModShortcut(combos, handler, enabled) {
	const wrappedHandler = useCallback((event) => {
		if (!isPostgresSqlMonacoFocused()) return;
		event.preventDefault();
		event.stopPropagation();
		handler();
	}, [handler]);
	useKeyboardShortcut(combos[0], wrappedHandler, {
		...WORKBENCH_SHORTCUT_OPTIONS,
		enabled
	});
	useKeyboardShortcut(combos[1], wrappedHandler, {
		...WORKBENCH_SHORTCUT_OPTIONS,
		enabled
	});
}
function useWorkbenchCtrlShortcut(combo, handler, enabled) {
	useKeyboardShortcut(combo, useCallback((event) => {
		if (!isPostgresSqlMonacoFocused()) return;
		event.preventDefault();
		event.stopPropagation();
		handler();
	}, [handler]), {
		...WORKBENCH_SHORTCUT_OPTIONS,
		enabled
	});
}
function useWorkbenchModDigitShortcuts(selectTabByIndex, enabled) {
	useEffect(() => {
		if (!enabled) return;
		const handleKeyDown = (event) => {
			if (!isPostgresSqlMonacoFocused()) return;
			if (!event.metaKey && !event.ctrlKey) return;
			if (event.altKey || event.shiftKey) return;
			const digit = event.key >= "1" && event.key <= "9" ? Number(event.key) : null;
			if (!digit) return;
			event.preventDefault();
			event.stopPropagation();
			selectTabByIndex(digit);
		};
		window.addEventListener("keydown", handleKeyDown, { capture: true });
		return () => window.removeEventListener("keydown", handleKeyDown, { capture: true });
	}, [enabled, selectTabByIndex]);
}
function usePostgresSqlEditorShortcuts(actions, enabled = true) {
	const run = useCallback(() => {
		if (actions.canRun) actions.run();
	}, [actions]);
	const explain = useCallback(() => {
		if (actions.canExplain) actions.explain();
	}, [actions]);
	const save = useCallback(() => {
		if (actions.canSave) actions.save();
	}, [actions]);
	const format = useCallback(() => {
		if (actions.canFormat) actions.format();
	}, [actions]);
	const selectNextTab = useCallback(() => {
		if (actions.canSelectNextTab) actions.selectNextTab();
	}, [actions]);
	const selectPreviousTab = useCallback(() => {
		if (actions.canSelectPreviousTab) actions.selectPreviousTab();
	}, [actions]);
	const createTab = useCallback(() => {
		if (actions.canCreateTab) actions.createTab();
	}, [actions]);
	const closeTab = useCallback(() => {
		if (actions.canCloseTab) actions.closeTab();
	}, [actions]);
	const openJumpToTabPicker = useCallback(() => {
		if (actions.canJumpToTab) actions.openJumpToTabPicker();
	}, [actions]);
	const selectTabByIndex = useCallback((index) => {
		if (actions.canJumpToTab) actions.selectTabByIndex(index);
	}, [actions]);
	useWorkbenchModShortcut(POSTGRES_SQL_RUN_SHORTCUT_COMBOS, run, enabled);
	useWorkbenchModShortcut(POSTGRES_SQL_EXPLAIN_SHORTCUT_COMBOS, explain, enabled);
	useWorkbenchModShortcut(POSTGRES_SQL_SAVE_SHORTCUT_COMBOS, save, enabled);
	useWorkbenchModShortcut(POSTGRES_SQL_NEW_TAB_SHORTCUT_COMBOS, createTab, enabled);
	useWorkbenchModShortcut(POSTGRES_SQL_CLOSE_TAB_SHORTCUT_COMBOS, closeTab, enabled);
	useWorkbenchModShortcut(POSTGRES_SQL_JUMP_TAB_PICKER_SHORTCUT_COMBOS, openJumpToTabPicker, enabled);
	useWorkbenchCtrlShortcut(POSTGRES_SQL_NEXT_TAB_SHORTCUT_COMBOS[0], selectNextTab, enabled);
	useWorkbenchCtrlShortcut(POSTGRES_SQL_PREV_TAB_SHORTCUT_COMBOS[0], selectPreviousTab, enabled);
	useWorkbenchModDigitShortcuts(selectTabByIndex, enabled);
	const wrappedFormatHandler = useCallback((event) => {
		if (!isPostgresSqlMonacoFocused()) return;
		event.preventDefault();
		event.stopPropagation();
		format();
	}, [format]);
	useKeyboardShortcut(POSTGRES_SQL_FORMAT_SHORTCUT_COMBOS[0], wrappedFormatHandler, {
		...WORKBENCH_SHORTCUT_OPTIONS,
		enabled
	});
}
var HANDLE_CLASS = cn("relative z-[45] flex h-px w-full shrink-0 items-center justify-center bg-border", "after:absolute after:inset-x-0 after:top-1/2 after:h-2 after:w-full after:-translate-y-1/2", "before:pointer-events-none before:absolute before:inset-x-0 before:top-1/2 before:h-2 before:w-full before:-translate-y-1/2 before:bg-border before:opacity-0 before:transition-opacity", "hover:before:opacity-100 data-[resize-handle-state=drag]:before:opacity-100", "cursor-row-resize touch-none");
function clampEditorHeightInContainer(heightPx, containerHeightPx) {
	const clamped = clampPostgresSqlEditorHeightPx(heightPx);
	if (containerHeightPx <= 0) return clamped;
	const maxInContainer = Math.min(720, Math.max(120, containerHeightPx - 160));
	return Math.min(maxInContainer, Math.max(120, clamped));
}
function PostgresSqlEditorContainer({ editor, children, className }) {
	const t = useT();
	const containerRef = useRef(null);
	const editorPaneRef = useRef(null);
	const isDraggingRef = useRef(false);
	const dragStartYRef = useRef(0);
	const dragStartHeightRef = useRef(0);
	const latestHeightRef = useRef(0);
	const { account } = useAuth();
	const { heightPx: persistedHeightPx, persistEditorHeightPx } = usePostgresSqlEditorHeight(account);
	const [editorHeightPx, setEditorHeightPx] = useState(clampPostgresSqlEditorHeightPx(persistedHeightPx ?? 220));
	const [isDragging, setIsDragging] = useState(false);
	const getContainerHeight = useCallback(() => containerRef.current?.getBoundingClientRect().height ?? 0, []);
	const applyEditorHeight = useCallback((nextPx) => {
		const clamped = clampEditorHeightInContainer(nextPx, getContainerHeight());
		latestHeightRef.current = clamped;
		if (editorPaneRef.current) editorPaneRef.current.style.height = `${clamped}px`;
		return clamped;
	}, [getContainerHeight]);
	const handlePointerDown = useCallback((e) => {
		isDraggingRef.current = true;
		setIsDragging(true);
		dragStartYRef.current = e.clientY;
		dragStartHeightRef.current = editorPaneRef.current?.getBoundingClientRect().height ?? editorHeightPx;
		latestHeightRef.current = dragStartHeightRef.current;
		e.currentTarget.setPointerCapture(e.pointerId);
		setBodyResizeDragActive(true, "row-resize");
	}, [editorHeightPx]);
	const handlePointerMove = useCallback((e) => {
		if (!isDraggingRef.current) return;
		const delta = e.clientY - dragStartYRef.current;
		applyEditorHeight(dragStartHeightRef.current + delta);
	}, [applyEditorHeight]);
	const finishDrag = useCallback((e) => {
		if (!isDraggingRef.current) return;
		isDraggingRef.current = false;
		setIsDragging(false);
		setBodyResizeDragActive(false);
		if (e?.currentTarget.hasPointerCapture(e.pointerId)) try {
			e.currentTarget.releasePointerCapture(e.pointerId);
		} catch {}
		const nextPx = latestHeightRef.current;
		setEditorHeightPx(nextPx);
		persistEditorHeightPx(nextPx);
	}, [persistEditorHeightPx]);
	useEffect(() => {
		return () => {
			setBodyResizeDragActive(false);
		};
	}, []);
	useEffect(() => {
		if (!isDragging) return;
		const handleWindowPointerEnd = () => {
			finishDrag();
		};
		window.addEventListener("pointerup", handleWindowPointerEnd);
		window.addEventListener("pointercancel", handleWindowPointerEnd);
		return () => {
			window.removeEventListener("pointerup", handleWindowPointerEnd);
			window.removeEventListener("pointercancel", handleWindowPointerEnd);
		};
	}, [finishDrag, isDragging]);
	return /* @__PURE__ */ jsxs("div", {
		ref: containerRef,
		className: cn("flex h-full min-h-0 min-w-0 flex-1 flex-col", className),
		children: [
			/* @__PURE__ */ jsx("div", {
				ref: editorPaneRef,
				className: "flex shrink-0 flex-col overflow-hidden bg-background",
				style: { height: editorHeightPx },
				children: editor
			}),
			/* @__PURE__ */ jsx("div", {
				role: "separator",
				"aria-orientation": "horizontal",
				"aria-label": t("Resize SQL editor"),
				"data-resize-handle-state": isDragging ? "drag" : "idle",
				className: HANDLE_CLASS,
				onPointerDown: handlePointerDown,
				onPointerMove: handlePointerMove,
				onPointerUp: finishDrag,
				onPointerCancel: finishDrag
			}),
			/* @__PURE__ */ jsx("div", {
				className: "flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden",
				children
			})
		]
	});
}
var IDENT = String.raw`(?:"[^"]+"|[a-zA-Z_][\w$]*)`;
var TABLE_CONTEXT_PATTERN = /\b(?:FROM|JOIN|INTO|UPDATE|TABLE|ONLY)\s+$/i;
function unquoteIdentifier(identifier) {
	if (identifier.startsWith("\"") && identifier.endsWith("\"")) return identifier.slice(1, -1).replace(/""/g, "\"");
	return identifier;
}
function matchesPrefix(label, prefix) {
	if (!prefix) return true;
	return label.toLowerCase().startsWith(prefix.toLowerCase());
}
function parseQualifiedPrefix(text) {
	const tableColumnMatch = text.match(new RegExp(`(${IDENT}\\.${IDENT})\\.\\s*$`, "i"));
	if (tableColumnMatch) {
		const [schema, table] = tableColumnMatch[1].split(".").map(unquoteIdentifier);
		return {
			type: "columns",
			schema,
			table
		};
	}
	const schemaMatch = text.match(new RegExp(`(${IDENT})\\.\\s*$`, "i"));
	if (schemaMatch) return {
		type: "tables",
		schema: unquoteIdentifier(schemaMatch[1])
	};
	return null;
}
function addTableRefToken(refs, token) {
	if (!token) return;
	const cleaned = token.replace(/^["'`(]+|["'`)]+$/g, "").trim();
	if (cleaned) refs.add(cleaned);
}
function extractReferencedTableTokens(text) {
	const refs = /* @__PURE__ */ new Set();
	const fromMatch = text.match(/\bFROM\s+([\s\S]+?)(?=\bWHERE\b|\bGROUP\b|\bORDER\b|\bLIMIT\b|\bHAVING\b|\bUNION\b|$)/i);
	if (fromMatch) for (const part of fromMatch[1].split(",")) {
		const token = part.trim().split(/\s+/)[0];
		addTableRefToken(refs, token);
	}
	for (const joinMatch of text.matchAll(/\b(?:INNER\s+|LEFT\s+|RIGHT\s+|FULL\s+|CROSS\s+)?JOIN\s+((?:"[^"]+"|[\w.]+))/gi)) addTableRefToken(refs, joinMatch[1]);
	addTableRefToken(refs, text.match(/\bUPDATE\s+((?:"[^"]+"|[\w.]+))/i)?.[1]);
	return Array.from(refs);
}
function tablesNeedingColumns(textUntilPosition) {
	const qualified = parseQualifiedPrefix(textUntilPosition);
	if (qualified?.type === "columns") return [{
		schema: qualified.schema,
		table: qualified.table
	}];
	return [];
}
function schemaSuggestions(monaco, schemas, prefix, range) {
	return schemas.filter((schema) => matchesPrefix(schema, prefix)).map((schema) => ({
		label: schema,
		kind: monaco.languages.CompletionItemKind.Module,
		insertText: schema,
		detail: "schema",
		sortText: `1_${schema}`,
		range
	}));
}
function tableSuggestions(monaco, tables, prefix, range) {
	return tables.filter((row) => {
		const qualified = `${row.table_schema}.${row.table_name}`;
		return matchesPrefix(row.table_name, prefix) || matchesPrefix(qualified, prefix);
	}).map((row) => {
		const qualified = `${row.table_schema}.${row.table_name}`;
		const typeLabel = row.table_type === "VIEW" ? "view" : "table";
		return {
			label: row.table_name,
			kind: monaco.languages.CompletionItemKind.Struct,
			insertText: qualified,
			detail: `${row.table_schema} · ${typeLabel}`,
			sortText: `2_${qualified}`,
			range
		};
	});
}
function columnSuggestions(monaco, columns, prefix, range, tables, qualifyColumns) {
	const suggestions = [];
	const seen = /* @__PURE__ */ new Set();
	for (const table of tables) {
		const tableColumns = columns.filter((column) => column.table_schema === table.schema && column.table_name === table.table);
		for (const column of tableColumns) {
			const label = column.column_name;
			const insertText = qualifyColumns ? `${table.table}.${label}` : label;
			const dedupeKey = `${table.schema}.${table.table}.${label}`;
			if (seen.has(dedupeKey)) continue;
			if (!matchesPrefix(label, prefix) && !matchesPrefix(insertText, prefix)) continue;
			seen.add(dedupeKey);
			suggestions.push({
				label,
				kind: monaco.languages.CompletionItemKind.Field,
				insertText,
				detail: `${column.data_type} · ${table.schema}.${table.table}`,
				sortText: `3_${dedupeKey}`,
				range
			});
		}
	}
	return suggestions;
}
async function resolveReferencedTables(text, resolveTableRef) {
	const tokens = extractReferencedTableTokens(text);
	const resolved = [];
	const seen = /* @__PURE__ */ new Set();
	for (const token of tokens) {
		const table = await resolveTableRef(token);
		if (!table) continue;
		const key = `${table.schema}.${table.table}`;
		if (seen.has(key)) continue;
		seen.add(key);
		resolved.push(table);
	}
	return resolved;
}
async function buildSuggestions(monaco, resolvers, textUntilPosition, prefix, range) {
	const qualified = parseQualifiedPrefix(textUntilPosition);
	if (qualified?.type === "columns") return columnSuggestions(monaco, await resolvers.resolveTableColumns([{
		schema: qualified.schema,
		table: qualified.table
	}]), prefix, range, [{
		schema: qualified.schema,
		table: qualified.table
	}], false);
	if (qualified?.type === "tables") return tableSuggestions(monaco, await resolvers.searchTables(qualified.schema, prefix), prefix, range);
	if (TABLE_CONTEXT_PATTERN.test(textUntilPosition)) return tableSuggestions(monaco, await resolvers.searchTables(void 0, prefix), prefix, range);
	const explicitTables = tablesNeedingColumns(textUntilPosition);
	if (explicitTables.length > 0) return columnSuggestions(monaco, await resolvers.resolveTableColumns(explicitTables), prefix, range, explicitTables, false);
	const referencedTables = await resolveReferencedTables(textUntilPosition, resolvers.resolveTableRef);
	if (referencedTables.length > 0) {
		const columnItems = columnSuggestions(monaco, await resolvers.resolveTableColumns(referencedTables), prefix, range, referencedTables, referencedTables.length > 1);
		if (columnItems.length > 0) return columnItems;
	}
	const [schemas, tables] = await Promise.all([resolvers.searchSchemas(prefix), resolvers.searchTables(void 0, prefix)]);
	return [...schemaSuggestions(monaco, schemas, prefix, range), ...tableSuggestions(monaco, tables, prefix, range)];
}
function registerPostgresSqlCompletionProvider(monaco, getResolvers) {
	return monaco.languages.registerCompletionItemProvider("sql", {
		triggerCharacters: [
			".",
			" ",
			","
		],
		async provideCompletionItems(model, position) {
			const resolvers = getResolvers();
			const textUntilPosition = model.getValueInRange({
				startLineNumber: 1,
				startColumn: 1,
				endLineNumber: position.lineNumber,
				endColumn: position.column
			});
			const word = model.getWordUntilPosition(position);
			const range = {
				startLineNumber: position.lineNumber,
				endLineNumber: position.lineNumber,
				startColumn: word.startColumn,
				endColumn: position.column
			};
			return { suggestions: await buildSuggestions(monaco, resolvers, textUntilPosition, word.word, range) };
		}
	});
}
var AUTOCOMPLETE_RESULT_LIMIT = 50;
var completionDisposable = null;
var getCompletionResolvers = () => ({
	searchSchemas: async () => [],
	searchTables: async () => [],
	resolveTableColumns: async () => [],
	resolveTableRef: async () => null
});
function focusEditorInstance(editorInstance) {
	requestAnimationFrame(() => {
		editorInstance.focus();
	});
}
const PostgresSqlCodeEditor = forwardRef(function PostgresSqlCodeEditor$1({ projectId, databaseId, tabId, sql, onSqlChange, onUndoRedoStateChange }, ref) {
	const queryClient = useQueryClient();
	const resolversRef = useRef({
		searchSchemas: async () => [],
		searchTables: async () => [],
		resolveTableColumns: async () => [],
		resolveTableRef: async () => null
	});
	const editorRef = useRef(null);
	const undoRedoDisposeRef = useRef(null);
	const onUndoRedoStateChangeRef = useRef(onUndoRedoStateChange);
	useEffect(() => {
		onUndoRedoStateChangeRef.current = onUndoRedoStateChange;
	}, [onUndoRedoStateChange]);
	useImperativeHandle(ref, () => ({
		undo: () => {
			const model = editorRef.current?.getModel();
			if (!model?.canUndo()) return;
			model.undo();
		},
		redo: () => {
			const model = editorRef.current?.getModel();
			if (!model?.canRedo()) return;
			model.redo();
		}
	}));
	useEffect(() => {
		resolversRef.current = {
			searchSchemas: async (search) => {
				return (await fetchPostgresSchemasPage(projectId, databaseId, {
					search: search.trim() || void 0,
					page: 0,
					limit: AUTOCOMPLETE_RESULT_LIMIT
				})).schemas;
			},
			searchTables: async (schema, search) => {
				return (await fetchPostgresTablesPage(projectId, databaseId, {
					schema: schema?.trim() || void 0,
					search: search.trim() || void 0,
					page: 0,
					limit: AUTOCOMPLETE_RESULT_LIMIT
				})).tables;
			},
			resolveTableColumns: async (tables) => {
				const uniqueTables = Array.from(new Map(tables.map((table) => [`${table.schema}.${table.table}`, table])).values());
				return (await Promise.all(uniqueTables.map((table) => queryClient.fetchQuery({
					...postgresTableAutocompleteColumnsQueryOptions(projectId, databaseId, table.schema, table.table),
					queryFn: () => fetchPostgresTableAutocompleteColumns(projectId, databaseId, table.schema, table.table)
				})))).flat();
			},
			resolveTableRef: async (ref$1) => {
				const parts = ref$1.split(".").map((part) => part.replace(/^["']|["']$/g, "").replace(/""/g, "\""));
				if (parts.length === 2) return {
					schema: parts[0],
					table: parts[1]
				};
				if (parts.length !== 1) return null;
				const page = await fetchPostgresTablesPage(projectId, databaseId, {
					search: parts[0],
					page: 0,
					limit: 8
				});
				const matches = page.tables.filter((row) => row.table_name.toLowerCase() === parts[0].toLowerCase());
				if (matches.length === 0) return page.tables[0] ? {
					schema: page.tables[0].table_schema,
					table: page.tables[0].table_name
				} : null;
				const chosen = matches.find((row) => row.table_schema === "public") ?? matches[0];
				return {
					schema: chosen.table_schema,
					table: chosen.table_name
				};
			}
		};
		getCompletionResolvers = () => resolversRef.current;
	}, [
		databaseId,
		projectId,
		queryClient
	]);
	useEffect(() => {
		return () => {
			undoRedoDisposeRef.current?.();
			undoRedoDisposeRef.current = null;
			editorRef.current = null;
		};
	}, []);
	const attachUndoRedoListeners = useCallback((editorInstance) => {
		undoRedoDisposeRef.current?.();
		undoRedoDisposeRef.current = null;
		const refreshUndoRedoState = () => {
			queueMicrotask(() => {
				const model = editorInstance.getModel();
				onUndoRedoStateChangeRef.current?.({
					canUndo: model?.canUndo() ?? false,
					canRedo: model?.canRedo() ?? false
				});
			});
		};
		refreshUndoRedoState();
		const contentDispose = editorInstance.onDidChangeModelContent(refreshUndoRedoState);
		const modelDispose = editorInstance.onDidChangeModel(refreshUndoRedoState);
		undoRedoDisposeRef.current = () => {
			contentDispose.dispose();
			modelDispose.dispose();
			undoRedoDisposeRef.current = null;
		};
	}, []);
	useEffect(() => {
		const editorInstance = editorRef.current;
		if (!editorInstance) return;
		const frame = requestAnimationFrame(() => {
			attachUndoRedoListeners(editorInstance);
			focusEditorInstance(editorInstance);
		});
		return () => cancelAnimationFrame(frame);
	}, [attachUndoRedoListeners, tabId]);
	const handleEditorMount = useCallback((editorInstance, monaco) => {
		editorRef.current = editorInstance;
		attachUndoRedoListeners(editorInstance);
		if (!completionDisposable) completionDisposable = registerPostgresSqlCompletionProvider(monaco, () => getCompletionResolvers());
		editorInstance.updateOptions({
			quickSuggestions: {
				other: true,
				comments: false,
				strings: false
			},
			suggestOnTriggerCharacters: true,
			wordBasedSuggestions: "off"
		});
		editorInstance.addAction({
			id: `postgres-format-sql-${projectId}-${databaseId}`,
			label: "Format SQL",
			keybindings: [monaco.KeyMod.Shift | monaco.KeyMod.Alt | monaco.KeyCode.KeyF],
			run: () => {
				const actions = getPostgresSqlEditorActions();
				if (actions?.canFormat) actions.format();
			}
		});
		focusEditorInstance(editorInstance);
	}, [
		attachUndoRedoListeners,
		databaseId,
		projectId
	]);
	const handleEditorAreaMouseDown = useCallback(() => {
		requestAnimationFrame(() => {
			editorRef.current?.focus();
		});
	}, []);
	return /* @__PURE__ */ jsx("div", {
		className: "relative h-full min-h-0 flex-1 overflow-hidden",
		"data-postgres-sql-editor": true,
		onMouseDown: handleEditorAreaMouseDown,
		children: /* @__PURE__ */ jsx("div", {
			className: "absolute inset-0 overflow-hidden",
			children: /* @__PURE__ */ jsx(CodeEditor, {
				value: sql,
				onChange: onSqlChange,
				language: "sql",
				height: "100%",
				modelPath: `postgres-sql/${projectId}/${databaseId}/${tabId}`,
				className: cn("h-full rounded-none border-0", POSTGRES_SQL_EDITOR_SURFACE_CLASS),
				onEditorMount: handleEditorMount
			})
		})
	});
});
function ActionDivider() {
	return /* @__PURE__ */ jsx("div", {
		className: "mx-0.5 h-5 w-px shrink-0 bg-border/80",
		"aria-hidden": true
	});
}
function formatShortcutLabel(raw, isMac) {
	return formatDisplayKeys(raw, isMac).join("");
}
function ShortcutTooltip({ enabled, disabledReason, enabledLabel, children }) {
	return /* @__PURE__ */ jsxs(Tooltip, { children: [/* @__PURE__ */ jsx(TooltipTrigger, {
		asChild: true,
		children: /* @__PURE__ */ jsx("span", {
			className: "inline-flex",
			children
		})
	}), /* @__PURE__ */ jsx(TooltipContent, {
		side: "top",
		sideOffset: 6,
		className: "text-[12px]",
		children: enabled ? enabledLabel : disabledReason
	})] });
}
function SqlEditorActionBar({ canUndo, canRedo, canSave, canFormat, canRun, canExplain, isRunning, isExplaining, onUndo, onRedo, onSave, onFormat, onRun, onExplain, runDisabledTooltip, explainDisabledTooltip }) {
	const t = useT();
	const { isMac } = usePlatform();
	const undoShortcut = formatShortcutLabel(POSTGRES_SQL_UNDO_SHORTCUT_RAW, isMac);
	const redoShortcut = formatShortcutLabel(POSTGRES_SQL_REDO_SHORTCUT_RAW, isMac);
	const saveShortcut = formatShortcutLabel(POSTGRES_SQL_SAVE_SHORTCUT_RAW, isMac);
	const formatShortcut = formatShortcutLabel(POSTGRES_SQL_FORMAT_SHORTCUT_RAW, isMac);
	const explainShortcut = formatShortcutLabel(POSTGRES_SQL_EXPLAIN_SHORTCUT_RAW, isMac);
	const runShortcut = formatShortcutLabel(POSTGRES_SQL_RUN_SHORTCUT_RAW, isMac);
	return /* @__PURE__ */ jsx("div", {
		className: cn("shrink-0 border-t border-border px-4 py-2.5 sm:px-6", POSTGRES_SQL_EDITOR_SURFACE_CLASS),
		children: /* @__PURE__ */ jsxs("div", {
			className: "flex items-center justify-between gap-2",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex items-center gap-0.5",
				children: [/* @__PURE__ */ jsx(ShortcutTooltip, {
					enabled: canUndo,
					disabledReason: t("Nothing to undo."),
					enabledLabel: `${t("Undo")} (${undoShortcut})`,
					children: /* @__PURE__ */ jsx(Button, {
						type: "button",
						variant: "ghost",
						size: "sm",
						className: "h-8 w-8 p-0 text-muted-foreground hover:bg-muted/70 hover:text-foreground",
						onMouseDown: (event) => event.preventDefault(),
						onClick: onUndo,
						disabled: !canUndo,
						"aria-label": t("Undo"),
						children: /* @__PURE__ */ jsx(Undo2, { className: "h-3.5 w-3.5 shrink-0" })
					})
				}), /* @__PURE__ */ jsx(ShortcutTooltip, {
					enabled: canRedo,
					disabledReason: t("Nothing to redo."),
					enabledLabel: `${t("Redo")} (${redoShortcut})`,
					children: /* @__PURE__ */ jsx(Button, {
						type: "button",
						variant: "ghost",
						size: "sm",
						className: "h-8 w-8 p-0 text-muted-foreground hover:bg-muted/70 hover:text-foreground",
						onMouseDown: (event) => event.preventDefault(),
						onClick: onRedo,
						disabled: !canRedo,
						"aria-label": t("Redo"),
						children: /* @__PURE__ */ jsx(Redo2, { className: "h-3.5 w-3.5 shrink-0" })
					})
				})]
			}), /* @__PURE__ */ jsxs("div", {
				className: cn("flex items-center gap-1"),
				children: [
					/* @__PURE__ */ jsx(ShortcutTooltip, {
						enabled: canSave,
						disabledReason: t("Write SQL before saving a query."),
						enabledLabel: `${t("Save query")} (${saveShortcut})`,
						children: /* @__PURE__ */ jsxs(Button, {
							type: "button",
							variant: "ghost",
							size: "sm",
							className: "h-8 gap-1.5 px-2.5 text-[12px] font-medium text-muted-foreground hover:bg-muted/70 hover:text-foreground",
							onClick: onSave,
							disabled: !canSave,
							children: [/* @__PURE__ */ jsx(Bookmark, { className: "h-3.5 w-3.5 shrink-0" }), t("Save")]
						})
					}),
					/* @__PURE__ */ jsx(ActionDivider, {}),
					/* @__PURE__ */ jsx(ShortcutTooltip, {
						enabled: canFormat,
						disabledReason: t("Write SQL before formatting."),
						enabledLabel: `${t("Format SQL")} (${formatShortcut})`,
						children: /* @__PURE__ */ jsxs(Button, {
							type: "button",
							variant: "ghost",
							size: "sm",
							className: "h-8 gap-1.5 px-2.5 text-[12px] font-medium text-muted-foreground hover:bg-muted/70 hover:text-foreground",
							onClick: onFormat,
							disabled: !canFormat,
							children: [/* @__PURE__ */ jsx(Braces, { className: "h-3.5 w-3.5 shrink-0" }), t("Format")]
						})
					}),
					/* @__PURE__ */ jsx(ActionDivider, {}),
					/* @__PURE__ */ jsx(ShortcutTooltip, {
						enabled: canExplain,
						disabledReason: !canExplain && explainDisabledTooltip ? explainDisabledTooltip : isExplaining ? t("Query explanation is running.") : isRunning ? t("Query is running.") : t("Write SQL before explaining a query."),
						enabledLabel: `${t("Explain")} (${explainShortcut})`,
						children: /* @__PURE__ */ jsxs(Button, {
							type: "button",
							variant: "outline",
							size: "sm",
							className: "h-8 gap-1.5 px-3 text-[12px] font-medium",
							onClick: onExplain,
							disabled: !canExplain,
							children: [isExplaining ? /* @__PURE__ */ jsx(Loader2, { className: "h-3.5 w-3.5 shrink-0 animate-spin" }) : /* @__PURE__ */ jsx(ListTree, { className: "h-3.5 w-3.5 shrink-0" }), t("Explain")]
						})
					}),
					/* @__PURE__ */ jsx(ActionDivider, {}),
					/* @__PURE__ */ jsx(ShortcutTooltip, {
						enabled: canRun,
						disabledReason: !canRun && runDisabledTooltip ? runDisabledTooltip : isRunning ? t("Query is running.") : isExplaining ? t("Query explanation is running.") : t("Write SQL before running a query."),
						enabledLabel: `${t("Run")} (${runShortcut})`,
						children: /* @__PURE__ */ jsxs(Button, {
							type: "button",
							variant: "brandCta",
							size: "sm",
							className: "h-8 gap-1.5 px-3 text-[12px] font-semibold shadow-sm",
							onClick: onRun,
							disabled: !canRun,
							children: [isRunning ? /* @__PURE__ */ jsx(Loader2, { className: "h-3.5 w-3.5 shrink-0 animate-spin" }) : /* @__PURE__ */ jsx(Play, { className: POSTGRES_RUN_QUERY_PLAY_ICON_CLASS }), t("Run")]
						})
					})
				]
			})]
		})
	});
}
var TABLE_TABS = [
	{
		id: "rows",
		label: "Rows",
		path: "rows",
		icon: Rows3
	},
	{
		id: "columns",
		label: "Columns",
		path: "columns",
		icon: LayoutGrid
	},
	{
		id: "indexes",
		label: "Indexes",
		path: "indexes",
		icon: Key
	},
	{
		id: "settings",
		label: "Settings",
		path: "settings",
		icon: Settings
	}
];
function SqlEditorTabContextMenu({ projectId, databaseId, tab, canClose, hasOtherTabs, onSelectTab, onCloseTab, children }) {
	const t = useT();
	const navigate = useNavigate();
	const { duplicateQueryTab, closeOtherTabs } = usePostgresSidebar();
	const tableId = tab.tableId;
	const { table: tableName } = tableId ? parsePostgresTableId(tableId) : { table: tab.title };
	const tableNav = useMemo(() => tableId ? postgresNav({
		projectId,
		databaseId
	}).table({ tableId }) : null, [
		databaseId,
		projectId,
		tableId
	]);
	const tableRowsHref = useMemo(() => {
		if (!tableId) return null;
		return buildConsoleUrl(`/projects/${projectId}/databases/postgres/${databaseId}/tables/${encodeURIComponent(tableId)}/rows`);
	}, [
		databaseId,
		projectId,
		tableId
	]);
	const handleGoToTableTab = (path) => {
		if (!tableNav) return;
		if (path === "rows") {
			navigate({ ...tableNav.rows() });
			return;
		}
		if (path === "columns") {
			navigate({ ...tableNav.columns() });
			return;
		}
		if (path === "indexes") {
			navigate({ ...tableNav.indexes() });
			return;
		}
		navigate({ ...tableNav.settings() });
	};
	const handleCopyAsJson = () => {
		copyToClipboard("JSON", toPrettyJson({
			id: tab.id,
			title: tab.title,
			sql: tab.sql,
			...tab.tableId ? { tableId: tab.tableId } : {}
		}));
	};
	const handleDuplicate = () => {
		duplicateQueryTab(tab.sql);
	};
	const handleCloseOtherTabs = () => {
		closeOtherTabs(tab.id);
		onSelectTab(tab.id);
	};
	return /* @__PURE__ */ jsxs(ContextMenu, { children: [/* @__PURE__ */ jsx(ContextMenuTrigger, {
		asChild: true,
		children
	}), /* @__PURE__ */ jsxs(ContextMenuContent, {
		className: "w-52",
		children: [
			tableId && tableNav ? /* @__PURE__ */ jsxs(Fragment, { children: [TABLE_TABS.map(({ id, label, path, icon: Icon$1 }) => /* @__PURE__ */ jsxs(ContextMenuItem, {
				onSelect: () => handleGoToTableTab(path),
				children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Icon$1 }), label]
			}, id)), /* @__PURE__ */ jsx(ContextMenuSeparator, {})] }) : null,
			/* @__PURE__ */ jsxs(ContextMenuSub, { children: [/* @__PURE__ */ jsxs(ContextMenuSubTrigger, { children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Copy }), t("Copy")] }), /* @__PURE__ */ jsxs(ContextMenuSubContent, { children: [
				tableId ? /* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: () => void copyToClipboard("ID", tableId),
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Copy }), t("Copy ID")]
				}) : null,
				/* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: () => void copyToClipboard("Name", tableName),
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Copy }), t("Copy name")]
				}),
				tableRowsHref ? /* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: () => void copyToClipboard("Link", tableRowsHref),
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Copy }), t("Copy link")]
				}) : null,
				/* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: () => void copyToClipboard("SQL", tab.sql),
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Copy }), t("Copy SQL")]
				}),
				!tableId ? /* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: handleDuplicate,
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Copy }), t("Duplicate")]
				}) : null,
				/* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: handleCopyAsJson,
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: FileJson }), t("Copy as JSON")]
				})
			] })] }),
			tableRowsHref ? /* @__PURE__ */ jsxs(Fragment, { children: [
				/* @__PURE__ */ jsx(ContextMenuSeparator, {}),
				/* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: () => openInNewTab(tableRowsHref),
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: ExternalLink }), t("Open in new tab")]
				}),
				/* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: () => openInNewWindow(tableRowsHref),
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: Square }), t("Open in new window")]
				})
			] }) : null,
			canClose ? /* @__PURE__ */ jsxs(Fragment, { children: [
				/* @__PURE__ */ jsx(ContextMenuSeparator, {}),
				hasOtherTabs ? /* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: handleCloseOtherTabs,
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: X }), t("Close other tabs")]
				}) : null,
				/* @__PURE__ */ jsxs(ContextMenuItem, {
					onSelect: () => onCloseTab(tab.id),
					children: [/* @__PURE__ */ jsx(ContextMenuIcon, { icon: X }), t("Close")]
				})
			] }) : null
		]
	})] });
}
var tabDragModifiers = getAxisRestrictedDragModifiers("horizontal");
function SortableTab({ projectId, databaseId, tab, isActive, canClose, hasOtherTabs, onSelectTab, onCloseTab, onRenameTab }) {
	const t = useT();
	const [isEditing, setIsEditing] = useState(false);
	const [draftTitle, setDraftTitle] = useState(tab.title);
	const editInputRef = useRef(null);
	const { attributes, listeners, setNodeRef, setActivatorNodeRef, transform, transition, isDragging } = useSortable({
		id: tab.id,
		animateLayoutChanges: () => false,
		transition: null
	});
	const style = isDragging ? void 0 : {
		transform: sortableAxisTransform(transform, "horizontal"),
		transition
	};
	useEffect(() => {
		if (!isEditing) setDraftTitle(tab.title);
	}, [isEditing, tab.title]);
	useEffect(() => {
		if (!isEditing) return;
		editInputRef.current?.focus();
		editInputRef.current?.select();
	}, [isEditing]);
	const startEditing = () => {
		onSelectTab(tab.id);
		setDraftTitle(tab.title);
		setIsEditing(true);
	};
	const cancelEditing = () => {
		setDraftTitle(tab.title);
		setIsEditing(false);
	};
	const commitEditing = () => {
		const trimmed = draftTitle.trim();
		if (!trimmed || trimmed === tab.title) {
			cancelEditing();
			return;
		}
		onRenameTab(tab.id, trimmed);
		setIsEditing(false);
	};
	return /* @__PURE__ */ jsx("div", {
		ref: setNodeRef,
		style,
		"data-tab-id": tab.id,
		className: cn("shrink-0", isDragging && "opacity-0"),
		children: /* @__PURE__ */ jsx(SqlEditorTabContextMenu, {
			projectId,
			databaseId,
			tab,
			canClose,
			hasOtherTabs,
			onSelectTab,
			onCloseTab,
			children: /* @__PURE__ */ jsxs("div", {
				className: cn("group relative flex max-w-[220px] items-center rounded-t-md border-x border-t border-b-0 transition-opacity", isActive ? "z-[1] h-10 border-border border-b-background bg-background text-foreground shadow-[inset_0_1px_0_0_var(--border)]" : "h-10 border-border/45 text-muted-foreground/70 opacity-85 shadow-[inset_0_1px_0_0_color-mix(in_oklch,var(--border)_40%,transparent)] hover:opacity-100 hover:border-border/65 hover:bg-background/50 hover:text-muted-foreground"),
				children: [/* @__PURE__ */ jsx("button", {
					ref: setActivatorNodeRef,
					type: "button",
					onClick: () => {
						if (!isEditing) onSelectTab(tab.id);
					},
					className: cn("flex h-full min-w-0 flex-1 items-center px-2.5 text-start", !isEditing && "cursor-grab active:cursor-grabbing"),
					title: isEditing ? void 0 : tab.title,
					...isEditing ? {} : attributes,
					...isEditing ? {} : listeners,
					children: isEditing ? /* @__PURE__ */ jsx(Input, {
						ref: editInputRef,
						value: draftTitle,
						maxLength: 64,
						onChange: (event) => setDraftTitle(event.target.value),
						onBlur: commitEditing,
						onKeyDown: (event) => {
							if (event.key === "Enter") {
								event.preventDefault();
								commitEditing();
							}
							if (event.key === "Escape") {
								event.preventDefault();
								cancelEditing();
							}
						},
						onClick: (event) => event.stopPropagation(),
						onDoubleClick: (event) => event.stopPropagation(),
						onPointerDown: (event) => event.stopPropagation(),
						className: "h-6 min-h-6 max-h-6 w-full min-w-0 rounded-sm border-0 bg-transparent px-0 py-0 text-[12px] font-medium leading-6 shadow-none outline-none focus-visible:border-0 focus-visible:ring-1 focus-visible:ring-inset focus-visible:ring-ring/50 focus-visible:ring-offset-0",
						"aria-label": t("Rename query tab")
					}) : /* @__PURE__ */ jsx("span", {
						className: "truncate text-[12px] font-medium",
						onDoubleClick: (event) => {
							event.stopPropagation();
							event.preventDefault();
							startEditing();
						},
						children: tab.title
					})
				}), canClose ? /* @__PURE__ */ jsx("button", {
					type: "button",
					onClick: () => onCloseTab(tab.id),
					onPointerDown: (event) => event.stopPropagation(),
					className: cn("me-1 flex h-4 w-4 shrink-0 items-center justify-center rounded-sm text-muted-foreground transition-opacity hover:bg-muted hover:text-foreground", isActive ? "opacity-70 hover:opacity-100" : "opacity-0 group-hover:opacity-70 group-hover:hover:opacity-100"),
					"aria-label": `Close ${tab.title}`,
					children: /* @__PURE__ */ jsx(X, { className: "h-3 w-3" })
				}) : null]
			})
		})
	});
}
function TabPreview({ tab, isActive }) {
	return /* @__PURE__ */ jsx("div", {
		className: cn("flex max-w-[220px] items-center rounded-t-md border-x border-b-0 px-2.5 shadow-md", "h-10 border-border/75 bg-muted/30 text-muted-foreground shadow-[inset_0_1px_0_0_var(--border)]", isActive && "border-border border-b-background bg-background text-foreground"),
		children: /* @__PURE__ */ jsx("span", {
			className: "truncate text-[12px] font-medium",
			children: tab.title
		})
	});
}
function SqlEditorTabBar({ projectId, databaseId, tabs, activeTabId, onSelectTab, onCreateTab, onCloseTab, onReorderTabs, onRenameTab, headerCollapsed = false, onToggleHeaderCollapsed }) {
	const t = useT();
	const [activeDragTabId, setActiveDragTabId] = useState(null);
	const tabListRef = useRef(null);
	const canReorder = tabs.length > 1;
	const canClose = tabs.length > 1;
	useEffect(() => {
		const container = tabListRef.current;
		if (!container) return;
		const scrollActiveTabIntoView = () => {
			container.querySelector(`[data-tab-id="${activeTabId}"]`)?.scrollIntoView({
				block: "nearest",
				inline: "nearest"
			});
		};
		const frame = requestAnimationFrame(() => {
			scrollActiveTabIntoView();
			requestAnimationFrame(scrollActiveTabIntoView);
		});
		return () => cancelAnimationFrame(frame);
	}, [activeTabId, tabs.length]);
	const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 6 } }), useSensor(KeyboardSensor, { coordinateGetter: sortableKeyboardCoordinates }));
	const handleDragStart = (event) => {
		setActiveDragTabId(String(event.active.id));
	};
	const handleDragEnd = (event) => {
		setActiveDragTabId(null);
		const { active, over } = event;
		if (!over || active.id === over.id) return;
		onReorderTabs(String(active.id), String(over.id));
	};
	const activeDragTab = activeDragTabId ? tabs.find((tab) => tab.id === activeDragTabId) : void 0;
	const newTabButton = /* @__PURE__ */ jsx(Button, {
		type: "button",
		variant: "ghost",
		size: "icon",
		className: "mb-1.5 h-7 w-7 shrink-0 text-muted-foreground hover:text-foreground",
		onClick: onCreateTab,
		"aria-label": t("New query tab"),
		children: /* @__PURE__ */ jsx(Plus, { className: "h-3 w-3" })
	});
	const sortableTabs = /* @__PURE__ */ jsx("div", {
		className: "flex min-w-max items-end gap-1.5",
		children: tabs.map((tab) => /* @__PURE__ */ jsx(SortableTab, {
			projectId,
			databaseId,
			tab,
			isActive: tab.id === activeTabId,
			canClose,
			hasOtherTabs: canClose,
			onSelectTab,
			onCloseTab,
			onRenameTab
		}, tab.id))
	});
	const sortableTabsRegion = canReorder ? /* @__PURE__ */ jsxs(DndContext, {
		sensors,
		collisionDetection: closestCenter,
		modifiers: tabDragModifiers,
		onDragStart: handleDragStart,
		onDragEnd: handleDragEnd,
		onDragCancel: handleDragEnd,
		children: [/* @__PURE__ */ jsx(SortableContext, {
			items: tabs.map((tab) => tab.id),
			strategy: horizontalListSortingStrategy,
			children: sortableTabs
		}), /* @__PURE__ */ jsx(DragOverlay, {
			dropAnimation: null,
			children: activeDragTab ? /* @__PURE__ */ jsx(TabPreview, {
				tab: activeDragTab,
				isActive: activeDragTab.id === activeTabId
			}) : null
		})]
	}) : sortableTabs;
	return /* @__PURE__ */ jsxs("div", {
		className: cn("relative z-20 flex h-10 min-w-0 w-full shrink-0 items-stretch border-b border-border bg-background px-4 sm:px-6", headerCollapsed ? "mt-3 sm:mt-4" : "mt-2"),
		children: [/* @__PURE__ */ jsx("div", {
			ref: tabListRef,
			className: "flex min-w-0 flex-1 items-center overflow-x-auto overflow-y-hidden overscroll-x-contain [scrollbar-width:none] [&::-webkit-scrollbar]:hidden",
			children: /* @__PURE__ */ jsxs("div", {
				className: "flex min-w-max items-end gap-1.5 pe-1.5",
				children: [sortableTabsRegion, newTabButton]
			})
		}), onToggleHeaderCollapsed ? /* @__PURE__ */ jsx("div", {
			className: "relative z-[1] flex shrink-0 items-center bg-background",
			children: /* @__PURE__ */ jsx(TooltipProvider, {
				delayDuration: 0,
				children: /* @__PURE__ */ jsxs(Tooltip, { children: [/* @__PURE__ */ jsx(TooltipTrigger, {
					asChild: true,
					children: /* @__PURE__ */ jsx(Button, {
						type: "button",
						variant: "ghost",
						size: "icon",
						onClick: onToggleHeaderCollapsed,
						className: "h-7 w-7 shrink-0 text-muted-foreground hover:text-foreground",
						"aria-label": headerCollapsed ? t("Expand header") : t("Collapse header"),
						children: /* @__PURE__ */ jsx(ChevronUp, { className: cn("h-3 w-3 transition-transform duration-200", headerCollapsed && "rotate-180") })
					})
				}), /* @__PURE__ */ jsx(TooltipContent, {
					side: "bottom",
					children: /* @__PURE__ */ jsx("p", { children: headerCollapsed ? t("Expand header") : t("Collapse header") })
				})] })
			})
		}) : null]
	});
}
var planNodeId = 0;
function nextPlanNodeId() {
	planNodeId += 1;
	return `plan-node-${planNodeId}`;
}
function isPlainObject(value) {
	return typeof value === "object" && value !== null && !Array.isArray(value);
}
function tryParseJsonValue(value) {
	const trimmed = value.trim();
	if (!trimmed.startsWith("{") && !trimmed.startsWith("[")) return null;
	try {
		return JSON.parse(trimmed);
	} catch {
		return null;
	}
}
function isPsqlExplainWrapper(raw) {
	return /^QUERY PLAN\s*$/m.test(raw) || /\(\d+\s+rows?\)\s*$/im.test(raw);
}
function stripPsqlExplainLine(line) {
	return line.replace(/\s*\+\s*$/, "").replace(/^\s*\|/, "").replace(/\|\s*$/, "").trim();
}
function extractJsonFromPsqlExplainRaw(raw) {
	const trimmed = raw.trim();
	if (!trimmed) return null;
	const direct = tryParseJsonValue(trimmed);
	if (direct != null) return direct;
	const payload = trimmed.split("\n").map(stripPsqlExplainLine).filter((line) => {
		if (!line) return false;
		if (line === "QUERY PLAN") return false;
		if (/^[-─]+$/.test(line)) return false;
		if (/^\(\d+\s+rows?\)$/i.test(line)) return false;
		return true;
	}).join("");
	if (payload) {
		const parsed = tryParseJsonValue(payload);
		if (parsed != null) return parsed;
	}
	const start = trimmed.search(/[\[{]/);
	const end = Math.max(trimmed.lastIndexOf("]"), trimmed.lastIndexOf("}"));
	if (start >= 0 && end > start) return tryParseJsonValue(trimmed.slice(start, end + 1));
	return null;
}
function looksLikeTextExplainPlan(raw) {
	if (isPsqlExplainWrapper(raw)) return false;
	return /\bcost=[\d.]+\.\.[\d.]+/i.test(raw) && /(?:^|\n)\s*(?:->\s*)?(?:Limit|Seq Scan|Index(?: Only)? Scan|Bitmap|Hash Join|Merge Join|Nested Loop|Sort|Aggregate|Subquery Scan)\b/i.test(raw);
}
function resolveExplainPayload(explanation) {
	if (extractJsonPlanRoots(explanation.plan).length > 0) return explanation.plan ?? null;
	const raw = explanation.raw?.trim();
	if (!raw) return explanation.plan ?? null;
	return extractJsonFromPsqlExplainRaw(raw) ?? explanation.plan ?? null;
}
function classifyNodeType(nodeType) {
	const normalized = nodeType.toLowerCase();
	if (normalized.includes("index")) return "index";
	if (normalized.includes("scan")) return "scan";
	if (normalized.includes("join") || normalized.includes("nested loop") || normalized.includes("hash") || normalized.includes("merge")) return "join";
	if (normalized.includes("sort")) return "sort";
	if (normalized.includes("aggregate") || normalized.includes("group") || normalized.includes("materialize") || normalized.includes("limit")) return "aggregate";
	return "other";
}
function parseCostMetrics(text) {
	const metrics = [];
	const costMatch = text.match(/\(cost=([\d.]+)\.\.([\d.]+)(?:\s+rows=(\d+))?(?:\s+width=(\d+))?\)/i);
	if (costMatch) {
		metrics.push({
			label: "Cost",
			value: `${costMatch[1]}..${costMatch[2]}`
		});
		if (costMatch[3]) metrics.push({
			label: "Rows",
			value: costMatch[3]
		});
		if (costMatch[4]) metrics.push({
			label: "Width",
			value: costMatch[4]
		});
	}
	const actualTimeMatch = text.match(/actual time=([\d.]+)\.\.([\d.]+)\s+rows=(\d+)/i);
	if (actualTimeMatch) {
		metrics.push({
			label: "Time",
			value: `${actualTimeMatch[1]}..${actualTimeMatch[2]} ms`
		});
		metrics.push({
			label: "Actual rows",
			value: actualTimeMatch[3]
		});
	}
	return metrics;
}
function stripCostSuffix(text) {
	return text.replace(/\s*\([^)]*\)\s*$/, "").trim();
}
function isExplainMetaLine(text) {
	const trimmed = text.trim();
	return trimmed === "QUERY PLAN" || /^─+$/.test(trimmed) || trimmed.length === 0;
}
function isJsonExplainLine(text) {
	const trimmed = text.trim();
	return trimmed.startsWith("{") || trimmed.startsWith("[") || trimmed.startsWith("}") || trimmed.startsWith("]") || trimmed === "," || /^"[^"]+":/.test(trimmed);
}
function isNoiseExplainLine(text) {
	const trimmed = text.trim();
	if (!trimmed) return true;
	if (trimmed === "(" || trimmed === ")" || trimmed === "+" || trimmed === "[" || trimmed === "]") return true;
	if (/^[-─]+$/.test(trimmed)) return true;
	return isExplainMetaLine(trimmed) || isJsonExplainLine(trimmed);
}
function isDetailLine(text) {
	return /^(Filter|Hash Cond|Merge Cond|Join Filter|Sort Key|Group Key|Output|Batches|Rewritten|Index Cond|Recheck Cond|Sort Method|Sort Space|Planning Time|Execution Time):/i.test(text);
}
function parseExplainTextLine(line) {
	const arrowMatch = line.match(/^(\s*)->\s*(.*)$/);
	if (arrowMatch) return {
		indent: arrowMatch[1].length + 2,
		text: arrowMatch[2].trim()
	};
	const spaceMatch = line.match(/^(\s*)(.+)$/);
	return {
		indent: spaceMatch?.[1].length ?? 0,
		text: spaceMatch?.[2].trim() ?? ""
	};
}
function createTextPlanNode(text) {
	const metrics = parseCostMetrics(text);
	const label = stripCostSuffix(text);
	const nodeType = label.split(/\s+on\s+|\s+using\s+/i)[0]?.trim() || label;
	return {
		id: nextPlanNodeId(),
		label,
		nodeType,
		details: [],
		metrics,
		children: [],
		variant: isDetailLine(label) ? "detail" : classifyNodeType(nodeType)
	};
}
function parseExplainTextTree(raw) {
	const roots = [];
	const stack = [];
	for (const line of raw.split("\n")) {
		const { indent, text } = parseExplainTextLine(line);
		if (!text || isNoiseExplainLine(text)) continue;
		const node = createTextPlanNode(text);
		while (stack.length > 0 && stack[stack.length - 1].indent >= indent) stack.pop();
		if (stack.length === 0) roots.push(node);
		else stack[stack.length - 1].node.children.push(node);
		if (!isDetailLine(text)) stack.push({
			indent,
			node
		});
	}
	return roots;
}
function readJsonMetric(raw, keys) {
	for (const key of keys) {
		const value = raw[key];
		if (value !== void 0 && value !== null) return String(value);
	}
}
function buildJsonPlanMetrics(raw) {
	const metrics = [];
	const startupCost = readJsonMetric(raw, ["Startup Cost", "startup_cost"]);
	const totalCost = readJsonMetric(raw, ["Total Cost", "total_cost"]);
	if (startupCost && totalCost) metrics.push({
		label: "Cost",
		value: `${startupCost}..${totalCost}`
	});
	const planRows = readJsonMetric(raw, ["Plan Rows", "plan_rows"]);
	if (planRows) metrics.push({
		label: "Rows",
		value: planRows
	});
	const planWidth = readJsonMetric(raw, ["Plan Width", "plan_width"]);
	if (planWidth) metrics.push({
		label: "Width",
		value: planWidth
	});
	const actualTime = readJsonMetric(raw, ["Actual Total Time", "actual_total_time"]);
	if (actualTime) metrics.push({
		label: "Time",
		value: `${actualTime} ms`
	});
	const actualRows = readJsonMetric(raw, ["Actual Rows", "actual_rows"]);
	if (actualRows) metrics.push({
		label: "Actual rows",
		value: actualRows
	});
	const actualLoops = readJsonMetric(raw, ["Actual Loops", "actual_loops"]);
	if (actualLoops) metrics.push({
		label: "Loops",
		value: actualLoops
	});
	return metrics;
}
function buildJsonPlanDetails(raw) {
	const detailKeys = [
		"Filter",
		"Hash Cond",
		"Merge Cond",
		"Join Filter",
		"Sort Key",
		"Group Key",
		"Index Cond",
		"Recheck Cond",
		"Output"
	];
	const details = [];
	for (const key of detailKeys) {
		const value = raw[key];
		if (value !== void 0 && value !== null && String(value).trim()) details.push(`${key}: ${String(value)}`);
	}
	return details;
}
function buildJsonPlanLabel(raw) {
	const nodeType = String(raw["Node Type"] ?? raw.node_type ?? "Plan");
	const relation = raw["Relation Name"] ?? raw.relation_name;
	const schema = raw["Schema Name"] ?? raw.schema_name;
	const alias = raw.Alias ?? raw.alias;
	const indexName = raw["Index Name"] ?? raw.index_name;
	const joinType = raw["Join Type"] ?? raw.join_type;
	const parts = [joinType ? String(joinType) : null, nodeType];
	if (relation) {
		const qualified = schema ? `${schema}.${relation}` : String(relation);
		parts.push(`on ${qualified}`);
		if (alias && alias !== relation) parts.push(`(${alias})`);
	}
	if (indexName) parts.push(`using ${indexName}`);
	return {
		label: parts.filter(Boolean).join(" "),
		nodeType
	};
}
function parseJsonPlanNode(raw) {
	const { label, nodeType } = buildJsonPlanLabel(raw);
	const childPlans = Array.isArray(raw.Plans) ? raw.Plans : Array.isArray(raw.plans) ? raw.plans : [];
	return {
		id: nextPlanNodeId(),
		label,
		nodeType,
		details: buildJsonPlanDetails(raw),
		metrics: buildJsonPlanMetrics(raw),
		children: childPlans.filter(isPlainObject).map((child) => parseJsonPlanNode(child)),
		variant: classifyNodeType(nodeType)
	};
}
function normalizePlanInput(plan) {
	if (!plan) return [];
	if (typeof plan === "string") {
		const parsed = tryParseJsonValue(plan);
		return parsed != null ? normalizePlanInput(parsed) : [];
	}
	if (Array.isArray(plan)) return plan.filter(isPlainObject);
	if (isPlainObject(plan)) {
		if (isPlainObject(plan.Plan)) return [plan];
		if (plan["Node Type"] || plan.node_type) return [plan];
		const queryPlan = plan["QUERY PLAN"];
		if (queryPlan !== void 0) return normalizePlanInput(queryPlan);
		return [plan];
	}
	return [];
}
function extractJsonPlanRoots(plan) {
	const roots = [];
	for (const item of normalizePlanInput(plan)) {
		if (isPlainObject(item.Plan)) {
			roots.push(parseJsonPlanNode(item.Plan));
			continue;
		}
		if (item["Node Type"] || item.node_type) roots.push(parseJsonPlanNode(item));
	}
	return roots;
}
function formatRawPlanForDisplay(explanation) {
	const payload = resolveExplainPayload(explanation);
	if (payload != null) try {
		return JSON.stringify(payload, null, 2);
	} catch {}
	const raw = explanation.raw?.trim();
	if (raw && looksLikeTextExplainPlan(raw)) return raw;
	return raw ?? "";
}
function normalizePostgresQueryPlan(explanation) {
	planNodeId = 0;
	const structured = extractJsonPlanRoots(explanation.plan);
	if (structured.length > 0) return structured;
	const payload = resolveExplainPayload(explanation);
	if (payload != null) {
		const jsonRoots = extractJsonPlanRoots(payload);
		if (jsonRoots.length > 0) return jsonRoots;
	}
	const raw = explanation.raw?.trim();
	if (raw && looksLikeTextExplainPlan(raw)) return parseExplainTextTree(raw);
	return [];
}
function formatPostgresQueryPlanRaw(explanation) {
	return formatRawPlanForDisplay(explanation);
}
function countPostgresQueryPlanNodes(nodes) {
	return nodes.reduce((total, node) => total + 1 + countPostgresQueryPlanNodes(node.children), 0);
}
var VARIANT_BADGE = {
	index: "success",
	scan: "warning",
	join: "info",
	sort: "info",
	aggregate: "info",
	detail: "secondary",
	other: "secondary"
};
var VARIANT_GUIDE = {
	index: "bg-emerald-500/30",
	scan: "bg-amber-500/30",
	join: "bg-slate-500/30",
	sort: "bg-blue-500/30",
	aggregate: "bg-violet-500/30",
	detail: "bg-border/70",
	other: "bg-border/70"
};
function PlanNodeRow({ node, depth = 0 }) {
	if (node.variant === "detail") return /* @__PURE__ */ jsx("p", {
		className: "font-mono text-[11px] leading-relaxed text-muted-foreground",
		children: node.label
	});
	return /* @__PURE__ */ jsxs("div", {
		className: "min-w-0",
		children: [/* @__PURE__ */ jsx("div", {
			className: "rounded-xl border border-border/60 bg-card/40 px-3.5 py-3 shadow-sm",
			children: /* @__PURE__ */ jsxs("div", {
				className: "flex flex-col gap-2.5 lg:flex-row lg:items-start lg:justify-between",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "min-w-0 flex-1 space-y-2",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "flex min-w-0 flex-wrap items-center gap-2",
						children: [/* @__PURE__ */ jsx(Badge, {
							variant: VARIANT_BADGE[node.variant],
							className: "text-[10px] shrink-0",
							children: node.nodeType
						}), /* @__PURE__ */ jsx("p", {
							className: "min-w-0 break-words text-[13px] font-medium leading-snug text-foreground",
							children: node.label
						})]
					}), node.details.length > 0 ? /* @__PURE__ */ jsx("div", {
						className: "space-y-1 rounded-lg bg-muted/25 px-2.5 py-2",
						children: node.details.map((detail) => /* @__PURE__ */ jsx("p", {
							className: "font-mono text-[11px] leading-relaxed text-muted-foreground",
							children: detail
						}, detail))
					}) : null]
				}), node.metrics.length > 0 ? /* @__PURE__ */ jsx("div", {
					className: "flex shrink-0 flex-wrap gap-1.5 lg:justify-end",
					children: node.metrics.map((metric) => /* @__PURE__ */ jsxs("span", {
						className: "inline-flex items-center gap-1.5 rounded-md bg-muted/40 px-2 py-1 text-[11px] text-muted-foreground",
						children: [/* @__PURE__ */ jsx("span", {
							className: "font-medium uppercase tracking-wide",
							children: metric.label
						}), /* @__PURE__ */ jsx("span", {
							className: "tabular-nums text-foreground/90",
							children: metric.value
						})]
					}, `${metric.label}-${metric.value}`))
				}) : null]
			})
		}), node.children.length > 0 ? /* @__PURE__ */ jsxs("div", {
			className: cn("relative mt-2.5 space-y-2.5", depth > 0 ? "ms-3 ps-4" : "ms-2 ps-4"),
			children: [/* @__PURE__ */ jsx("div", {
				className: cn("absolute bottom-2 start-0 top-2 w-px", VARIANT_GUIDE[node.variant]),
				"aria-hidden": true
			}), node.children.map((child) => /* @__PURE__ */ jsx(PlanNodeRow, {
				node: child,
				depth: depth + 1
			}, child.id))]
		}) : null]
	});
}
function PostgresQueryPlanView({ explanation, isLoading = false, loadingLabel = "Explaining query…", className }) {
	const t = useT();
	const [viewMode, setViewMode] = useState("visual");
	const planNodes = useMemo(() => explanation ? normalizePostgresQueryPlan(explanation) : [], [explanation]);
	const nodeCount = countPostgresQueryPlanNodes(planNodes);
	const rawPlan = useMemo(() => explanation ? formatPostgresQueryPlanRaw(explanation) : "", [explanation]);
	const rawLanguage = rawPlan.trimStart().startsWith("{") || rawPlan.trimStart().startsWith("[") ? "json" : "sql";
	return /* @__PURE__ */ jsxs("div", {
		className: cn("flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden", className),
		children: [/* @__PURE__ */ jsx("div", {
			className: "shrink-0 border-b border-border px-4 py-3 sm:px-6",
			children: /* @__PURE__ */ jsxs("div", {
				className: "flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between",
				children: [/* @__PURE__ */ jsx(PostgresQueryResultsMeta, {
					title: t("Query plan"),
					rowCount: nodeCount,
					countLabel: "step"
				}), /* @__PURE__ */ jsxs(ToggleGroup, {
					type: "single",
					variant: "outline",
					size: "sm",
					value: viewMode,
					onValueChange: (value) => {
						if (value === "visual" || value === "raw") setViewMode(value);
					},
					className: "shrink-0",
					children: [/* @__PURE__ */ jsx(ToggleGroupItem, {
						value: "visual",
						className: "h-8 px-3 text-[12px]",
						children: t("Visual")
					}), /* @__PURE__ */ jsx(ToggleGroupItem, {
						value: "raw",
						className: "h-8 px-3 text-[12px]",
						children: t("Raw")
					})]
				})]
			})
		}), /* @__PURE__ */ jsx("div", {
			className: "min-h-0 flex-1 overflow-y-auto px-4 py-4 sm:px-6",
			children: isLoading && !explanation ? /* @__PURE__ */ jsxs("div", {
				className: "flex h-full min-h-[12rem] items-center justify-center gap-2 text-[13px] text-muted-foreground",
				children: [/* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin" }), loadingLabel]
			}) : viewMode === "raw" ? rawPlan ? /* @__PURE__ */ jsx(ConnectCodePanel, {
				code: rawPlan,
				language: rawLanguage,
				headless: true,
				wrapLines: true,
				className: "min-h-[12rem] rounded-lg border border-border"
			}) : /* @__PURE__ */ jsx(SqlWorkbenchPanelEmptyState, { variant: "query-no-rows" }) : planNodes.length > 0 ? /* @__PURE__ */ jsx("div", {
				className: "space-y-3",
				children: planNodes.map((node) => /* @__PURE__ */ jsx(PlanNodeRow, { node }, node.id))
			}) : rawPlan ? /* @__PURE__ */ jsx(ConnectCodePanel, {
				code: rawPlan,
				language: rawLanguage,
				headless: true,
				wrapLines: true,
				className: "min-h-[12rem] rounded-lg border border-border"
			}) : /* @__PURE__ */ jsx(SqlWorkbenchPanelEmptyState, { variant: "query-no-rows" })
		})]
	});
}
function SavePostgresQueryDialog({ open, onOpenChange, databaseId, sql, account, teamId, canSaveTeam }) {
	const t = useT();
	const [name, setName] = useState("");
	const [level, setLevel] = useState("user");
	const { setPanel, setSavedQueryLevel, selectSavedQuery, savedQueryLevel } = usePostgresSidebar();
	const { addSavedQuery, isAdding, hasTeamLevel } = usePostgresSavedQueries(databaseId, account, teamId);
	useEffect(() => {
		if (!open) return;
		setName(queryPreviewLabel(sql));
		setLevel("user");
	}, [open, sql]);
	useEffect(() => {
		if (!canSaveTeam && level === "team") setLevel("user");
	}, [canSaveTeam, level]);
	const handleSave = async () => {
		const trimmedName = name.trim();
		if (!trimmedName || isAdding) return;
		if (level === "team" && !canSaveTeam) return;
		try {
			const savedLevel = hasTeamLevel ? level : "user";
			const savedQuery = await addSavedQuery({
				name: trimmedName,
				sql,
				level: savedLevel
			});
			toast.success(savedLevel === "team" ? "Query saved for team" : "Query saved for you");
			if (savedLevel !== savedQueryLevel) setSavedQueryLevel(savedLevel);
			if (savedQuery) selectSavedQuery(savedLevel, savedQuery);
			setPanel("queries");
			onOpenChange(false);
		} catch (error) {
			toast.error(getErrorMessage(error));
		}
	};
	return /* @__PURE__ */ jsx(Dialog, {
		open,
		onOpenChange,
		children: /* @__PURE__ */ jsxs(DialogContent, {
			className: "sm:max-w-md p-0",
			children: [
				/* @__PURE__ */ jsxs(DialogHeader, {
					className: "px-6 pt-6 pb-4 text-start",
					children: [/* @__PURE__ */ jsx(DialogTitle, { children: t("Save query") }), /* @__PURE__ */ jsx(DialogDescription, {
						className: "text-[13px] mt-2",
						children: t("Save this SQL query to reopen it later from the Queries panel.")
					})]
				}),
				/* @__PURE__ */ jsx("div", { className: "border-t border-border" }),
				/* @__PURE__ */ jsxs("div", {
					className: "space-y-4 px-6 py-4",
					children: [hasTeamLevel ? /* @__PURE__ */ jsxs("div", {
						className: "space-y-2",
						children: [/* @__PURE__ */ jsx(Label, {
							className: "text-[13px]",
							children: t("Save for")
						}), /* @__PURE__ */ jsxs("div", {
							className: "flex overflow-hidden rounded-md border border-border",
							children: [/* @__PURE__ */ jsx("button", {
								type: "button",
								onClick: () => setLevel("user"),
								className: cn("flex h-9 flex-1 cursor-pointer items-center justify-center text-[12px] transition-colors", level === "user" ? "bg-muted text-foreground" : "text-muted-foreground hover:bg-muted/60"),
								"aria-pressed": level === "user",
								children: t("For me")
							}), /* @__PURE__ */ jsxs(Tooltip, { children: [/* @__PURE__ */ jsx(TooltipTrigger, {
								asChild: true,
								children: /* @__PURE__ */ jsx("button", {
									type: "button",
									onClick: () => canSaveTeam && setLevel("team"),
									disabled: !canSaveTeam,
									className: cn("flex h-9 flex-1 cursor-pointer items-center justify-center border-s border-border text-[12px] transition-colors disabled:cursor-not-allowed", level === "team" ? "bg-muted text-foreground" : "text-muted-foreground hover:bg-muted/60 disabled:opacity-50"),
									"aria-pressed": level === "team",
									children: t("For team")
								})
							}), !canSaveTeam ? /* @__PURE__ */ jsx(TooltipContent, {
								side: "top",
								sideOffset: 4,
								children: t("Only owners and developers can save team queries.")
							}) : null] })]
						})]
					}) : null, /* @__PURE__ */ jsxs("div", {
						className: "space-y-2",
						children: [/* @__PURE__ */ jsx(Label, {
							htmlFor: "postgres-query-name",
							className: "text-[13px]",
							children: t("Query name")
						}), /* @__PURE__ */ jsx(Input, {
							id: "postgres-query-name",
							value: name,
							onChange: (event) => setName(event.target.value),
							onKeyDown: (event) => {
								if (event.key === "Enter") {
									event.preventDefault();
									handleSave();
								}
							},
							className: "h-9 text-[13px]",
							maxLength: 64,
							disabled: isAdding
						})]
					})]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "px-6 py-4 border-t border-border bg-muted/30 flex flex-col-reverse gap-2 sm:flex-row sm:justify-end",
					children: [/* @__PURE__ */ jsx(Button, {
						type: "button",
						variant: "outline",
						onClick: () => onOpenChange(false),
						disabled: isAdding,
						children: t("Cancel")
					}), /* @__PURE__ */ jsx(Button, {
						type: "button",
						onClick: () => void handleSave(),
						disabled: !name.trim() || isAdding,
						children: t("Save")
					})]
				})
			]
		})
	});
}
function SqlWorkbench({ projectId, databaseId, tabs, activeTabId, sql, onSqlChange, onSelectTab, onCreateTab, onCloseTab, onReorderTabs, onRenameTab, onRun, onExplain, isRunning, isExplaining, error, result, explainResult, resultKind = "query", account, teamId, canSaveTeam, canRun: operationsEnabled = true, runDisabledTooltip, canExplain: explainEnabled = true, explainDisabledTooltip, children }) {
	const t = useT();
	const [saveDialogOpen, setSaveDialogOpen] = useState(false);
	const [isHeaderCollapsed, setIsHeaderCollapsed] = useState(false);
	const [canUndo, setCanUndo] = useState(false);
	const [canRedo, setCanRedo] = useState(false);
	const sqlEditorRef = useRef(null);
	const hasSql = !!sql.trim();
	const isBusy = isRunning || isExplaining;
	const canRunQuery = operationsEnabled && !isBusy && hasSql;
	const canExplainQuery = explainEnabled && !isBusy && hasSql;
	const canSaveQuery = hasSql;
	const canFormatQuery = hasSql;
	const errorMessage = error ? getErrorMessage(error) : null;
	const errorTitle = resultKind === "explain" ? "Explain failed" : "Query failed";
	const loadingLabel = isExplaining ? "Explaining query…" : "Running query…";
	const resultRows = useMemo(() => result ? executionResultRows(result) : [], [result]);
	const resultColumns = useMemo(() => {
		if (result?.columns?.length) return result.columns.map((column) => ({
			key: column.name,
			label: column.name
		}));
		const first = resultRows[0];
		return first ? Object.keys(first).map((key) => ({
			key,
			label: key
		})) : [];
	}, [result?.columns, resultRows]);
	const showQueryResults = result != null || explainResult != null || isBusy;
	const showExplainResults = isExplaining || resultKind === "explain" && !isRunning;
	const canSwitchTabs = tabs.length > 1;
	const canCloseTab = tabs.length > 1;
	const tabRefs = useMemo(() => tabs.map((tab) => ({
		id: tab.id,
		title: tab.title
	})), [tabs]);
	const selectNextTab = useCallback(() => {
		if (!canSwitchTabs) return;
		const currentIndex = tabs.findIndex((tab) => tab.id === activeTabId);
		if (currentIndex < 0) return;
		onSelectTab(tabs[(currentIndex + 1) % tabs.length].id);
	}, [
		activeTabId,
		canSwitchTabs,
		onSelectTab,
		tabs
	]);
	const selectPreviousTab = useCallback(() => {
		if (!canSwitchTabs) return;
		const currentIndex = tabs.findIndex((tab) => tab.id === activeTabId);
		if (currentIndex < 0) return;
		onSelectTab(tabs[(currentIndex - 1 + tabs.length) % tabs.length].id);
	}, [
		activeTabId,
		canSwitchTabs,
		onSelectTab,
		tabs
	]);
	const selectTabByIndex = useCallback((index) => {
		if (tabs.length === 0) return;
		const targetIndex = index === 9 ? tabs.length - 1 : index - 1;
		if (targetIndex < 0 || targetIndex >= tabs.length) return;
		onSelectTab(tabs[targetIndex].id);
	}, [onSelectTab, tabs]);
	const selectTab = useCallback((tabId) => {
		if (tabs.some((tab) => tab.id === tabId)) onSelectTab(tabId);
	}, [onSelectTab, tabs]);
	const closeActiveTab = useCallback(() => {
		if (!canCloseTab) return;
		onCloseTab(activeTabId);
	}, [
		activeTabId,
		canCloseTab,
		onCloseTab
	]);
	useEffect(() => {
		setCanUndo(false);
		setCanRedo(false);
	}, [activeTabId]);
	const editorActions = useMemo(() => ({
		canUndo,
		canRedo,
		canSave: canSaveQuery,
		canFormat: canFormatQuery,
		canRun: canRunQuery,
		canExplain: canExplainQuery,
		canCreateTab: true,
		canCloseTab,
		canSelectNextTab: canSwitchTabs,
		canSelectPreviousTab: canSwitchTabs,
		canJumpToTab: tabs.length > 0,
		tabs: tabRefs,
		undo: () => sqlEditorRef.current?.undo(),
		redo: () => sqlEditorRef.current?.redo(),
		save: () => setSaveDialogOpen(true),
		format: () => onSqlChange(formatPostgresSql(sql)),
		run: onRun,
		explain: onExplain,
		createTab: onCreateTab,
		closeTab: closeActiveTab,
		selectNextTab,
		selectPreviousTab,
		selectTabByIndex,
		selectTab,
		openJumpToTabPicker: openPostgresSqlJumpToTabPicker
	}), [
		canCloseTab,
		canExplainQuery,
		canFormatQuery,
		canRedo,
		canRunQuery,
		canSaveQuery,
		canSwitchTabs,
		canUndo,
		closeActiveTab,
		onCreateTab,
		onExplain,
		onRun,
		onSqlChange,
		selectNextTab,
		selectPreviousTab,
		selectTab,
		selectTabByIndex,
		sql,
		tabRefs,
		tabs.length
	]);
	useEffect(() => {
		registerPostgresSqlEditorActions(editorActions);
		return () => registerPostgresSqlEditorActions(null);
	}, [editorActions]);
	usePostgresSqlEditorShortcuts(editorActions);
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsxs(PostgresSqlEditorContainer, {
		className: "h-full min-h-0 flex-1",
		editor: /* @__PURE__ */ jsxs("div", {
			className: "relative flex h-full min-h-0 flex-col",
			"data-postgres-sql-workbench": true,
			children: [/* @__PURE__ */ jsx("div", {
				className: "relative z-20 shrink-0 overflow-hidden bg-background",
				children: /* @__PURE__ */ jsx(ServiceHeader, {
					title: t("SQL editor"),
					fullWidthBorder: true,
					fullWidth: true,
					hideTitle: isHeaderCollapsed,
					contentAfterBorder: /* @__PURE__ */ jsx(SqlEditorTabBar, {
						projectId,
						databaseId,
						tabs,
						activeTabId,
						onSelectTab,
						onCreateTab,
						onCloseTab,
						onReorderTabs,
						onRenameTab,
						headerCollapsed: isHeaderCollapsed,
						onToggleHeaderCollapsed: () => setIsHeaderCollapsed((collapsed) => !collapsed)
					})
				})
			}), /* @__PURE__ */ jsxs("div", {
				className: cn("flex min-h-0 flex-1 flex-col overflow-hidden", POSTGRES_SQL_EDITOR_SURFACE_CLASS),
				children: [/* @__PURE__ */ jsx(PostgresSqlCodeEditor, {
					ref: sqlEditorRef,
					projectId,
					databaseId,
					tabId: activeTabId,
					sql,
					onSqlChange,
					onUndoRedoStateChange: ({ canUndo: nextCanUndo, canRedo: nextCanRedo }) => {
						setCanUndo(nextCanUndo);
						setCanRedo(nextCanRedo);
					}
				}), /* @__PURE__ */ jsx(SqlEditorActionBar, {
					canUndo,
					canRedo,
					onUndo: () => sqlEditorRef.current?.undo(),
					onRedo: () => sqlEditorRef.current?.redo(),
					canSave: canSaveQuery,
					canFormat: canFormatQuery,
					canRun: canRunQuery,
					canExplain: canExplainQuery,
					isRunning,
					isExplaining,
					onSave: () => setSaveDialogOpen(true),
					onFormat: () => onSqlChange(formatPostgresSql(sql)),
					onRun,
					onExplain,
					runDisabledTooltip: !operationsEnabled ? runDisabledTooltip : void 0,
					explainDisabledTooltip: !explainEnabled ? explainDisabledTooltip : void 0
				})]
			})]
		}),
		children: [errorMessage ? /* @__PURE__ */ jsx("div", {
			className: "shrink-0 border-b border-border px-4 py-3 sm:px-6",
			children: /* @__PURE__ */ jsxs(Alert, { children: [
				/* @__PURE__ */ jsx(AlertCircle, { className: "h-4 w-4 text-muted-foreground" }),
				/* @__PURE__ */ jsx(AlertTitle, { children: errorTitle }),
				/* @__PURE__ */ jsx(AlertDescription, {
					className: "text-[13px]",
					children: errorMessage
				})
			] })
		}) : null, showQueryResults ? showExplainResults ? /* @__PURE__ */ jsx(PostgresQueryPlanView, {
			className: "min-h-0 flex-1",
			explanation: explainResult,
			isLoading: isExplaining && !explainResult,
			loadingLabel
		}) : /* @__PURE__ */ jsx(ReadOnlyDataSpreadsheet, {
			className: "min-h-0 flex-1",
			variant: "studio",
			showRowNumbers: true,
			enableColumnResize: true,
			columns: resultColumns,
			rows: resultRows,
			getRowKey: (_, index) => `sql-result-${index}`,
			isLoading: isBusy && !result,
			loadingLabel,
			emptyContent: /* @__PURE__ */ jsx(SqlWorkbenchPanelEmptyState, { variant: "query-no-rows" }),
			header: result ? /* @__PURE__ */ jsx(PostgresQueryResultsMeta, {
				title: t("Query results"),
				rowCount: result.rowCount,
				durationMs: result.durationMs,
				truncated: result.truncated
			}) : void 0
		}) : /* @__PURE__ */ jsx("div", {
			className: "flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden",
			children
		})]
	}), /* @__PURE__ */ jsx(SavePostgresQueryDialog, {
		open: saveDialogOpen,
		onOpenChange: setSaveDialogOpen,
		databaseId,
		sql,
		account,
		teamId,
		canSaveTeam
	})] });
}
function PostgresSqlWorkbench({ databaseId, routeTableId }) {
	return /* @__PURE__ */ jsx(PostgresSqlWorkbenchContent, {
		databaseId,
		routeTableId
	});
}
function PostgresSqlWorkbenchContent({ databaseId, routeTableId }) {
	const t = useT();
	const { projectId } = useParams({ strict: false });
	const navigate = useNavigate();
	const { account } = useAuth();
	const accountPrefs = account;
	const { project } = useProject(projectId);
	const teamId = project?.teamId ?? null;
	const { features } = useConsoleProfile();
	const { access } = useOrganizationScopes(teamId ?? void 0);
	const canSaveTeam = canSaveTeamFilters(access, features);
	const { isOperationsLocked, operationsLockTooltip } = useDatabaseOperationsLock();
	const { tabs, activeTabId, activeTab, setActiveTabId, updateActiveTabSql, setActiveTabResult, createTab, closeTab, reorderTabs, renameTab, addRecentQuery, focusTableRouteTab } = usePostgresSidebar();
	useLayoutEffect(() => {
		if (!routeTableId) return;
		focusTableRouteTab(routeTableId);
	}, [focusTableRouteTab, routeTableId]);
	const normalizedRouteTableId = routeTableId ? normalizePostgresTableRouteId(routeTableId) : void 0;
	const editorActiveTabId = useMemo(() => normalizedRouteTableId ? tabs.find((tab) => tab.tableId === normalizedRouteTableId) : void 0, [normalizedRouteTableId, tabs])?.id ?? activeTabId;
	const editorActiveTab = tabs.find((tab) => tab.id === editorActiveTabId) ?? activeTab;
	const activeTableId = normalizedRouteTableId ?? editorActiveTab.tableId ?? null;
	const selectedTable = activeTableId ? parsePostgresTableId(activeTableId) : void 0;
	const [page, setPage] = useState(1);
	const [pageSize, setPageSize] = useState(25);
	useEffect(() => {
		setPage(1);
		setPageSize(25);
	}, [activeTableId]);
	const { rows, total, columns, durationMs, truncated, isLoading: rowsLoading, isFetching: rowsFetching } = usePostgresTableRows(projectId, databaseId, activeTableId ?? null, page - 1, pageSize);
	const executeSql = useExecutePostgresSql(projectId, databaseId);
	const explainSql = useExplainPostgresSql(projectId, databaseId);
	const tableColumns = columns.length > 0 ? columns.map((column) => ({
		key: column.name,
		label: column.name
	})) : rows[0] ? Object.keys(rows[0]).map((key) => ({
		key,
		label: key
	})) : [];
	const handleRunSql = useCallback(async () => {
		const trimmed = editorActiveTab.sql.trim();
		if (!trimmed) return;
		setActiveTabResult(null, null, "query");
		try {
			setActiveTabResult(await executeSql.mutateAsync(trimmed), null, "query");
			addRecentQuery(trimmed);
		} catch (error) {
			setActiveTabResult(null, error, "query");
		}
	}, [
		addRecentQuery,
		editorActiveTab.sql,
		executeSql,
		setActiveTabResult
	]);
	const handleExplainSql = useCallback(async () => {
		const trimmed = editorActiveTab.sql.trim();
		if (!trimmed) return;
		setActiveTabResult(null, null, "explain", null);
		try {
			setActiveTabResult(null, null, "explain", await explainSql.mutateAsync(trimmed));
		} catch (error) {
			setActiveTabResult(null, error, "explain", null);
		}
	}, [
		editorActiveTab.sql,
		explainSql,
		setActiveTabResult
	]);
	const handleSelectTab = useCallback((tabId) => {
		setActiveTabId(tabId);
		const tab = tabs.find((entry) => entry.id === tabId);
		if (!tab) return;
		if (!tab.tableId) {
			navigate({
				...postgresNav({
					projectId,
					databaseId
				}).sql(),
				replace: true
			});
			return;
		}
		navigate({
			...postgresNav({
				projectId,
				databaseId
			}).sql(),
			replace: true
		});
	}, [
		databaseId,
		navigate,
		projectId,
		setActiveTabId,
		tabs
	]);
	const tableRowsPanel = selectedTable ? /* @__PURE__ */ jsx(ReadOnlyDataSpreadsheet, {
		variant: "studio",
		showRowNumbers: true,
		enableColumnResize: true,
		rowNumberOffset: (page - 1) * pageSize,
		columns: tableColumns,
		rows,
		getRowKey: (row, index) => typeof row.$id === "string" ? row.$id : `table-row-${index}`,
		isLoading: rowsLoading || rowsFetching,
		loadingLabel: t("Loading rows…"),
		emptyContent: /* @__PURE__ */ jsx(PostgresTableRowsEmptyState, {}),
		header: /* @__PURE__ */ jsx(PostgresQueryResultsMeta, {
			title: `${selectedTable.schema}.${selectedTable.table}`,
			rowCount: total,
			durationMs,
			truncated
		}),
		footer: total > pageSize ? /* @__PURE__ */ jsx(Pagination, {
			currentPage: page,
			totalItems: total,
			pageSize,
			pageSizeOptions: [
				10,
				25,
				50,
				100
			],
			onPageChange: setPage,
			onPageSizeChange: (nextPageSize) => {
				setPageSize(nextPageSize);
				setPage(1);
			},
			itemLabel: "rows",
			className: "h-full min-h-0 border-0 mt-0 py-0"
		}) : void 0
	}) : /* @__PURE__ */ jsx(SqlWorkbenchPanelEmptyState, { variant: "results" });
	return /* @__PURE__ */ jsx("div", {
		className: "flex h-full min-h-0 min-w-0 flex-1 flex-col overflow-hidden",
		children: /* @__PURE__ */ jsx(SqlWorkbench, {
			projectId,
			databaseId,
			tabs,
			activeTabId: editorActiveTabId,
			sql: editorActiveTab.sql,
			onSqlChange: updateActiveTabSql,
			onSelectTab: handleSelectTab,
			onCreateTab: createTab,
			onCloseTab: closeTab,
			onReorderTabs: reorderTabs,
			onRenameTab: renameTab,
			onRun: handleRunSql,
			onExplain: handleExplainSql,
			isRunning: executeSql.isPending,
			isExplaining: explainSql.isPending,
			error: editorActiveTab.error ?? executeSql.error ?? explainSql.error,
			result: editorActiveTab.result,
			explainResult: editorActiveTab.explainResult,
			resultKind: editorActiveTab.resultKind,
			account: accountPrefs,
			teamId,
			canSaveTeam,
			canRun: !isOperationsLocked,
			runDisabledTooltip: operationsLockTooltip,
			canExplain: !isOperationsLocked,
			explainDisabledTooltip: operationsLockTooltip,
			children: tableRowsPanel
		})
	});
}
function PostgresSqlEditorPage() {
	const { databaseId } = Route$1.useParams();
	return /* @__PURE__ */ jsx(PostgresSqlWorkbench, { databaseId });
}
export { PostgresSqlEditorPage as component };
