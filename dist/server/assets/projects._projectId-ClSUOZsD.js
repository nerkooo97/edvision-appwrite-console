import { i as isResolvedThemeDarkChrome } from "./html-theme-zz5wyKPq.js";
import { t as cn } from "./utils-DoqqkI3X.js";
import "./runtime-config-DK7G0iKr.js";
import "./debug-endpoint-BvungD5q.js";
import { c as getProjectApiEndpoint, d as sdk, i as createRegionalConsoleRealtime, m as generateFingerprintToken, p as ensureFingerprintServerTimeSynced, s as getBaseEndpoint } from "./sdk-DjIJ_hjn.js";
import "./console-session-cookie-7RV5Zfr4.js";
import "./i18n-Db4baE06.js";
import "./ticket-layout-B97VGq99.js";
import "./ticket-types-BpqSrvYB.js";
import { n as useT } from "./translate-DZcqveGn.js";
import { c as isHttpPaymentRequiredError, l as isHttpProjectAccessError, s as isHttpNotFoundError } from "./error-formatting-CL2hjGy5.js";
import "./console-projects-C0b0tMaH.js";
import "./constants-CL7SLzjY.js";
import "./constants-Dd6QzW31.js";
import "./constants-BDeF927R.js";
import { Dt as useOrganizationById, F as isBudgetLimitReached, I as isOrganizationBillingReadonlyStatus, Pt as useOrganizationScopes, Xt as getSingleRecognizedPlanUsageLimitLabel, Y as organizationsQueryOptions, Zt as isPlanUsageLimitReached, et as resolveProjectTeamIdFromConsole, kt as useOrganizationFailedInvoicePresence } from "./organizations-BKtnlNrj.js";
import "./console-profiles-D__E5Kgi.js";
import { Gc as useCsvExportMigrations, Kc as useCsvImportMigrations, Xc as getMigrationTableRef } from "./hooks-BONwG3Mt.js";
import { Ft as MAX_CLI_SHELL_SESSION_NAME_LENGTH, Ka as CLI_SHELL_COLLAPSED_HEIGHT_PX, W as useCliShellSessionsSidebarWidth, Za as CLI_TERMINAL_RESET, Zr as parseCliShellSessionsSidebarWidthPx, ao as createCliTerminalWebLinksAddon, oo as withCliTerminalLinkCursor, qa as CLI_SHELL_COLLAPSE_MS, z as useBuildNotificationsOptedOut } from "./auth-BPuxYQAc.js";
import { R as useProject, W as useResumeProject } from "./projects-BaTJenfQ.js";
import "./appwrite-id-L15yEGeF.js";
import { Et as useProjectTable, rt as refetchProjectTableRelatedQueries } from "./databases-Dh0pwZ6h.js";
import "./database-specs-CBc802K0.js";
import "./database-row-inline-edits-CdyGeTxj.js";
import "./form-field-type-badge-C7qMzJo0.js";
import { H as clampCliShellSessionsSidebarWidthPx, _ as CLI_SHELL_SESSIONS_STRIP_MAX_WIDTH_PX, g as CLI_SHELL_SESSIONS_SIDEBAR_MIN_WIDTH_PX, h as CLI_SHELL_SESSIONS_SIDEBAR_MAX_WIDTH_PX, m as CLI_SHELL_SESSIONS_SIDEBAR_DEFAULT_WIDTH_PX, nt as syncPanelGroupFirstPanePx, q as computeTwoPanelHorizontalLayout, v as CLI_SHELL_TERMINAL_MAIN_MIN_WIDTH_PX } from "./resizable-layout-BVnWw80t.js";
import "./chart-interval-Dbrn19qD.js";
import "./is-marketing-page-dgx45Oqy.js";
import "./use-console-impersonation-revision-BiI0c7pX.js";
import "./affiliates-BOg1SHC6.js";
import "./social-stats-X1CQqP0k.js";
import "./cimd-CRIktQxf.js";
import "./account-applications-Bo8bAeE0.js";
import "./date-format-BD1j7PxK.js";
import "./invite-url-B18y3cBt.js";
import "./format-metric-6jsfxd5f.js";
import "./domains-Bfw8HsXF.js";
import "./parse-spec-DW3UGcrS.js";
import "./page-direction-CnacIIOa.js";
import { S as isStatusFaviconVariant, T as usesThemeAwareFaviconHost, a as useFavicon, y as getDefaultFaviconVariant } from "./AgentChat-DNlva4IH.js";
import "./DebugMode-DFSPYy81.js";
import "./ThinkingBubble-U48KAaRY.js";
import "./slider-BKjrzSmD.js";
import { t as Button } from "./button-Bnm2QhOm.js";
import { t as Input } from "./input-yKHNPhDZ.js";
import "./collapsible-BcDIDOgI.js";
import "./popover-BjTNxuf9.js";
import "./tabs-XaWkg9jR.js";
import "./select-BYGLGp-f.js";
import "./context-menu-D55xedo-.js";
import "./CodeBlock-BGAzMP_K.js";
import "./WizardLayout-DWqXFGuX.js";
import "./code-language-RiwE0Xft.js";
import "./prose-typography-BMJgwhz7.js";
import "./table-CsPM4E9L.js";
import { i as ConsoleImpersonationBanner, r as useAuth, t as RequireAuth } from "./RequireAuth-DahioVQe.js";
import "./HeaderAlertBar-CK7Gy4sE.js";
import "./agent-paths-CTRM_FvO.js";
import "./mcp-CgjPVMsn.js";
import "./debug-mcp-endpoint-B4hkK2QF.js";
import "./badge-L9aO6DfA.js";
import "./label-D8nNLJBa.js";
import "./switch-D-U5gDIQ.js";
import "./textarea-CfKMnSVC.js";
import "./modal-auto-focus-BO41bKmA.js";
import "./dialog-CFWmsJbI.js";
import "./command-Cizl9kMJ.js";
import "./skeleton-8d0Q_D56.js";
import "./Avatar-D1PavDBA.js";
import "./use-localized-date-format-Dy8J1Ssn.js";
import "./DateTooltip-wgOggQgS.js";
import "./EmptyState-DZDwceHm.js";
import { a as DropdownMenuItem, l as DropdownMenuSeparator, o as DropdownMenuLabel, p as DropdownMenuTrigger, r as DropdownMenuContent, t as DropdownMenu } from "./dropdown-menu-DH51wH-m.js";
import "./Pagination-BDei8M4v.js";
import "./alert-BTaNwkUC.js";
import "./CronScheduleEditor-Dctcdb1H.js";
import "./overlay-lock-CIY7GeXu.js";
import "./sheet-CbM5lIV1.js";
import "./BaseDrawer-B4vv4Sf_.js";
import "./context-menu-Ca6WjjAw.js";
import "./ContextMenuIcon-DPnw7e0V.js";
import { n as usePlatform } from "./use-keyboard-shortcuts-C2m0wYFf.js";
import { i as formatDisplayKeys } from "./display-DbRQIyxk.js";
import "./McpIcon-D1Jv-oq2.js";
import "./SettingsSearchContext-DPkuZW4D.js";
import "./SettingsLayoutShell-B7hNlMNA.js";
import "./calendar-6OJ5dwYN.js";
import "./use-media-min-width-T-T6WgXi.js";
import "./DateRangePicker-BwmpXdP_.js";
import "./toggle-group-qQyCSBun.js";
import "./tooltip-DUssQZhw.js";
import "./UsageChartIntervalToggle-Bbo7DqjH.js";
import "./CartesianChart-IK-OMdOm.js";
import "./UsageChartBrushReferenceArea-PlRYgCRx.js";
import "./ChartXAxis-Sg7PTtJF.js";
import "./chart-panel-CCGEGd61.js";
import "./OverviewChartPanelError-D9UA3Ssz.js";
import "./upgrade-curtain-D427ml_E.js";
import "./addons-DpAB_yDA.js";
import "./stripe-B07yV6XF.js";
import "./utils-DMkzhjmw.js";
import "./EnablePremiumGeoDBDialog-B57n4Kqr.js";
import { t as useConsoleProfile } from "./use-console-profile-DiUZxZ_6.js";
import "./HostnameFaviconIcon-BEkCxwPN.js";
import "./console-project-scopes-nd4nTLJF.js";
import "./UsageTimeSeriesChartCard-DvSTn7lw.js";
import "./DatabaseTypeIcon-CqLDDPFP.js";
import "./database-mascot-icons-mAQ4uqbH.js";
import "./chart-animation-CE90Rh4_.js";
import "./DocsRouteLink-cLPNKZ9F.js";
import "./ChartSeriesDot-DRSaLZ-2.js";
import "./UsageSectionChartError-Bjd51l80.js";
import { h as verticalPanelResizeHandleClass, i as RESIZE_HANDLE_PSEUDO_AFTER_X, m as setBodyResizeDragActive, o as RESIZE_HANDLE_PSEUDO_BEFORE_X } from "./horizontal-resize-BcegzCwH.js";
import { n as ResizablePanel, r as ResizablePanelGroup, t as ResizableHandle } from "./resizable-CfBrThFG.js";
import "./SentryContext-BM5Kx9zs.js";
import "./checkbox-r_hqIB3d.js";
import "./CopyableId-DPIWAPIb.js";
import { a as CliShellProvider, c as formatCliTerminalSearchLabel, d as stripAnsi, l as getCliShellSplitChildren, o as useCliShell, p as CLI_SHELL_NEW_TERMINAL_SHORTCUT_RAW, t as ProjectConnectDialogProvider, u as getTerminalBufferText } from "./ProjectConnectDialogContext-DgcmISfV.js";
import "./Icon-BtIL187e.js";
import "./FrameworkIcon-DTkSe6r3.js";
import "./MCPSection-k-iSVVVO.js";
import "./ConnectCodeExample-Ujo3XkoF.js";
import "./radio-group-aZurL4eE.js";
import "./DateTimePicker-DySgezub.js";
import "./accordion-DmQmnCa5.js";
import "./separator-B2hXZdKL.js";
import "./ScopeEditor-DGe3mP1w.js";
import "./ApiKeyDrawer-C9r-i_O0.js";
import "./use-user-os-Cwg5asTC.js";
import { B as canShowProjectTerminal } from "./console-access-checks-BTMEOKcL.js";
import "./PostgresCopyableField-eNLUNLhf.js";
import "./TerraformIcon-DDZR7KCM.js";
import "./providers-8aVvAoJZ.js";
import "./agent-discovery-SMCX1bvP.js";
import "./analytics-C_KnVoso.js";
import "./avif-support-fkUYDvxs.js";
import { t as registerConsoleRealtimeListener } from "./console-hub-DIz9opmN.js";
import "./agent-docs-feature-COYbd_m1.js";
import "./navigation-BOrhbgOp.js";
import { i as KeyboardShortcutsProvider, t as ConsoleLayout } from "./ConsoleLayout-c5WGBGep.js";
import "./partners-docs-feature-C-dnxcxd.js";
import "./BlogPageAnchor-BwvdqqDT.js";
import "./MarketingSiteLink-Cxu9rqvv.js";
import "./manifest-THOJt7eC.js";
import "./events-s0i9XY3r.js";
import "./progress-DDUqzOsb.js";
import "./postgres-sql-editor-actions-C3EmLCXJ.js";
import "./postgres-sql-editor-shortcuts-CXAI529H.js";
import "./mysql-sql-editor-actions-Bp-OLXJY.js";
import "./mysql-sql-editor-shortcuts-vHKpSCi0.js";
import "./network-connectivity-D-2A27IF.js";
import { t as ErrorComponent } from "./Component-Fg6-kkbf.js";
import { c as setInitialLoaderShellGate, r as INITIAL_LOADER_SHELL_GATE, t as CloudStatusBanner } from "./CloudStatusBanner-CLph43RA.js";
import "./upload-manager-DVbeAVI1.js";
import { n as GlobalUploadProgress, t as Route$1 } from "./projects._projectId-f7MYC5Cv.js";
import { t as ProgressBarRow } from "./ProgressBarRow-ec10tuU_.js";
import "./mock-data-bi-y2wwb.js";
import "./date-utils-C_g8GS8c.js";
import "./id-input-CfyIhJ26.js";
import "./use-mobile-C9thwzsE.js";
import "./feedback-BwuMSGir.js";
import "./support-BA-5OzxM.js";
import "./CloudMarkIcon-ChnstmGW.js";
import "./content-NlXhGy_g.js";
import "./nav-badge-CqWauT26.js";
import "./registry-C4rxXMsK.js";
import "./secondary-sidebar-nav-F3D8GuLT.js";
import { a as isDeploymentTimeout } from "./deployment-status-gtgbrodz.js";
import { t as OrganizationFailedInvoiceHeaderBanner } from "./OrganizationFailedInvoiceHeaderBanner-voBgqGBe.js";
import { n as REALTIME_EVENTS, t as PROJECT_CHANNELS } from "./constants-DeVF3k3a.js";
import { n as useSessionMigrations, t as SessionMigrationsProvider } from "./SessionMigrationsContext-BV_euDtz.js";
import { n as sortableAxisTransform, t as getAxisRestrictedDragModifiers } from "./dnd-modifiers-CiFEVwzL.js";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { Link, Outlet, useLocation, useNavigate } from "@tanstack/react-router";
import { Fragment as Fragment$1, createContext, memo, useCallback, useContext, useEffect, useLayoutEffect, useMemo, useRef, useState } from "react";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { AlertCircle, ArrowUpCircle, Bell, CheckCircle2, ChevronDown, ChevronUp, Columns2, Download, Ellipsis, GripVertical, Loader2, Lock, Maximize2, Minimize2, PauseCircle, Plus, RotateCcw, Search, Terminal, Trash2, X } from "lucide-react";
import { useTheme } from "next-themes";
import { DndContext, DragOverlay, KeyboardSensor, PointerSensor, closestCenter, useSensor, useSensors } from "@dnd-kit/core";
import { SortableContext, horizontalListSortingStrategy, sortableKeyboardCoordinates, useSortable, verticalListSortingStrategy } from "@dnd-kit/sortable";
function BudgetLimitProjectCurtain({ teamId }) {
	const t = useT();
	const navigate = useNavigate();
	const hasTeamId = !!teamId;
	return /* @__PURE__ */ jsx("div", {
		className: "fixed inset-0 z-[200] flex items-center justify-center bg-background/95 backdrop-blur-sm",
		children: /* @__PURE__ */ jsxs("div", {
			className: "mx-4 flex max-w-md flex-col items-center text-center",
			children: [
				/* @__PURE__ */ jsx("div", {
					className: "mb-6 flex size-16 items-center justify-center rounded-full bg-muted text-muted-foreground",
					children: /* @__PURE__ */ jsx(Lock, { className: "size-9" })
				}),
				/* @__PURE__ */ jsx("h1", {
					className: "text-[22px] font-semibold text-foreground",
					children: t("Budget limit reached")
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-3 text-[15px] text-muted-foreground",
					children: t("This organization has reached its budget limit and is now blocked. To continue using Appwrite services, update the budget limit.")
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "mt-8 flex flex-col gap-3 sm:flex-row sm:gap-2",
					children: [hasTeamId ? /* @__PURE__ */ jsx(Button, {
						asChild: true,
						variant: "brandCta",
						className: "gap-1.5",
						children: /* @__PURE__ */ jsx(Link, {
							to: "/organizations/$orgId/settings/billing",
							params: { orgId: teamId },
							hash: "update-budget",
							children: t("Update limit")
						})
					}) : null, /* @__PURE__ */ jsx(Button, {
						variant: hasTeamId ? "outline" : "brandCta",
						onClick: () => {
							if (hasTeamId) {
								navigate({
									to: "/organizations/$orgId",
									params: { orgId: teamId }
								});
								return;
							}
							navigate({ to: "/" });
						},
						children: hasTeamId ? t("Back to organization") : t("Back to console")
					})]
				})
			]
		})
	});
}
function PlanUsageLimitProjectCurtain({ teamId, billingLimits }) {
	const t = useT();
	const navigate = useNavigate();
	const hasTeamId = !!teamId;
	const resourceLabel = getSingleRecognizedPlanUsageLimitLabel(billingLimits);
	return /* @__PURE__ */ jsx("div", {
		className: "fixed inset-0 z-[120] flex items-center justify-center bg-background/95 backdrop-blur-sm",
		children: /* @__PURE__ */ jsxs("div", {
			className: "mx-4 flex max-w-md flex-col items-center text-center",
			children: [
				/* @__PURE__ */ jsx("div", {
					className: "mb-6 flex size-16 items-center justify-center rounded-full bg-muted text-muted-foreground",
					children: /* @__PURE__ */ jsx(Lock, { className: "size-9" })
				}),
				/* @__PURE__ */ jsx("h1", {
					className: "text-[22px] font-semibold text-foreground",
					children: t("Plan limit reached")
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-3 text-[15px] text-muted-foreground",
					children: resourceLabel ? /* @__PURE__ */ jsxs(Fragment, { children: [
						t("This organization has reached its plan limit for"),
						" ",
						t(resourceLabel),
						t(". Upgrade your plan or wait until the end of the billing cycle to restore access.")
					] }) : t("This organization has reached its plan usage limit and is now blocked. Upgrade your plan or wait until the end of the billing cycle to restore access.")
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "mt-8 flex flex-col gap-3 sm:flex-row sm:gap-2",
					children: [hasTeamId ? /* @__PURE__ */ jsx(Button, {
						asChild: true,
						variant: "brandCta",
						className: "gap-1.5",
						children: /* @__PURE__ */ jsxs(Link, {
							to: "/upgrade",
							search: { orgId: teamId },
							children: [/* @__PURE__ */ jsx(ArrowUpCircle, { className: "size-4" }), t("Upgrade plan")]
						})
					}) : null, hasTeamId ? /* @__PURE__ */ jsx(Button, {
						asChild: true,
						variant: "outline",
						children: /* @__PURE__ */ jsx(Link, {
							to: "/organizations/$orgId/settings/billing",
							params: { orgId: teamId },
							hash: "current-cycle-usage",
							children: t("View current cycle usage")
						})
					}) : /* @__PURE__ */ jsx(Button, {
						variant: "brandCta",
						onClick: () => navigate({ to: "/" }),
						children: t("Back to console")
					})]
				}),
				hasTeamId ? /* @__PURE__ */ jsx(Button, {
					variant: "ghost",
					className: "mt-6 text-muted-foreground",
					onClick: () => navigate({
						to: "/organizations/$orgId",
						params: { orgId: teamId }
					}),
					children: t("Back to organization")
				}) : null
			]
		})
	});
}
function PausedProjectCurtain({ projectId, teamId, onRestoreSuccess }) {
	const t = useT();
	const navigate = useNavigate();
	const resumeMutation = useResumeProject(projectId);
	async function handleRestore() {
		try {
			await resumeMutation.mutateAsync();
			onRestoreSuccess?.();
			toast.success(t("Project resumed successfully"));
			navigate({
				to: "/projects/$projectId",
				params: { projectId }
			});
		} catch (e) {
			const message = e && typeof e === "object" && "message" in e ? String(e.message) : t("Failed to resume project. Please try again.");
			toast.error(message);
		}
	}
	return /* @__PURE__ */ jsx("div", {
		className: "fixed inset-0 z-[120] flex items-center justify-center bg-background/95 backdrop-blur-sm",
		children: /* @__PURE__ */ jsxs("div", {
			className: "mx-4 flex max-w-md flex-col items-center text-center",
			children: [
				/* @__PURE__ */ jsx("div", {
					className: "mb-6 flex size-16 items-center justify-center rounded-full bg-muted text-muted-foreground",
					children: /* @__PURE__ */ jsx(PauseCircle, { className: "size-9" })
				}),
				/* @__PURE__ */ jsx("h1", {
					className: "text-[22px] font-semibold text-foreground",
					children: t("Project paused")
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-3 text-[15px] text-muted-foreground",
					children: t("This project has been paused due to inactivity. Your data is safe and will remain intact.")
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-2 text-[14px] text-muted-foreground",
					children: t("Upgrade your plan to avoid pausing, or restore the project to continue using it now.")
				}),
				resumeMutation.error && /* @__PURE__ */ jsx("p", {
					className: "mt-4 text-[13px] text-destructive",
					children: resumeMutation.error instanceof Error ? resumeMutation.error.message : t("Failed to resume project.")
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "mt-8 flex flex-col gap-3 sm:flex-row sm:gap-2",
					children: [/* @__PURE__ */ jsx(Button, {
						asChild: true,
						variant: "brandCta",
						className: "gap-1.5",
						children: /* @__PURE__ */ jsxs(Link, {
							to: "/upgrade",
							search: { orgId: teamId },
							children: [/* @__PURE__ */ jsx(ArrowUpCircle, { className: "size-4" }), t("Upgrade plan")]
						})
					}), /* @__PURE__ */ jsxs(Button, {
						variant: "outline",
						disabled: resumeMutation.isPending,
						onClick: handleRestore,
						className: "gap-1.5",
						children: [/* @__PURE__ */ jsx(RotateCcw, { className: "size-4" }), t("Restore project")]
					})]
				}),
				/* @__PURE__ */ jsx(Button, {
					variant: "ghost",
					className: "mt-6 text-muted-foreground",
					onClick: () => navigate({
						to: "/organizations/$orgId",
						params: { orgId: teamId }
					}),
					children: t("Back to organization")
				})
			]
		})
	});
}
var hubStates = /* @__PURE__ */ new Map();
function normalizeEndpoint(url) {
	return url.replace(/\/$/, "").toLowerCase();
}
function getOrCreateHub(endpointKey) {
	let hub = hubStates.get(endpointKey);
	if (!hub) {
		hub = {
			listeners: /* @__PURE__ */ new Map(),
			realtime: null,
			opChain: Promise.resolve()
		};
		hubStates.set(endpointKey, hub);
	}
	return hub;
}
function runExclusive(hub, fn) {
	const next = hub.opChain.then(() => fn());
	hub.opChain = next.catch(() => {});
	return next;
}
function anchorProjectId(hub) {
	return hub.listeners.values().next().value?.projectId ?? null;
}
async function attachListener(endpointKey, hub, id) {
	const listener = hub.listeners.get(id);
	if (!listener) return;
	if (listener.subscription) {
		await listener.subscription.close();
		listener.subscription = null;
	}
	const projectId = anchorProjectId(hub);
	if (!projectId) return;
	if (!hub.realtime) hub.realtime = createRegionalConsoleRealtime(projectId);
	listener.subscription = await hub.realtime.subscribe(listener.channels, listener.handler);
}
async function detachListener(endpointKey, hub, id) {
	const listener = hub.listeners.get(id);
	if (!listener) return;
	if (listener.subscription) {
		await listener.subscription.close();
		listener.subscription = null;
	}
	hub.listeners.delete(id);
	if (hub.listeners.size === 0) {
		hub.realtime = null;
		hubStates.delete(endpointKey);
	}
}
async function registerRegionalConsoleRealtimeListener(projectId, channels, handler) {
	const regionalUrl = normalizeEndpoint(getProjectApiEndpoint(projectId));
	if (regionalUrl === normalizeEndpoint(getBaseEndpoint())) return async () => {};
	const endpointKey = regionalUrl;
	const hub = getOrCreateHub(endpointKey);
	const id = Symbol("regional-console-realtime-listener");
	await runExclusive(hub, async () => {
		hub.listeners.set(id, {
			projectId,
			channels: [...channels],
			handler,
			subscription: null
		});
		await attachListener(endpointKey, hub, id);
	});
	return async () => {
		await runExclusive(hub, async () => {
			await detachListener(endpointKey, hub, id);
		});
	};
}
function normalizeMigrationPayload(payload) {
	const out = { ...payload };
	if (typeof out.statusCounters === "string") try {
		out.statusCounters = JSON.parse(out.statusCounters);
	} catch {}
	if (typeof out.resourceData === "string") try {
		out.resourceData = JSON.parse(out.resourceData);
	} catch {}
	return out;
}
function mergeMigrationPayloadIntoCache(queryClient, projectId, payload) {
	const id = payload.$id;
	if (!id) return;
	const normalized = normalizeMigrationPayload(payload);
	queryClient.setQueriesData({
		queryKey: [
			"migrations",
			"project",
			projectId
		],
		exact: false
	}, (old) => {
		const data = old;
		if (!data?.migrations || !Array.isArray(data.migrations)) return old;
		const next = data.migrations.map((m) => m.$id === id ? {
			...m,
			...normalized
		} : m);
		return {
			...data,
			migrations: next
		};
	});
	queryClient.setQueriesData({
		queryKey: [
			"migration",
			"project",
			projectId
		],
		exact: false
	}, (old) => {
		if (!old || typeof old !== "object") return old;
		const migration = old;
		if (migration.$id !== id) return old;
		return {
			...migration,
			...normalized
		};
	});
}
function mergeDeploymentPayloadIntoCache(queryClient, projectId, resourceType, payload) {
	const id = payload.$id;
	if (!id) return false;
	const resourceId = payload.resourceId;
	if (!resourceId) return false;
	const listPrefix = resourceType === "site" ? [
		"deployments",
		"site",
		projectId,
		resourceId
	] : [
		"deployments",
		"function",
		projectId,
		resourceId
	];
	let mergedInList = false;
	queryClient.setQueriesData({
		queryKey: listPrefix,
		exact: false
	}, (old) => {
		const data = old;
		if (!data?.deployments || !Array.isArray(data.deployments)) return old;
		const idx = data.deployments.findIndex((d) => d.$id === id);
		if (idx < 0) return old;
		mergedInList = true;
		const next = [...data.deployments];
		next[idx] = {
			...next[idx],
			...payload
		};
		return {
			...data,
			deployments: next
		};
	});
	const detailKey = resourceType === "site" ? [
		"deployment",
		"site",
		projectId,
		resourceId,
		id
	] : [
		"deployment",
		"function",
		projectId,
		resourceId,
		id
	];
	queryClient.setQueryData(detailKey, (old) => {
		if (!old || typeof old !== "object") return payload;
		return {
			...old,
			...payload
		};
	});
	return mergedInList;
}
function refetchDeploymentQueries(queryClient, projectId, resourceType, resourceId) {
	const listKey = resourceId ? resourceType === "site" ? [
		"deployments",
		"site",
		projectId,
		resourceId
	] : [
		"deployments",
		"function",
		projectId,
		resourceId
	] : resourceType === "site" ? [
		"deployments",
		"site",
		projectId
	] : [
		"deployments",
		"function",
		projectId
	];
	const detailKey = resourceId ? resourceType === "site" ? [
		"deployment",
		"site",
		projectId,
		resourceId
	] : [
		"deployment",
		"function",
		projectId,
		resourceId
	] : resourceType === "site" ? [
		"deployment",
		"site",
		projectId
	] : [
		"deployment",
		"function",
		projectId
	];
	queryClient.refetchQueries({
		queryKey: listKey,
		exact: false
	});
	queryClient.refetchQueries({
		queryKey: detailKey,
		exact: false
	});
}
function isDeploymentDeleteEvent(events) {
	return hasEvent(events, REALTIME_EVENTS.SITES_DEPLOYMENT_DELETE) || hasEvent(events, REALTIME_EVENTS.FUNCTIONS_DEPLOYMENT_DELETE) || events.some((e) => e.includes(".deployments.") && e.endsWith(".delete"));
}
function isDeploymentCreateEvent(events) {
	return hasEvent(events, REALTIME_EVENTS.SITES_DEPLOYMENT_CREATE) || hasEvent(events, REALTIME_EVENTS.FUNCTIONS_DEPLOYMENT_CREATE) || events.some((e) => e.includes(".deployments.") && e.endsWith(".create"));
}
function extractDeploymentResourceIdFromEvents(events, resourceType) {
	const prefix = resourceType === "site" ? "sites." : "functions.";
	for (const event of events) {
		const id = event.match(/* @__PURE__ */ new RegExp(`^${prefix}([^.]+)\\.deployments\\.`))?.[1];
		if (id && id !== "*") return id;
	}
}
function handleDeploymentRealtimeEvent(queryClient, projectId, resourceType, events, payload, parentQueryKey) {
	const isCreate = isDeploymentCreateEvent(events);
	const isDelete = isDeploymentDeleteEvent(events);
	const resourceId = payload?.resourceId ?? extractDeploymentResourceIdFromEvents(events, resourceType);
	const canMerge = payload != null && typeof payload.$id === "string" && !!resourceId && !isCreate && !isDelete;
	let mergedInList = false;
	if (canMerge) mergedInList = mergeDeploymentPayloadIntoCache(queryClient, projectId, resourceType, {
		...payload,
		resourceId
	});
	if (isCreate || isDelete || !canMerge || !mergedInList) refetchDeploymentQueries(queryClient, projectId, resourceType, resourceId);
	queryClient.invalidateQueries({ queryKey: parentQueryKey });
}
function mergeRulePayloadIntoCache(queryClient, payload) {
	const id = payload.$id;
	if (!id) return;
	queryClient.setQueriesData({
		queryKey: ["proxy-rules"],
		exact: false
	}, (old) => {
		const data = old;
		if (!data?.rules || !Array.isArray(data.rules)) return old;
		const idx = data.rules.findIndex((r) => r.$id === id);
		if (idx < 0) return old;
		const next = [...data.rules];
		next[idx] = {
			...next[idx],
			...payload
		};
		return {
			...data,
			rules: next
		};
	});
}
function eventMatches(events, prefix) {
	return events.some((e) => e === prefix || e.startsWith(prefix));
}
function hasEvent(events, name) {
	if (events.includes(name)) return true;
	if (name.endsWith(".*") && events.some((e) => e.startsWith(name.slice(0, -2)))) return true;
	return false;
}
function extractDatabaseIdFromRealtimeEvents(events) {
	for (const event of events) {
		const id = event.match(/^databases\.([^.]+)\.tables\./)?.[1];
		if (id && id !== "*") return id;
	}
}
function extractTableIdFromRealtimeEvents(events) {
	for (const event of events) {
		const id = event.match(/^databases\.[^.]+\.tables\.([^.]+)\./)?.[1];
		if (id && id !== "*") return id;
	}
}
function tableScopeFromRealtime(events, payload) {
	return {
		databaseId: typeof payload?.databaseId === "string" && payload.databaseId || typeof payload?.$databaseId === "string" && payload.$databaseId || extractDatabaseIdFromRealtimeEvents(events),
		tableId: typeof payload?.tableId === "string" && payload.tableId || typeof payload?.$tableId === "string" && payload.$tableId || typeof payload?.collectionId === "string" && payload.collectionId || extractTableIdFromRealtimeEvents(events)
	};
}
function invalidateAssistantQueries(queryClient, payload) {
	const conversationId = payload?.conversationId;
	queryClient.invalidateQueries({ queryKey: ["agent", "conversations"] });
	if (conversationId) queryClient.invalidateQueries({ queryKey: [
		"agent",
		"messages",
		conversationId
	] });
	else queryClient.invalidateQueries({ queryKey: ["agent", "messages"] });
}
function isAgentRealtimeSignal(values) {
	return values.some((value) => {
		const lower = value.toLowerCase();
		return lower.includes("agentconversation") || lower.includes("agentmessage") || lower.includes("agenttool") || lower.includes("agentmcp") || lower.includes("agentmodel") || lower.includes("agentautomation") || lower.includes("agent.conversation") || lower.includes("agent.message") || lower.includes("agent.tool") || lower.includes("agent.mcp") || lower.includes("agent.model") || lower.includes("agent.automation") || lower.includes("assistant");
	});
}
function handleRealtimeEvent(queryClient, projectId, response, onMigrationEvent) {
	const { events, channels } = response;
	const payload = response.payload && typeof response.payload === "object" ? response.payload : null;
	if (isAgentRealtimeSignal(events) || isAgentRealtimeSignal(channels)) invalidateAssistantQueries(queryClient, payload);
	const projectChannel = `projects.${projectId}`;
	const isForThisProject = channels.includes(projectChannel) || channels.some((c) => c.startsWith("projects.") && c.includes(projectId));
	if (hasEvent(events, REALTIME_EVENTS.SITES_ANY)) queryClient.invalidateQueries({ queryKey: [
		"sites",
		"project",
		projectId
	] });
	if (hasEvent(events, REALTIME_EVENTS.SITES_DEPLOYMENT_CREATE) || hasEvent(events, REALTIME_EVENTS.SITES_DEPLOYMENT_UPDATE) || hasEvent(events, REALTIME_EVENTS.SITES_DEPLOYMENT_DELETE) || eventMatches(events, REALTIME_EVENTS.SITES_DEPLOYMENTS_ANY)) handleDeploymentRealtimeEvent(queryClient, projectId, "site", events, payload, [
		"site",
		"project",
		projectId
	]);
	if (hasEvent(events, REALTIME_EVENTS.SITES_EXECUTIONS_ANY)) queryClient.invalidateQueries({ queryKey: [
		"logs",
		"site",
		projectId
	] });
	if (hasEvent(events, REALTIME_EVENTS.FUNCTIONS_DEPLOYMENT_CREATE) || hasEvent(events, REALTIME_EVENTS.FUNCTIONS_DEPLOYMENT_UPDATE) || hasEvent(events, REALTIME_EVENTS.FUNCTIONS_DEPLOYMENT_DELETE) || eventMatches(events, REALTIME_EVENTS.FUNCTIONS_DEPLOYMENTS_ANY)) {
		queryClient.invalidateQueries({ queryKey: [
			"functions",
			"project",
			projectId
		] });
		queryClient.invalidateQueries({ queryKey: [
			"function",
			"project",
			projectId
		] });
		handleDeploymentRealtimeEvent(queryClient, projectId, "function", events, payload, [
			"function",
			"project",
			projectId
		]);
	}
	if (hasEvent(events, REALTIME_EVENTS.FUNCTIONS_EXECUTIONS_ANY)) queryClient.invalidateQueries({ queryKey: [
		"executions",
		"function",
		projectId
	] });
	if (hasEvent(events, REALTIME_EVENTS.MIGRATIONS_ANY)) if (payload != null && typeof payload === "object" && payload !== null && "$id" in payload) {
		mergeMigrationPayloadIntoCache(queryClient, projectId, payload);
		onMigrationEvent?.(payload);
	} else {
		queryClient.invalidateQueries({ queryKey: [
			"migrations",
			"project",
			projectId
		] });
		queryClient.invalidateQueries({ queryKey: [
			"migration",
			"project",
			projectId
		] });
	}
	if (!isForThisProject) return;
	if (eventMatches(events, REALTIME_EVENTS.DATABASES_TABLES_COLUMNS_ANY) || hasEvent(events, REALTIME_EVENTS.DATABASES_TABLES_COLUMNS_CREATE) || hasEvent(events, REALTIME_EVENTS.DATABASES_TABLES_COLUMNS_UPDATE) || hasEvent(events, REALTIME_EVENTS.DATABASES_TABLES_COLUMNS_DELETE)) {
		const { databaseId, tableId } = tableScopeFromRealtime(events, payload);
		if (databaseId && tableId) refetchProjectTableRelatedQueries(queryClient, projectId, databaseId, tableId);
		else {
			queryClient.refetchQueries({
				queryKey: [
					"columns",
					"project",
					projectId
				],
				type: "all"
			});
			queryClient.refetchQueries({
				queryKey: [
					"tables",
					"project",
					projectId
				],
				type: "active"
			});
			queryClient.refetchQueries({
				queryKey: [
					"table",
					"project",
					projectId
				],
				type: "active"
			});
		}
	}
	if (hasEvent(events, REALTIME_EVENTS.DATABASES_TABLES_INDEXES_ANY)) {
		queryClient.refetchQueries({
			queryKey: [
				"indexes",
				"project",
				projectId
			],
			type: "all"
		});
		queryClient.refetchQueries({
			queryKey: [
				"table",
				"project",
				projectId
			],
			type: "active"
		});
	}
	if (hasEvent(events, REALTIME_EVENTS.DATABASES_TABLES_COLUMNS_ANY)) queryClient.invalidateQueries({ queryKey: [
		"databases",
		"project",
		projectId
	] });
	if (hasEvent(events, REALTIME_EVENTS.ARCHIVES_ANY)) queryClient.invalidateQueries({ queryKey: [
		"backup-archives",
		"project",
		projectId
	] });
	if (hasEvent(events, REALTIME_EVENTS.RESTORATIONS_ANY)) queryClient.invalidateQueries({ queryKey: [
		"restorations",
		"project",
		projectId
	] });
	if (hasEvent(events, REALTIME_EVENTS.POLICIES_ANY)) {
		queryClient.invalidateQueries({ queryKey: [
			"backup-policies",
			"project",
			projectId
		] });
		queryClient.invalidateQueries({ queryKey: [
			"dedicated-backup-policies",
			"project",
			projectId
		] });
	}
	if (events.includes(`projects.${projectId}.ping`)) queryClient.invalidateQueries({ queryKey: ["project", projectId] });
	if (hasEvent(events, REALTIME_EVENTS.STATS_CONNECTIONS)) queryClient.invalidateQueries({ queryKey: ["project", projectId] });
	if (hasEvent(events, REALTIME_EVENTS.RULES_UPDATE)) if (payload != null && typeof payload === "object" && payload !== null && "$id" in payload) {
		mergeRulePayloadIntoCache(queryClient, payload);
		const ruleId = payload.$id;
		queryClient.setQueriesData({
			queryKey: ["proxy-rule"],
			exact: false
		}, (old) => {
			const data = old;
			if (!data || data.$id !== ruleId) return old;
			return {
				...data,
				...payload
			};
		});
	} else {
		queryClient.invalidateQueries({ queryKey: ["proxy-rules"] });
		queryClient.invalidateQueries({ queryKey: ["proxy-rule"] });
	}
}
async function subscribeProjectRealtime(projectId, queryClient, options) {
	const { onMigrationEvent } = options ?? {};
	const handler = (response) => {
		handleRealtimeEvent(queryClient, projectId, response, onMigrationEvent);
	};
	const channels = [...PROJECT_CHANNELS];
	const unregisterMain = await registerConsoleRealtimeListener(channels, handler);
	const unregisterRegional = await registerRegionalConsoleRealtimeListener(projectId, channels, handler);
	return async () => {
		await Promise.all([unregisterRegional(), unregisterMain()]);
	};
}
function RealtimeProvider({ children, projectId }) {
	const queryClient = useQueryClient();
	const { addMigrationFromRealtime } = useSessionMigrations(projectId);
	const addMigrationRef = useRef(addMigrationFromRealtime);
	addMigrationRef.current = addMigrationFromRealtime;
	const onMigrationEvent = useCallback((payload) => {
		addMigrationRef.current?.(projectId, payload);
	}, [projectId]);
	const cleanupRef = useRef(null);
	const cleanupPromiseRef = useRef(null);
	useEffect(() => {
		if (!projectId) return;
		let cancelled = false;
		async function run() {
			const previousCleanupPromise = cleanupPromiseRef.current;
			cleanupPromiseRef.current = null;
			if (previousCleanupPromise) await previousCleanupPromise;
			if (cancelled) return;
			const cleanup = await subscribeProjectRealtime(projectId, queryClient, { onMigrationEvent });
			if (cancelled) {
				await cleanup();
				return;
			}
			cleanupRef.current = cleanup;
		}
		run();
		return () => {
			cancelled = true;
			const cleanup = cleanupRef.current;
			cleanupRef.current = null;
			if (cleanup) cleanupPromiseRef.current = cleanup();
		};
	}, [
		projectId,
		queryClient,
		onMigrationEvent
	]);
	return /* @__PURE__ */ jsx(Fragment, { children });
}
var TERMINAL_FAVICON_RESET_MS = 1e4;
var RESERVED_SECTION_SEGMENTS = new Set([
	"create",
	"usage",
	"templates",
	"editor",
	"index"
]);
function parseRelevance(pathname) {
	const sites = pathname.match(/\/projects\/[^/]+\/sites(?:\/([^/]+))?/);
	if (sites) {
		const seg = sites[1] ?? null;
		return {
			type: "site",
			resourceId: seg && !RESERVED_SECTION_SEGMENTS.has(seg) ? seg : null
		};
	}
	const fns = pathname.match(/\/projects\/[^/]+\/functions(?:\/([^/]+))?/);
	if (fns) {
		const seg = fns[1] ?? null;
		return {
			type: "function",
			resourceId: seg && !RESERVED_SECTION_SEGMENTS.has(seg) ? seg : null
		};
	}
	return {
		type: null,
		resourceId: null
	};
}
function isUpdateRelevant(ctx, update) {
	if (ctx.type === null) return false;
	if (ctx.type !== update.resourceType) return false;
	if (ctx.resourceId !== null && ctx.resourceId !== update.resourceId) return false;
	return true;
}
function isInProgressStatus(status) {
	return status === "building" || status === "processing" || status === "waiting";
}
function isTerminalStatus(status) {
	return status === "ready" || status === "failed" || status === "timeout" || status === "canceled" || status === "cancelled";
}
function buildInProgressFavicon() {
	return usesThemeAwareFaviconHost() ? "theme-blue" : "blue";
}
function buildSuccessFavicon() {
	return usesThemeAwareFaviconHost() ? "theme-green" : "green";
}
function buildFailureFavicon() {
	return usesThemeAwareFaviconHost() ? "theme-red" : "red";
}
function buildMeta(reason, builds, extras) {
	const list = Array.from(builds);
	return {
		source: "build-notifications",
		reason,
		detail: formatActiveBuildsDetail(list),
		context: {
			projectId: extras?.projectId,
			projectName: extras?.projectName,
			organizationId: extras?.organizationId,
			pathname: extras?.pathname,
			resources: list.map((b) => ({
				type: b.resourceType,
				id: b.resourceId,
				status: b.status,
				deploymentId: b.deploymentId
			}))
		}
	};
}
function formatActiveBuildsDetail(builds) {
	const list = Array.isArray(builds) ? builds : Array.from(builds);
	if (list.length === 0) return void 0;
	return list.map((b) => `${b.resourceType} ${b.resourceId} · ${b.status} · deployment ${b.deploymentId}`).join("; ");
}
var permissionPromptShown = false;
function notificationsSupported() {
	return typeof window !== "undefined" && typeof Notification !== "undefined";
}
function handleEnableClick(toastId, rememberOptedOut, translate) {
	try {
		const result = Notification.requestPermission();
		const handle = (perm) => {
			toast.dismiss(toastId);
			if (perm === "granted") {
				toast.success(translate("Notifications enabled"), {
					description: translate("Reloading to apply…"),
					duration: 1500
				});
				window.setTimeout(() => {
					try {
						window.location.reload();
					} catch {}
				}, 800);
			} else if (perm === "denied") {
				rememberOptedOut();
				toast.message(translate("Notifications blocked"), { description: translate("You can re-enable them anytime from your browser settings.") });
			}
		};
		if (result && typeof result.then === "function") result.then(handle);
		else if (typeof result === "string") handle(result);
	} catch {}
}
function maybeShowEnableToast(rememberOptedOut, optedOut, translate) {
	if (permissionPromptShown) return;
	if (!notificationsSupported()) return;
	if (Notification.permission !== "default") return;
	if (optedOut) return;
	permissionPromptShown = true;
	toast.custom((toastId) => /* @__PURE__ */ jsxs("div", {
		className: "w-[360px] rounded-xl border border-border bg-popover p-4 text-popover-foreground shadow-lg",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "flex items-start gap-3",
			children: [/* @__PURE__ */ jsx("div", {
				className: "flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-muted text-muted-foreground",
				children: /* @__PURE__ */ jsx(Bell, { className: "h-4 w-4" })
			}), /* @__PURE__ */ jsxs("div", {
				className: "min-w-0 flex-1",
				children: [/* @__PURE__ */ jsx("div", {
					className: "text-[13px] font-semibold leading-5",
					children: translate("Get notified when builds finish")
				}), /* @__PURE__ */ jsx("p", {
					className: "mt-1 text-[12px] leading-relaxed text-muted-foreground",
					children: translate("Allow browser notifications to hear about build completion, even from another tab.")
				})]
			})]
		}), /* @__PURE__ */ jsxs("div", {
			className: "mt-3 flex justify-end gap-2",
			children: [/* @__PURE__ */ jsx("button", {
				type: "button",
				onClick: () => {
					rememberOptedOut();
					toast.dismiss(toastId);
				},
				className: "h-8 rounded-md px-3 text-[12px] font-medium text-muted-foreground transition-colors hover:bg-muted hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring",
				children: translate("Not now")
			}), /* @__PURE__ */ jsx("button", {
				type: "button",
				onClick: () => handleEnableClick(toastId, rememberOptedOut, translate),
				className: "h-8 rounded-md bg-primary px-3 text-[12px] font-medium text-primary-foreground transition-colors hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring",
				children: translate("Enable")
			})]
		})]
	}), { duration: 2e4 });
}
function BuildNotificationsProvider({ projectId }) {
	const { account } = useAuth();
	const t = useT();
	const tRef = useRef(t);
	tRef.current = t;
	const { optedOut, setOptedOut } = useBuildNotificationsOptedOut(account);
	const { setFavicon, getCurrentFavicon } = useFavicon();
	const queryClient = useQueryClient();
	const location = useLocation();
	const optedOutRef = useRef(optedOut);
	optedOutRef.current = optedOut;
	const rememberOptedOutRef = useRef(() => setOptedOut(true));
	rememberOptedOutRef.current = () => setOptedOut(true);
	const relevanceCtx = parseRelevance(location.pathname);
	const relevanceKey = `${relevanceCtx.type ?? ""}::${relevanceCtx.resourceId ?? ""}`;
	const activeBuildsRef = useRef(/* @__PURE__ */ new Map());
	const trackedRef = useRef(/* @__PURE__ */ new Set());
	const notifiedTerminalRef = useRef(/* @__PURE__ */ new Set());
	const resetTimerRef = useRef(null);
	const originalFaviconRef = useRef(null);
	const lastDesiredFaviconRef = useRef(null);
	const projectIdRef = useRef(projectId);
	projectIdRef.current = projectId;
	const queryClientRef = useRef(queryClient);
	queryClientRef.current = queryClient;
	const setFaviconRef = useRef(setFavicon);
	setFaviconRef.current = setFavicon;
	const getCurrentFaviconRef = useRef(getCurrentFavicon);
	getCurrentFaviconRef.current = getCurrentFavicon;
	const relevanceCtxRef = useRef(relevanceCtx);
	relevanceCtxRef.current = relevanceCtx;
	const pathnameRef = useRef(location.pathname);
	pathnameRef.current = location.pathname;
	useEffect(() => {
		if (typeof window === "undefined") return;
		function lookupProjectMeta() {
			const projectId$1 = projectIdRef.current;
			const cached = queryClientRef.current.getQueryData(["project", projectId$1]);
			return {
				projectId: projectId$1,
				projectName: cached?.name?.trim() || void 0,
				organizationId: cached?.teamId?.trim() || void 0
			};
		}
		function captureOriginalFavicon() {
			if (originalFaviconRef.current) return;
			const resolved = getCurrentFaviconRef.current() ?? getDefaultFaviconVariant();
			originalFaviconRef.current = isStatusFaviconVariant(resolved) ? getDefaultFaviconVariant() : resolved;
		}
		function clearResetTimer() {
			if (resetTimerRef.current !== null) {
				window.clearTimeout(resetTimerRef.current);
				resetTimerRef.current = null;
			}
		}
		function applyDesiredFavicon(target, reason) {
			lastDesiredFaviconRef.current = target;
			if (relevanceCtxRef.current.type === null) return;
			const projectMeta = lookupProjectMeta();
			const builds = Array.from(activeBuildsRef.current.values());
			const meta = buildMeta(reason, builds, {
				...projectMeta,
				pathname: pathnameRef.current
			});
			if (meta.context) meta.context.resources = builds.map((b) => ({
				type: b.resourceType,
				id: b.resourceId,
				name: lookupResourceName(b.resourceType, b.resourceId),
				status: b.status,
				deploymentId: b.deploymentId
			}));
			if (target === null) {
				const original = originalFaviconRef.current ?? getDefaultFaviconVariant();
				setFaviconRef.current(original, meta);
				originalFaviconRef.current = null;
			} else {
				captureOriginalFavicon();
				setFaviconRef.current(target, meta);
			}
		}
		function restoreOriginalFavicon(reason = "Restored after builds settled") {
			applyDesiredFavicon(null, reason);
		}
		function scheduleFaviconReset() {
			clearResetTimer();
			resetTimerRef.current = window.setTimeout(() => {
				resetTimerRef.current = null;
				if (activeBuildsRef.current.size === 0) restoreOriginalFavicon();
			}, TERMINAL_FAVICON_RESET_MS);
		}
		function lookupResourceName(resourceType, resourceId) {
			const key = resourceType === "site" ? [
				"site",
				"project",
				projectIdRef.current,
				resourceId
			] : [
				"function",
				"project",
				projectIdRef.current,
				resourceId
			];
			const name = queryClientRef.current.getQueryData(key)?.name;
			return name && name.trim().length > 0 ? name : resourceId;
		}
		function notify(title, body, tag) {
			if (!notificationsSupported()) return;
			if (typeof document.hasFocus === "function" ? document.hasFocus() && document.visibilityState === "visible" : document.visibilityState === "visible") return;
			if (Notification.permission !== "granted") {
				if (relevanceCtxRef.current.type !== null) maybeShowEnableToast(() => rememberOptedOutRef.current(), optedOutRef.current, tRef.current);
				return;
			}
			try {
				const notification = new Notification(title, {
					body,
					icon: "/logo.svg",
					tag,
					requireInteraction: true,
					renotify: true
				});
				notification.onclick = () => {
					try {
						window.focus();
						notification.close();
					} catch {}
				};
			} catch (err) {
				console.warn("[BuildNotifications] failed to show notification", err);
			}
		}
		function handleStatusChange(update) {
			const builds = activeBuildsRef.current;
			const tracked = trackedRef.current;
			const createdAtForTimeout = update.createdAt ?? builds.get(update.deploymentId)?.createdAt;
			if (isInProgressStatus(update.status) && isDeploymentTimeout(update.status, createdAtForTimeout)) {
				const had = builds.has(update.deploymentId);
				builds.delete(update.deploymentId);
				tracked.delete(update.deploymentId);
				if (had) if (builds.size === 0) applyDesiredFavicon(null, "Restored after timed-out build removed from active set");
				else applyDesiredFavicon(buildInProgressFavicon(), `${builds.size} build(s) still in progress after timeout cleanup`);
				return;
			}
			if (isInProgressStatus(update.status)) {
				const isNew = !builds.has(update.deploymentId);
				if (isNew && !isUpdateRelevant(relevanceCtxRef.current, update)) return;
				notifiedTerminalRef.current.delete(update.deploymentId);
				builds.set(update.deploymentId, {
					...update,
					createdAt: createdAtForTimeout ?? update.createdAt
				});
				tracked.add(update.deploymentId);
				clearResetTimer();
				applyDesiredFavicon(buildInProgressFavicon(), `${builds.size} ${update.resourceType} build(s) in progress`);
				if (isNew) maybeShowEnableToast(() => rememberOptedOutRef.current(), optedOutRef.current, tRef.current);
				return;
			}
			if (!isTerminalStatus(update.status)) return;
			if (!tracked.has(update.deploymentId)) {
				builds.delete(update.deploymentId);
				return;
			}
			if (notifiedTerminalRef.current.has(update.deploymentId)) {
				builds.delete(update.deploymentId);
				return;
			}
			notifiedTerminalRef.current.add(update.deploymentId);
			builds.delete(update.deploymentId);
			tracked.delete(update.deploymentId);
			const failed = update.status === "failed";
			const canceled = update.status === "canceled" || update.status === "cancelled";
			const timedOut = update.status === "timeout";
			const name = lookupResourceName(update.resourceType, update.resourceId);
			const label = update.resourceType === "site" ? "Site" : "Function";
			const verb = update.status === "ready" ? "completed successfully" : update.status === "failed" ? "failed" : timedOut ? "timed out" : "was canceled";
			const notifyResultWord = update.status === "ready" ? "ready" : update.status === "failed" ? "failed" : timedOut ? "timed out" : "canceled";
			notify(tRef.current(`${label} build ${notifyResultWord}`), `${name} ${tRef.current(`build ${verb}.`)}`, `appwrite-build-${update.deploymentId}`);
			if (builds.size === 0) if (canceled) applyDesiredFavicon(null, `Restored after ${update.resourceType} build was canceled`);
			else {
				applyDesiredFavicon(failed || timedOut ? buildFailureFavicon() : buildSuccessFavicon(), failed || timedOut ? `${label} build ${timedOut ? "timed out" : "failed"}` : `${label} build completed successfully`);
				scheduleFaviconReset();
			}
			else applyDesiredFavicon(buildInProgressFavicon(), `${builds.size} build(s) still in progress after another finished`);
		}
		function handleRealtimeEvent$1(event) {
			const { events } = event;
			const isSite = events.some((name) => name.startsWith("sites.") && name.includes(".deployments."));
			const isFunction = events.some((name) => name.startsWith("functions.") && name.includes(".deployments."));
			if (!isSite && !isFunction) return;
			const payload = event.payload && typeof event.payload === "object" ? event.payload : null;
			if (!payload) return;
			const deploymentId = payload.$id;
			const status = payload.status;
			if (!deploymentId || !status) return;
			const resourceId = payload.resourceId ?? "";
			if (!resourceId) return;
			const resourceType = (payload.resourceType ?? (isSite ? "site" : "function")) === "function" ? "function" : "site";
			const createdAt = payload.$createdAt;
			handleStatusChange({
				deploymentId,
				resourceId,
				resourceType,
				status,
				createdAt
			});
		}
		function seedFromCache() {
			queryClientRef.current.getQueryCache().getAll().forEach((query) => {
				const key = query.queryKey;
				if (!Array.isArray(key) || key.length < 3) return;
				const [a, b, projId] = key;
				if (projId !== projectIdRef.current) return;
				let resourceType = null;
				if (b === "site") resourceType = "site";
				else if (b === "function") resourceType = "function";
				else return;
				const data = query.state.data;
				if (!data) return;
				const deployments = a === "deployments" && "deployments" in data && Array.isArray(data.deployments) ? data.deployments : a === "deployment" && "$id" in data ? [data] : [];
				for (const dep of deployments) {
					if (!dep || typeof dep !== "object") continue;
					const status = dep.status;
					if (!status || !isInProgressStatus(status)) continue;
					const depId = dep.$id;
					const resId = dep.resourceId;
					const createdAt = dep.$createdAt;
					if (!depId || !resId) continue;
					if (isDeploymentTimeout(status, createdAt)) continue;
					if (activeBuildsRef.current.has(depId)) continue;
					if (!isUpdateRelevant(relevanceCtxRef.current, {
						resourceType,
						resourceId: resId
					})) continue;
					activeBuildsRef.current.set(depId, {
						deploymentId: depId,
						resourceId: resId,
						resourceType,
						status,
						createdAt
					});
					trackedRef.current.add(depId);
				}
			});
			if (activeBuildsRef.current.size > 0) {
				clearResetTimer();
				applyDesiredFavicon(buildInProgressFavicon(), `${activeBuildsRef.current.size} build(s) in progress (seeded from cache)`);
			}
		}
		function onVisibilityChange() {
			if (document.visibilityState !== "visible") return;
			if (activeBuildsRef.current.size === 0 && originalFaviconRef.current) {
				clearResetTimer();
				restoreOriginalFavicon("Restored when tab became visible after builds settled");
			}
		}
		document.addEventListener("visibilitychange", onVisibilityChange);
		seedFromCache();
		let cancelled = false;
		let unregisterMain = null;
		let unregisterRegional = null;
		(async () => {
			const channels = [...PROJECT_CHANNELS];
			const main = await registerConsoleRealtimeListener(channels, handleRealtimeEvent$1);
			const regional = await registerRegionalConsoleRealtimeListener(projectId, channels, handleRealtimeEvent$1);
			if (cancelled) {
				await Promise.all([main(), regional()]);
				return;
			}
			unregisterMain = main;
			unregisterRegional = regional;
		})();
		return () => {
			cancelled = true;
			document.removeEventListener("visibilitychange", onVisibilityChange);
			clearResetTimer();
			if (originalFaviconRef.current) restoreOriginalFavicon("Restored on build-notifications provider unmount");
			activeBuildsRef.current.clear();
			trackedRef.current.clear();
			notifiedTerminalRef.current.clear();
			originalFaviconRef.current = null;
			lastDesiredFaviconRef.current = null;
			Promise.all([unregisterMain ? unregisterMain() : Promise.resolve(), unregisterRegional ? unregisterRegional() : Promise.resolve()]);
		};
	}, [projectId]);
	useEffect(() => {
		if (typeof window === "undefined") return;
		const ctx = relevanceCtxRef.current;
		let hasRelevantActive = false;
		activeBuildsRef.current.forEach((b) => {
			if (isDeploymentTimeout(b.status, b.createdAt)) return;
			if (isUpdateRelevant(ctx, {
				resourceType: b.resourceType,
				resourceId: b.resourceId
			})) hasRelevantActive = true;
		});
		if (ctx.type !== null && hasRelevantActive) {
			if (!originalFaviconRef.current) {
				const resolved = getCurrentFaviconRef.current() ?? getDefaultFaviconVariant();
				originalFaviconRef.current = isStatusFaviconVariant(resolved) ? getDefaultFaviconVariant() : resolved;
			}
			const cached = queryClientRef.current.getQueryData(["project", projectIdRef.current]);
			const builds = Array.from(activeBuildsRef.current.values());
			setFaviconRef.current(buildInProgressFavicon(), buildMeta(`Relevant ${ctx.type} build(s) in progress on this route`, builds, {
				projectId: projectIdRef.current,
				projectName: cached?.name?.trim() || void 0,
				organizationId: cached?.teamId?.trim() || void 0,
				pathname: location.pathname
			}));
		} else if (originalFaviconRef.current) {
			const cached = queryClientRef.current.getQueryData(["project", projectIdRef.current]);
			setFaviconRef.current(originalFaviconRef.current, buildMeta(ctx.type === null ? "Restored after leaving Sites/Functions route" : "Restored because no relevant builds on this route", activeBuildsRef.current.values(), {
				projectId: projectIdRef.current,
				projectName: cached?.name?.trim() || void 0,
				organizationId: cached?.teamId?.trim() || void 0,
				pathname: location.pathname
			}));
			originalFaviconRef.current = null;
		}
	}, [relevanceKey]);
	return null;
}
var CONSOLE_FINGERPRINT_HEADER = "X-Appwrite-Console-Fingerprint";
function reportConsoleAccess(projectId) {
	if (!projectId) return;
	const client = sdk.forConsole.client;
	ensureFingerprintServerTimeSynced(client.config?.endpoint ?? "", client.config?.project ?? "console").then(() => generateFingerprintToken()).then((fingerprint) => {
		if (client.headers) client.headers[CONSOLE_FINGERPRINT_HEADER] = fingerprint;
		const projects = sdk.forConsole.projects;
		if (typeof projects.updateConsoleAccess === "function") return projects.updateConsoleAccess({ projectId });
	}).catch((e) => console.error("Failed to update console access:", e)).finally(() => {
		const client$1 = sdk.forConsole.client;
		if (client$1.headers) delete client$1.headers[CONSOLE_FINGERPRINT_HEADER];
	});
}
var statusToLabel = (status) => {
	switch (status) {
		case "pending": return "Preparing export...";
		case "processing": return "Exporting...";
		case "completed": return "Export completed";
		case "failed": return "Export failed";
		default: return `Export (${status})`;
	}
};
var ExportStatusHeader = memo(function ExportStatusHeader$1({ status }) {
	const t = useT();
	const isPendingOrProcessing = status === "pending" || status === "processing";
	const isCompleted = status === "completed";
	const isFailed = status === "failed";
	const label = t(statusToLabel(status));
	return /* @__PURE__ */ jsxs("div", {
		className: "flex items-center gap-2 mb-1",
		children: [isPendingOrProcessing ? /* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin-smooth text-primary shrink-0" }) : isCompleted ? /* @__PURE__ */ jsx(CheckCircle2, { className: "h-4 w-4 text-green-600 shrink-0" }) : isFailed ? /* @__PURE__ */ jsx(AlertCircle, { className: "h-4 w-4 text-destructive shrink-0" }) : /* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin-smooth text-muted-foreground shrink-0" }), /* @__PURE__ */ jsx("p", {
			className: "text-[13px] font-medium text-foreground truncate",
			children: label
		})]
	});
});
function getExportedRowCount(m) {
	const row = m.statusCounters?.row;
	if (!row) return null;
	return (row.success ?? 0) + (row.processing ?? 0);
}
function CsvExportBox({ projectId }) {
	const t = useT();
	const { sessionExportIds, dismissedExportIds, dismissExport } = useSessionMigrations(projectId);
	const { migrations } = useCsvExportMigrations(projectId, sessionExportIds);
	const dismissedSet = useMemo(() => new Set(dismissedExportIds), [dismissedExportIds]);
	const visible = migrations.filter((m) => !dismissedSet.has(m.$id));
	const latestCountsRef = useRef({});
	const [displayedCounts, setDisplayedCounts] = useState({});
	const intervalRef = useRef(null);
	visible.forEach((m) => {
		const count = getExportedRowCount(m);
		if (count !== null) latestCountsRef.current[m.$id] = count;
	});
	useEffect(() => {
		const throttleMs = 400;
		const sync = () => {
			setDisplayedCounts((prev) => {
				const next = { ...prev };
				let changed = false;
				for (const [id, count] of Object.entries(latestCountsRef.current)) if (prev[id] !== count) {
					next[id] = count;
					changed = true;
				}
				return changed ? next : prev;
			});
		};
		sync();
		intervalRef.current = setInterval(sync, throttleMs);
		return () => {
			if (intervalRef.current) clearInterval(intervalRef.current);
		};
	}, [visible.length]);
	if (visible.length === 0) return null;
	return /* @__PURE__ */ jsx("div", {
		className: "w-full max-w-sm space-y-2",
		children: visible.map((m) => {
			const isCompleted = m.status === "completed";
			const isInProgress = m.status === "pending" || m.status === "processing";
			const url = m.options?.downloadUrl;
			const liveCount = getExportedRowCount(m);
			const rowCount = isCompleted && liveCount !== null ? liveCount : displayedCounts[m.$id] ?? liveCount;
			const statusLine = rowCount !== null && rowCount > 0 ? `${rowCount.toLocaleString()} ${t("rows exported")}` : m.status === "pending" ? t("Preparing export...") : m.status === "processing" ? t("Exporting...") : null;
			return /* @__PURE__ */ jsx("div", {
				className: "rounded-lg border border-border bg-background p-3",
				children: /* @__PURE__ */ jsxs("div", {
					className: "flex items-start gap-3",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "flex-1 min-w-0",
						children: [
							/* @__PURE__ */ jsx(ExportStatusHeader, { status: m.status }),
							statusLine ? /* @__PURE__ */ jsx("p", {
								className: "text-[11px] text-muted-foreground mb-2",
								children: statusLine
							}) : /* @__PURE__ */ jsx("div", { className: "mb-2 min-h-[14px]" }),
							/* @__PURE__ */ jsx("div", {
								className: "min-h-[28px] flex items-center",
								children: isCompleted && url ? /* @__PURE__ */ jsxs(Button, {
									variant: "link",
									size: "sm",
									className: "h-auto p-0 text-[12px]",
									onClick: () => window.open(url, "_blank"),
									children: [/* @__PURE__ */ jsx(Download, { className: "h-3 w-3 me-1" }), t("Download")]
								}) : isInProgress ? /* @__PURE__ */ jsxs(Button, {
									variant: "link",
									size: "sm",
									className: "h-auto p-0 text-[12px]",
									disabled: true,
									children: [/* @__PURE__ */ jsx(Download, { className: "h-3 w-3 me-1" }), t("Download")]
								}) : null
							})
						]
					}), /* @__PURE__ */ jsx(Button, {
						variant: "ghost",
						size: "sm",
						className: "h-6 w-6 p-0 shrink-0",
						onClick: () => dismissExport(projectId, m.$id),
						"aria-label": t("Dismiss"),
						children: /* @__PURE__ */ jsx(X, { className: "h-3.5 w-3.5" })
					})]
				})
			}, m.$id);
		})
	});
}
var statusToProgress = {
	pending: 10,
	uploading: 30,
	processing: 60,
	completed: 100,
	failed: 100
};
function MigrationItem({ migration, projectId, onViewDetails, onDismiss }) {
	const t = useT();
	const parsed = getMigrationTableRef(migration);
	const { table } = useProjectTable(projectId, parsed?.databaseId ?? null, "tablesdb", parsed?.tableId ?? null);
	const tableName = table?.name ?? parsed?.tableId ?? migration.resourceId;
	const status = migration.status;
	const progress = statusToProgress[status] ?? 50;
	const isFailed = status === "failed";
	const isCompleted = status === "completed";
	const isPendingOrProcessing = status === "pending" || status === "processing" || status === "uploading";
	let statusLabel;
	switch (status) {
		case "pending":
			statusLabel = t("Preparing CSV for import...");
			break;
		case "processing":
		case "uploading":
			statusLabel = `${t("Importing to")} ${tableName}`;
			break;
		case "completed":
			statusLabel = `${t("Import completed to")} ${tableName}`;
			break;
		case "failed":
			statusLabel = `${t("Import failed to")} ${tableName}`;
			break;
		default: statusLabel = `Import (${status})`;
	}
	return /* @__PURE__ */ jsx("div", {
		className: "rounded-lg border border-border bg-background p-3",
		children: /* @__PURE__ */ jsxs("div", {
			className: "flex items-start gap-3",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex-1 min-w-0",
				children: [
					/* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-2 mb-1",
						children: [isPendingOrProcessing ? /* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin text-primary shrink-0" }) : isCompleted ? /* @__PURE__ */ jsx(CheckCircle2, { className: "h-4 w-4 text-green-600 shrink-0" }) : isFailed ? /* @__PURE__ */ jsx(AlertCircle, { className: "h-4 w-4 text-destructive shrink-0" }) : /* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin text-muted-foreground shrink-0" }), /* @__PURE__ */ jsx("p", {
							className: "text-[13px] font-medium text-foreground truncate",
							children: statusLabel
						})]
					}),
					/* @__PURE__ */ jsx(ProgressBarRow, { value: progress }),
					isFailed && migration.errors?.length > 0 && /* @__PURE__ */ jsx(Button, {
						variant: "link",
						size: "sm",
						className: "h-auto p-0 text-[12px] mt-0.5",
						onClick: () => onViewDetails(migration),
						children: t("View details")
					})
				]
			}), /* @__PURE__ */ jsx(Button, {
				variant: "ghost",
				size: "sm",
				className: "h-6 w-6 p-0 shrink-0",
				onClick: onDismiss,
				"aria-label": t("Dismiss"),
				children: /* @__PURE__ */ jsx(X, { className: "h-3.5 w-3.5" })
			})]
		})
	});
}
function CsvImportBox({ projectId }) {
	const t = useT();
	const { sessionImportIds, dismissedImportIds, dismissImport } = useSessionMigrations(projectId);
	const { migrations } = useCsvImportMigrations(projectId, sessionImportIds);
	const queryClient = useQueryClient();
	const [detailsMigration, setDetailsMigration] = useState(null);
	const notifiedCompletedRef = useRef(/* @__PURE__ */ new Set());
	const dismissedSet = useMemo(() => new Set(dismissedImportIds), [dismissedImportIds]);
	const visible = migrations.filter((m) => !dismissedSet.has(m.$id));
	useEffect(() => {
		visible.forEach((m) => {
			if (m.status === "completed") {
				if (!notifiedCompletedRef.current.has(m.$id)) {
					notifiedCompletedRef.current.add(m.$id);
					const parsed = getMigrationTableRef(m);
					queryClient.invalidateQueries({ queryKey: [
						"rows",
						"project",
						projectId,
						parsed?.databaseId,
						parsed?.tableId
					] });
					queryClient.invalidateQueries({ queryKey: [
						"tables",
						"project",
						projectId,
						parsed?.databaseId
					] });
				}
			}
		});
	}, [
		visible,
		projectId,
		queryClient
	]);
	if (visible.length === 0) return null;
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
		className: "w-full max-w-sm space-y-2",
		children: visible.map((m) => /* @__PURE__ */ jsx(MigrationItem, {
			migration: m,
			projectId,
			onViewDetails: setDetailsMigration,
			onDismiss: () => dismissImport(projectId, m.$id)
		}, m.$id))
	}), detailsMigration && /* @__PURE__ */ jsx("div", {
		className: "fixed inset-0 z-50 flex items-center justify-center bg-black/50",
		onClick: () => setDetailsMigration(null),
		onKeyDown: (e) => e.key === "Escape" && setDetailsMigration(null),
		role: "dialog",
		"aria-modal": "true",
		children: /* @__PURE__ */ jsxs("div", {
			className: "rounded-lg border border-border bg-card p-4 max-w-md max-h-[80dvh] overflow-auto shadow-xl",
			onClick: (e) => e.stopPropagation(),
			children: [
				/* @__PURE__ */ jsx("div", {
					className: "text-[13px] font-semibold mb-2",
					children: t("Import error details")
				}),
				/* @__PURE__ */ jsx("pre", {
					className: "text-[12px] text-muted-foreground whitespace-pre-wrap break-words",
					children: JSON.stringify(detailsMigration.errors ?? [], null, 2)
				}),
				/* @__PURE__ */ jsx(Button, {
					variant: "outline",
					size: "sm",
					className: "mt-3",
					onClick: () => setDetailsMigration(null),
					children: t("Close")
				})
			]
		})
	})] });
}
var CliTerminalLayoutContext = createContext("sidebar");
function CliTerminalLayoutProvider({ mode, children }) {
	return /* @__PURE__ */ jsx(CliTerminalLayoutContext.Provider, {
		value: mode,
		children
	});
}
function useCliTerminalSessionsLayoutMode() {
	return useContext(CliTerminalLayoutContext);
}
function SessionRow({ session, isRoot, isNested = false, isActive, isFocused, isInSplitPane, isRunning, isEditing, editingName, editInputRef, canRemoveRoot, layout, onSelect, onSplit, onCloseSplit, onRemoveRoot, onStartEditing, onEditingNameChange, onCommitRename, onCancelRename, dragHandleProps }) {
	const t = useT();
	const isStrip = layout === "strip";
	const isHighlighted = isNested ? isRunning : isActive || isFocused || isInSplitPane;
	return /* @__PURE__ */ jsxs("div", {
		className: cn("group flex items-center gap-0.5", isStrip ? cn("h-7 max-w-[180px] shrink-0 rounded-md border px-0.5", isHighlighted ? "border-border bg-background text-foreground shadow-sm" : "border-transparent text-muted-foreground hover:bg-muted/60") : cn("h-11", isRoot && "ps-1 pe-1", !isNested && "hover:bg-muted/60", !isNested && isFocused && "bg-muted/60", isRoot && isActive && !isFocused && "bg-muted/40")),
		children: [
			isRoot && dragHandleProps && !isStrip ? /* @__PURE__ */ jsx("button", {
				type: "button",
				ref: dragHandleProps.ref,
				className: "flex h-7 w-5 shrink-0 cursor-grab touch-none items-center justify-center rounded-sm text-muted-foreground opacity-0 transition-opacity hover:bg-muted/60 hover:opacity-100 group-hover:opacity-100 focus-visible:opacity-100 active:cursor-grabbing",
				"aria-label": `${t("Reorder")} ${session.name}`,
				onPointerDown: dragHandleProps.onPointerDown,
				onKeyDown: dragHandleProps.onKeyDown,
				"aria-describedby": dragHandleProps["aria-describedby"],
				"aria-pressed": dragHandleProps["aria-pressed"],
				"aria-roledescription": dragHandleProps["aria-roledescription"],
				tabIndex: dragHandleProps.tabIndex,
				children: /* @__PURE__ */ jsx(GripVertical, { className: "h-3.5 w-3.5" })
			}) : null,
			/* @__PURE__ */ jsxs("div", {
				role: isEditing ? void 0 : "button",
				tabIndex: isEditing ? -1 : 0,
				onClick: () => {
					if (!isEditing) onSelect();
				},
				onKeyDown: (event) => {
					if (isEditing) return;
					if (event.key === "Enter" || event.key === " ") {
						event.preventDefault();
						onSelect();
					}
				},
				className: cn("flex min-w-0 flex-1 items-center gap-2 rounded-md text-start text-foreground transition-colors", isStrip ? "h-7 px-1.5" : cn("h-11", isNested ? "px-0" : "px-2"), !isEditing && "cursor-pointer"),
				"aria-current": !isNested && isFocused ? "true" : void 0,
				children: [isRunning ? /* @__PURE__ */ jsx(Loader2, {
					className: "h-3.5 w-3.5 shrink-0 animate-spin text-muted-foreground",
					strokeWidth: isHighlighted ? 3 : 2
				}) : /* @__PURE__ */ jsx(Terminal, {
					className: "h-3.5 w-3.5 shrink-0 text-muted-foreground rtl:-scale-x-100",
					strokeWidth: isHighlighted ? 3 : 2
				}), /* @__PURE__ */ jsx("div", {
					className: "min-w-0 flex-1 h-7",
					onDoubleClick: (event) => {
						event.stopPropagation();
						if (!isEditing) onStartEditing();
					},
					children: isEditing ? /* @__PURE__ */ jsx(Input, {
						ref: editInputRef,
						value: editingName,
						maxLength: 48,
						onChange: (event) => onEditingNameChange(event.target.value),
						onBlur: onCommitRename,
						onKeyDown: (event) => {
							if (event.key === "Enter") {
								event.preventDefault();
								onCommitRename();
							}
							if (event.key === "Escape") {
								event.preventDefault();
								onCancelRename();
							}
						},
						className: cn("h-7 min-h-7 max-h-7 w-full min-w-0 rounded-sm border-0 bg-transparent px-0 py-0 text-[12px] font-medium leading-7 shadow-none outline-none", "focus-visible:border-0 focus-visible:ring-1 focus-visible:ring-inset focus-visible:ring-ring/50 focus-visible:ring-offset-0", isNested && "text-muted-foreground"),
						"aria-label": t("Rename terminal session")
					}) : /* @__PURE__ */ jsx("span", {
						className: cn("block truncate text-[12px] font-medium leading-7 h-7", isNested && "text-muted-foreground"),
						children: session.name
					})
				})]
			}),
			/* @__PURE__ */ jsx(Button, {
				type: "button",
				variant: "ghost",
				size: "icon",
				className: cn("h-7 w-7 shrink-0 text-muted-foreground", isStrip ? cn(isHighlighted && "opacity-100", !isHighlighted && "opacity-0 group-hover:opacity-100 focus-visible:opacity-100") : "opacity-0 transition-opacity group-hover:opacity-100 focus-visible:opacity-100"),
				onClick: (event) => {
					event.stopPropagation();
					onSplit();
				},
				title: t("Split terminal"),
				"aria-label": `${t("Split")} ${session.name}`,
				children: /* @__PURE__ */ jsx(Columns2, { className: "h-3.5 w-3.5" })
			}),
			isNested ? /* @__PURE__ */ jsx(Button, {
				type: "button",
				variant: "ghost",
				size: "icon",
				className: cn("h-7 w-7 shrink-0 text-muted-foreground", isStrip ? cn(isHighlighted && "opacity-100", !isHighlighted && "opacity-0 group-hover:opacity-100 focus-visible:opacity-100") : "opacity-0 transition-opacity group-hover:opacity-100 focus-visible:opacity-100"),
				onClick: (event) => {
					event.stopPropagation();
					onCloseSplit();
				},
				title: t("Delete terminal"),
				"aria-label": `${t("Delete")} ${session.name}`,
				children: /* @__PURE__ */ jsx(Trash2, { className: "h-3.5 w-3.5" })
			}) : canRemoveRoot ? /* @__PURE__ */ jsx(Button, {
				type: "button",
				variant: "ghost",
				size: "icon",
				className: cn("h-7 w-7 shrink-0 text-muted-foreground", isStrip ? cn(isHighlighted && "opacity-100", !isHighlighted && "opacity-0 group-hover:opacity-100 focus-visible:opacity-100") : "opacity-0 transition-opacity group-hover:opacity-100 focus-visible:opacity-100"),
				onClick: (event) => {
					event.stopPropagation();
					onRemoveRoot();
				},
				title: t("Delete terminal"),
				"aria-label": `${t("Delete")} ${session.name}`,
				children: /* @__PURE__ */ jsx(Trash2, { className: "h-3.5 w-3.5" })
			}) : null
		]
	});
}
function RootSessionGroupBody({ session, children, layout, rowProps, canReorder, dragHandleProps }) {
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(SessionRow, {
		session,
		isRoot: true,
		layout,
		dragHandleProps: canReorder ? dragHandleProps : void 0,
		...rowProps
	}), children] });
}
function SortableRootSessionGroup({ session, children, layout, canReorder, rowProps }) {
	const { attributes, listeners, setNodeRef, setActivatorNodeRef, transform, transition, isDragging } = useSortable({
		id: session.id,
		animateLayoutChanges: () => false,
		transition: null
	});
	return /* @__PURE__ */ jsx("div", {
		ref: setNodeRef,
		style: isDragging ? void 0 : {
			transform: sortableAxisTransform(transform, layout === "strip" ? "horizontal" : "vertical"),
			transition
		},
		className: cn(isDragging && "opacity-0", layout === "strip" && "flex shrink-0 items-center gap-1"),
		children: layout === "strip" && canReorder ? /* @__PURE__ */ jsx("div", {
			ref: setActivatorNodeRef,
			className: "flex shrink-0 cursor-grab touch-none items-center gap-1 active:cursor-grabbing",
			...attributes,
			...listeners,
			children: /* @__PURE__ */ jsx(RootSessionGroupBody, {
				session,
				layout,
				rowProps,
				canReorder,
				children
			})
		}) : /* @__PURE__ */ jsx(RootSessionGroupBody, {
			session,
			layout,
			rowProps,
			canReorder,
			dragHandleProps: {
				ref: setActivatorNodeRef,
				...attributes,
				...listeners
			},
			children
		})
	});
}
function CliSessionSidebar() {
	const t = useT();
	const layout = useCliTerminalSessionsLayoutMode();
	const isStrip = layout === "strip";
	const { sessions, activeSessionId, focusedSessionId, splitPaneSessionIds, selectSession, splitSession, removeSession, renameSession, reorderRootSessions, isSessionRunning } = useCliShell();
	const [editingSessionId, setEditingSessionId] = useState(null);
	const [editingName, setEditingName] = useState("");
	const [activeDragSessionId, setActiveDragSessionId] = useState(null);
	const editInputRef = useRef(null);
	const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 6 } }), useSensor(KeyboardSensor, { coordinateGetter: sortableKeyboardCoordinates }));
	useEffect(() => {
		if (!editingSessionId) return;
		const raf = requestAnimationFrame(() => {
			editInputRef.current?.focus();
			editInputRef.current?.select();
		});
		return () => cancelAnimationFrame(raf);
	}, [editingSessionId]);
	const startEditing = (sessionId, currentName) => {
		setEditingSessionId(sessionId);
		setEditingName(currentName);
	};
	const commitRename = () => {
		if (!editingSessionId) return;
		renameSession(editingSessionId, editingName);
		setEditingSessionId(null);
		setEditingName("");
	};
	const cancelRename = () => {
		setEditingSessionId(null);
		setEditingName("");
	};
	const rootSessions = sessions.filter((session) => !session.parentSessionId);
	const rootCount = rootSessions.length;
	const canReorder = rootCount > 1;
	const dragModifiers = useMemo(() => getAxisRestrictedDragModifiers(isStrip ? "horizontal" : "vertical"), [isStrip]);
	const handleDragStart = (event) => {
		setActiveDragSessionId(String(event.active.id));
	};
	const handleDragEnd = (event) => {
		setActiveDragSessionId(null);
		const { active, over } = event;
		if (!over || active.id === over.id) return;
		const fromIndex = rootSessions.findIndex((session) => session.id === active.id);
		const toIndex = rootSessions.findIndex((session) => session.id === over.id);
		if (fromIndex === -1 || toIndex === -1) return;
		reorderRootSessions(fromIndex, toIndex);
	};
	const handleDragCancel = () => {
		setActiveDragSessionId(null);
	};
	const activeDragSession = activeDragSessionId ? rootSessions.find((session) => session.id === activeDragSessionId) : null;
	const renderSplitChildren = (parentSessionId) => {
		const children = getCliShellSplitChildren(parentSessionId, sessions).sort((a, b) => {
			const aIndex = splitPaneSessionIds.indexOf(a.id);
			const bIndex = splitPaneSessionIds.indexOf(b.id);
			if (aIndex === -1 && bIndex === -1) return 0;
			if (aIndex === -1) return 1;
			if (bIndex === -1) return -1;
			return aIndex - bIndex;
		});
		if (children.length === 0) return null;
		return /* @__PURE__ */ jsx("div", {
			className: cn(isStrip ? void 0 : "border-s border-border/80 ps-3", !isStrip && (canReorder ? "ms-8" : "ms-5"), isStrip && "flex shrink-0 items-center gap-1"),
			children: children.map((child) => {
				const isChildEditing = editingSessionId === child.id;
				return /* @__PURE__ */ jsx(SessionRow, {
					session: child,
					isRoot: false,
					isNested: true,
					layout,
					isActive: activeSessionId === parentSessionId,
					isFocused: focusedSessionId === child.id,
					isInSplitPane: splitPaneSessionIds.includes(child.id),
					isRunning: isSessionRunning(child.id),
					isEditing: isChildEditing,
					editingName,
					editInputRef,
					canRemoveRoot: false,
					onSelect: () => selectSession(child.id),
					onSplit: () => splitSession(child.id),
					onCloseSplit: () => removeSession(child.id),
					onRemoveRoot: () => removeSession(child.id),
					onStartEditing: () => startEditing(child.id, child.name),
					onEditingNameChange: setEditingName,
					onCommitRename: commitRename,
					onCancelRename: cancelRename
				}, child.id);
			})
		});
	};
	const buildRootRowProps = (session, childCount) => {
		const isEditing = editingSessionId === session.id;
		return {
			isActive: activeSessionId === session.id,
			isFocused: focusedSessionId === session.id,
			isInSplitPane: splitPaneSessionIds.includes(session.id),
			isRunning: isSessionRunning(session.id),
			isEditing,
			editingName,
			editInputRef,
			canRemoveRoot: rootCount > 1 || childCount > 0,
			onSelect: () => selectSession(session.id),
			onSplit: () => splitSession(session.id),
			onCloseSplit: () => removeSession(session.id),
			onRemoveRoot: () => removeSession(session.id),
			onStartEditing: () => startEditing(session.id, session.name),
			onEditingNameChange: setEditingName,
			onCommitRename: commitRename,
			onCancelRename: cancelRename
		};
	};
	return /* @__PURE__ */ jsx("div", {
		className: cn("flex min-w-0 bg-muted/20", isStrip ? "h-11 w-full shrink-0 flex-row border-b border-border" : "h-full w-full flex-col pb-1"),
		"aria-label": t("Terminal sessions"),
		children: /* @__PURE__ */ jsx("div", {
			className: cn(isStrip ? "flex min-w-0 flex-1 items-center overflow-x-auto px-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden" : "min-h-0 flex-1 overflow-y-auto"),
			children: /* @__PURE__ */ jsxs(DndContext, {
				sensors,
				collisionDetection: closestCenter,
				modifiers: dragModifiers,
				onDragStart: handleDragStart,
				onDragEnd: handleDragEnd,
				onDragCancel: handleDragCancel,
				children: [/* @__PURE__ */ jsx(SortableContext, {
					items: rootSessions.map((session) => session.id),
					strategy: isStrip ? horizontalListSortingStrategy : verticalListSortingStrategy,
					disabled: !canReorder,
					children: /* @__PURE__ */ jsx("div", {
						className: cn(isStrip && "flex min-w-max items-center gap-1 py-1.5"),
						children: rootSessions.map((session) => {
							return /* @__PURE__ */ jsx(SortableRootSessionGroup, {
								session,
								layout,
								canReorder,
								rowProps: buildRootRowProps(session, getCliShellSplitChildren(session.id, sessions).length),
								children: renderSplitChildren(session.id)
							}, session.id);
						})
					})
				}), /* @__PURE__ */ jsx(DragOverlay, {
					dropAnimation: null,
					children: activeDragSession ? /* @__PURE__ */ jsx("div", {
						className: cn("rounded-md bg-muted/60 shadow-md ring-1 ring-border", isStrip ? "w-auto" : "w-full"),
						children: /* @__PURE__ */ jsx(RootSessionGroupBody, {
							session: activeDragSession,
							layout,
							canReorder,
							rowProps: buildRootRowProps(activeDragSession, getCliShellSplitChildren(activeDragSession.id, sessions).length),
							children: renderSplitChildren(activeDragSession.id)
						})
					}) : null
				})]
			})
		})
	});
}
var HANDLE_CLASS = verticalPanelResizeHandleClass("z-[45]");
var PERSIST_DEBOUNCE_MS = 250;
function CliTerminalResizableLayout({ main, sidebar, onSidebarResizingChange }) {
	const containerRef = useRef(null);
	const firstPanelRef = useRef(null);
	const prevContainerWidthRef = useRef(0);
	const [containerWidth, setContainerWidth] = useState(0);
	const { account } = useAuth();
	const accountPrefs = account;
	const { persistSidebarWidthPx } = useCliShellSessionsSidebarWidth(accountPrefs);
	const sidebarWidthPx = useMemo(() => clampCliShellSessionsSidebarWidthPx(parseCliShellSessionsSidebarWidthPx(accountPrefs?.prefs) ?? 208), [accountPrefs?.prefs]);
	const [mountedSidebarPx, setMountedSidebarPx] = useState(null);
	const isSidebarResizingRef = useRef(false);
	const effectiveSidebarPx = mountedSidebarPx ?? sidebarWidthPx;
	useLayoutEffect(() => {
		const el = containerRef.current;
		if (!el) return;
		const ro = new ResizeObserver((entries) => {
			setContainerWidth(entries[0]?.contentRect.width ?? 0);
		});
		ro.observe(el);
		setContainerWidth(el.getBoundingClientRect().width);
		return () => ro.disconnect();
	}, []);
	useEffect(() => {
		if (containerWidth <= 0 || isSidebarResizingRef.current) return;
		setMountedSidebarPx((prev) => prev === null || prev !== sidebarWidthPx ? sidebarWidthPx : prev);
	}, [containerWidth, sidebarWidthPx]);
	const isStripLayout = containerWidth > 0 && containerWidth < 640;
	useEffect(() => {
		if (typeof window === "undefined" || containerWidth <= 0) return;
		window.dispatchEvent(new Event("resize"));
	}, [containerWidth, isStripLayout]);
	const panelLayout = useMemo(() => {
		if (isStripLayout || containerWidth <= 0) return null;
		return computeTwoPanelHorizontalLayout({
			containerWidth,
			firstPx: containerWidth - effectiveSidebarPx,
			firstMinPx: Math.max(240, containerWidth - 320),
			firstMaxPx: containerWidth - 176,
			secondMinPx: 176
		});
	}, [
		containerWidth,
		effectiveSidebarPx,
		isStripLayout
	]);
	useLayoutEffect(() => {
		if (isStripLayout || containerWidth <= 0 || mountedSidebarPx === null) return;
		if (isSidebarResizingRef.current) return;
		const prevWidth = prevContainerWidthRef.current;
		prevContainerWidthRef.current = containerWidth;
		if (prevWidth <= 0 || prevWidth === containerWidth) return;
		const mainPx = containerWidth - mountedSidebarPx;
		const nextSidebarPx = clampCliShellSessionsSidebarWidthPx(containerWidth - syncPanelGroupFirstPanePx(firstPanelRef.current, containerWidth, mainPx, {
			firstMinPx: Math.max(240, containerWidth - 320),
			firstMaxPx: containerWidth - 176,
			secondMinPx: 176
		}));
		if (nextSidebarPx !== mountedSidebarPx) setMountedSidebarPx(nextSidebarPx);
	}, [
		containerWidth,
		isStripLayout,
		mountedSidebarPx
	]);
	const persistTimerRef = useRef(null);
	const lastPersistedPxRef = useRef(sidebarWidthPx);
	const latestSidebarPxRef = useRef(sidebarWidthPx);
	useEffect(() => {
		if (isSidebarResizingRef.current) return;
		lastPersistedPxRef.current = sidebarWidthPx;
		latestSidebarPxRef.current = sidebarWidthPx;
	}, [sidebarWidthPx]);
	const handleLayout = useCallback((sizes) => {
		const sidebarPercent = sizes[1];
		if (typeof sidebarPercent !== "number" || !Number.isFinite(sidebarPercent)) return;
		if (containerWidth <= 0) return;
		const nextSidebarPx = clampCliShellSessionsSidebarWidthPx(sidebarPercent / 100 * containerWidth);
		latestSidebarPxRef.current = nextSidebarPx;
		if (!isSidebarResizingRef.current) return;
		if (Math.abs(nextSidebarPx - lastPersistedPxRef.current) < 2) return;
		if (persistTimerRef.current !== null) window.clearTimeout(persistTimerRef.current);
		persistTimerRef.current = window.setTimeout(() => {
			persistTimerRef.current = null;
			lastPersistedPxRef.current = nextSidebarPx;
			persistSidebarWidthPx(nextSidebarPx);
		}, PERSIST_DEBOUNCE_MS);
	}, [containerWidth, persistSidebarWidthPx]);
	const handleSidebarDragging = useCallback((isDragging) => {
		if (isDragging) {
			isSidebarResizingRef.current = true;
			onSidebarResizingChange?.(true);
			return;
		}
		if (!isSidebarResizingRef.current) return;
		isSidebarResizingRef.current = false;
		onSidebarResizingChange?.(false);
		if (persistTimerRef.current !== null) {
			window.clearTimeout(persistTimerRef.current);
			persistTimerRef.current = null;
		}
		const nextPx = latestSidebarPxRef.current;
		lastPersistedPxRef.current = nextPx;
		setMountedSidebarPx(nextPx);
		persistSidebarWidthPx(nextPx);
		if (typeof window !== "undefined") window.dispatchEvent(new Event("resize"));
	}, [onSidebarResizingChange, persistSidebarWidthPx]);
	useEffect(() => {
		return () => {
			if (persistTimerRef.current !== null) window.clearTimeout(persistTimerRef.current);
		};
	}, []);
	if (isStripLayout) return /* @__PURE__ */ jsx(CliTerminalLayoutProvider, {
		mode: "strip",
		children: /* @__PURE__ */ jsxs("div", {
			ref: containerRef,
			className: "flex h-full min-h-0 min-w-0 flex-1 flex-col overflow-hidden",
			children: [/* @__PURE__ */ jsx("div", {
				className: "shrink-0",
				children: sidebar
			}), /* @__PURE__ */ jsx("div", {
				className: "flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden",
				children: main
			})]
		})
	});
	if (!panelLayout) return /* @__PURE__ */ jsx(CliTerminalLayoutProvider, {
		mode: "sidebar",
		children: /* @__PURE__ */ jsxs("div", {
			ref: containerRef,
			className: "flex h-full min-h-0 min-w-0 flex-1 overflow-hidden",
			children: [/* @__PURE__ */ jsx("div", {
				className: "flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden",
				children: main
			}), /* @__PURE__ */ jsx("div", {
				className: "flex min-h-0 shrink-0 flex-col overflow-hidden border-s border-border bg-muted/20",
				style: { width: sidebarWidthPx },
				children: sidebar
			})]
		})
	});
	return /* @__PURE__ */ jsx(CliTerminalLayoutProvider, {
		mode: "sidebar",
		children: /* @__PURE__ */ jsx("div", {
			ref: containerRef,
			className: "flex h-full min-h-0 min-w-0 flex-1 overflow-hidden",
			children: /* @__PURE__ */ jsxs(ResizablePanelGroup, {
				direction: "horizontal",
				className: "h-full min-h-0 min-w-0 flex-1",
				onLayout: handleLayout,
				children: [
					/* @__PURE__ */ jsx(ResizablePanel, {
						ref: firstPanelRef,
						defaultSize: panelLayout.firstPercent,
						minSize: panelLayout.firstMinPercent,
						maxSize: panelLayout.firstMaxPercent,
						className: "min-w-0",
						children: /* @__PURE__ */ jsx("div", {
							className: "flex h-full min-h-0 min-w-0 flex-col overflow-hidden",
							children: main
						})
					}),
					/* @__PURE__ */ jsx(ResizableHandle, {
						className: HANDLE_CLASS,
						onDragging: handleSidebarDragging
					}),
					/* @__PURE__ */ jsx(ResizablePanel, {
						defaultSize: panelLayout.secondPercent,
						minSize: panelLayout.secondMinPercent,
						className: "min-w-0",
						children: /* @__PURE__ */ jsx("div", {
							className: "flex h-full min-h-0 min-w-0 flex-col overflow-hidden border-s border-border bg-muted/20",
							children: sidebar
						})
					})
				]
			})
		})
	});
}
function CliTerminalSearch({ open, onOpenChange, results, onSearch, onFindNext, onFindPrevious }) {
	const t = useT();
	const [query, setQuery] = useState("");
	const inputRef = useRef(null);
	useEffect(() => {
		if (!open) {
			setQuery("");
			return;
		}
		const raf = requestAnimationFrame(() => {
			inputRef.current?.focus();
			inputRef.current?.select();
		});
		return () => cancelAnimationFrame(raf);
	}, [open]);
	const handleClose = useCallback(() => {
		onOpenChange(false);
		setQuery("");
	}, [onOpenChange]);
	const handleSubmit = useCallback((event) => {
		event.preventDefault();
		if (!query.trim()) return;
		onSearch(query, { caseSensitive: false });
	}, [onSearch, query]);
	const handleEscape = useCallback((event) => {
		if (event.key !== "Escape") return;
		event.preventDefault();
		event.stopPropagation();
		handleClose();
	}, [handleClose]);
	const handleInputKeyDown = useCallback((event) => {
		if (event.key === "Escape") {
			handleEscape(event);
			return;
		}
		if (event.key === "Enter") {
			event.preventDefault();
			if (event.shiftKey) onFindPrevious();
			else onFindNext();
		}
	}, [
		handleEscape,
		onFindNext,
		onFindPrevious
	]);
	const preventInputBlur = useCallback((event) => {
		event.preventDefault();
	}, []);
	const matchLabel = formatCliTerminalSearchLabel(query, results);
	const hasMatches = Boolean(query.trim() && results && results.resultCount > 0);
	if (!open) return null;
	return /* @__PURE__ */ jsx("div", {
		className: "flex h-11 w-full shrink-0 items-center border-b border-border bg-muted/20 px-4 sm:px-6",
		onKeyDown: handleEscape,
		children: /* @__PURE__ */ jsxs("form", {
			onSubmit: handleSubmit,
			className: "flex w-full min-w-0 items-center gap-2",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex min-w-0 flex-1 items-center",
				children: [
					/* @__PURE__ */ jsx(Search, { className: "h-3.5 w-3.5 shrink-0 text-muted-foreground" }),
					/* @__PURE__ */ jsx(Input, {
						ref: inputRef,
						value: query,
						onChange: (event) => {
							const next = event.target.value;
							setQuery(next);
							onSearch(next, { caseSensitive: false });
						},
						onKeyDown: handleInputKeyDown,
						placeholder: t("Search output..."),
						className: "h-8 min-w-0 flex-1 border-0 bg-transparent px-2 text-[13px] shadow-none focus-visible:border-transparent focus-visible:ring-0 dark:bg-transparent",
						"aria-label": t("Search terminal output")
					}),
					query.trim() ? /* @__PURE__ */ jsxs("div", {
						className: "flex shrink-0 items-center gap-0.5 ps-1",
						children: [
							/* @__PURE__ */ jsx("span", {
								className: "min-w-[4.5rem] shrink-0 px-1 text-end text-[11px] tabular-nums text-muted-foreground",
								"aria-live": "polite",
								children: t(matchLabel)
							}),
							/* @__PURE__ */ jsx(Button, {
								type: "button",
								variant: "ghost",
								size: "icon",
								className: "h-7 w-7 shrink-0 text-muted-foreground",
								onMouseDown: preventInputBlur,
								onClick: onFindPrevious,
								disabled: !hasMatches,
								title: t("Previous match"),
								"aria-label": t("Previous match"),
								children: /* @__PURE__ */ jsx(ChevronUp, { className: "h-3.5 w-3.5" })
							}),
							/* @__PURE__ */ jsx(Button, {
								type: "button",
								variant: "ghost",
								size: "icon",
								className: "h-7 w-7 shrink-0 text-muted-foreground",
								onMouseDown: preventInputBlur,
								onClick: onFindNext,
								disabled: !hasMatches,
								title: t("Next match"),
								"aria-label": t("Next match"),
								children: /* @__PURE__ */ jsx(ChevronDown, { className: "h-3.5 w-3.5" })
							})
						]
					}) : null
				]
			}), /* @__PURE__ */ jsx(Button, {
				type: "button",
				variant: "ghost",
				size: "icon",
				className: "h-8 w-8 shrink-0 text-muted-foreground",
				onMouseDown: preventInputBlur,
				onClick: handleClose,
				title: t("Close search"),
				"aria-label": t("Close search"),
				children: /* @__PURE__ */ jsx(X, { className: "h-3.5 w-3.5" })
			})]
		})
	});
}
function findCommandsInLine(lineText, commands) {
	if (!lineText.includes("Try:")) return [];
	const sorted = [...commands].sort((a, b) => b.length - a.length);
	const matches = [];
	const occupied = /* @__PURE__ */ new Set();
	for (const command of sorted) {
		let from = 0;
		while (from <= lineText.length) {
			const index = lineText.indexOf(command, from);
			if (index === -1) break;
			const end = index + command.length;
			let overlaps = false;
			for (let i = index; i < end; i++) if (occupied.has(i)) overlaps = true;
			if (!overlaps) {
				for (let i = index; i < end; i++) occupied.add(i);
				matches.push({
					command,
					startX: index + 1
				});
			}
			from = index + 1;
		}
	}
	return matches.sort((a, b) => a.startX - b.startX);
}
function linksForStoredRangeOnLine(terminal, y, range, handler) {
	const { command, start, end } = range;
	if (y < start.y || y > end.y) return [];
	const linkStartX = y === start.y ? start.x : 1;
	const linkEndX = y === end.y ? end.x : terminal.cols;
	if (linkEndX <= linkStartX) return [];
	return [withCliTerminalLinkCursor(terminal, {
		text: command,
		range: {
			start: {
				x: linkStartX,
				y
			},
			end: {
				x: linkEndX,
				y
			}
		},
		decorations: {
			underline: true,
			pointerCursor: true
		},
		activate: (event) => {
			handler(event, command);
			event.preventDefault();
		}
	})];
}
function linksFromLineText(terminal, y, lineText, commands, handler) {
	const links = [];
	for (const { command, startX } of findCommandsInLine(lineText, commands)) {
		const range = {
			start: {
				x: startX,
				y
			},
			end: {
				x: startX + command.length,
				y
			}
		};
		links.push(withCliTerminalLinkCursor(terminal, {
			text: command,
			range,
			decorations: {
				underline: true,
				pointerCursor: true
			},
			activate: (event) => {
				handler(event, command);
				event.preventDefault();
			}
		}));
	}
	return links;
}
function createCliTerminalSuggestionLinksAddon(getCommands, handler) {
	let terminal = null;
	let storedRanges = [];
	const disposables = [];
	return {
		addon: {
			activate(nextTerminal) {
				terminal = nextTerminal;
				disposables.push(terminal.registerLinkProvider({ provideLinks(y, callback) {
					if (!terminal) {
						callback(void 0);
						return;
					}
					const line = terminal.buffer.active.getLine(y - 1);
					if (!line) {
						callback(void 0);
						return;
					}
					const lineText = line.translateToString(true);
					const links = [];
					if (storedRanges.length > 0) for (const range of storedRanges) links.push(...linksForStoredRangeOnLine(terminal, y, range, handler));
					else if (lineText.includes("Try:")) links.push(...linksFromLineText(terminal, y, lineText, getCommands(), handler));
					callback(links.length > 0 ? links : void 0);
				} }));
			},
			dispose() {
				for (const disposable of disposables) disposable.dispose();
				disposables.length = 0;
				storedRanges = [];
				terminal = null;
			}
		},
		setSuggestionCommandLinks: (links) => {
			storedRanges = links;
		},
		clearSuggestionCommandLinks: () => {
			storedRanges = [];
		}
	};
}
var KEY_BINDING_MAP = new Map([
	["", { type: "cursor-start" }],
	["", { type: "cursor-end" }],
	["\x1B[H", { type: "cursor-start" }],
	["\x1B[1~", { type: "cursor-start" }],
	["\x1BOH", { type: "cursor-start" }],
	["\x1B[F", { type: "cursor-end" }],
	["\x1B[4~", { type: "cursor-end" }],
	["\x1BOF", { type: "cursor-end" }],
	["\x1B[A", { type: "history-prev" }],
	["\x1B[B", { type: "history-next" }],
	["\x1B[C", { type: "cursor-right" }],
	["\x1B[D", { type: "cursor-left" }],
	["\x1B[1;3D", { type: "cursor-word-left" }],
	["\x1B[1;5D", { type: "cursor-word-left" }],
	["\x1B b", { type: "cursor-word-left" }],
	["\x1B[1;3C", { type: "cursor-word-right" }],
	["\x1B[1;5C", { type: "cursor-word-right" }],
	["\x1B f", { type: "cursor-word-right" }],
	["", { type: "delete-backward" }],
	["\b", { type: "delete-backward" }],
	["\x1B", { type: "delete-word-backward" }],
	["", { type: "delete-word-backward" }],
	["\x1B[3~", { type: "delete-forward" }],
	["", { type: "delete-forward" }],
	["", { type: "kill-line-before" }],
	["\v", { type: "kill-line-after" }],
	["", { type: "clear-line" }],
	["\f", { type: "clear-screen" }],
	["\r", { type: "submit" }],
	["\r\n", { type: "submit" }],
	["	", { type: "tab" }]
]);
function resolveTerminalKeyAction(data) {
	if (data === "\n") return { type: "submit" };
	const binding = KEY_BINDING_MAP.get(data);
	if (binding) return binding;
	if (data.startsWith("\x1B")) return null;
	if (data.length === 1 && data >= " ") return {
		type: "insert",
		text: data
	};
	return null;
}
function consumeTerminalEscapeSequence(data, start) {
	if (data[start] !== "\x1B") return start + 1;
	if (start + 1 >= data.length) return start + 1;
	const second = data[start + 1];
	if (second === "[") {
		let index = start + 2;
		while (index < data.length) {
			const code = data.charCodeAt(index);
			if (code >= 64 && code <= 126) return index + 1;
			index += 1;
		}
		return data.length;
	}
	if (second === "]") {
		let index = start + 2;
		while (index < data.length) {
			if (data[index] === "\x07") return index + 1;
			if (data[index] === "\x1B" && data[index + 1] === "\\") return index + 2;
			index += 1;
		}
		return data.length;
	}
	if (second === "O" && start + 2 < data.length) return start + 3;
	if (second === "P") {
		let index = start + 2;
		while (index < data.length) {
			if (data[index] === "\x1B" && data[index + 1] === "\\") return index + 2;
			index += 1;
		}
		return data.length;
	}
	return start + 2;
}
function findWordBoundaryLeft(text, pos) {
	if (pos <= 0) return 0;
	let index = pos;
	while (index > 0 && /\s/.test(text[index - 1] ?? "")) index -= 1;
	while (index > 0 && !/\s/.test(text[index - 1] ?? "")) index -= 1;
	return index;
}
function findWordBoundaryRight(text, pos) {
	if (pos >= text.length) return text.length;
	let index = pos;
	while (index < text.length && /\s/.test(text[index] ?? "")) index += 1;
	while (index < text.length && !/\s/.test(text[index] ?? "")) index += 1;
	return index;
}
var CLEAR_COMMANDS = new Set(["clear", "cls"]);
var INTERRUPT_SEQUENCE = "";
function normalizeSubmittedCommand(raw) {
	return stripAnsi(raw).trim();
}
function isPersistableHistoryCommand(command) {
	if (!command) return false;
	if (!/[a-zA-Z0-9]/.test(command)) return false;
	return /^[\x20-\x7e]+$/.test(command);
}
function createCliTerminalInputHandler(options) {
	let inputBuffer = "";
	let cursorPos = 0;
	const history = [...options.initialHistory ?? []];
	let historyIndex = null;
	let lastTab = null;
	let awaitingPrompt = true;
	let promptVisible = false;
	let welcomeOutputComplete = false;
	let lastSubmittedCommand = history.length > 0 ? history[history.length - 1] ?? null : null;
	const persistHistory = () => {
		options.onHistoryChange?.([...history]);
	};
	const writePrompt = () => {
		if (!welcomeOutputComplete || options.terminal.cols <= 0) return;
		options.terminal.write(options.getPrompt());
	};
	const showPrompt = () => {
		if (!welcomeOutputComplete) return;
		if (promptVisible && inputBuffer.length === 0 && cursorPos === 0) return;
		if (options.terminal.cols <= 0) {
			awaitingPrompt = true;
			promptVisible = false;
			return;
		}
		writePrompt();
		promptVisible = true;
		awaitingPrompt = false;
	};
	const prepareInputLine = (force = false) => {
		if (!welcomeOutputComplete || options.terminal.cols <= 0) return;
		if (promptVisible && inputBuffer.length === 0 && cursorPos === 0) {
			if (force) redrawInputLine();
			return;
		}
		inputBuffer = "";
		cursorPos = 0;
		historyIndex = null;
		awaitingPrompt = false;
		const buffer = options.terminal.buffer.active;
		if ((buffer.getLine(buffer.baseY + buffer.cursorY)?.translateToString(false).replace(/\s+$/, "").length ?? 0) > 0) options.terminal.write("\r\n");
		writePrompt();
		promptVisible = true;
		awaitingPrompt = false;
	};
	const resetForWelcome = () => {
		welcomeOutputComplete = false;
		resetInputState();
		awaitingPrompt = true;
		promptVisible = false;
		options.terminal.reset();
		options.terminal.clear();
	};
	const markWelcomeComplete = () => {
		welcomeOutputComplete = true;
	};
	const markAwaitingPrompt = () => {
		awaitingPrompt = true;
		promptVisible = false;
	};
	const resetInputState = () => {
		inputBuffer = "";
		cursorPos = 0;
		historyIndex = null;
		promptVisible = false;
	};
	const clearScreen = () => {
		welcomeOutputComplete = true;
		resetInputState();
		options.terminal.reset();
		resetInputState();
		showPrompt();
	};
	const redrawInputLine = () => {
		if (!welcomeOutputComplete) return;
		options.terminal.write(`\r${options.getPrompt()}${inputBuffer}${CLI_TERMINAL_RESET}\x1b[K`);
		const tail = inputBuffer.length - cursorPos;
		if (tail > 0) options.terminal.write(`\x1b[${tail}D`);
		awaitingPrompt = false;
		promptVisible = true;
	};
	const moveCursorTo = (nextPos) => {
		const clamped = Math.max(0, Math.min(nextPos, inputBuffer.length));
		if (clamped === cursorPos) return;
		const delta = clamped - cursorPos;
		if (delta > 0) options.terminal.write(`\x1b[${delta}C`);
		else options.terminal.write(`\x1b[${-delta}D`);
		cursorPos = clamped;
	};
	const moveCursorToStart = () => {
		moveCursorTo(0);
	};
	const moveCursorToEnd = () => {
		moveCursorTo(inputBuffer.length);
	};
	const deleteBackward = () => {
		if (cursorPos <= 0) return;
		const deletingAtEnd = cursorPos === inputBuffer.length;
		inputBuffer = inputBuffer.slice(0, cursorPos - 1) + inputBuffer.slice(cursorPos);
		cursorPos -= 1;
		if (deletingAtEnd) options.terminal.write("\b \b");
		else redrawInputLine();
	};
	const deleteForward = () => {
		if (cursorPos >= inputBuffer.length) return;
		const deletingAtEnd = cursorPos === inputBuffer.length - 1;
		inputBuffer = inputBuffer.slice(0, cursorPos) + inputBuffer.slice(cursorPos + 1);
		if (deletingAtEnd) options.terminal.write("\x1B[K");
		else redrawInputLine();
	};
	const deleteWordBackward = () => {
		if (cursorPos <= 0) return;
		const start = findWordBoundaryLeft(inputBuffer, cursorPos);
		inputBuffer = inputBuffer.slice(0, start) + inputBuffer.slice(cursorPos);
		cursorPos = start;
		redrawInputLine();
	};
	const killLineBefore = () => {
		if (cursorPos <= 0) return;
		inputBuffer = inputBuffer.slice(cursorPos);
		cursorPos = 0;
		redrawInputLine();
	};
	const killLineAfter = () => {
		if (cursorPos >= inputBuffer.length) return;
		inputBuffer = inputBuffer.slice(0, cursorPos);
		redrawInputLine();
	};
	const clearLine = () => {
		inputBuffer = "";
		cursorPos = 0;
		historyIndex = null;
		redrawInputLine();
	};
	const submitCommand = () => {
		const command = normalizeSubmittedCommand(inputBuffer);
		resetInputState();
		if (command && CLEAR_COMMANDS.has(command)) {
			if (isPersistableHistoryCommand(command) && history[history.length - 1] !== command) {
				history.push(command);
				persistHistory();
			}
			lastSubmittedCommand = command;
			clearScreen();
			return;
		}
		options.terminal.writeln("");
		if (command) {
			if (isPersistableHistoryCommand(command) && history[history.length - 1] !== command) {
				history.push(command);
				persistHistory();
			}
			lastSubmittedCommand = command;
			markAwaitingPrompt();
			options.onRunCommand(command);
		} else showPrompt();
	};
	const handleTab = () => {
		const now = Date.now();
		const listOnly = !!lastTab && lastTab.input === inputBuffer && lastTab.cursor === cursorPos && now - lastTab.at < 400;
		lastTab = {
			input: inputBuffer,
			cursor: cursorPos,
			at: now
		};
		const result = options.onTabComplete(inputBuffer, cursorPos, listOnly);
		if (!result) return;
		inputBuffer = result.input;
		cursorPos = result.cursor;
		redrawInputLine();
	};
	const handleHistoryPrev = () => {
		if (history.length === 0) return;
		historyIndex = historyIndex === null ? history.length - 1 : Math.max(0, historyIndex - 1);
		inputBuffer = history[historyIndex] ?? "";
		cursorPos = inputBuffer.length;
		redrawInputLine();
	};
	const handleHistoryNext = () => {
		if (historyIndex === null) return;
		const nextIndex = historyIndex + 1;
		if (nextIndex >= history.length) {
			historyIndex = null;
			inputBuffer = "";
		} else {
			historyIndex = nextIndex;
			inputBuffer = history[nextIndex] ?? "";
		}
		cursorPos = inputBuffer.length;
		redrawInputLine();
	};
	const insertText = (text) => {
		inputBuffer = inputBuffer.slice(0, cursorPos) + text + inputBuffer.slice(cursorPos);
		cursorPos += text.length;
		options.terminal.write(text);
	};
	const applyKeyAction = (action) => {
		if (!action) return;
		switch (action.type) {
			case "cursor-start":
				moveCursorToStart();
				return;
			case "cursor-end":
				moveCursorToEnd();
				return;
			case "cursor-left":
				moveCursorTo(cursorPos - 1);
				return;
			case "cursor-right":
				moveCursorTo(cursorPos + 1);
				return;
			case "cursor-word-left":
				moveCursorTo(findWordBoundaryLeft(inputBuffer, cursorPos));
				return;
			case "cursor-word-right":
				moveCursorTo(findWordBoundaryRight(inputBuffer, cursorPos));
				return;
			case "history-prev":
				handleHistoryPrev();
				return;
			case "history-next":
				handleHistoryNext();
				return;
			case "delete-backward":
				deleteBackward();
				return;
			case "delete-forward":
				deleteForward();
				return;
			case "delete-word-backward":
				deleteWordBackward();
				return;
			case "kill-line-before":
				killLineBefore();
				return;
			case "kill-line-after":
				killLineAfter();
				return;
			case "clear-line":
				clearLine();
				return;
			case "clear-screen":
				clearScreen();
				return;
			case "submit":
				submitCommand();
				return;
			case "tab":
				handleTab();
				return;
			case "insert":
				insertText(action.text);
				return;
		}
	};
	const handleInterrupt = () => {
		if (options.getIsRunning()) {
			options.onCancelRunning?.();
			return;
		}
		applyKeyAction({ type: "clear-line" });
	};
	const handleLocalData = (data) => {
		if (data === INTERRUPT_SEQUENCE) {
			handleInterrupt();
			return;
		}
		const action = resolveTerminalKeyAction(data);
		if (action) {
			if (action.type === "submit" && data === "\n" && inputBuffer === "") return;
			applyKeyAction(action);
			return;
		}
		if (data.startsWith("\x1B")) return;
	};
	const isSubmitKey = (char) => char === "\r" || char === "\n";
	const ensurePromptBeforeInput = (char) => {
		if (awaitingPrompt && !isSubmitKey(char)) showPrompt();
	};
	const onData = (data) => {
		if (data === INTERRUPT_SEQUENCE || data.includes(INTERRUPT_SEQUENCE)) {
			handleInterrupt();
			return;
		}
		if (options.getIsRunning()) return;
		if (data.startsWith("\x1B") || data.length === 1) {
			ensurePromptBeforeInput(data);
			handleLocalData(data);
			return;
		}
		ensurePromptBeforeInput(data[0] ?? "");
		const action = resolveTerminalKeyAction(data);
		if (action?.type === "insert" && data.length > 1) {
			insertText(data);
			return;
		}
		if (action) {
			applyKeyAction(action);
			return;
		}
		let index = 0;
		while (index < data.length) {
			if (data[index] === "\x1B") {
				const nextIndex = consumeTerminalEscapeSequence(data, index);
				if (nextIndex <= index + 1) {
					index += 1;
					continue;
				}
				applyKeyAction(resolveTerminalKeyAction(data.slice(index, nextIndex)));
				index = nextIndex;
				continue;
			}
			const char = data[index];
			ensurePromptBeforeInput(char);
			applyKeyAction(resolveTerminalKeyAction(char));
			index += 1;
		}
	};
	const setInput = (command) => {
		if (options.getIsRunning()) return;
		inputBuffer = command;
		cursorPos = command.length;
		historyIndex = null;
		awaitingPrompt = false;
		redrawInputLine();
		options.terminal.focus();
	};
	return {
		onData,
		showPrompt,
		prepareInputLine,
		resetForWelcome,
		markWelcomeComplete,
		clearScreen,
		setInput,
		getLastCommand: () => lastSubmittedCommand,
		getHistory: () => [...history]
	};
}
function rgbCssToHex(rgb) {
	const m = rgb.match(/rgba?\((\d+),\s*(\d+),\s*(\d+)/);
	if (!m) return null;
	return "#" + [
		Number(m[1]),
		Number(m[2]),
		Number(m[3])
	].map((x) => x.toString(16).padStart(2, "0")).join("");
}
function cssVarToHex(varName, kind, fallback) {
	if (typeof document === "undefined") return fallback;
	const el = document.createElement("div");
	if (kind === "background") el.style.cssText = `position:absolute;left:-9999px;width:1px;height:1px;background:var(${varName})`;
	else if (kind === "foreground") el.style.cssText = `position:absolute;left:-9999px;color:var(${varName})`;
	else el.style.cssText = `position:absolute;left:-9999px;border:1px solid var(${varName})`;
	document.documentElement.appendChild(el);
	const style = getComputedStyle(el);
	const raw = kind === "background" ? style.backgroundColor : kind === "foreground" ? style.color : style.borderColor;
	document.documentElement.removeChild(el);
	if (!raw || raw === "transparent") return fallback;
	const hex = rgbCssToHex(raw);
	return hex && /^#[0-9a-fA-F]{6}$/.test(hex) ? hex : fallback;
}
function getCliTerminalTheme(resolvedTheme) {
	const isDark = isResolvedThemeDarkChrome(resolvedTheme);
	const background = cssVarToHex("--background", "background", isDark ? "#19191c" : "#ffffff");
	const foreground = cssVarToHex("--foreground", "foreground", isDark ? "#fafafa" : "#18181b");
	const mutedForeground = cssVarToHex("--muted-foreground", "foreground", isDark ? "#a1a1aa" : "#71717a");
	const primary = cssVarToHex("--primary", "foreground", "#e91e63");
	const destructive = cssVarToHex("--destructive", "foreground", "#ef4444");
	return {
		background,
		foreground,
		cursor: foreground,
		cursorAccent: background,
		selectionBackground: isDark ? "#3f3f4666" : "#e4e4e7b3",
		selectionForeground: foreground,
		black: isDark ? "#27272a" : "#f4f4f5",
		red: destructive,
		green: isDark ? "#4ade80" : "#16a34a",
		yellow: isDark ? "#facc15" : "#ca8a04",
		blue: isDark ? "#60a5fa" : "#2563eb",
		magenta: primary,
		cyan: isDark ? "#22d3ee" : "#0891b2",
		white: foreground,
		brightBlack: mutedForeground,
		brightRed: destructive,
		brightGreen: isDark ? "#86efac" : "#22c55e",
		brightYellow: isDark ? "#fde047" : "#eab308",
		brightBlue: isDark ? "#93c5fd" : "#3b82f6",
		brightMagenta: primary,
		brightCyan: isDark ? "#67e8f9" : "#06b6d4",
		brightWhite: foreground
	};
}
function getCliTerminalSearchOptions(resolvedTheme, caseSensitive = false) {
	const isDark = isResolvedThemeDarkChrome(resolvedTheme);
	const matchBackground = isDark ? "#334155" : "#dbeafe";
	const activeMatchBackground = isDark ? "#1d4ed8" : "#3b82f6";
	const matchBorder = isDark ? "#64748b" : "#93c5fd";
	const activeMatchBorder = isDark ? "#93c5fd" : "#1d4ed8";
	const overviewRuler = isDark ? "#60a5fa" : "#2563eb";
	return {
		caseSensitive,
		decorations: {
			matchBackground,
			matchBorder,
			matchOverviewRuler: overviewRuler,
			activeMatchBackground,
			activeMatchBorder,
			activeMatchColorOverviewRuler: overviewRuler
		}
	};
}
function escapeRegExp(value) {
	return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function countTextSearchMatches(text, query, caseSensitive = false) {
	const trimmed = query.trim();
	if (!trimmed || !text) return 0;
	const flags = caseSensitive ? "g" : "gi";
	return text.match(new RegExp(escapeRegExp(trimmed), flags))?.length ?? 0;
}
function countTerminalSearchMatches(terminal, query, caseSensitive = false) {
	return countTextSearchMatches(getTerminalBufferText(terminal), query, caseSensitive);
}
function resolveNavigationResultIndex(direction, addonResultCount, addonResultIndex, matchCount, hasMatch) {
	if (addonResultCount > 0 && addonResultIndex >= 0) return addonResultIndex;
	if (!hasMatch || matchCount <= 0) return -1;
	if (direction === "last") return matchCount - 1;
	return 0;
}
function navigateCliTerminalSearch(terminal, searchAddon, query, options, direction) {
	const trimmed = query.trim();
	if (!trimmed) return {
		found: false,
		matchCount: 0,
		resultIndex: -1
	};
	let addonResultCount = 0;
	let addonResultIndex = -1;
	const resultsDisposable = searchAddon.onDidChangeResults((results) => {
		addonResultCount = results.resultCount;
		addonResultIndex = results.resultIndex;
	});
	try {
		if (direction === "first" || direction === "last") {
			searchAddon.clearDecorations();
			terminal.clearSelection();
		}
		let found = false;
		switch (direction) {
			case "next":
				found = searchAddon.findNext(trimmed, options);
				break;
			case "previous":
				found = searchAddon.findPrevious(trimmed, options);
				break;
			case "first":
				found = searchAddon.findNext(trimmed, options) || searchAddon.findPrevious(trimmed, options);
				break;
			case "last":
				found = searchAddon.findPrevious(trimmed, options) || searchAddon.findNext(trimmed, options);
				break;
		}
		const bufferMatchCount = countTerminalSearchMatches(terminal, trimmed, options.caseSensitive ?? false);
		const matchCount = Math.max(bufferMatchCount, addonResultCount);
		const hasMatch = found || matchCount > 0;
		return {
			found: hasMatch,
			matchCount,
			resultIndex: resolveNavigationResultIndex(direction, addonResultCount, addonResultIndex, matchCount, hasMatch)
		};
	} finally {
		resultsDisposable.dispose();
	}
}
function runCliTerminalSearch(terminal, searchAddon, query, options) {
	const trimmed = query.trim();
	if (!trimmed) {
		searchAddon.clearDecorations();
		return {
			found: false,
			matchCount: 0,
			resultIndex: -1
		};
	}
	terminal.clearSelection();
	let addonResultCount = 0;
	let addonResultIndex = -1;
	const resultsDisposable = searchAddon.onDidChangeResults((results) => {
		addonResultCount = results.resultCount;
		addonResultIndex = results.resultIndex;
	});
	try {
		const found = searchAddon.findNext(trimmed, options) || searchAddon.findPrevious(trimmed, options);
		const bufferMatchCount = countTerminalSearchMatches(terminal, trimmed, options.caseSensitive ?? false);
		const matchCount = Math.max(bufferMatchCount, addonResultCount);
		const hasMatch = found || matchCount > 0;
		return {
			found: hasMatch,
			matchCount,
			resultIndex: resolveNavigationResultIndex("first", addonResultCount, addonResultIndex, matchCount, hasMatch)
		};
	} finally {
		resultsDisposable.dispose();
	}
}
function scheduleCliTerminalMount(callback) {
	if (typeof window === "undefined") {
		callback();
		return () => {};
	}
	let cancelled = false;
	let collapseTimer;
	let idleId;
	collapseTimer = globalThis.setTimeout(() => {
		collapseTimer = void 0;
		if (cancelled) return;
		if (typeof window.requestIdleCallback === "function") {
			idleId = window.requestIdleCallback(() => {
				idleId = void 0;
				if (!cancelled) callback();
			}, { timeout: 250 });
			return;
		}
		callback();
	}, 200);
	return () => {
		cancelled = true;
		if (collapseTimer !== void 0) globalThis.clearTimeout(collapseTimer);
		if (idleId !== void 0) window.cancelIdleCallback(idleId);
	};
}
function runTerminalSearchNavigation(sessionId, searchAddon, terminal, query, direction, options) {
	const term = query.trim();
	if (!term || !options.terminalSearchOpen) return false;
	const result = navigateCliTerminalSearch(terminal, searchAddon, term, getCliTerminalSearchOptions(options.resolvedTheme, options.caseSensitive), direction);
	if (result.found && result.matchCount > 0) options.reportSearchResults(sessionId, {
		resultIndex: result.resultIndex,
		resultCount: result.matchCount
	});
	return result.found;
}
function CliTerminalSession({ sessionId, isVisible, isFocused, isPanelResizing = false, isSplitPane = false, deferInit = false }) {
	const { open, registerTerminal, unregisterTerminal, writeSessionWelcome, runCommand, completeTab, isSessionRunning, cancelRunning, fullscreen, toggleFullscreen, exitFullscreen, getTerminalPrompt, getCommandHistory, persistCommandHistory, getSuggestionCommands, focusSessionPane, reportSearchResults, terminalSearchOpen, terminalSearchQuery, searchSessionIds, searchCaseSensitive, setTerminalSearchOpen } = useCliShell();
	const { resolvedTheme } = useTheme();
	const resolvedThemeRef = useRef(resolvedTheme);
	resolvedThemeRef.current = resolvedTheme;
	const terminalContainerRef = useRef(null);
	const terminalRef = useRef(null);
	const fitAddonRef = useRef(null);
	const searchAddonRef = useRef(null);
	const isSessionRunningRef = useRef(isSessionRunning);
	const inputSessionRef = useRef(null);
	const isPanelResizingRef = useRef(isPanelResizing);
	const fitRafRef = useRef(null);
	const runCommandRef = useRef(runCommand);
	const completeTabRef = useRef(completeTab);
	const registerTerminalRef = useRef(registerTerminal);
	const unregisterTerminalRef = useRef(unregisterTerminal);
	const writeSessionWelcomeRef = useRef(writeSessionWelcome);
	const isFocusedRef = useRef(isFocused);
	const isVisibleRef = useRef(isVisible);
	const welcomeFitAttemptsRef = useRef(0);
	const welcomeCompleteRef = useRef(false);
	const fullscreenRef = useRef(fullscreen);
	const toggleFullscreenRef = useRef(toggleFullscreen);
	const exitFullscreenRef = useRef(exitFullscreen);
	const getTerminalPromptRef = useRef(getTerminalPrompt);
	const getSuggestionCommandsRef = useRef(getSuggestionCommands);
	const sessionIdRef = useRef(sessionId);
	const persistCommandHistoryRef = useRef(persistCommandHistory);
	const cancelRunningRef = useRef(cancelRunning);
	const getCommandHistoryRef = useRef(getCommandHistory);
	const reportSearchResultsRef = useRef(reportSearchResults);
	const terminalSearchOpenRef = useRef(terminalSearchOpen);
	const setTerminalSearchOpenRef = useRef(setTerminalSearchOpen);
	const searchCaseSensitiveRef = useRef(searchCaseSensitive);
	const prevOpenForFitRef = useRef(false);
	const wasPanelResizingRef = useRef(isPanelResizing);
	const prevIsFocusedRef = useRef(isFocused);
	isSessionRunningRef.current = isSessionRunning;
	fullscreenRef.current = fullscreen;
	toggleFullscreenRef.current = toggleFullscreen;
	exitFullscreenRef.current = exitFullscreen;
	getTerminalPromptRef.current = getTerminalPrompt;
	getSuggestionCommandsRef.current = getSuggestionCommands;
	isPanelResizingRef.current = isPanelResizing;
	runCommandRef.current = runCommand;
	completeTabRef.current = completeTab;
	registerTerminalRef.current = registerTerminal;
	unregisterTerminalRef.current = unregisterTerminal;
	writeSessionWelcomeRef.current = writeSessionWelcome;
	isFocusedRef.current = isFocused;
	isVisibleRef.current = isVisible;
	sessionIdRef.current = sessionId;
	persistCommandHistoryRef.current = persistCommandHistory;
	cancelRunningRef.current = cancelRunning;
	getCommandHistoryRef.current = getCommandHistory;
	reportSearchResultsRef.current = reportSearchResults;
	terminalSearchOpenRef.current = terminalSearchOpen;
	setTerminalSearchOpenRef.current = setTerminalSearchOpen;
	searchCaseSensitiveRef.current = searchCaseSensitive;
	const tryWriteSessionWelcome = useCallback(async () => {
		if (!isVisibleRef.current) return;
		const fitAddon = fitAddonRef.current;
		const terminal = terminalRef.current;
		if (!fitAddon || !terminal) return;
		const proposed = fitAddon.proposeDimensions();
		if (!proposed || proposed.cols <= 0 || proposed.rows <= 0) {
			welcomeFitAttemptsRef.current += 1;
			if (welcomeFitAttemptsRef.current < 120) requestAnimationFrame(tryWriteSessionWelcome);
			return;
		}
		welcomeFitAttemptsRef.current = 0;
		if (!await writeSessionWelcomeRef.current(sessionIdRef.current)) return;
		welcomeCompleteRef.current = true;
		try {
			fitAddon.fit();
		} catch {}
	}, []);
	const fitTerminal = useCallback(() => {
		if (fitRafRef.current !== null) cancelAnimationFrame(fitRafRef.current);
		fitRafRef.current = requestAnimationFrame(() => {
			fitRafRef.current = null;
			try {
				const fitAddon = fitAddonRef.current;
				const terminal = terminalRef.current;
				if (!fitAddon || !terminal) return;
				const proposed = fitAddon.proposeDimensions();
				if (!proposed || proposed.cols <= 0 || proposed.rows <= 0) return;
				if (proposed.cols === terminal.cols && proposed.rows === terminal.rows) return;
				fitAddon.fit();
				requestAnimationFrame(() => {
					try {
						const next = fitAddon.proposeDimensions();
						if (next && next.cols > 0 && next.rows > 0 && (next.cols !== terminal.cols || next.rows !== terminal.rows)) fitAddon.fit();
					} catch {}
				});
			} catch {}
		});
	}, []);
	useEffect(() => {
		const container = terminalContainerRef.current;
		if (!container) return;
		let disposed = false;
		let cancelScheduledMount = () => {};
		let terminalCleanup;
		const mountTerminal = async () => {
			const [{ Terminal: Terminal$1 }, { FitAddon }, { SearchAddon }] = await Promise.all([
				import("@xterm/xterm"),
				import("@xterm/addon-fit"),
				import("@xterm/addon-search")
			]);
			if (disposed || !terminalContainerRef.current) return;
			const terminal = new Terminal$1({
				cursorBlink: true,
				fontSize: 13,
				lineHeight: 1.4,
				fontFamily: "ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, \"Liberation Mono\", \"Courier New\", monospace",
				theme: getCliTerminalTheme(resolvedThemeRef.current),
				scrollback: 5e3,
				convertEol: true,
				allowTransparency: true,
				scrollSensitivity: 1,
				allowProposedApi: true
			});
			const fitAddon = new FitAddon();
			const searchAddon = new SearchAddon();
			const openLink = (event, uri) => {
				window.open(uri, "_blank", "noopener,noreferrer");
				event.preventDefault();
			};
			const webLinksAddon = createCliTerminalWebLinksAddon(openLink);
			const suggestionLinksController = createCliTerminalSuggestionLinksAddon(() => getSuggestionCommandsRef.current(), (event, command) => {
				event.preventDefault();
				inputSessionRef.current?.setInput(command);
			});
			terminal.loadAddon(fitAddon);
			terminal.loadAddon(searchAddon);
			terminal.loadAddon(webLinksAddon);
			terminal.loadAddon(suggestionLinksController.addon);
			terminal.attachCustomKeyEventHandler((event) => {
				if (event.type !== "keydown") return true;
				if (event.key === "Escape") {
					if (terminalSearchOpenRef.current) {
						event.preventDefault();
						setTerminalSearchOpenRef.current(false);
						return false;
					}
					if (fullscreenRef.current) {
						event.preventDefault();
						exitFullscreenRef.current();
						return false;
					}
				}
				return true;
			});
			terminal.open(container);
			if (disposed) {
				terminal.dispose();
				return;
			}
			terminalRef.current = terminal;
			fitAddonRef.current = fitAddon;
			searchAddonRef.current = searchAddon;
			const currentSessionId = sessionIdRef.current;
			const inputSession = createCliTerminalInputHandler({
				terminal,
				getIsRunning: () => isSessionRunningRef.current(currentSessionId),
				getPrompt: () => getTerminalPromptRef.current(),
				initialHistory: getCommandHistoryRef.current(),
				onHistoryChange: (history) => persistCommandHistoryRef.current(history),
				onCancelRunning: () => cancelRunningRef.current(currentSessionId),
				onRunCommand: (command) => runCommandRef.current(command, currentSessionId),
				onTabComplete: (...args) => completeTabRef.current(...args, currentSessionId)
			});
			inputSessionRef.current = inputSession;
			const api = {
				write: (data, callback) => terminal.write(data, callback),
				writeln: (data, callback) => terminal.writeln(data, callback),
				clear: () => terminal.clear(),
				focus: () => terminal.focus(),
				focusInputLine: () => {
					terminal.scrollToBottom();
					terminal.focus();
				},
				showInputPrompt: inputSession.showPrompt,
				prepareInputLine: inputSession.prepareInputLine,
				resetForWelcome: () => {
					suggestionLinksController.clearSuggestionCommandLinks();
					inputSession.resetForWelcome();
				},
				markWelcomeComplete: inputSession.markWelcomeComplete,
				clearScreen: inputSession.clearScreen,
				getPrompt: () => getTerminalPromptRef.current(),
				getLastCommand: () => inputSession.getLastCommand(),
				getSelection: () => terminal.getSelection(),
				getBufferText: () => getTerminalBufferText(terminal),
				getBufferCursor: () => {
					const buffer = terminal.buffer.active;
					return {
						x: buffer.cursorX + 1,
						y: buffer.baseY + buffer.cursorY + 1
					};
				},
				registerSuggestionCommandLinks: (links) => {
					suggestionLinksController.setSuggestionCommandLinks(links);
				},
				clearTerminalSearch: () => {
					searchAddon.clearDecorations();
				},
				findNextMatch: (query) => runTerminalSearchNavigation(currentSessionId, searchAddon, terminal, query, "next", {
					resolvedTheme: resolvedThemeRef.current,
					caseSensitive: searchCaseSensitiveRef.current,
					reportSearchResults: reportSearchResultsRef.current,
					terminalSearchOpen: terminalSearchOpenRef.current
				}),
				findPreviousMatch: (query) => runTerminalSearchNavigation(currentSessionId, searchAddon, terminal, query, "previous", {
					resolvedTheme: resolvedThemeRef.current,
					caseSensitive: searchCaseSensitiveRef.current,
					reportSearchResults: reportSearchResultsRef.current,
					terminalSearchOpen: terminalSearchOpenRef.current
				}),
				findFirstMatch: (query) => runTerminalSearchNavigation(currentSessionId, searchAddon, terminal, query, "first", {
					resolvedTheme: resolvedThemeRef.current,
					caseSensitive: searchCaseSensitiveRef.current,
					reportSearchResults: reportSearchResultsRef.current,
					terminalSearchOpen: terminalSearchOpenRef.current
				}),
				findLastMatch: (query) => runTerminalSearchNavigation(currentSessionId, searchAddon, terminal, query, "last", {
					resolvedTheme: resolvedThemeRef.current,
					caseSensitive: searchCaseSensitiveRef.current,
					reportSearchResults: reportSearchResultsRef.current,
					terminalSearchOpen: terminalSearchOpenRef.current
				})
			};
			registerTerminalRef.current(currentSessionId, api);
			const resultsDisposable = searchAddon.onDidChangeResults((results) => {
				if (!terminalSearchOpenRef.current) return;
				if (results.resultCount <= 0) return;
				reportSearchResultsRef.current(currentSessionId, {
					resultIndex: results.resultIndex,
					resultCount: results.resultCount
				});
			});
			requestAnimationFrame(tryWriteSessionWelcome);
			const dataDisposable = terminal.onData(inputSession.onData);
			const resizeObserver = new ResizeObserver(() => {
				if (!isPanelResizingRef.current) {
					fitTerminal();
					if (!welcomeCompleteRef.current) requestAnimationFrame(tryWriteSessionWelcome);
				}
			});
			resizeObserver.observe(container);
			terminalCleanup = () => {
				if (fitRafRef.current !== null) {
					cancelAnimationFrame(fitRafRef.current);
					fitRafRef.current = null;
				}
				dataDisposable.dispose();
				resultsDisposable.dispose();
				resizeObserver.disconnect();
				unregisterTerminalRef.current(currentSessionId);
				terminal.dispose();
				inputSessionRef.current = null;
				terminalRef.current = null;
				fitAddonRef.current = null;
				searchAddonRef.current = null;
				welcomeFitAttemptsRef.current = 0;
				welcomeCompleteRef.current = false;
			};
		};
		const startMount = () => {
			mountTerminal();
		};
		if (deferInit) cancelScheduledMount = scheduleCliTerminalMount(startMount);
		else startMount();
		return () => {
			disposed = true;
			cancelScheduledMount();
			terminalCleanup?.();
		};
	}, [
		deferInit,
		fitTerminal,
		sessionId,
		tryWriteSessionWelcome
	]);
	useEffect(() => {
		const terminal = terminalRef.current;
		if (!terminal) return;
		terminal.options.theme = getCliTerminalTheme(resolvedTheme);
	}, [resolvedTheme]);
	useEffect(() => {
		if (!open || !isVisible) return;
		requestAnimationFrame(tryWriteSessionWelcome);
	}, [
		isVisible,
		open,
		tryWriteSessionWelcome
	]);
	useEffect(() => {
		const opening = open && !prevOpenForFitRef.current;
		prevOpenForFitRef.current = open;
		if (!isVisible || !open || isPanelResizing) return;
		const runFit = () => fitTerminal();
		if (opening) {
			const timer = window.setTimeout(runFit, 200);
			return () => clearTimeout(timer);
		}
		runFit();
	}, [
		fitTerminal,
		isPanelResizing,
		isVisible,
		open
	]);
	useEffect(() => {
		const wasResizing = wasPanelResizingRef.current;
		wasPanelResizingRef.current = isPanelResizing;
		if (!open || !isVisible || isPanelResizing || !wasResizing) return;
		fitTerminal();
	}, [
		fitTerminal,
		isPanelResizing,
		isVisible,
		open
	]);
	useEffect(() => {
		const wasFocused = prevIsFocusedRef.current;
		prevIsFocusedRef.current = isFocused;
		if (!open || !isFocused || !isVisible || terminalSearchOpen) return;
		if (wasFocused || !isFocused) return;
		requestAnimationFrame(() => {
			const terminal = terminalRef.current;
			if (!terminal) return;
			terminal.scrollToBottom();
			terminal.focus();
		});
	}, [
		isFocused,
		isVisible,
		open,
		terminalSearchOpen
	]);
	useEffect(() => {
		if (!open || !terminalSearchOpen) return;
		const query = terminalSearchQuery.trim();
		if (!query || !searchSessionIds.includes(sessionId)) return;
		const terminal = terminalRef.current;
		const searchAddon = searchAddonRef.current;
		if (!terminal || !searchAddon) return;
		const result = runCliTerminalSearch(terminal, searchAddon, query, getCliTerminalSearchOptions(resolvedTheme, searchCaseSensitive));
		let matchCount = result.matchCount;
		let resultIndex = result.resultIndex;
		if (matchCount === 0) {
			matchCount = countTextSearchMatches(getTerminalBufferText(terminal), query, searchCaseSensitive);
			if (matchCount > 0 && resultIndex < 0) resultIndex = result.found ? 0 : -1;
		}
		if (matchCount > 0 && resultIndex < 0) resultIndex = 0;
		reportSearchResults(sessionId, {
			resultIndex,
			resultCount: matchCount
		});
	}, [
		open,
		reportSearchResults,
		resolvedTheme,
		searchCaseSensitive,
		searchSessionIds,
		sessionId,
		terminalSearchOpen,
		terminalSearchQuery
	]);
	return /* @__PURE__ */ jsx("div", {
		className: cn("relative flex h-full min-h-0 flex-col", !isSplitPane && "pt-2 pb-2", !isVisible && "hidden"),
		children: /* @__PURE__ */ jsx("div", {
			ref: terminalContainerRef,
			className: cn("cli-terminal min-h-0 w-full flex-1 overflow-hidden", "[&_.xterm]:h-full [&_.xterm-viewport]:!overflow-y-auto"),
			onPointerDown: () => {
				focusSessionPane(sessionId);
				if (!terminalSearchOpenRef.current) terminalRef.current?.focus();
			}
		})
	});
}
var SPLIT_HANDLE_CLASS = cn("relative z-10 h-full w-px shrink-0 self-stretch items-stretch bg-border", "before:pointer-events-none before:absolute before:inset-y-0 before:w-2 before:bg-border before:opacity-0 before:transition-opacity", RESIZE_HANDLE_PSEUDO_BEFORE_X, "hover:before:opacity-100 data-[resize-handle-state=hover]:before:opacity-100 data-[resize-handle-state=drag]:before:opacity-100", "after:absolute after:inset-y-0 after:w-3", RESIZE_HANDLE_PSEUDO_AFTER_X);
function getSplitPaneContentClass(index, total) {
	if (total <= 1) return "";
	const beforeSeparator = "pe-3";
	const afterSeparator = "ps-5 sm:ps-6";
	if (index === 0) return cn("ps-4 sm:ps-6", total > 1 && beforeSeparator);
	if (index === total - 1) return cn(afterSeparator, "pe-4 sm:pe-6");
	return cn(afterSeparator, beforeSeparator);
}
function CliTerminalSessionsLayout({ sessions, displaySessionIds, focusedSessionId, isPanelResizing = false, onSplitResizingChange }) {
	const { open } = useCliShell();
	const [isSplitResizing, setIsSplitResizing] = useState(false);
	const [activatedSessions, setActivatedSessions] = useState(() => /* @__PURE__ */ new Map());
	const panelOpenRef = useRef(false);
	const panelRefs = useRef({});
	const effectiveDisplaySessionIds = useMemo(() => {
		const valid = displaySessionIds.filter((id) => sessions.some((session) => session.id === id));
		if (valid.length > 0) return valid;
		if (sessions.length > 0) return [sessions[0].id];
		return [];
	}, [displaySessionIds, sessions]);
	const isSplit = effectiveDisplaySessionIds.length > 1;
	const displayedCount = effectiveDisplaySessionIds.length;
	const defaultPaneSize = displayedCount > 0 ? 100 / displayedCount : 100;
	const isResizing = isPanelResizing || isSplitResizing;
	const handleSplitDragging = useCallback((dragging) => {
		setIsSplitResizing(dragging);
		onSplitResizingChange?.(dragging);
	}, [onSplitResizingChange]);
	const handleSplitLayout = useCallback(() => {
		if (typeof window === "undefined") return;
		window.dispatchEvent(new Event("resize"));
	}, []);
	useEffect(() => {
		handleSplitLayout();
	}, [effectiveDisplaySessionIds, handleSplitLayout]);
	useEffect(() => {
		const wasOpen = panelOpenRef.current;
		panelOpenRef.current = open;
		const deferInitForNewSessions = open && !wasOpen;
		setActivatedSessions((previous) => {
			const next = new Map(previous);
			let changed = false;
			for (const sessionId of effectiveDisplaySessionIds) if (!next.has(sessionId)) {
				next.set(sessionId, { deferInit: deferInitForNewSessions });
				changed = true;
			}
			return changed ? next : previous;
		});
	}, [effectiveDisplaySessionIds, open]);
	useLayoutEffect(() => {
		const activeDisplayId = effectiveDisplaySessionIds.length === 1 ? effectiveDisplaySessionIds[0] : null;
		const splitSize = isSplit ? 100 / displayedCount : 100;
		for (const session of sessions) {
			const panel = panelRefs.current[session.id];
			if (!panel) continue;
			if (!effectiveDisplaySessionIds.includes(session.id)) {
				if (!panel.isCollapsed()) panel.collapse();
				continue;
			}
			if (panel.isCollapsed()) panel.expand();
			if (isSplit) {
				const currentSize = panel.getSize();
				if (Math.abs(currentSize - splitSize) > .5) panel.resize(splitSize);
			} else if (activeDisplayId === session.id) {
				if (panel.getSize() < 99) panel.resize(100);
			} else if (!panel.isCollapsed()) panel.collapse();
		}
	}, [
		displayedCount,
		effectiveDisplaySessionIds,
		isSplit,
		sessions
	]);
	if (effectiveDisplaySessionIds.length === 0) return /* @__PURE__ */ jsx("div", {
		className: "h-full min-h-0",
		"aria-hidden": true
	});
	return /* @__PURE__ */ jsx(ResizablePanelGroup, {
		direction: "horizontal",
		className: "h-full min-h-0 min-w-0 items-stretch gap-0",
		onLayout: handleSplitLayout,
		children: sessions.map((session, index) => {
			const isDisplayed = effectiveDisplaySessionIds.includes(session.id);
			const displayIndex = effectiveDisplaySessionIds.indexOf(session.id);
			const hasDisplayedBeforeInList = sessions.slice(0, index).some((item) => effectiveDisplaySessionIds.includes(item.id));
			const showHandle = isSplit && isDisplayed && displayIndex > 0 && hasDisplayedBeforeInList;
			const activation = activatedSessions.get(session.id);
			return /* @__PURE__ */ jsxs(Fragment$1, { children: [showHandle ? /* @__PURE__ */ jsx(ResizableHandle, {
				className: SPLIT_HANDLE_CLASS,
				onDragging: handleSplitDragging
			}) : null, /* @__PURE__ */ jsx(ResizablePanel, {
				id: session.id,
				order: index + 1,
				ref: (panel) => {
					panelRefs.current[session.id] = panel;
				},
				defaultSize: isDisplayed ? isSplit ? defaultPaneSize : 100 : 0,
				minSize: isDisplayed ? 12 : 0,
				collapsible: true,
				collapsedSize: 0,
				className: cn("flex min-h-0 min-w-0 flex-col", !isDisplayed && "min-w-0 max-w-0 overflow-hidden"),
				children: /* @__PURE__ */ jsx("div", {
					className: cn("flex h-full min-h-0 flex-col", isSplit && isDisplayed && "pt-3 pb-3"),
					children: /* @__PURE__ */ jsx("div", {
						className: cn("box-border flex min-h-0 w-full min-w-0 flex-1 flex-col overflow-hidden", isSplit && isDisplayed && getSplitPaneContentClass(displayIndex, displayedCount)),
						children: activation ? /* @__PURE__ */ jsx(CliTerminalSession, {
							sessionId: session.id,
							isVisible: isDisplayed,
							isFocused: focusedSessionId === session.id,
							isPanelResizing: isResizing,
							isSplitPane: isSplit && isDisplayed,
							deferInit: activation.deferInit
						}) : /* @__PURE__ */ jsx("div", {
							className: "h-full min-h-0",
							"aria-hidden": "true"
						})
					})
				})
			})] }, session.id);
		})
	});
}
function ProjectCliShell() {
	const { open, fullscreen, height, panelEverOpened } = useCliShell();
	const [isCollapsing, setIsCollapsing] = useState(false);
	const [isResizing, setIsResizing] = useState(false);
	const prevOpenRef = useRef(open);
	useEffect(() => {
		const wasOpen = prevOpenRef.current;
		prevOpenRef.current = open;
		if (open) {
			setIsCollapsing(false);
			return;
		}
		if (!panelEverOpened || !wasOpen) return;
		setIsCollapsing(true);
		const timer = window.setTimeout(() => setIsCollapsing(false), 200);
		return () => window.clearTimeout(timer);
	}, [open, panelEverOpened]);
	const showCollapsedBar = !open && !isCollapsing;
	const isFullyCollapsed = showCollapsedBar && !fullscreen;
	const isFullscreenOpen = open && fullscreen;
	const containerHeight = open && !fullscreen ? height : 54;
	return /* @__PURE__ */ jsxs("div", {
		"data-cli-shell": true,
		className: cn("bg-background", isFullscreenOpen ? "fixed inset-0 z-[140] flex min-h-0 flex-col" : cn("relative shrink-0 overflow-hidden border-t border-border", !isResizing && "transition-[height] ease-in-out")),
		style: isFullscreenOpen ? void 0 : {
			height: containerHeight,
			transitionDuration: isResizing ? "0ms" : `200ms`
		},
		children: [panelEverOpened ? /* @__PURE__ */ jsx("div", {
			className: cn("flex min-h-0 flex-col", isFullscreenOpen ? "h-full flex-1" : void 0, !isFullscreenOpen && !open && "pointer-events-none", isFullyCollapsed && "opacity-0"),
			style: isFullscreenOpen ? void 0 : {
				height,
				minHeight: height
			},
			"aria-hidden": !isFullscreenOpen && !open,
			children: /* @__PURE__ */ jsx(ProjectCliShellPanel, { onResizingChange: setIsResizing })
		}) : null, showCollapsedBar ? /* @__PURE__ */ jsx("div", {
			className: "absolute inset-0 z-10 flex min-h-0 flex-col",
			children: /* @__PURE__ */ jsx(ProjectCliShellCollapsedBar, {})
		}) : null]
	});
}
function CliShellHeaderDivider() {
	return /* @__PURE__ */ jsx("div", {
		className: "mx-0.5 h-4 w-px shrink-0 bg-border",
		"aria-hidden": "true"
	});
}
function CliShellHeaderIconButton({ title, "aria-label": ariaLabel, onClick, pressed, children }) {
	return /* @__PURE__ */ jsx(Button, {
		type: "button",
		variant: "ghost",
		size: "icon",
		className: cn("h-8 w-8 shrink-0 text-muted-foreground", pressed && "bg-muted text-foreground"),
		onClick,
		title,
		"aria-label": ariaLabel ?? title,
		"aria-pressed": pressed,
		children
	});
}
function CliShellHeaderTitle({ showBootstrapSpinner = false, showRetry = false, onRetry }) {
	const t = useT();
	return /* @__PURE__ */ jsxs("div", {
		className: "flex min-w-0 items-center gap-2",
		children: [/* @__PURE__ */ jsx(Terminal, {
			className: "h-3.5 w-3.5 shrink-0 text-muted-foreground rtl:-scale-x-100",
			strokeWidth: 2.5,
			"aria-hidden": "true"
		}), /* @__PURE__ */ jsxs("div", {
			className: "flex min-w-0 items-center gap-2",
			children: [
				/* @__PURE__ */ jsx("span", {
					className: "truncate text-[13px] font-semibold text-foreground",
					children: t("Terminal")
				}),
				showBootstrapSpinner ? /* @__PURE__ */ jsx(Loader2, {
					className: "h-3.5 w-3.5 shrink-0 animate-spin text-muted-foreground",
					"aria-label": t("Setting up CLI")
				}) : null,
				showRetry ? /* @__PURE__ */ jsxs(Button, {
					type: "button",
					variant: "outline",
					size: "sm",
					className: "h-7 shrink-0 gap-1.5 px-2 text-[12px] text-destructive hover:text-destructive",
					onClick: onRetry,
					children: [/* @__PURE__ */ jsx(RotateCcw, { className: "h-3 w-3" }), t("Retry setup")]
				}) : null
			]
		})]
	});
}
function ProjectCliShellCollapsedBar() {
	const t = useT();
	const { setOpen, status } = useCliShell();
	const isBootstrapping = status === "bootstrapping";
	return /* @__PURE__ */ jsxs("button", {
		type: "button",
		onClick: () => setOpen(true),
		className: "flex h-full w-full cursor-pointer items-center justify-between gap-4 bg-background px-4 text-start transition-colors hover:bg-muted/40 sm:px-6",
		"aria-label": t("Open terminal"),
		children: [/* @__PURE__ */ jsx(CliShellHeaderTitle, { showBootstrapSpinner: isBootstrapping }), /* @__PURE__ */ jsx("span", {
			className: "flex h-8 w-8 shrink-0 items-center justify-center text-muted-foreground",
			"aria-hidden": "true",
			children: /* @__PURE__ */ jsx(ChevronUp, { className: "h-3.5 w-3.5" })
		})]
	});
}
function ProjectCliShellPanel({ onResizingChange }) {
	const t = useT();
	const { isMac } = usePlatform();
	const newTerminalShortcutKeys = formatDisplayKeys(CLI_SHELL_NEW_TERMINAL_SHORTCUT_RAW, isMac).join("");
	const { open, toggle, status, sessions, activeSessionId, focusedSessionId, visiblePaneSessionIds, clearOutput, createSession, height, setHeight, retryBootstrap, bootstrapError, fullscreen, toggleFullscreen, terminalSearchOpen, setTerminalSearchOpen, terminalSearchResults, toggleTerminalSearch, searchTerminalOutput, findNextTerminalMatch, findPreviousTerminalMatch, copyTerminalSelection, copyLastCommand, copyTerminalOutput, exportTerminalOutput } = useCliShell();
	const [isResizing, setIsResizing] = useState(false);
	const [isSplitResizing, setIsSplitResizing] = useState(false);
	const [isSidebarResizing, setIsSidebarResizing] = useState(false);
	const resizeSessionRef = useRef(null);
	const handleResizeMouseDown = useCallback((event) => {
		event.preventDefault();
		resizeSessionRef.current = {
			startY: event.clientY,
			startHeight: height
		};
		setBodyResizeDragActive(true, "row-resize");
		setIsResizing(true);
	}, [height]);
	useEffect(() => {
		onResizingChange?.(isResizing || isSplitResizing || isSidebarResizing);
	}, [
		isResizing,
		isSplitResizing,
		isSidebarResizing,
		onResizingChange
	]);
	useEffect(() => {
		if (!isResizing || fullscreen) return;
		const handleMouseMove = (event) => {
			const session = resizeSessionRef.current;
			if (!session) return;
			setHeight(session.startHeight + (session.startY - event.clientY));
		};
		const handleMouseUp = () => {
			resizeSessionRef.current = null;
			setBodyResizeDragActive(false);
			setIsResizing(false);
		};
		document.addEventListener("mousemove", handleMouseMove);
		document.addEventListener("mouseup", handleMouseUp);
		return () => {
			document.removeEventListener("mousemove", handleMouseMove);
			document.removeEventListener("mouseup", handleMouseUp);
			setBodyResizeDragActive(false);
		};
	}, [
		fullscreen,
		isResizing,
		setHeight
	]);
	const isBootstrapping = status === "bootstrapping";
	const [headerActionsVisible, setHeaderActionsVisible] = useState(open);
	useEffect(() => {
		setHeaderActionsVisible(open);
	}, [open]);
	return /* @__PURE__ */ jsxs("div", {
		className: cn("relative flex min-h-0 flex-col overflow-hidden bg-background", fullscreen ? "h-full min-h-0 flex-1" : "h-full"),
		children: [
			!fullscreen && /* @__PURE__ */ jsx("div", {
				role: "separator",
				"aria-orientation": "horizontal",
				"aria-label": t("Resize terminal"),
				onMouseDown: handleResizeMouseDown,
				className: cn("absolute inset-x-0 top-0 z-10 flex h-1.5 w-full cursor-row-resize items-center justify-center transition-colors hover:bg-primary/20", isResizing && "bg-primary/30")
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "flex h-11 shrink-0 items-center justify-between gap-4 border-b border-border bg-muted/20 px-4 sm:px-6",
				children: [/* @__PURE__ */ jsx(CliShellHeaderTitle, {
					showBootstrapSpinner: isBootstrapping,
					showRetry: status === "error" || status === "bootstrapping" || !!bootstrapError,
					onRetry: retryBootstrap
				}), /* @__PURE__ */ jsxs("div", {
					className: cn("flex shrink-0 items-center", headerActionsVisible ? "opacity-100" : "pointer-events-none opacity-0"),
					children: [
						/* @__PURE__ */ jsx(CliShellHeaderIconButton, {
							title: `${t("New terminal")} (${newTerminalShortcutKeys})`,
							onClick: createSession,
							children: /* @__PURE__ */ jsx(Plus, { className: "h-3.5 w-3.5" })
						}),
						/* @__PURE__ */ jsx(CliShellHeaderIconButton, {
							title: terminalSearchOpen ? t("Close search") : `${t("Search output...")} (⌘F)`,
							"aria-label": terminalSearchOpen ? t("Close terminal search") : t("Search terminal output"),
							pressed: terminalSearchOpen,
							onClick: toggleTerminalSearch,
							children: /* @__PURE__ */ jsx(Search, { className: "h-3.5 w-3.5" })
						}),
						/* @__PURE__ */ jsx(CliShellHeaderDivider, {}),
						/* @__PURE__ */ jsxs(DropdownMenu, { children: [/* @__PURE__ */ jsx(DropdownMenuTrigger, {
							asChild: true,
							children: /* @__PURE__ */ jsx(Button, {
								type: "button",
								variant: "ghost",
								size: "icon",
								className: "h-8 w-8 shrink-0 text-muted-foreground",
								title: t("Output actions"),
								"aria-label": t("Output actions"),
								children: /* @__PURE__ */ jsx(Ellipsis, { className: "h-3.5 w-3.5" })
							})
						}), /* @__PURE__ */ jsxs(DropdownMenuContent, {
							align: "end",
							className: "min-w-48",
							children: [
								/* @__PURE__ */ jsx(DropdownMenuLabel, {
									className: "text-[11px] font-semibold uppercase tracking-wider text-muted-foreground",
									children: t("Copy")
								}),
								/* @__PURE__ */ jsx(DropdownMenuItem, {
									onClick: copyTerminalSelection,
									children: t("Copy selection")
								}),
								/* @__PURE__ */ jsx(DropdownMenuItem, {
									onClick: copyLastCommand,
									children: t("Copy last command")
								}),
								/* @__PURE__ */ jsx(DropdownMenuItem, {
									onClick: copyTerminalOutput,
									children: t("Copy all output")
								}),
								/* @__PURE__ */ jsx(DropdownMenuSeparator, {}),
								/* @__PURE__ */ jsx(DropdownMenuLabel, {
									className: "text-[11px] font-semibold uppercase tracking-wider text-muted-foreground",
									children: t("Output")
								}),
								/* @__PURE__ */ jsxs(DropdownMenuItem, {
									onClick: exportTerminalOutput,
									className: "gap-1.5",
									children: [/* @__PURE__ */ jsx(Download, { className: "h-3.5 w-3.5" }), t("Download output")]
								}),
								/* @__PURE__ */ jsxs(DropdownMenuItem, {
									onClick: () => clearOutput(focusedSessionId),
									className: "gap-1.5",
									children: [/* @__PURE__ */ jsx(Trash2, { className: "h-3.5 w-3.5" }), t("Clear output")]
								})
							]
						})] }),
						/* @__PURE__ */ jsx(CliShellHeaderDivider, {}),
						/* @__PURE__ */ jsx(CliShellHeaderIconButton, {
							title: fullscreen ? t("Exit full screen") : t("Full screen"),
							onClick: toggleFullscreen,
							children: fullscreen ? /* @__PURE__ */ jsx(Minimize2, { className: "h-3.5 w-3.5" }) : /* @__PURE__ */ jsx(Maximize2, { className: "h-3.5 w-3.5" })
						}),
						/* @__PURE__ */ jsx(CliShellHeaderIconButton, {
							title: t("Minimize shell"),
							onClick: toggle,
							children: /* @__PURE__ */ jsx(ChevronDown, { className: "h-3.5 w-3.5" })
						})
					]
				})]
			}),
			/* @__PURE__ */ jsx(CliTerminalResizableLayout, {
				onSidebarResizingChange: setIsSidebarResizing,
				main: /* @__PURE__ */ jsxs("div", {
					className: "flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden",
					children: [terminalSearchOpen ? /* @__PURE__ */ jsx(CliTerminalSearch, {
						open: true,
						onOpenChange: setTerminalSearchOpen,
						results: terminalSearchResults,
						onSearch: (query, options) => searchTerminalOutput(query, options, focusedSessionId),
						onFindNext: findNextTerminalMatch,
						onFindPrevious: findPreviousTerminalMatch
					}) : null, /* @__PURE__ */ jsx("div", {
						className: cn("flex min-h-0 flex-1 flex-col overflow-hidden", visiblePaneSessionIds.length > 1 ? "px-0" : "px-4 pt-3 pb-3 sm:px-6", terminalSearchOpen && visiblePaneSessionIds.length <= 1 && "pt-0"),
						children: /* @__PURE__ */ jsx("div", {
							className: "h-full min-h-0 flex-1",
							children: /* @__PURE__ */ jsx(CliTerminalSessionsLayout, {
								sessions,
								displaySessionIds: visiblePaneSessionIds.length > 1 ? visiblePaneSessionIds : [activeSessionId],
								focusedSessionId,
								isPanelResizing: isResizing || isSplitResizing || isSidebarResizing,
								onSplitResizingChange: setIsSplitResizing
							})
						})
					})]
				}),
				sidebar: /* @__PURE__ */ jsx(CliSessionSidebar, {})
			})
		]
	});
}
function ProjectCliShellLayout({ projectId, sidebar, headerBanner, fixedLayout, children }) {
	const { features } = useConsoleProfile();
	const { project } = useProject(projectId);
	const { access } = useOrganizationScopes(project?.teamId);
	const showTerminal = canShowProjectTerminal(access, features);
	const layout = /* @__PURE__ */ jsx(ProjectConnectDialogProvider, {
		projectId,
		children: /* @__PURE__ */ jsxs(KeyboardShortcutsProvider, {
			projectId,
			children: [/* @__PURE__ */ jsx(ConsoleLayout, {
				sidebar,
				header: { projectId },
				headerBanner,
				showFooter: false,
				fixedLayout,
				bottomPanel: showTerminal ? /* @__PURE__ */ jsx(ProjectCliShell, {}) : void 0,
				children
			}), /* @__PURE__ */ jsxs("div", {
				className: "fixed bottom-4 end-4 z-50 flex flex-col gap-2 max-w-sm w-full",
				children: [
					/* @__PURE__ */ jsx(GlobalUploadProgress, { embedded: true }),
					/* @__PURE__ */ jsx(CsvImportBox, { projectId }),
					/* @__PURE__ */ jsx(CsvExportBox, { projectId })
				]
			})]
		})
	});
	if (!showTerminal) return layout;
	return /* @__PURE__ */ jsx(CliShellProvider, {
		projectId,
		children: layout
	});
}
var DATABASE_TABLE_VIEW_TABS = new Set([
	"rows",
	"documents",
	"columns",
	"indexes",
	"security",
	"settings",
	"visualizer",
	"monitor",
	"backups",
	"export-import",
	"db-security",
	"db-settings",
	"json"
]);
function getDatabaseTablesRouteTab(pathParts) {
	if (pathParts[3] !== "databases") return void 0;
	if (pathParts.length >= 8 && pathParts[5] === "tables") {
		const tab = pathParts[7];
		if (tab && DATABASE_TABLE_VIEW_TABS.has(tab)) return tab;
	}
	if (pathParts.length >= 9 && pathParts[6] === "tables") {
		const tab = pathParts[8];
		if (tab && DATABASE_TABLE_VIEW_TABS.has(tab)) return tab;
	}
	if (pathParts.length >= 9 && pathParts[6] === "collections") {
		const tab = pathParts[8];
		if (tab && DATABASE_TABLE_VIEW_TABS.has(tab)) return tab;
	}
}
var DATABASE_LEVEL_LAYOUT_SEGMENTS = new Set([
	"visualizer",
	"monitor",
	"backups",
	"export-import",
	"db-security",
	"browser",
	"security",
	"settings",
	"connections",
	"connect"
]);
function getDatabaseLevelLayoutSegment(pathParts) {
	if (pathParts[3] !== "databases") return void 0;
	if (pathParts.length >= 7 && pathParts[6]) {
		const seg = pathParts[6];
		if (DATABASE_LEVEL_LAYOUT_SEGMENTS.has(seg)) return seg;
	}
}
function ProjectAccessErrorView({ error, reset }) {
	useLayoutEffect(() => {
		setInitialLoaderShellGate(INITIAL_LOADER_SHELL_GATE.projectSelector, true);
	}, []);
	return /* @__PURE__ */ jsxs("div", {
		className: "org-layout-container flex h-full flex-col bg-background",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "sticky top-0 z-[110] flex shrink-0 flex-col bg-background",
			children: [/* @__PURE__ */ jsx(CloudStatusBanner, {}), /* @__PURE__ */ jsx(ConsoleImpersonationBanner, {})]
		}), /* @__PURE__ */ jsx("main", {
			className: "min-h-0 flex-1 overflow-y-auto",
			children: /* @__PURE__ */ jsx(ErrorComponent, {
				error,
				info: void 0,
				reset
			})
		})]
	});
}
function ProjectLayout() {
	const { projectId } = Route$1.useParams();
	const location = useLocation();
	const [sidebarOpen, setSidebarOpen] = useState(false);
	const [hidePausedCurtain, setHidePausedCurtain] = useState(false);
	const loaderData = Route$1.useLoaderData();
	const routeContext = Route$1.useRouteContext();
	const { project, isLoading: isProjectLoading, error: projectError } = useProject(projectId);
	const { features } = useConsoleProfile();
	const projectPaymentRequired = isHttpPaymentRequiredError(projectError);
	const [consoleTeamId, setConsoleTeamId] = useState(null);
	useEffect(() => {
		if (!features.billing || !projectId) return;
		if (!projectPaymentRequired && routeContext.budgetLimitReached !== true && loaderData?.budgetLimitReached !== true) return;
		if (project?.teamId || loaderData?.project?.teamId || routeContext.budgetLimitTeamId || consoleTeamId) return;
		let cancelled = false;
		resolveProjectTeamIdFromConsole(projectId).then((teamId) => {
			if (!cancelled && teamId) setConsoleTeamId(teamId);
		});
		return () => {
			cancelled = true;
		};
	}, [
		features.billing,
		projectId,
		projectPaymentRequired,
		project?.teamId,
		loaderData?.project?.teamId,
		loaderData?.budgetLimitReached,
		routeContext.budgetLimitReached,
		routeContext.budgetLimitTeamId,
		consoleTeamId
	]);
	const teamIdForBilling = project?.teamId || (loaderData?.project?.teamId && loaderData.project.teamId.length > 0 ? loaderData.project.teamId : void 0) || routeContext.budgetLimitTeamId || consoleTeamId || void 0;
	const { organization: billingOrganization } = useOrganizationById(features.billing ? teamIdForBilling : void 0);
	const budgetLimitReached = Boolean(features.billing && (routeContext.budgetLimitReached === true || loaderData?.budgetLimitReached === true || projectPaymentRequired || isBudgetLimitReached(project) || isBudgetLimitReached(billingOrganization)));
	const planUsageLimitReached = Boolean(features.billing && !budgetLimitReached && (routeContext.planUsageLimitReached === true || loaderData?.planUsageLimitReached === true || isPlanUsageLimitReached(billingOrganization) || isPlanUsageLimitReached(project)));
	const planUsageBillingLimits = billingOrganization?.billingLimits && isPlanUsageLimitReached(billingOrganization) ? billingOrganization.billingLimits : project?.billingLimits;
	const { data: failedInvoicePresence } = useOrganizationFailedInvoicePresence(budgetLimitReached || planUsageLimitReached ? void 0 : teamIdForBilling);
	const showFailedInvoiceBanner = !budgetLimitReached && !planUsageLimitReached && features.billing && failedInvoicePresence?.hasFailedInvoice === true;
	const { data: organizationsListData } = useQuery({
		...organizationsQueryOptions(),
		enabled: !budgetLimitReached && !planUsageLimitReached && features.multiTenancy
	});
	const orgBillingReadonlyForFailedInvoice = useMemo(() => {
		if (!showFailedInvoiceBanner || !teamIdForBilling || !organizationsListData?.teams) return false;
		return isOrganizationBillingReadonlyStatus(organizationsListData.teams.find((t) => t.$id === teamIdForBilling)?.status);
	}, [
		showFailedInvoiceBanner,
		organizationsListData,
		teamIdForBilling
	]);
	const projectForPaused = loaderData?.project ?? (project ? {
		$id: project.$id,
		teamId: project.teamId,
		status: project.status
	} : null);
	const isPausedFromProject = project?.status === "paused";
	const isPausedFromLoader = !project && loaderData?.project?.status === "paused";
	const isPaused = !budgetLimitReached && !planUsageLimitReached && (isPausedFromProject || isPausedFromLoader) && !hidePausedCurtain;
	const budgetCurtainTeamId = (teamIdForBilling && teamIdForBilling.length > 0 ? teamIdForBilling : null) || (projectForPaused?.teamId && projectForPaused.teamId.length > 0 ? projectForPaused.teamId : null) || (routeContext.budgetLimitTeamId && routeContext.budgetLimitTeamId.length > 0 ? routeContext.budgetLimitTeamId : null);
	const errorMessage = projectError?.message || "";
	const isNotFound = Boolean(projectError && isHttpNotFoundError(projectError));
	const isAccessDenied = Boolean(projectError && isHttpProjectAccessError(projectError) && !isNotFound);
	useLayoutEffect(() => {
		if (!(budgetLimitReached || !isProjectLoading && (isNotFound || isAccessDenied))) return;
		setInitialLoaderShellGate(INITIAL_LOADER_SHELL_GATE.projectSelector, true);
	}, [
		budgetLimitReached,
		isProjectLoading,
		isNotFound,
		isAccessDenied,
		location.pathname
	]);
	const pathParts = location.pathname.split("/");
	const activeSection = pathParts[3] || "overview";
	const databaseTablesRouteTab = getDatabaseTablesRouteTab(pathParts);
	const databaseLevelLayoutSegment = getDatabaseLevelLayoutSegment(pathParts);
	const isDatabaseSpreadsheetView = activeSection === "databases" && (databaseTablesRouteTab != null || databaseLevelLayoutSegment != null);
	const isDatabaseVisualizerView = databaseTablesRouteTab === "visualizer" || databaseLevelLayoutSegment === "visualizer";
	const isFunctionsEditorView = activeSection === "functions" && pathParts[4] === "editor";
	const isFunctionExecutionsTab = activeSection === "functions" && pathParts.length >= 6 && pathParts[5] === "executions";
	const isSiteLogsTab = activeSection === "sites" && pathParts.length >= 6 && pathParts[5] === "logs";
	const isFixedLayoutView = isDatabaseSpreadsheetView || isDatabaseVisualizerView || activeSection === "usage" || activeSection === "activity" || activeSection === "realtime" || activeSection === "storage" || activeSection === "explorer" || isFunctionsEditorView || isFunctionExecutionsTab || isSiteLogsTab;
	useEffect(() => {
		setSidebarOpen(false);
	}, [location.pathname]);
	useEffect(() => {
		if (project?.status === "paused") setHidePausedCurtain(false);
	}, [project?.status]);
	const reportedConsoleAccessForRef = useRef(null);
	useEffect(() => {
		if (typeof window === "undefined" || !projectId || isPaused || budgetLimitReached || planUsageLimitReached || !features.billing) return;
		if (reportedConsoleAccessForRef.current === projectId) return;
		reportedConsoleAccessForRef.current = projectId;
		reportConsoleAccess(projectId);
	}, [
		projectId,
		isPaused,
		budgetLimitReached,
		planUsageLimitReached,
		features.billing
	]);
	if (!budgetLimitReached && !planUsageLimitReached && projectError && (isNotFound || isAccessDenied)) return /* @__PURE__ */ jsx(ProjectAccessErrorView, {
		error: projectError instanceof Error ? projectError : new Error(errorMessage || "Project error occurred"),
		reset: () => {
			window.location.reload();
		}
	});
	if (!budgetLimitReached && !planUsageLimitReached && isProjectLoading && !project && !loaderData?.project) return null;
	return /* @__PURE__ */ jsxs(RequireAuth, { children: [
		budgetLimitReached ? /* @__PURE__ */ jsx(BudgetLimitProjectCurtain, { teamId: budgetCurtainTeamId }) : planUsageLimitReached ? /* @__PURE__ */ jsx(PlanUsageLimitProjectCurtain, {
			teamId: budgetCurtainTeamId,
			billingLimits: planUsageBillingLimits
		}) : null,
		isPaused && projectForPaused && /* @__PURE__ */ jsx(PausedProjectCurtain, {
			projectId: projectForPaused.$id,
			teamId: projectForPaused.teamId,
			onRestoreSuccess: () => setHidePausedCurtain(true)
		}),
		/* @__PURE__ */ jsx(SessionMigrationsProvider, { children: /* @__PURE__ */ jsxs(RealtimeProvider, {
			projectId,
			children: [/* @__PURE__ */ jsx(BuildNotificationsProvider, { projectId }), /* @__PURE__ */ jsx(ProjectCliShellLayout, {
				projectId,
				sidebar: {
					projectId,
					activeSection,
					mobileOpen: sidebarOpen,
					onMobileClose: () => setSidebarOpen(false),
					onMenuClick: () => setSidebarOpen(true)
				},
				headerBanner: /* @__PURE__ */ jsx(OrganizationFailedInvoiceHeaderBanner, {
					organizationId: teamIdForBilling,
					show: showFailedInvoiceBanner,
					orgBillingReadonly: orgBillingReadonlyForFailedInvoice
				}),
				fixedLayout: isFixedLayoutView,
				children: /* @__PURE__ */ jsx(Outlet, {})
			})]
		}) })
	] });
}
export { ProjectLayout as component };
